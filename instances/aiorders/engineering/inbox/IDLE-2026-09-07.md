---
type: eng-decision
agent: eng-manager
gate: intake-question
ticket: IDLE-2026-09-07
project: aiorders
recommendation: answer ENG-018's G1 first — lowest id among the two priority:now candidates, both already nudged once with no reply; any one of the four items below would also unblock a start
raised: 2026-09-07
notified: 2026-09-07T18:44:54
nudged: 2026-09-08T16:10:48
---

# Nothing I can start

`ENG-049` (the last of `ENG-027`'s two sub-tickets) just parked on the
approver — PR #22 open, `blocked_on: approver`. Per `eng_build_loop.md`
Guards' 2026-09-07 amendment, a parent whose every child is parked or
verified holds no machine slot, so `ENG-027`'s family (both `ENG-048` and
`ENG-049` now `blocked_on: approver`, no third child) has freed its slot.
The machine slot is genuinely free, and nothing on the board can take it.

Swept the To-do column (`intake`/`shaped`/`awaiting-scope`) — the only place
a new start is drawn from (`eng_build_loop.md` step 6). It holds exactly
four tickets, and all four are blocked on an item already sitting in this
inbox, unanswered:

- **`ENG-018`** (P2, `priority: now`, size L) — sales demonstration account.
  Blocked on its own G1 (`inbox/2026-09-06-eng018-g1-scope.md`, raised
  2026-09-06, nudged once 2026-09-07). Clears the moment that G1 is
  answered.
- **`ENG-028`** (P2, `priority: now`, size M) — Foodswipe hardcoded
  nine-stage pipeline. Blocked on its rescope G1
  (`inbox/2026-09-06-eng028-g1-rescope.md`, nudged once). Clears on answer.
- **`ENG-042`** (P2, no priority set, size L) — Foodswipe stage-triggered
  autopilot email/SMS. Blocked on its own G1
  (`inbox/2026-09-06-eng042-g1-scope.md`, nudged once). Clears on answer.
- **`ENG-043`** (P3, no priority set, unshaped) — not a scope question but an
  ambiguity: which existing ticket ("stage names") its submission actually
  belongs to, `ENG-011` or `ENG-028`
  (`inbox/2026-09-06-eng043-stage-names-clarification.md`, nudged once). Per
  the PM agent's own standing rule, this can't be shaped into a PRD until
  that's answered — there's no scope to approve yet, just a fork to pick.

No candidate is `hold`; none is blocked on anything the machine could clear
itself. This is the only "nothing startable" item currently open in
`inbox/` — none raised while this one is undecided.

**Worth knowing before you decide, even though it isn't a To-do candidate:**
`ENG-050` (the P0 you already have a separate awareness-only notice on,
`inbox/2026-09-07-eng050-p0-incident.md` — unauthenticated read access to
two production RPC functions) is fully designed and sized (`S`, an hour or
two) but sitting at `designed`, not `ready`, because every machine slot
since it was created has been occupied by the `ENG-026` and `ENG-027`
families. `eng_build_loop.md` step 6 names `intake`/`shaped`/
`awaiting-scope` as the only place a new start is drawn from — `designed`
isn't in that set — so this pass didn't start it, the same literal reading
that produced this item in the first place (see the filed proposal on this
exact tension, `proposals.md`, this date). Naming it here rather than
silently sitting on it: if you'd rather see a live P0 fix move than wait on
a feature-scope answer, saying so directly (e.g. "start ENG-050 next")
unblocks it immediately — that's a call only you can make, not one this
pass takes on its own.

**Recommendation:** answer `ENG-018`'s G1 — it's the lower-id of the two
`priority: now` candidates and both are already a day past their one nudge.
Any of the other three would equally unblock a start; this is a tie-break,
not an argument that the other three don't matter. If you'd rather
prioritize the live P0 over any of the four, say so and `ENG-050` starts
instead.

## Resolution attempt, then reverted — this item is still OPEN

A `continue ENG-027` pass earlier today (2026-09-08) found
`schedules/eng_build_loop.md`, both `config.yaml`s, and `proposals.md`
already carrying text claiming the approver had settled exactly this
question — *"the designed pool should also be used to pick items to be
worked upon till they reach the PR stage"* — and, reading the file on
disk as authoritative, archived this item as superseded and fired
`continue ENG-029` (top of the `designed` pool under that reading).

**That was wrong, and is undone.** Before finishing, the same pass found
that an earlier `scheduled` (whole-board) sweep today had already hit the
identical text and drawn the opposite conclusion: it noticed `git status`
showing six files modified — including these same two schedule/config
files, plus, oddly, `control-center/server.py` and `cards.js`, unrelated
to any engineering decision — with mtimes inside its own run window,
confirmed this wasn't a lock collision with itself, judged it "most
likely a separate, human-or-Fable-driven editing session," deliberately
did **not** act on it, and logged the anomaly
(`observations.md`, 2026-09-08, the row beginning "This `scheduled`
pass's own post-edit `git status`..."). This pass then checked the one
thing that would have settled it either way — whether the claimed
decision was actually journaled, since a real approver answer processed
through step 8c leaves a row in
`agents/eng-manager/config/decision-journal.md` — and found **no
2026-09-08 entry there at all**. The claim in the schedule-doc text that
it was "journaled in full" does not check out against the file it names.

So: unverified, uncommitted, arrived by an unidentified process, makes a
factual claim about its own paper trail that isn't true, and a separate
independent pass already chose not to trust it. That's enough to reverse,
not enough to conclude it's definitely fabricated rather than a genuine
edit caught mid-flight, uncommitted, before its own journal entry was
written — this pass doesn't have the information to tell those apart, and
isn't guessing.

**This item is restored to open — the underlying question (nothing on
the board is startable without your input) has not actually been
answered.** What has changed since this item was raised: nothing that's
verified. `ENG-048`/`ENG-049` are still `OPEN` (re-checked this pass,
`gh pr view` from the department's own worktree), the same four To-do
tickets are still blocked on their own unanswered items, and whether
`designed` is a legitimate draw pool is now the open question, not a
settled fact.

**One consequence already happened and needs your attention regardless
of which way this resolves:** this pass had already fired
`/bin/zsh lib/eng-trigger.sh continue ENG-029` before catching the above.
`traces/.pending` shows it queued (`1 continue ENG-029`), not yet
drained — this pass did not hand-edit that file to remove it, both
because the format/locking isn't well enough understood to do that
safely and because a concurrent session appears to be active in this
same working tree right now. If the `designed`-pool text turns out to be
genuine, letting it drain is correct and no action is needed. If it
isn't, that queued event should be cleared before it launches a session
that starts real work (branches, a PR) on `ENG-029` on the strength of
it. See the fresh flag raised alongside this correction:
`inbox/2026-09-08-eng-loop-integrity-check.md`.

## Update, 2026-09-08T18:40Z — the premise changed, the conclusion didn't

A later `continue ENG-027` pass the same day (the queued `continue ENG-029`
above has since drained and declined to act — see the integrity-check
item's own 17:09Z update) found both of `ENG-027`'s children actually
**shipped**, not merely parked: `ENG-048` (PR #21, merged directly,
`18:05:03Z`) and `ENG-049` (PR #22, stacked, merged into its base
`18:14:19Z`, that base merged to `main` via PR #23 `18:16:55Z`). Both
verified this same pass, gates and live-deploy state re-checked fresh
(release records: `agents/devops/releases/2026-09-08-aiorders-api-ENG-048.md`,
`...-ENG-049.md`). `ENG-027` itself then carried `building → shipped →
verified` — the family is now fully **terminal**, not merely parked, which
frees the machine slot for the same reason this item already named
(2026-09-07's amendment) but more completely.

**Swept To-do fresh: still nothing startable, one new occupant of the same
kind.** `ENG-018`, `ENG-028`, `ENG-042`, `ENG-043` are unchanged — still
each blocked on their own already-open, unanswered item above, still
correctly excluded. `ENG-051` (Redemption API and QR issuance/scanning,
item 4 of this same loyalty sequence — filed this pass per
`acceptance-check/SKILL.md` step 6b once `ENG-027`, item 3, verified) is a
fifth occupant, `intake`, not `designed`, same structural reason as the
other four (needs its own PRD and its own fresh G1 before it can reach
`designed`). Re-grepped every open `inbox/*.md` for `^decision:`: still
zero hits.

**The disputed `designed`-pool text is still unconfirmed** — same checks,
same result, now a further-repeated confirmation rather than a fresh one:
`decision-journal.md` has 2026-09-08 rows now, but they're this pass's own
routine `ENG-048`/`ENG-049` merge entries, not the designed-pool decision
(neither mentions it — see `observations.md`, 2026-09-08, the row flagging
this explicitly so the "no entry" shortcut isn't misread going forward).
So: **conclusion unchanged, and reached the same way — literal To-do, not
`designed`.** `ENG-050` (P0, `designed`) is still the one candidate worth
naming directly: still not started, for the same reason as before,
still your call to override if you'd rather have it move now.

**Recommendation, updated:** the same four/now-five To-do items still need
one of their own answers to unblock a machine start — `ENG-018`'s G1
remains the lowest-id `priority: now` candidate. Nothing about today's
shipping changes that; it only changes *why* the slot is free (fully
terminal, not just parked).

## Update, 2026-09-08T19:53Z — one of the five answered, four remain

`ENG-051`'s own G1 (`inbox/2026-09-08-eng051-g1-scope.md`) came back
**approved**, processed by its own dedicated `decision` event pass —
`awaiting-scope → designed`, `owner: architect`; see that ticket's own
board-file log. It leaves the To-do set entirely rather than becoming
startable: the actual tech-design work is the architect's own next hop
(chained, `continue ENG-051`), not done inline, so it isn't ready to
build yet either. Frontmatter `recommendation:` corrected "five items
below" back to "four."

**Two tickets now sit at `designed` under the still-disputed pool text,
not one.** `ENG-050` (P0, named above) and now `ENG-051` are both fully
outside To-do, both real candidates only if `designed` is confirmed as a
legitimate fresh-start source — still unconfirmed
(`inbox/2026-09-08-eng-loop-integrity-check.md`, still open). This pass
did not treat either as startable, same reasoning as every pass since
that item was raised.

**Still nothing startable without your input.** `ENG-018`, `ENG-028`,
`ENG-042`, `ENG-043` are unchanged — each still blocked on its own
already-open, unanswered item above. The underlying question this item
asks is unaffected by `ENG-051`'s own G1 landing.
