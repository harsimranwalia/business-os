---
type: eng-decision
agent: eng-manager
gate: intake-question
ticket: IDLE-2026-09-07
project: aiorders
recommendation: answer ENG-018's G1 first — lowest id among the two priority:now candidates, both already nudged once with no reply; any one of the four items below would also unblock a start
raised: 2026-09-07
notified: 2026-09-07T18:44:54
nudged:
---

# Nothing I can start

`ENG-049` (the last of `ENG-027`'s two sub-tickets) just parked on the
approver — PR #22 open, `blocked_on: approver`. Per `eng_build_loop.md`
Guards' 2026-09-07 amendment, a parent whose every child is parked or
verified holds no machine slot, so `ENG-027`'s family (both `ENG-048` and
`ENG-049` now `blocked_on: approver`, no third child) has freed its slot.
The machine slot is genuinely free, and nothing on the board can take it.

Swept the To-do column (`intake`/`shaped`/`awaiting-scope`) — the only place
a new start is drawn from (`eng_build_loop.md` step 6). It holds exactly
four tickets, and all four are blocked on an item already sitting in this
inbox, unanswered:

- **`ENG-018`** (P2, `priority: now`, size L) — sales demonstration account.
  Blocked on its own G1 (`inbox/2026-09-06-eng018-g1-scope.md`, raised
  2026-09-06, nudged once 2026-09-07). Clears the moment that G1 is
  answered.
- **`ENG-028`** (P2, `priority: now`, size M) — Foodswipe hardcoded
  nine-stage pipeline. Blocked on its rescope G1
  (`inbox/2026-09-06-eng028-g1-rescope.md`, nudged once). Clears on answer.
- **`ENG-042`** (P2, no priority set, size L) — Foodswipe stage-triggered
  autopilot email/SMS. Blocked on its own G1
  (`inbox/2026-09-06-eng042-g1-scope.md`, nudged once). Clears on answer.
- **`ENG-043`** (P3, no priority set, unshaped) — not a scope question but an
  ambiguity: which existing ticket ("stage names") its submission actually
  belongs to, `ENG-011` or `ENG-028`
  (`inbox/2026-09-06-eng043-stage-names-clarification.md`, nudged once). Per
  the PM agent's own standing rule, this can't be shaped into a PRD until
  that's answered — there's no scope to approve yet, just a fork to pick.

No candidate is `hold`; none is blocked on anything the machine could clear
itself. This is the only "nothing startable" item currently open in
`inbox/` — none raised while this one is undecided.

**Worth knowing before you decide, even though it isn't a To-do candidate:**
`ENG-050` (the P0 you already have a separate awareness-only notice on,
`inbox/2026-09-07-eng050-p0-incident.md` — unauthenticated read access to
two production RPC functions) is fully designed and sized (`S`, an hour or
two) but sitting at `designed`, not `ready`, because every machine slot
since it was created has been occupied by the `ENG-026` and `ENG-027`
families. `eng_build_loop.md` step 6 names `intake`/`shaped`/
`awaiting-scope` as the only place a new start is drawn from — `designed`
isn't in that set — so this pass didn't start it, the same literal reading
that produced this item in the first place (see the filed proposal on this
exact tension, `proposals.md`, this date). Naming it here rather than
silently sitting on it: if you'd rather see a live P0 fix move than wait on
a feature-scope answer, saying so directly (e.g. "start ENG-050 next")
unblocks it immediately — that's a call only you can make, not one this
pass takes on its own.

**Recommendation:** answer `ENG-018`'s G1 — it's the lower-id of the two
`priority: now` candidates and both are already a day past their one nudge.
Any of the other three would equally unblock a start; this is a tie-break,
not an argument that the other three don't matter. If you'd rather
prioritize the live P0 over any of the four, say so and `ENG-050` starts
instead.
