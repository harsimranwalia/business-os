---
type: eng-decision
agent: product-manager
gate: scope
project: aiorders-admin-hub
ticket: ENG-005
recommendation: no build recommendation — this G1 asks you to choose, not to approve a plan; wire A4PosterGenerator.tsx into a named surface, or revert bfddffe
raised: 2026-08-27
notified: 2026-08-27T09:59:41
decision: approved
decided: 2026-08-27T18:03:50.514589+00:00
---

# G1 — Decide and act on the orphaned A4PosterGenerator component

## Readback

**You said:** "`src/components/A4PosterGenerator.tsx` was committed to
`aiorders-admin-hub` on 2026-08-23 (`bfddffe`)... Nothing imports it... First
decide whether it is wanted, then act on the answer. If it is, wire it into a
route or a surface in the admin hub and say which. If it is not, delete it —
`bfddffe` is a single-file commit specifically so reverting it is clean.
Small, and genuinely low stakes." (`inbox/requests/2026-08-23-a4-poster-generator-unwired.md`)

**Understood as:** A component exists, fully committed, that nothing in the
running app can reach. You want a decision made on whether it's wanted at
all, then that decision executed cleanly in the same repo — wired into a
named route/surface if yes, reverted if no.

**Requirements:**
1. `[stated]` A decision: is `A4PosterGenerator.tsx` wanted?
2. `[stated]` If yes: wire it into a named route or surface in
   `aiorders-admin-hub`.
3. `[stated]` If no: revert `bfddffe` (a clean, single-file revert).
4. `[inferred]` "Wired in" means reachable by a human via normal navigation,
   not merely present in the bundle.

**Assumed, and worth correcting if wrong:**
- That you (or whoever committed `bfddffe`) don't already have a specific
  surface in mind — the request leaves this open ("say which"), so naming
  one is exactly what this G1 is asking.
- That the component's own completeness (does it compile, does it depend on
  anything only present in `admin-hub`'s 64 uncommitted human-checkout
  files) hasn't been checked yet — worth confirming at `building`, whichever
  branch is chosen.

**Not doing:** guessing which surface the component belongs on; touching any
of `admin-hub`'s other 64 uncommitted files; pursuing more than smoke-level
testing either way.

## Recommendation

**No build recommendation — this G1 is the fork itself, not a plan to
approve.** The request's own first line is "first decide whether it is
wanted"; there is no PRD-writable acceptance criterion for either branch
until you choose one, and guessing a direction would be exactly what the
request asks not to do. Please answer with one of:
- **Wire it in** — and name the route/surface it should appear on, so
  acceptance criteria can be written against it.
- **Revert** — cleanly reverts `bfddffe`; `A4PosterGenerator.tsx` stops
  existing on the default branch.

## Problem

A committed, fully-merged component in `aiorders-admin-hub` is unreachable
from the running app — nothing imports it. Left as-is, a committed-but-dead
component reads as intentional to the next person who finds it, when it's
actually just unresolved.

## Acceptance criteria

Not written — deliberate, per `skills/prd-writer/SKILL.md` step 5: "wire it
in" and "delete it" produce entirely different, mutually exclusive criteria,
and writing both as if either might apply would be padding. Once you answer:
- **If wired in:** Given the admin hub after this ships, when a human
  navigates to {the named surface}, then `A4PosterGenerator` renders without
  error and appears in whatever navigation makes that surface discoverable.
- **If reverted:** Given `aiorders-admin-hub` after this ships, when `git
  log` is checked, then `bfddffe` is cleanly reverted and
  `A4PosterGenerator.tsx` no longer exists on the default branch.

## Non-goals

Guessing which surface the component belongs on; touching any of
`admin-hub`'s other 64 uncommitted files regardless of relation;
comprehensive testing either way — sized `S`, smoke-level at most, matching
the request's own "small, genuinely low stakes."

## Risks and unknowns

Whether the component depends on anything that exists only in `admin-hub`'s
64 uncommitted human-checkout files — if so, "wire it in" could fail for
reasons invisible to a department session working only from `main` (the
architect's blind reading flagged this; the PM's first pass had missed it).
If wire-in is chosen, actual size may exceed this ticket's provisional `S` —
flagged for the EM/architect to resize after the decision.

## Cost

Build: `S`, provisional — the delete branch is trivially `XS`; the wire-in
branch is the one that could grow, per Risks above. Run: $0/month either way.

## Dissent

None — `agents/critic/agent.md` doesn't exist at the department or instance
level, so the dissent pass this step normally runs couldn't be run. Already
an open proposal (`agents/eng-manager/proposals.md`, 2026-08-25 row) — not
refiled here.

## PRD

Full detail: `agents/product-manager/specs/ENG-005-a4-poster-generator-decision.md`

## Decision

Filled in by the approver.

## Decision

**approved** — 2026-08-27T18:03:50.514589+00:00

wire it in

---

**Processed 2026-08-27 (`watch` pass).** Fork resolved — component stays,
revert branch closed. The route/surface this item asked you to name ("wire
it in — and name the route/surface") wasn't in the answer, so that one
question was carried forward rather than guessed: see
`inbox/2026-08-27-eng005-g1-followup-surface.md`. Ticket stays at
`awaiting-scope` until that's answered too.
