---
ticket: ENG-045
project: aiorders-api
author: qa
created: 2026-09-07
suite_command: "cd supabase/functions/_shared && deno test --no-check openingHours.test.ts; cd ../restaurant-marketplace && SUPABASE_URL=<dummy> SUPABASE_SERVICE_ROLE_KEY=<dummy> deno test --no-check --allow-env --allow-net handlers/restaurants.test.ts (env vars needed only because the handler test's import chain constructs the supabase singleton at module-eval time; not registered in config/projects.md's Commands table, no deno.json at repo root)"
last_run: 2026-09-07
last_result: pass
coverage_note: AC3 (in full) and AC4's backend half (filter + status computation) — this ticket's owned criteria, per agents/eng-manager/notebook/2026-09-07-eng026-work-breakdown.md's AC-ownership mapping. AC4's UI half (the "Open Now" chip) belongs to ENG-047. Round 1 found zero coverage on either owned criterion and closed it in the same round the review failed on an unrelated finding (export); this receipt is round 2's, the first non-discarded one for this ticket.
---

# Test plan — FoodSwipe channel-visibility discovery handlers (ENG-045)

## Scope note

Against `agents/product-manager/specs/ENG-026-foodswipe-channel-visibility.md`'s
5 acceptance criteria. This ticket owns AC3 in full and AC4's backend
(filter/status) half; AC1/AC2 belong to `ENG-044`/`ENG-046`, AC4's UI half
and AC5 (a decision-record criterion, already satisfied at PRD time) sit
outside this diff.

## Acceptance coverage

| AC | Criterion | Test | Level | Status |
|---|---|---|---|---|
| 3 | FoodSwipe's Dine-In/Catering tabs show only merchants with the respective flag `true`; a `false`-flagged merchant never appears, regardless of `open_now` (negative case) | `restaurants.test.ts :: "handleRestaurantDiscovery excludes a channel-disabled restaurant, regardless of open_now (AC3)"` (RPC path, both with and without `open_now=true`) + `:: "...fallback path also excludes a channel-disabled restaurant (AC3)"` (independent `.eq()` gate, RPC forced to `42883`) + `:: "...catering tab is unaffected by the dine-in flag"` (proves the *matching* flag is checked, not any flag) | unit | pass |
| 4 (backend half) | A flag-enabled merchant that is currently closed still appears by default, with a status string, and is excluded only when `open_now` is explicitly on | `restaurants.test.ts :: "...includes a confirmed-closed restaurant by default, with a status string (AC4)"` (open/closed/unknown-hours fixtures, asserts `status` per row) + `:: "...excludes a confirmed-closed restaurant only when open_now is explicitly true (AC4)"` | unit | pass |
| — (Outcome, `handleRestaurantDetail`) | Detail endpoint includes the three `has_*` flags in its response | `restaurants.test.ts :: "handleRestaurantDetail passes the three channel flags through"` | unit | pass |
| — (parser correctness, shared by both discovery paths) | `_shared/openingHours.ts` correctly evaluates the live `{day,open,close,isClosed?,h24?}` shape found in production, not just the originally-assumed `weekday_text` strings | `openingHours.test.ts` — 9 cases: missing/malformed data, live-shape open/closed/`isClosed`/`h24`/overnight-past-midnight, the original string shape (regression guard), an unrecognized shape (fail-open) | unit | pass |

## Failure paths

| Scenario | Expected behaviour | Test |
|---|---|---|
| `opening_hours` null or unparseable | Never excluded; `status` stays `null` (fail-open, `ADR-010`) | `restaurants.test.ts`'s `unknown-hours` fixture row in the AC4-default test; `openingHours.test.ts :: "returns unknown for missing data"` / `"...a plain, non-list string"` / `"...fails open on a per-row shape it does not recognize"` |
| RPC reports function-not-found (`42883`) | Falls back to the direct-query path, which re-applies the same channel gate independently | `restaurants.test.ts :: "...fallback path also excludes a channel-disabled restaurant (AC3)"` |
| Hours span past midnight (open yesterday, still open now) | Correctly counted open, not dropped as "yesterday's shift already ended" | `openingHours.test.ts :: "carries an overnight live-shape range past midnight"` |
| Unrecognized `mode` query param (not one of the three channels) | No channel filter applied — same as today's unfiltered behavior, not an error | Covered by inspection: `channelForMode` returns `null` for any key outside `CHANNEL_BY_MODE`, and both call sites treat `null` as "no filter" (RPC's own `p_channel IS NULL` branch; fallback's `if (channel)` guard). Not a dedicated test — pre-existing precedent from `ENG-044`'s own equivalent gap on the RPC's other free-text params (`agents/qa/notebook/2026-09-07-coverage-gaps.md`), not a new one this ticket introduces. |

## Regression risk

**None to the pre-existing, independent `orderingLink` filter** — untouched
by this diff (confirmed: `git diff origin/main...HEAD` touches no line of
that filter's own logic in either discovery path), and the ticket's own
Notes are explicit that the two gates are `AND`ed, neither replacing the
other.

**`total`/`hasMore` stay pre-filter, unchanged by this ticket** — both
counts are computed from the RPC's `total_count` / the fallback's `count:
'exact'`, before the `open_now` exclusion runs (`ADR-010`'s accepted
pagination trade-off, not a bug). No test asserts `total`/`hasMore`
directly; this is a known, named trade-off, not an unnoticed gap.

## Not automated

- **The pre-existing `orderingLink` filter's own success/failure paths** —
  `restaurants.ts` had no test file at all before this ticket; this gap
  predates this diff and isn't this ticket's to close.
- **`total`/`hasMore` under the `open_now` exclusion** — see Regression risk
  above; the trade-off is `ADR-010`'s decision, not a code path this ticket
  should add pagination-compensation tests for.
- **A live end-to-end call against the real `bmnmnejwdxbcqinqkwko` project**
  — out of scope per every test file's own boundary (no test touches
  production). The build hop's own read-only live check (`p_channel='dine_in'`
  narrowing 211→209 rows, zero leaks) stands as evidence but is manual, not
  part of this suite.

## Result

- **Run:** `deno test --no-check`, 2026-09-07, this worktree
  (`~/Documents/projects/_eng/aiorders-api`,
  `feat/ENG-045-foodswipe-channel-visibility-discovery-handlers@5f35b92`).
- **`openingHours.test.ts`:** 9 passed, 0 failed, 0 skipped.
- **`restaurants.test.ts`:** 6 passed, 0 failed, 0 skipped.
- **Total: 15/15 pass**, independently re-run this round (not taken on the
  build or round-1 hop's own account) — matches both prior hops' counts
  exactly.
- **`deno check`** on all four touched/relevant files: clean.
- **`deno lint`** on `restaurants.ts`: 5 problems (baseline 4 +
  `visibleRestaurants`'s load-bearing `any`) — informational only, not a
  registered gate for this project.
- **Bugs filed:** none. No real defect found.
- **Verdict: PASS.** Both owned criteria (AC3 in full, AC4's backend half)
  covered by tests proven to fail for the right reason (round 1's mutation
  check, not re-run this round since the only delta since is a
  non-executable visibility keyword — see
  `agents/principal-engineer/reviews/ENG-045.md`). Ticket proceeds past this
  gate; code review also passed this round.
