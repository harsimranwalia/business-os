---
ticket: ENG-022
project: aiorders-api
author: qa
created: 2026-09-03
suite_command: "cd supabase/functions/brand-portal && deno check utils.ts feedback.ts customers.ts hiring.ts website.ts offers.ts && deno test --no-check feedback.test.ts customers.test.ts hiring.test.ts website.test.ts offers.test.ts (must run from inside supabase/functions/brand-portal/ — from the repo root, Deno's npm-specifier resolution fails with a byonm/node_modules error even though the same deno.json is picked up either way; not registered in config/projects.md's Commands table, no deno.json at repo root)"
last_run: 2026-09-03
last_result: pass
coverage_note: all 4 acceptance criteria covered; 19 negative tests (one per call site, exhaustive against AC2's own list) plus 5 positive tests (one per file/code-path shape, sampling AC3 — see Acceptance coverage row 3 for why per-file sampling is adequate here)
---

# Test plan — Brand-portal restaurant-scoped access check (ENG-022)

## Acceptance coverage

Against `agents/product-manager/specs/ENG-022-brand-portal-tenant-isolation-broken.md`.

| AC | Criterion | Test | Level | Status |
|---|---|---|---|---|
| 1 | Authenticated user with access to restaurant A only, calling any listed action with restaurant B's id, is denied | All 19 negative tests below (row 2) — each is exactly this scenario for one action | unit | pass |
| 2 | Full audit — every `verifyRestaurantAccess` call site in `brand-portal/` denied for restaurant B, not just the ones named in the ticket title | 19 `Deno.test` cases, one per call site: `feedback.test.ts` (`get_feedback`); `customers.test.ts` (`get_customers`, `get_customer`, `create_customer`, `update_customer`, `delete_customer`); `hiring.test.ts` (`get_jobs`, `update_jobs`, `get_candidates`); `website.test.ts` (`get_website_content`, `update_website_content`); `offers.test.ts` (`get_offers`, `create_offer`, `update_offer`, `delete_offer`, `toggle_offer_status`, `create_special`, `update_special`, `delete_special`) — matches the PRD's own 19-action enumeration one-for-one, confirmed by counting both lists side by side | unit | pass |
| 3 | Legitimate owner of restaurant A: behavior unchanged | 5 positive tests, one per file: `getFeedback returns feedback...`, `getCustomers returns customers...`, `getJobs returns jobs...`, `getWebsiteContent returns content...`, `getOffers returns offers...` — one action per file rather than all 19. **Deliberate sampling, not a gap**: the fix at every site within one file is the identical one-line substitution (either the same `requireRestaurantAccess` call, or the same argument-order-plus-`.hasAccess` correction), so the thing AC3 is actually about — does the corrected check still grant a legitimate owner access — is a property of the shared helper/pattern per file, not of each of the 19 call sites independently. One positive test per file exercises that shared path; the other 14 actions differ from their file's tested action only in which table/columns they touch after the (already-proven) access check passes, which is unchanged, pre-existing logic this ticket didn't touch | unit | pass |
| 4 | A regression test exists per fixed call site proving the negative case | Same 19 tests as row 2 — each fails against the pre-fix code (see Regression risk) | unit | pass |

## Failure paths

| Scenario | Expected behaviour | Test |
|---|---|---|
| Unauthorised caller (wrong tenant) — all 19 sites | Denied — thrown `Error` for the 4 throw-convention files, `{success:false}` for `offers.ts` | The 19 negative tests above |
| Legitimate caller, correct tenant | Served normally, real data returned | The 5 positive tests above |
| `verifyRestaurantAccess`'s own internal DB error (e.g. `restaurants` lookup fails) | Denied, not a crash — pre-existing `catch` in `verifyRestaurantAccess` returns `{hasAccess:false, error}` unchanged by this diff | Not directly tested this round — pre-existing behavior, not a new path this ticket introduces; out of scope per the PRD's own non-goals ("`verifyRestaurantAccess` itself... is correct; every failure is at the call site") |
| Admin/sub-admin caller (bypasses restaurant-level check by design) | Access granted regardless of tenant — this is intentional, pre-existing behavior, not the bug this ticket fixes | Not tested this round — every fake in this ticket's test files stubs `profiles` to return no admin role, so the admin-bypass branch is deliberately not exercised by these tests; pre-existing and unchanged, same scope boundary as the row above |

## Regression risk

**Every one of the 19 negative tests is itself the regression test AC4 asks
for, independently confirmed this round** (not taken on the build pass's own
account): temporarily disabled the access check at `customers.ts:73`
(`getCustomers`, throw-convention) and `offers.ts:80` (`getOffers`,
return-convention), one from each of the two code-path families, and re-ran
both affected test files. Exactly the two matching negative tests
(`getCustomers denies...`, `getOffers denies...`) went red; every other test
in both files — including the other 4 untouched `customers.ts` call sites and
7 untouched `offers.ts` call sites — stayed green. Reverted immediately after
(`git status --short` confirms the worktree matches the ticket's own recorded
commit, only the pre-existing untracked `deno.lock` present). This is direct
evidence the tests fail against the old (broken) behavior, not an inference
from test shape — the same standard `test-suite-run/SKILL.md` and
`engineering-standards.md`'s mutation rule ask for.

No other regression surface: this diff touches only the access-check call
sites and one doc comment. No query shape, response field, or route changed
for a request that was already correctly authorized.

## Not automated

- **The admin/sub-admin bypass branch and `verifyRestaurantAccess`'s own
  internal-error path** (Failure paths table above) — both pre-existing,
  unchanged by this diff, outside the PRD's own non-goals boundary. Not a gap
  this ticket needs to close.
- **A live end-to-end call against a real Supabase project** — out of scope
  per every test file's own boundary (`engineering-standards.md`: no test
  touches production; `definition-of-done.md`: synthetic test data only).
  The fake `SupabaseClient` stub is a minimal, table-routed
  `.from().select().eq()...single()`/thenable chain, not a live connection.

## Result

- **Run:** `deno check` + `deno test --no-check`, 2026-09-03, this worktree
  (`~/Documents/projects/_eng/aiorders-api`, `fix/ENG-022-brand-portal-tenant-isolation@d5078c5`),
  from inside `supabase/functions/brand-portal/` (see `suite_command`).
- **`deno check`** on the 6 changed files: **10 errors**, all pre-existing
  and outside this diff's changed lines (3 in `hiring.ts`'s untouched catch
  blocks, 3 in `utils.ts`'s unrelated `getUserAccessibleRestaurants`, 4 in
  `website.ts`'s untouched catch blocks and a supabase-js generic-select
  quirk) — independently re-derived, not trusted from the ticket log's own
  19→10 claim.
- **`deno test --no-check`** on the 5 new test files: **24 passed, 0
  failed, 0 skipped.**
- **Bugs filed:** none. No real defect found in the code or the tests.
- **Verdict: pass.** All 4 acceptance criteria covered (AC3's per-file
  sampling reasoned above, not silently assumed adequate). Regression
  evidence for AC4 executed directly this round, not carried from the build
  pass. No open P0/P1 bug anywhere on this board (`agents/qa/bugs/_index.md`:
  one open item, `BUG-001`, P2, unrelated project area).
