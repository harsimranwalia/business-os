---
ticket: ENG-048
project: aiorders-api
author: qa
created: 2026-09-07
suite_command: none — the diff is one SQL migration file, no `.ts` changed. `aiorders-api`'s row in `config/projects.md`'s Commands table is empty across Test/Lint/Typecheck/Build (no `package.json`, no `deno.json`).
last_run: 2026-09-07
last_result: no suite
coverage_note: Round 2. This ticket owns parent ENG-027's AC4, AC5, AC8, AC9, AC12 in full, and the guard/crediting half of AC1, AC2, AC3, AC7, AC11, AC15, AC16 (agents/eng-manager/notebook/2026-09-07-eng027-work-breakdown.md). Round 1's own two failing rows (unauthorised caller, over-discounted order) independently re-tested this round against a fresh disposable replica — not re-accepted on the fix's own text.
---

# Test plan — Loyalty ledger schema, credit function, and auto-complete cron (ENG-048), round 2

## Round 1 recap

Round 1 (`agents/qa/notebook/2026-09-07-coverage-gaps.md`) found the same two
root causes code review named (B1 unauthorised caller, B2 over-discounted
order) via inspection, discarded with no receipt per
`code-review-gate/SKILL.md` step 9 ("QA writes no receipt for a discarded
round either"). This is the first QA receipt this ticket has produced.

## Failure paths — the two round-1 fails, re-tested this round

| Scenario | Expected behaviour | Round 1 | Round 2 |
|---|---|---|---|
| Direct RPC call bypassing the webhook/sweep (`supabase.rpc('credit_order_if_eligible', ...)` from `anon`/`authenticated`) | Rejected — service-role-only | **Failed** — no grant/revoke at all | **Pass** — independently re-tested against a fresh disposable replica (not the one on file): `has_function_privilege` returns `anon → false`, `authenticated → false`, `service_role → true`. Confirms the *corrected* fix (naming `anon`/`authenticated` directly), not round 1's own literal recommendation, which the database agent's own re-test found insufficient on this project's `pg_default_acl` setup. |
| `bill.discount > bill.cart` (over-discounted order) | Some defined, non-negative outcome | **Failed** — produced a negative row | **Pass** — independently re-tested with a fixture built fresh this round (`cart: 20, discount: 35`): `credited`, `amount 0.00`, `points 0.0000`. Floored, not negative. |
| Cancelled order, credit attempted | Blocked, zero ledger rows, `already_processed` | Pass (round 1) | Unaffected by this round's diff (`git diff 9fccdad 60fa06e` touches only the two rows above) — not re-tested, no re-test needed. |
| Two concurrent callers (webhook + sweep) | Exactly one credit | Pass (round 1) | Unaffected by this round's diff — not re-tested. |
| Repeat call on an already-credited order | `already_processed`, no second ledger row | Pass (round 1, implicit) | **Re-tested as part of this round's own regression check** (below) — still holds after both fixes. |
| Malformed/non-numeric `bill.cart`/`bill.discount` | Some defined outcome | Accepted shape, not a gap (round 1) | Unaffected by either fix — not re-tested. |

## Acceptance coverage (fully-owned: AC4, AC5, AC8, AC9, AC12)

Unaffected by this round's diff — neither fix touches the
`loyalty_processed_at` guard, the identity walk, or the rate lookup. All five
still hold on the same basis round 1 established (inspection plus the
disposable-replica evidence on file, with AC12 independently re-traced through
real application code — `agents/principal-engineer/notebook/2026-09-07-review-log.md`,
round 1, point 2). Not re-derived this round; nothing in scope changed them.

## Regression check performed this round

Repeat-call idempotency, on the same fixture order used for the B2 re-test,
with the *other* `fulfillment_reason` (`window_elapsed` after `reported`):
`already_processed`, ledger row count still `1`. The guard clause is untouched
by either fix (`git diff 9fccdad 60fa06e` — confirmed, only the `v_amount`
line and the new revoke/grant block changed), so this is confirmation, not a
new finding.

## Not automated

Same two reasons as round 1 and every prior `aiorders-api` migration ticket:
no suite exists for this project (open proposal, 2026-08-29); the two re-tests
above ran against a disposable replica by direct query, not through any
harness. Container removed after use; nothing left running.

## Result

- **Run:** none — no suite command applies. Recorded as `no suite` per
  `test-suite-run/SKILL.md` step 8.
- **Outcome:** n/a — nothing executed through a suite. Evidence class is a
  fresh, independent disposable-replica re-test of exactly the two things
  round 1 found wrong, plus one regression check.
- **Bugs filed:** none. Ledger (`agents/qa/bugs/_index.md`) has one open item,
  `BUG-001`, P2, unrelated project area (production cron).
- **Verdict: pass.** Both round-1 failing rows now pass, independently
  re-tested rather than accepted on the fix's own text; no acceptance
  criterion this ticket owns lacks coverage; not a bug-fix ticket missing a
  regression test (the two defects were caught pre-ship, and both fixes are
  independently confirmed against a fresh replica, not just the one used to
  develop them); 0 open P0/P1. Continues to `in-qa` — code review passed
  concurrently on the same diff (`agents/principal-engineer/reviews/ENG-048.md`).
