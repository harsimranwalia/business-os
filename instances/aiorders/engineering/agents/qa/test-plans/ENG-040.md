---
ticket: ENG-040
project: aiorders-api
author: qa
created: 2026-09-05
suite_command: "cd supabase/functions/brand-portal && deno check website.ts && deno test --no-check website.test.ts (must run from inside supabase/functions/brand-portal/ — see ENG-022's own test plan for why; not registered in config/projects.md's Commands table, no deno.json at repo root)"
last_run: 2026-09-05
last_result: pass
coverage_note: AC4 (in full) and AC3 (write half) — this ticket's owned criteria, per agents/eng-manager/notebook/2026-09-05-eng021-work-breakdown.md's AC-ownership mapping. AC5 has no code-level test surface (satisfied structurally by not building a second store — see Acceptance coverage row below).
---

# Test plan — Brand-portal FAQ write path (ENG-040)

## Acceptance coverage

Against `agents/product-manager/specs/ENG-021-chat-bar-engagement-and-faq-self-service.md`,
scoped to the ACs this ticket owns (`agents/eng-manager/board/ENG-040-*.md`'s
own "AC ownership" note; AC1/AC2/AC6 and the UI half of AC3 belong to
`ENG-041`).

| AC | Criterion | Test | Level | Status |
|---|---|---|---|---|
| 3 (write half) | Owner can create/edit a website FAQ from the brand portal, no staff involvement | `updateWebsiteContent persists a faqs payload for a restaurant the caller manages` (new, this round) — proves `update_website_content` accepts `{faqs: [...]}` and the insert branch receives exactly `{restaurant_id, faqs}` | unit | pass |
| 4 | The brand-portal editor and the bot read/write the same `restaurant_website.faqs` column, not a separate list | Two round trips: `getWebsiteContent preserves an empty faqs array...` and `...returns faqs content...` (both from the build hop) prove the read side; the new write test above (row 1) proves the write side lands on the same column via the same generic per-key loop `catering`/`careers` already use | unit | pass |
| 5 | Staff admin-hub editor and brand-portal editor stay in sync (one store, two editors) | Not a code path this ticket adds — satisfied structurally: no second table, no second write path, `aiorders-admin-hub`'s own editor is untouched (design's own `## Components`, "no change" row). Nothing to test beyond confirming no second store was introduced, which the diff itself (3 files, no new table/migration) already shows | — | n/a — structural, not testable in isolation |

## Failure paths

| Scenario | Expected behaviour | Test |
|---|---|---|
| Unauthorised caller (wrong tenant) calls `update_website_content` | Denied before the per-key loop runs — `faqs` never reaches a code path the existing denial test doesn't already cover | `updateWebsiteContent denies a restaurant the caller does not manage` (pre-existing, unchanged by this ticket) |
| Owner deletes every FAQ (`faqs: []`) | Persists as `[]`, not nulled | `getWebsiteContent preserves an empty faqs array rather than nulling it` (build hop) |
| No `restaurant_website` row yet for this restaurant | `PGRST116` treated as "no content yet," not an error; write path takes the insert branch | Existence-check branch exercised directly by this round's new write test (stubs `PGRST116` to force insert) |

## Regression risk

**None to `catering`/`careers`.** `updateWebsiteContent`'s loop is unchanged
code, generic over `EditablePage`; the only literal that changed is the
`EDITABLE_PAGES` array. Mutation-checked this round (see Result) by removing
`'faqs'` from the array and re-running: only the new write test went red,
confirming the fix is both necessary and correctly isolated to the `faqs`
key — nothing about `catering`/`careers` shares a code path with `faqs` that
this diff could regress.

**`deno check`**: independently re-derived, 7→8 errors, the one new hit
pre-existing in shape (same `GenericStringError`/generic-select cause as the
two `catering`/`careers` instances). Not a regression signature — see the
review receipt.

## Not automated

- **`catering`/`careers`' own write-path success** (does `updateWebsiteContent`
  actually persist those two keys) — genuinely untested, but pre-existing:
  this file's fake Supabase client had never mocked `.update()`/`.insert()`
  for *any* key before this round. Extending the harness far enough to prove
  `faqs` writes correctly (this ticket's own scope) also makes closing the
  other two keys' identical gap a much smaller lift than it was — but doing
  that now would be fixing pre-existing technical debt inside an XS ticket
  that doesn't own it. Filed instead: `agents/eng-manager/proposals.md`,
  2026-09-05 row.
- **A live end-to-end call against the real `bmnmnejwdxbcqinqkwko` project**
  — out of scope per every test file's own boundary (no test touches
  production).
- **AC4's bot-side read** (does the chat-bar function actually read from the
  same `restaurant_website.faqs` column this ticket writes to) — that
  function isn't part of this diff and wasn't touched; the design's own
  read of the schema is the basis for AC4's "same column" claim, not
  something this ticket's tests can independently verify without reading a
  second function this ticket has no reason to touch.

## Result

- **Run:** `deno check` + `deno test --no-check`, 2026-09-05, this worktree
  (`~/Documents/projects/_eng/aiorders-api`,
  `feat/ENG-040-brand-portal-faq-write-path@104b057`), from inside
  `supabase/functions/brand-portal/`.
- **`deno check website.ts`**: **8 errors** — independently re-run, matches
  the build hop's own 7→8 account exactly (one new `TS2339` at the `faqs`
  line, same cause as the two pre-existing `catering`/`careers` hits).
- **`deno test --no-check website.test.ts`**: **6 passed, 0 failed, 0
  skipped** (5 from the build hop + 1 new this round).
- **Mutation check on the new test:** temporarily dropped `'faqs'` from
  `EDITABLE_PAGES`, re-ran — exactly the new write test failed
  (`result.success`: `true` → `false`), all 5 others held. Reverted,
  re-ran clean, `git status --short` confirmed only the intended test-file
  edit remained before committing.
- **Bugs filed:** none. No real defect found.
- **Verdict: pass.** Both owned ACs (4, and 3's write half) covered with a
  test that's actually evidence, not just present; AC5 confirmed structural.
  One coverage gap named and routed to a proposal rather than silently
  claimed as covered or silently fixed beyond scope.
