---
ticket: ENG-005
project: aiorders-admin-hub
author: qa
created: 2026-08-27
suite_command: none — aiorders-admin-hub has no test command registered (config/projects.md Commands table: lint + build only)
last_run: 2026-08-27
last_result: pass (structural/build verification; no automated suite exists for this project)
coverage_note: smoke-level only, explicit PRD non-goal ("does not pursue comprehensive testing of the component either way")
---

# Test plan — Wire A4PosterGenerator into RestaurantDetails.tsx

## Acceptance coverage

| AC | Criterion | Check | Level | Status |
|---|---|---|---|---|
| 1 | Given the admin hub after this ships, when a human navigates to a restaurant's detail page, then `A4PosterGenerator` renders without error in a new section | `npm run build` succeeds against the full page (a broken import, a bad prop type, or invalid JSX in this new block would fail the TypeScript/JSX compile step); structural read confirms the new `<Card>` sits inside the same `restaurant &&` — guarded render branch as every other card on the page, so it mounts only once `restaurant` is loaded, same as the rest of the page | build + structural review | pass |
| 2 | Given the same, then the poster section is reachable via the page's existing navigation, not only a direct URL | No new route, no new nav entry added or needed — placement is a new `<Card>` on an already-registered, already-linked-to route (`/restaurants/:id/details`); confirmed by reading the diff (route file itself unchanged) | inspection | pass |

## Failure paths

This ticket adds a UI section to an existing authenticated page — it
introduces no new input, no new endpoint of its own, and no data write. The
one runtime failure path that exists is inherited from the (unmodified)
`A4PosterGenerator` component now becoming reachable for the first time:

| Scenario | Expected behaviour | Check |
|---|---|---|
| Caller has no live Supabase session, or the session's token is rejected | QR fetch returns early / the `list`+`create` calls 401, `qrDataUrl` never sets, poster still renders without a QR — no exception, no page crash | Read `fetchQr()`'s own try/catch and optional-chaining (`?? null`) directly, `src/components/A4PosterGenerator.tsx` — this is pre-existing component behaviour, not introduced by this diff, but newly reachable, so re-confirmed here rather than assumed |
| Restaurant has no `logo_url` / no `website` | Component's own existing fallbacks apply (`baseUrl` empty string guard, `logoUrl` nullable) — no crash | Read the component's props handling; unchanged by this diff |
| `restaurant` not yet loaded | New Card does not render at all — same guard every other card on this page already relies on | Structural read of the page's render branch |

## Regression risk

`touches_data: false`; the diff is additive only (one import, one new JSX
block) with nothing removed and no existing prop, route, or interface changed.
The only thing at risk of regressing is the rest of this same page rendering
correctly, which the successful production build already covers (the whole
file has to compile and bundle, not just the new lines).

## Not automated

No test command exists for `aiorders-admin-hub` — verified directly against
`config/projects.md`'s Commands table (lint + build only, no Test cell) rather
than assumed. Matches this ticket's own PRD non-goal ("does not pursue
comprehensive testing of the component either way — this is sized `S`,
smoke-level at most"). No live dev-server/browser check was run this pass
either — the route requires an authenticated session and real restaurant
data, and this instance's own precedent for a project with no test infra
(`ENG-002`, `ENG-004`) has relied on build + lint + structural review rather
than a live render. Named here as a manual-verification gap with its reason,
per `definition-of-done.md`'s own checklist allowance, not silently assumed.

## Result

- **Run:** `npm run lint` (181 problems — 150 errors / 31 warnings — identical
  on this branch and a clean `origin/main` checkout, zero new) and `npm run
  build` (succeeds, `dist/` emitted) — both 2026-08-27, in
  `_eng/aiorders-admin-hub`
- **Bugs filed:** none
- **Verdict:** pass
