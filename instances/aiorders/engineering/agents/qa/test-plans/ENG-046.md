---
ticket: ENG-046
project: aiorders-admin-hub
author: qa
created: 2026-09-07
suite_command: "none — no test runner registered for aiorders-admin-hub (config/projects.md's Commands table: Test column empty; no `test` script in package.json; zero *.test.*/*.spec.* files anywhere in src/, confirmed this pass). Open, unexpired proposal: proposals.md, 2026-08-31."
last_run: 2026-09-07
last_result: no suite
coverage_note: This ticket owns parent ENG-026's AC2 in full (agents/eng-manager/notebook/2026-09-07-eng026-work-breakdown.md). Verified by inspection against the diff, not execution — see Acceptance coverage.
---

# Test plan — FoodSwipe channel-visibility toggles, admin-hub restaurant details (ENG-046)

## Scope note

Against `agents/product-manager/specs/ENG-026-foodswipe-channel-visibility.md`'s
5 acceptance criteria. This ticket owns AC2 only; AC1/AC5 belong to `ENG-044`
(shipped), AC3/AC4 to `ENG-045`/`ENG-047`.

## Acceptance coverage

| AC | Criterion | Test | Level | Status |
|---|---|---|---|---|
| 2 | Staff can view and set all three flags (`has_order_food`, `has_dine_in`, `has_catering`) per merchant from `aiorders-admin-hub` | No automated test (see Not automated). Verified by inspection: each of the three rows independently traced — `id`/`htmlFor`/`checked`/`onCheckedChange` all reference the same field, for all three fields, with no cross-wiring (confirmed by reading the diff, not assumed from the pattern alone); read mode renders the correct `Badge` per field when `editing` is false, matching the six pre-existing sibling rows' identical, already-working pattern exactly | inspection | pass |
| 1, 3, 4, 5 | Migration/backfill; Dine-In/Catering tab gating; Open Now filter; rollout evidence | N/A for this diff — this ticket is `aiorders-admin-hub` UI only. Owned by `ENG-044` (shipped, `verified`-bound), `ENG-045`, `ENG-047` respectively | N/A | N/A |

## Failure paths

| Scenario | Expected behaviour | Test |
|---|---|---|
| A pre-existing restaurant record with no value for one of the three new columns | Cannot occur — `ENG-044`'s migration backfilled all 243 existing rows under a `NOT NULL` constraint with real boolean values (confirmed against `ENG-044`'s own board-file Log, not merely assumed); the DB makes the missing-value state unreachable, so there is nothing to test | Not automated — verified by inspection of `ENG-044`'s own shipped migration |
| Toggling a switch while `editing` is false | Not reachable — `Switch` only renders in the editing branch; `Badge` (non-interactive) renders otherwise, same existing gate every other row in this card already uses, unchanged by this diff | Not automated — pre-existing pattern, not this ticket's to test |

## Regression risk

- **The six untouched sibling toggle rows** (`show_in_marketplace`, `cafe`,
  `bar`, `party_hall`, `live_catering`, `food_truck`, `dns_access_shared`,
  `google_business_shared`) — confirmed via diff that none of their markup
  changed.
- **Anything else in the repo reading the old `dine_in` field** — repo-wide
  grep for `\bdine_in\b` outside `has_dine_in` found one hit,
  `RestaurantAIWebsite.tsx:802`'s `conditions.location.dine_in` — read
  directly: an unrelated CSV-menu-import feature's per-dish availability
  flag (a different domain concept entirely, not this table or this local
  `Restaurant` interface), confirmed not affected by this ticket's repoint.

## Not automated

**No suite exists for `aiorders-admin-hub`** — confirmed fresh this pass:
`package.json` has no `test` script and no test-framework dependency
(`vitest`/`jest`/`@testing-library`); a repo-wide search found zero
`*.test.*`/`*.spec.*` files anywhere under `src/`; `config/projects.md`'s
Commands table carries an empty Test cell for this project. This is not a
new gap this ticket introduces — none of the six pre-existing toggle rows in
this same card have a test either, and the gap is already tracked as an open,
unexpired proposal (`proposals.md`, 2026-08-31, sized `M`: "wiring `vitest` +
`@testing-library/react` into an existing Vite project"). Not re-filed here.

Given no harness exists to add a test *to*, and this ticket's own diff is a
pure prop-through UI change with no new logic branch (no computed value, no
conditional beyond the binary switch pattern eight other flags in this same
file already use untested), the correct evidence class is inspection against
the actual diff, done above under Acceptance coverage, plus the independent
build/lint/typecheck re-run recorded in `agents/principal-engineer/reviews/ENG-046.md`
— not "tested" by assertion.

## Result

- **Run:** none — no suite command applies (see `suite_command` above).
  Recorded as `no suite` per `test-suite-run/SKILL.md` step 8, not rounded up
  to a pass.
- **Outcome:** n/a — nothing executed. Evidence class is inspection plus the
  independent `npm run build` / `npx tsc --noEmit` / `npx eslint` re-run
  (`agents/principal-engineer/reviews/ENG-046.md`), all clean.
- **Bugs filed:** none. No real defect found.
- **Verdict: pass.** No quality-gate fail condition is met: no suite is red
  (none exists), the one acceptance criterion this ticket owns is covered by
  inspection against the actual diff, this is not a bug fix missing a
  regression test, no P0/P1 bug is open. Continues to `in-security` — code
  review also passed on the same diff
  (`agents/principal-engineer/reviews/ENG-046.md`).
