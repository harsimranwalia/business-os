---
ticket: ENG-008
project: aiorders-admin-hub
author: qa
created: 2026-08-31
updated: 2026-09-02
suite_command: "aiorders-api: deno test --no-check --allow-net, scoped to admin-portal/handlers/influencers.test.ts (no deno.json exists yet — projects.md's own open item). aiorders-admin-hub: npm run lint / npm run build (no test script registered — projects.md's own Commands table)"
last_run: 2026-09-02
last_result: "aiorders-api: deno test executed for real (deno 2.9.6 on PATH this host) — 19 passed, 0 failed. aiorders-admin-hub: clean build, 1 pre-existing lint warning on the touched file, 0 new"
coverage_note: every acceptance criterion has either a direct Deno.test case or a hand-trace against the code at HEAD (see rows below). Frontend has no test runner on this project (projects.md); its acceptance criteria are traced by hand against the committed code.
---

# Test plan — Influencer board admin management (ENG-008)

## Round 3 update: `accepts_barter` dropped, `barter_visit` reused

The approver's merge-request reply (2026-09-01, reached this host 2026-09-02)
rejected the redundant `accepts_barter` column. The fix hop renamed every
reference to the pre-existing `barter_visit` column instead — this revision
updates AC3/AC4's coverage rows and the failure-path table below to match;
substance elsewhere (AC1/2/5/6/7/8) is unchanged since round 2 and not
re-derived.

## Acceptance coverage

Against
`agents/product-manager/specs/ENG-008-influencer-profile-admin-management.md`.

| AC | Criterion | Test | Level | Status |
|---|---|---|---|---|
| 1 | Staff can see a previously-unset region persist once set | Backend: no dedicated `city_preference` success test exists, but `handleInfluencers PATCH returns the updated row on success` proves the shared write mechanism (`if ('x' in body) validate; update.x = ...` then one `.update(update).eq(id).select()` call) for `staff_rating`/`collaboration_count`; `city_preference` uses the identical mechanism with its own validation independently tested (`rejects a non-array city_preference`, `rejects a...non-string element`) — no field-specific branching exists past validation, confirmed by reading `updateInfluencer` in full at HEAD | unit (executed) + inspection | pass |
| 2 | An already-set region can be edited and the board reflects it | Same mechanism as AC1 — editing is the same PATCH path with a non-empty prior value; no separate code path for "first set" vs "edit" anywhere in the handler or the form | inspection | pass |
| 3 | An unset campaign-type preference can be set to paid, barter, or both | `rejects a non-boolean accepts_paid` / **`rejects a non-boolean barter_visit`** (renamed from `...accepts_barter` this round, mutation-verified below) prove validation; the shared success-path mechanism (AC1) proves a valid value writes; frontend: two independent `Checkbox`es (not a radio) on `accepts_paid`/`barter_visit`, so "both" is representable at the UI layer — the table's badge row renders both `Paid` and `Barter` simultaneously when both are true, hand-confirmed against the JSX | unit (executed) + inspection | pass |
| 4 | An already-set campaign-type preference can be edited | Same as AC3; `openInfluencer` seeds both checkboxes independently from `accepts_paid`/`barter_visit` (widened to `boolean \| null` this round to match the live schema), not from a legacy combined field | inspection | pass |
| 5 | An unrated influencer can be rated | `rejects a staff_rating above 5` / `...below 1` prove validation range; `PATCH returns the updated row on success` sets `staff_rating: 4` and asserts it round-trips; frontend `Select` offers "Not rated" plus 1–5 | unit (executed) + inspection | pass |
| 6 | An already-rated influencer's rating can be changed | Same mechanism as AC5; backend explicitly allows `staff_rating: null` to clear it | unit (hand-traced) | pass |
| 7 | Collaboration count can be set or incremented | `rejects a negative collaboration_count`; success test sets `collaboration_count: 3` | unit (executed) | pass |
| 8 | A non-admin/non-staff request to any write path is rejected | 6 unit tests on `hasInfluencerAdminAccess` + 1 integration test (`rejects a non-admin/sub-admin caller with 403`), all unchanged and re-executed this round | unit (executed) + inspection | pass |

## Round-1/round-2 gaps, closed and unchanged this round

Automatic-failure #10 (code review round 1): zero test coverage on the new
admin-gated write path — closed by round 2's `57f8c4b`
(missing-profile/allowlist tests). The null-coalescing bug (round 1's second
finding) — closed by round 2's `aiorders-admin-hub@63be255`. Neither
regressed by this round's rename; see code review's round-3 receipt for the
independent re-verification of both.

## This round's own change: `accepts_barter → barter_visit`

Not a bug fix in the classic sense — a design correction ordered by the
approver. Verified as complete rather than assumed:

- Grepped `accepts_barter` across both repos' full tracked trees (not just
  the diff): **zero remaining references** in code. The only surviving
  mentions are the migration doc's own explanatory prose describing what was
  removed.
- **Executed `deno test` for real this round** (this host has `deno` 2.9.6 on
  `PATH`): 19/19 pass, 12ms.
- **Mutation-tested the renamed validation**: reverted
  `typeof body.barter_visit !== 'boolean'` by hand, re-ran — exactly
  `handleInfluencers PATCH rejects a non-boolean barter_visit` went red
  (18/19), nothing else. Confirms the renamed test still guards a real
  branch rather than passing vacuously after the rename.
- **Null-safety preserved by construction**: `barter_visit` now goes through
  the identical read-straight-through / `?? false` render / conditional-PATCH-
  inclusion pattern `accepts_paid` already used post-round-1 — no coalescing
  fallback was reintroduced for the renamed field.

## Failure paths

| Scenario | Expected behaviour | Test |
|---|---|---|
| Invalid input at the boundary — non-array/wrong-element-type `city_preference` | 400, specific message, nothing written | `rejects a non-array city_preference` / `...a city_preference array with a non-string element` |
| Invalid input at the boundary — non-boolean `accepts_paid`/`barter_visit` | 400, specific message | `rejects a non-boolean accepts_paid` / **`rejects a non-boolean barter_visit`** (renamed, mutation-verified this round) |
| Invalid input at the boundary — non-number `min_visit_payment` | 400, specific message | `rejects a non-number min_visit_payment` |
| Invalid input at the boundary — `staff_rating` outside 1–5 | 400, specific message, `null` still accepted to clear | `rejects a staff_rating above 5` / `...below 1`; null-clearing path hand-traced |
| Invalid input at the boundary — negative `collaboration_count` | 400, specific message | `rejects a negative collaboration_count` |
| Empty PATCH body | 400, nothing written | `rejects an empty body` |
| Unauthorised caller (GET or PATCH) | 403, denied before any database call | `rejects a non-admin/sub-admin caller with 403` + the 6 `hasInfluencerAdminAccess` unit tests |
| Disallowed method (DELETE etc.) | 405, checked before role | `rejects DELETE with 405 for an admin` |
| Privilege-escalation-shaped body (`role`, `is_admin`, `id` mixed with a valid field) | Only the valid field reaches the database | `PATCH strips fields outside the editable allowlist before writing` |
| Downstream Supabase error | 500 with a message, logged server-side | Not tested; traced by hand, same pre-existing shape every handler in this repo carries |
| Null preference round-trip (round-1 regression, both fields) | Opening a never-set influencer pre-checks neither checkbox; saving an unrelated field does not write a fabricated value | Not automated (no frontend test framework); hand-traced — `barter_visit` now follows the identical pattern `accepts_paid` already used |
| **Seam risk: `ENG-009`'s branch, already gated and PR'd, is stacked on this ticket's pre-fix commits** | `ENG-009` was rebased onto `ENG-008`'s round-2 tip earlier today specifically to fix this same staleness pattern (its own round-1 review caught it), passed round 2, then `ENG-008` moved again — so it's stale a second time. Not re-tested here — different ticket, different branch. Named so it isn't silently missed twice; see code review's round-3 receipt and `ENG-009`'s own ticket log for the cross-reference | Not this ticket's test surface — flagged as a regression-risk seam between two tickets, not a gap in this one. A proposal for a systemic check already exists (`proposals.md`, 2026-09-02) |

## Regression risk

Additive on both repos, unchanged assessment from round 2 for everything
except the renamed field: `aiorders-api`'s new route is an added `else if`
branch, every existing route above it unreached by this diff;
`aiorders-admin-hub`'s only removed code is the read-only "Location
Preferences" display block, superseded by the new editable region field.
**One new seam risk this round** (not a regression in this ticket's own
code, but a regression risk this ticket's own fix creates for a sibling): see
the failure-path table's last row above.

## Not automated

- **Any frontend interaction on `aiorders-admin-hub`.** Unchanged — no test
  runner exists on this repo; proposal already filed 2026-08-31. Hand-traced
  the renamed field's null-preference round-trip and checkbox-independence
  against the code at HEAD.
- **The migration itself**, not re-verified here — `database`'s own gate
  already carries the live-schema verification; not this gate's job to
  repeat. The amended migration doc's own numbers (306 rows / 226 true / 29
  false / 51 null on `barter_visit`) are unchanged from the original since
  this round only dropped a column, it didn't touch the backfill logic for
  `accepts_paid`.

## Result

- **Run:** `deno test --no-check --allow-net
  supabase/functions/admin-portal/handlers/influencers.test.ts`,
  2026-09-02, this worktree, real execution (`deno` 2.9.6 on `PATH`): **19
  passed, 0 failed (12ms)**. `deno check` on the modified handler: clean.
  `npm run lint` (`aiorders-admin-hub`): 150 errors / 31 warnings, identical
  to the recorded baseline across every round; `Influencers.tsx` carries
  exactly the one pre-existing `fetchData` warning, zero new. `npm run
  build`: clean, 3340 modules, same pre-existing chunk-size notice.
- **Bugs filed:** none new. Two non-blocking findings from this round are in
  code review's own receipt, not the bug ledger (documentation drift on the
  architect's design doc; the `ENG-009` stacked-branch seam risk above) —
  neither is a defect in this ticket's own shipped behaviour.
- **Verdict: pass.** All 8 acceptance criteria still covered; this round's
  own rename is verified complete (zero remaining `accepts_barter`
  references) and mutation-tested, not just re-read. No open P0/P1 bug
  anywhere on this board (`agents/qa/bugs/` is empty).

## Prior rounds (superseded)

Rounds 1 (2026-08-30, fail) and 2 (2026-08-31, pass) both reviewed a diff
that included `accepts_barter` as a real column/field — full detail in
`agents/principal-engineer/reviews/ENG-008.md`'s own "Prior pass" sections
and this file's git history. Substance not restated here: the full
`GET`-endpoint coverage gap, the negative-`min_visit_payment` non-deviation
note, and the round-2 mutation test on `Boolean(...)` all remain accurate
for the surviving code and are not re-derived in this revision.
