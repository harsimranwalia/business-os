---
ticket: ENG-031
project: aiorders-api
author: qa
created: 2026-09-03
suite_command: none — the diff is one SQL migration file, no .ts changed, so there is nothing for `deno check`/`deno test` to run against (contrast ENG-022's same-day plan, which changed 6 .ts files and ran both). aiorders-api's own row in config/projects.md's Commands table is empty across Test/Lint/Typecheck/Build (no package.json, no deno.json) regardless.
last_run: 2026-09-03
last_result: no suite
coverage_note: 0 of parent ENG-016's 13 acceptance criteria apply to this ticket's own diff — every one requires ENG-032/033/034, none of which exist yet. This ticket's own criterion (the two columns exist exactly as designed) is verified by direct inspection, not execution — see Acceptance coverage and Not automated.
---

# Test plan — Add order-capture columns to catering (ENG-031)

## Acceptance coverage

| AC | Criterion | Test | Level | Status |
|---|---|---|---|---|
| — | None of `ENG-016`'s ACs 1–13 apply to this ticket's own diff. Each one exercises the catering-request endpoint (`ENG-033`) or a frontend selector (`ENG-032`/`ENG-034`) — neither exists yet, and the ticket's own Outcome section says so plainly ("inert until `ENG-033`"). Inventing a test against code that doesn't exist would be testing something adjacent, not this diff. | N/A | — | N/A |
| this ticket's own | `public.catering` gains `action_type text` and `selections jsonb`, both nullable, matching `agents/architect/designs/ENG-016-catering-quote-generator.md`'s `## Data` section field-for-field (names, types, nullability, no default/CHECK/enum/index, comment shape) | Direct inspection: migration file diffed against the design's `## Data` section line-by-line, independently re-run this pass (not trusted from `agents/principal-engineer/reviews/ENG-031.md`'s own account) | inspection | pass |

## Failure paths

No runtime failure path exists yet for either column — nothing reads or writes them until `ENG-033` ships (confirmed this pass: `grep -rln "action_type\|selections" supabase/functions/` returns zero hits). The template's standard failure-path rows (invalid input, downstream timeout, unauthorised caller, concurrent write) all presuppose a code path that exercises the change; none does here. Deferred to `ENG-033`'s own test plan, where these fields first become reachable.

## Regression risk

Additive-only, `add column if not exists`, no default, no backfill — every existing row is unaffected by construction. Independently re-verified this pass, not carried from the build or review hop's own account:

- `grep -rn "action_type\|selections" supabase/migrations/` — only this ticket's own migration file defines either column; no collision with any prior migration.
- `grep -rln "action_type\|selections" supabase/functions/` — zero hits anywhere in the function tree.
- New migration's timestamp (`20260903130000`) sorts after every on-disk migration.

Zero blast radius on existing behaviour: no existing caller (`restaurant-marketplace`'s direct insert, the GoHighLevel path) sends either field today, and none is required to.

## Not automated

Schema correctness verified by inspection, not by an executed test, for two independent reasons, both named rather than hidden:

1. **No live Postgres or Supabase MCP connection reachable this session** — the same constraint `agents/database/migrations/ENG-031-catering-order-capture-migration.md` and `agents/principal-engineer/reviews/ENG-031.md` both already recorded, tracked as an open, unexpired proposal (`proposals.md`, 2026-08-29, eng-manager).
2. **No application code exists yet to exercise either column** — this is the correct state per the design's own rollout order, not a gap this ticket could close alone.

A live end-to-end insert against a real Supabase project is out of scope regardless (`engineering-standards.md`: no test touches production).

## Result

- **Run:** none — no suite command applies to a pure-DDL diff with no application code changed (see `suite_command` above). Not the same as a red suite; recorded as `no suite` per `test-suite-run/SKILL.md` step 8, not rounded up to a pass.
- **Outcome:** n/a — nothing executed.
- **Bugs filed:** none. No open P0/P1 anywhere on this board (`agents/qa/bugs/_index.md`: one open item, `BUG-001`, P2, unrelated project area — production cron, not this ticket).
- **Verdict: pass.** None of the quality gate's own fail conditions (`agents/qa/agent.md` → "The quality gate") are met: the suite isn't red (there is none to be red), no acceptance criterion lacks a passing test (none of the parent's apply to this diff, and this ticket's own criterion is covered by inspection), this isn't a bug fix missing a regression test, and no P0/P1 bug is open. Continues to `in-security` on the next hop — code review passed concurrently on the same diff (`agents/principal-engineer/reviews/ENG-031.md`).
