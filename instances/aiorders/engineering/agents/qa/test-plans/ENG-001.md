---
ticket: ENG-001
project: aiorders
author: qa
created: 2026-08-26
suite_command: none — no application code exists to run a suite against (ADR-001); lib/eng-gate-check.sh is the mechanical check AC3 names
last_run: 2026-08-26
last_result: pass
coverage_note: verification ticket — each AC is a direct disk/registry check, not an automated test (ADR-001); comprehensive suite coverage does not apply
---

# Test plan — Register this business's repos and prove the loop runs end to end

## Acceptance coverage

| AC | Criterion | Check | Level | Status |
|---|---|---|---|---|
| 1 | Every repo registered in `config/projects.md` at L1, approver-approved | Read `agents/eng-manager/config/projects.md` directly: five rows, all `**L1**`, preamble states approved 2026-07-28, re-verified 2026-08-23 | registry inspection | pass |
| 2 | Department-owned worktree under `_eng/` for each registered repo | Listed `~/Documents/projects/_eng/`; all five present; `git rev-parse --abbrev-ref HEAD` resolved cleanly in each (not empty/broken dirs) | filesystem + git inspection | pass |
| 3 | `lib/eng-gate-check.sh` exits 0 against this instance | Ran `/bin/sh departments/engineering/lib/eng-gate-check.sh` from the instance root this pass | mechanical check | pass |
| 4 | One real ticket has moved `intake → shaped` and the board renders it | Read `ENG-002`'s board file directly: now at `blocked` (PR open), having passed through `shaped` and every state after it; rendered in `_index.md`'s In-flight table | registry inspection | pass |

## Failure paths

This ticket adds no application behaviour, no endpoint, and no user input —
there is nothing with a negative/authz case to test. The one failure mode
that matters for a *registry-and-tooling* ticket is silent drift between what
the board claims and what is actually on disk, which is exactly why every
row above was independently re-read this pass rather than trusted from the
ticket log's prior citations.

| Scenario | Expected behaviour | Check |
|---|---|---|
| A registered project's autonomy silently regresses below L1, or a row goes missing | AC1 fails | Direct read of `config/projects.md`, this pass |
| A worktree is deleted or never created | AC2 fails | Direct `ls` + `git rev-parse` per worktree, this pass |
| `lib/eng-gate-check.sh` is absent, unreadable, or reports a violation | AC3 fails | Direct execution, this pass — exit 0 confirmed |
| No ticket besides `ENG-001` itself ever reaches `shaped` | AC4 fails | Direct read of `ENG-002`'s board file — long past `shaped` |

## Regression risk

None to any application — no registered project carries a diff for this
ticket (`branch:` is empty per ADR-001). The only thing that could regress
here is the registry/worktree state itself drifting out from under a future
ticket, which is exactly what `lib/eng-gate-check.sh`'s own unattended runs
and every future ticket's own pre-pass check continue to guard.

## Not automated

All four ACs are direct-inspection checks by design (ADR-001) — there is no
suite to wire into `test-suite-run` for a ticket with no application code.

## Result

- **Run:** `lib/eng-gate-check.sh` on 2026-08-26 (this pass, from the
  instance root)
- **Outcome:** exit 0, no violations, no parse errors
- **Bugs filed:** none
- **Verdict:** pass — all four acceptance criteria confirmed against disk
