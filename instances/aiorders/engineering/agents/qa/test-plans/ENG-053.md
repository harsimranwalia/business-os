---
ticket: ENG-053
project: aiorders-api
author: qa
created: 2026-09-08
suite_command: none unified (no repo-root `deno.json`) — run from the function's own directory: `cd supabase/functions/brand-portal && deno test --allow-net --allow-env loyalty.test.ts`
last_run: 2026-09-08
last_result: pass
coverage_note: Round 2. This ticket owns AC10 in full, and half of AC3, AC4, AC7, AC8, AC11 — the handler-caller half (`agents/eng-manager/notebook/2026-09-08-eng051-work-breakdown.md`); the guard/debit half of the latter five is `ENG-052`'s own, already covered there (`agents/qa/test-plans/ENG-052.md`). Round 1 discarded on a code-review fail (missing coverage) before a receipt was written — this is the first non-discarded round.
---

# Test plan — Loyalty redemption: brand-portal redeem_points action (ENG-053)

## Scope note

Round 1: code review failed on missing test coverage for `redeemPoints`; this
gate's own coverage lens independently landed on the identical root cause the
same round (`agents/qa/notebook/2026-09-08-coverage-gaps.md`) — acceptance
coverage confirmed correct by inspection but unproven by test, all three
named failure-path rows (unauthorised caller, boundary input, all four RPC
outcomes) unexercised. No suite/bug filed, no receipt written — a fail
discards the round. Round 2's fix hop extended `loyalty.test.ts` with 15 new
tests covering exactly those gaps; this round re-checks the acceptance table
fresh against the current diff.

## Ran the suite myself, not accepted on the fix hop's own counts

`deno test --no-check`, from outside the repo tree (this project's own
`npm:`-resolution workaround, same one every prior ticket on this file has
used):

| File | Round 1 | Round 2 |
|---|---|---|
| `brand-portal/loyalty.test.ts` | 19 passed, 0 failed | **34 passed, 0 failed** (+15 new — all `redeem_points`) |
| Full `brand-portal/*.test.ts` | — | **119 passed, 0 failed** (full-suite regression, nothing broken) |

`deno check` on `loyalty.test.ts`: same 3 pre-existing `utils.ts` errors
(untyped `catch` bindings, one implicit-`any` param) as every prior ticket on
this file has reported — zero in `loyalty.ts` or `loyalty.test.ts` themselves,
confirmed by filtering the output for those two filenames, not assumed.

## Acceptance coverage — fully owned (AC10)

| AC | Requirement (PRD `ENG-051` text) | Test | Result |
|---|---|---|---|
| AC10 | A diner's code and a restaurant staff member without access to that restaurant → rejected the same way every other restaurant-scoped action already rejects an unauthorized caller | `redeem_points: access denied for a staff member with no restaurant access (AC10), RPC never called` — asserts both the thrown `Error` message and `rpcCalls.length === 0` | Pass |

## Acceptance coverage — half-owned (this ticket's handler-caller half only; guard/debit half is `ENG-052`'s, already green there)

| AC | This ticket's half | Test | Result |
|---|---|---|---|
| AC3 | Resolves the submitted code to exactly one diner | Handler passes `code` verbatim as `p_platform_customer_id`, no resolution logic of its own (resolution is `ENG-052`'s, a primary-key lookup) — exercised indirectly by every RPC-outcome test, which all pass `code: "pc-1"` through unchanged | Pass (this ticket's half) |
| AC4 | Only that diner's balance **at that specific restaurant** is affected | `redeemPoints` scopes both the RPC call (`p_restaurant_id`) and the post-outcome `readBalance(supabase, code, restaurant_id)` call to the same `restaurant_id` throughout — `insufficient_balance`/`redeemed` tests seed ledger rows at `r1` only and assert the exact resulting sum, so a restaurant-id mix-up in either call would produce a wrong number, not just an untested path | Pass (this ticket's half) |
| AC7 | Converts redeemed points to a dollar value at the restaurant's own effective rate, writes one permanent ledger entry | Handler-caller half: returns the RPC's live `result` for `redeemed` (the DB function does the conversion and insert, `ENG-052`'s own AC7 half) rather than fabricating a response — `redeem_points: redeemed attaches the diner's current balance` asserts the pass-through result and the correctly-summed post-debit balance | Pass (this ticket's half) |
| AC8 | Diner's balance at that restaurant reflects the debit immediately when read afterward | `redeemed`/`insufficient_balance` tests both assert `readBalance` is called (`queryCount() > 0`) and returns the correctly-summed value against a ledger fixture that already includes the redemption row — proves the handler actually re-reads rather than returning a stale or fabricated number | Pass (this ticket's half) |
| AC11 | Earns and redemptions both visible in one ordered history | Handler-caller half is the type-level widening only (`LoyaltyLedgerEntry.source` gains `'redemption'`) — no new read path, `readBalance` is unchanged and already covered by `get_loyalty_balance`'s own existing tests. Nothing new to test here; confirmed by reading the diff that `readBalance` itself is untouched (0 lines changed) | Pass (this ticket's half — satisfied by the type widening, no runtime behavior to test) |

No sub-ticket proves AC3/AC4/AC7/AC8/AC11 alone, per this ticket's own Notes —
cross-checked against `ENG-052`'s own test plan for the guard/debit half of
each; neither side has a gap.

## Failure-path coverage — the three rows round 1 found unexercised, now closed

| Scenario | Expected behaviour | Result |
|---|---|---|
| Staff without restaurant access calls `redeem_points` | Rejected before any RPC call (AC10) | **Now proven**: `access denied ... RPC never called` asserts both the rejection and `rpcCalls.length === 0` |
| Missing `restaurant_id`/`code`/`idempotency_key`, or non-positive/non-finite/wrong-type `points` | Rejected, zero RPC calls (design's own "no RPC call made") | **Now proven**: 11 tests (3 required-field + 7 invalid-`points` + the field-presence ordering itself), each asserting `rpcCalls.length === 0` alongside the specific rejection message |
| Each of `invalid_code`/`not_enrolled`/`insufficient_balance`/`redeemed` | Distinct, correctly-shaped response per outcome | **Now proven**: 4 tests, one per outcome, each with its own stubbed `.rpc()` result; the two no-balance outcomes additionally assert `readBalance` was never reached (`queryCount() === 0`), and the two balance-attaching outcomes assert the correct summed value |

Every row that was "correct by inspection, but no test proves it" at round 1
now has a test that would fail if the property it names were broken — checked
this isn't cosmetic by reading what each assertion actually pins (call
counts and exact response shapes, not just "no exception was thrown").

## Regression check

Full `brand-portal/*.test.ts` run (119/119, above) covers the file's other
five test groups (`record_dine_in_earn`, `get_loyalty_balance`, `readBalance`,
`handleLoyalty` dispatch, plus every other action file in the directory) —
nothing this round's diff touches broke any of them. The one existing test
this diff modifies (`handleLoyalty routes both actions...` →
`...all three actions...`) still asserts its original two cases
(`get_loyalty_balance`, `record_dine_in_earn`) unchanged, plus the new third.

## Not automated

Nothing on this ticket's own owned or half-owned scope remains untested. The
500-vs-400 HTTP status shape (this ticket's validation throws surface as
`500` via `index.ts`'s generic catch, not a literal `400`) is not a PRD
acceptance criterion and not this ticket's gap to fix — systemic,
already-flagged (`proposals.md`, 2026-09-07), named in both build hops and
round 1's review, unchanged this round.

## Bugs filed

None. Ledger (`agents/qa/bugs/_index.md`) unchanged — one open item,
`BUG-001`, P2, unrelated project area (production analytics cron). 0 open
P0/P1 against this ticket.

## Result

- **Round 1 run:** 19/19 (pre-existing tests only, `redeemPoints` untested) —
  discarded, no receipt, per `code-review-gate/SKILL.md` step 9 (review
  failed the same round).
- **Round 2 run:** `loyalty.test.ts` alone, 34/34; full `brand-portal` suite,
  119/119. 0 failed, 0 skipped.
- **Verdict: pass, round 2.** Every acceptance criterion this ticket owns (in
  full or by half) has a passing, independently-re-run test; all three
  failure-path gaps round 1 found are now closed with mutation-sensitive
  assertions (call counts, exact response shapes), not just green tests; 0
  open P0/P1. Continues to `in-security` — code review passed concurrently on
  the same diff (`agents/principal-engineer/reviews/ENG-053.md`).
