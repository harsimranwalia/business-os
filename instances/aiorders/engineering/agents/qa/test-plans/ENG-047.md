---
ticket: ENG-047
project: restaurant-marketplace
author: qa
created: 2026-09-07
suite_command: "none — no test runner registered for restaurant-marketplace (config/projects.md's Commands table: Test column empty, only Lint/Typecheck/Build exist; no `test` script or vitest/jest/@testing-library dependency in package.json; zero *.test.*/*.spec.* files anywhere under src/, confirmed this pass). No open proposal tracked this gap for this project before now — filed this pass, proposals.md, 2026-09-07."
last_run: 2026-09-07
last_result: no suite
coverage_note: This ticket owns parent ENG-026's AC4 UI half only (chip + status render) — agents/eng-manager/notebook/2026-09-07-eng026-work-breakdown.md. Verified by inspection against the diff, not execution — see Acceptance coverage.
---

# Test plan — FoodSwipe Open Now filter and channel display, consumer marketplace (ENG-047)

## Scope note

Against `agents/product-manager/specs/ENG-026-foodswipe-channel-visibility.md`'s
5 acceptance criteria. This ticket owns AC4's **UI half only** — the chip and
the status render. AC4's filtering/status-computation half, plus AC3, belong
to `ENG-045` (shipped its own 15/15 mutation-checked suite). AC1/AC5 belong to
`ENG-044` (shipped). AC2 belongs to `ENG-046` (parked on the approver).

## Acceptance coverage

| AC | Criterion | Test | Level | Status |
|---|---|---|---|---|
| 4 (UI half) | A flag-enabled merchant that is currently closed still appears by default, with a status string, and is excluded only when "Open Now" is explicitly on | No automated test (see Not automated). Verified by inspection: `RestaurantCard.tsx` renders `restaurant.status` only when truthy — confirmed this is populated exclusively for a confirmed-closed row (`ENG-045`'s own `evaluateOpenNow`, traced directly) and never gates on `open_now` itself, so a closed-but-included merchant shows its status by default, matching the criterion's "still appears... with a status string" half. The "Open Now" chip defaults to `filters.openNow` unset (falsy) — confirmed by reading `FilterState`'s type and every initializer; no code path pre-sets it — so the exclusion only activates on an explicit user toggle, matching the criterion's "excluded only when explicitly on" half. Both halves traced against the actual diff, not assumed from the design | inspection | pass |
| 1, 2, 3, 5 | Migration/backfill; staff toggle UI; Dine-In/Catering tab gating (negative case); rollout evidence | N/A for this diff — this ticket is `restaurant-marketplace` UI only (chip + status render). Owned by `ENG-044` (shipped), `ENG-046` (parked on approver), `ENG-045` (shipped its own suite), `ENG-044` respectively | N/A | N/A |

## Failure paths

| Scenario | Expected behaviour | Test |
|---|---|---|
| `restaurant.status` is `undefined` or `null` (open, or unknown hours — ADR-010's fail-open case) | Renders nothing in the meta line; no separator dot orphaned | Not automated — traced: `{restaurant.status && (...)}` guards the whole clause, including the separator, so a falsy status renders zero output, not an empty dot |
| Every other meta-line field absent (no cuisine, no dine-in address, no distance, no price) and `status` is the *only* populated field | No leading separator dot before `status` | Not automated — traced: the separator condition ORs all four earlier fields; all-false means no dot renders, then `status` renders alone, matching the pattern the three pre-existing fields already establish |
| `open_now` toggled on with the API not yet returning `open_now`-filtered results (`ENG-045` parked, not yet merged at the time this diff was built) | Chip shows active state; query param sent; no runtime error | Not automated — traced: new fields are optional everywhere threaded (`openNow?`, `has_*?`, `status?`), so an API response that doesn't yet honor the param or populate the fields degrades to "chip active, nothing filtered/labeled yet," not a crash. Confirmed in the PR body's own "safe to merge before ENG-045" reasoning, re-derived here rather than taken on it |
| Tab switch while `openNow` is on | `openNow` stays on ("standing intent," per design) | Not automated — traced directly (see `agents/principal-engineer/reviews/ENG-047.md`'s Design conformance): the tab-reset effect is a merge that never names `openNow` |
| "Clear All" while `openNow` is on | `openNow` resets off | Not automated — traced directly: `handleClearFilters` is a full-replacement literal that never names `openNow`, so the key is absent (`undefined`) in the new state |

## Regression risk

- **The two pre-existing quick filters (`hasOffers`, `rating4Plus`)** — confirmed via diff that their own gating (`mode === 'order-food' || mode === 'dine-in'`), reset, and Clear-All behavior are all unchanged; the new chip is additive beside them, not interleaved into their logic.
- **The three existing meta-line fields** (`cuisine`, dine-in `address`, `distance`, `priceText`) — confirmed via diff their own separator conditions are byte-for-byte unchanged; `status` is appended as a fourth clause, not spliced in.
- **`hasActiveFilters`/`activeFilterCount`/`hasAnyFilter`** (gates the Clear-All button and the Collections module) — confirmed `openNow` folded into all three the same way `hasOffers`/`rating4Plus` already are, so an active Open Now filter now counts toward each exactly like its siblings.

## Not automated

**No suite exists for `restaurant-marketplace`** — confirmed fresh this pass:
`package.json` has no `test` script and no test-framework dependency; a
repo-wide search found zero `*.test.*`/`*.spec.*` files anywhere under `src/`;
`config/projects.md`'s Commands table carries an empty Test cell for this
project (Lint/Typecheck/Build only). **Unlike its three sibling projects**
(`aiorders-admin-hub`, `aiorders-api`, `config-site-builder`), no proposal was
already tracking this gap for `restaurant-marketplace` specifically before
this pass — filed now (`proposals.md`, 2026-09-07), same shape as the other
three.

This ticket's own diff is thinner than `config-site-builder`'s `ENG-034`
(the sibling that triggered that project's own proposal) but is not pure
boilerplate the way `ENG-046`'s admin-toggle rows were: the meta-line
separator composition is a real, if small, conditional. Given no harness
exists to add a test *to*, the correct evidence class is inspection against
the actual diff (above, under Acceptance coverage and Failure paths) plus the
independent typecheck/build re-run recorded in
`agents/principal-engineer/reviews/ENG-047.md` — not "tested" by assertion.
The actual open/closed filtering and status-computation *logic* this ticket
depends on is `ENG-045`'s, already covered by that ticket's own 15
mutation-checked tests; this ticket only threads and renders the result.

## Result

- **Run:** none — no suite command applies (see `suite_command` above).
  Recorded as `no suite` per `test-suite-run/SKILL.md` step 8, not rounded up
  to a pass.
- **Outcome:** n/a — nothing executed. Evidence class is inspection plus the
  independent `npm run typecheck` / `npm run build` re-run
  (`agents/principal-engineer/reviews/ENG-047.md`), both clean.
- **Bugs filed:** none. No real defect found.
- **Verdict: pass.** No quality-gate fail condition is met: no suite is red
  (none exists), the one acceptance criterion this ticket owns (AC4's UI
  half) is covered by inspection against the actual diff, this is not a bug
  fix missing a regression test, no P0/P1 bug is open. Continues to
  `in-security` — code review also passed on the same diff
  (`agents/principal-engineer/reviews/ENG-047.md`).
