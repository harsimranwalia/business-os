---
ticket: ENG-002
project: restaurant-portal
author: qa
created: 2026-08-26
suite_command: npm test
last_run: 2026-08-26
last_result: pass
coverage_note: single smoke test; no line-coverage tooling configured (comprehensive coverage is an explicit PRD non-goal)
---

# Test plan — Add a smoke-test harness to restaurant-portal

## Acceptance coverage

| AC | Criterion | Test | Level | Status |
|---|---|---|---|---|
| 1 | Test command executes real test code, exits non-zero on failure | `src/App.test.tsx` → `App > renders the entry route and lands signed-out visitors on the login screen` | unit/component | pass |
| 2 | A change that breaks the build or stops the entry route from rendering fails the suite | same test — mounts the real `<App/>` through `AuthProvider → Router → PrivateRoute → Login`; verified red by the implementing pass (route removed → suite failed; reverted byte-identical) and re-checked structurally at code review | unit/component | pass |
| 3 | `projects.md` Commands table Test cell names the real command | not a test — a registry edit, done at `ready-to-ship` (see ticket log) | process | pass |
| 4 | `test-suite-run` executes the new suite and produces a non-vacuous result | this plan plus the Result block below is that first non-vacuous run against `restaurant-portal` | process | pass |

## Failure paths

This ticket adds test infrastructure, not application behaviour — there is no
new endpoint, no new external input, and no data access, so the usual
authz/tenant/concurrency table doesn't apply here. The one failure path that
matters for a smoke harness is AC2 above: a broken build or a broken entry
route must turn the suite red, and it does.

| Scenario | Expected behaviour | Test |
|---|---|---|
| Entry route (`/login`) removed or broken | Suite fails | Verified by the implementing pass; see AC2 |
| Unauthorised/no session (the actual runtime case this test exercises) | Redirects to Login, renders it | `src/App.test.tsx` (this is the happy path *for this ticket* — the app's real default state) |

## Regression risk

None to the application: `touches_data: false`, and the design doc's
Interfaces section states no runtime contract changes. The only regression
risk going forward is to the suite itself (a future change silently
disabling or loosening this test) — out of this ticket's scope to guard
against further.

## Not automated

AC3 (the registry edit) is a one-line documentation change verified by direct
inspection at `ready-to-ship`, not something to write a test for.

## Result

- **Run:** `npm test` on 2026-08-26
- **Outcome:** 1 passed, 0 failed, 0 skipped
- **Bugs filed:** none
- **Verdict:** pass

Also run, per the `## Commands` table in `config/projects.md` (test-suite-run
step 2 — lint/typecheck/build run regardless of the Test column):
`npm run lint` — 96 problems (62 errors / 34 warnings), identical to a clean
`origin/main` checkout, zero new; `npm run build` — succeeds. No `typecheck`
column exists for this project.

## Verified at `verified` (2026-08-26)

Re-run against the actual merged tree (PR #1, `b3a81ef`), not just the
pre-merge branch — department worktree confirmed tree-identical to
`origin/main` via `git diff` (empty) before running:

- AC1/AC2/AC4: `npm test` → 1 passed, 0 failed, 0 skipped. Unchanged from the
  pre-merge result — same commit content, now confirmed on `main`.
- AC3: `agents/eng-manager/config/projects.md` → Commands table read directly
  this pass — `restaurant-portal` | Test: `npm run test` — present and
  correct on disk. Status updated `pending` → `pass` above.
- `npm run build` re-run on the merged tree: succeeds, `dist/` output shape
  unchanged (no new production artifact — see the release record's Deploy
  note for why).

All four acceptance criteria confirmed against the live (merged) thing.
Verdict for `verified`: **pass**.
