---
ticket: ENG-037
project: aiorders-api
author: qa
created: 2026-09-04
suite_command: none — the diff is one SQL migration file, no `.ts` changed, so there is nothing for `deno check`/`deno test` to run against. `aiorders-api`'s own row in `config/projects.md`'s Commands table is empty across Test/Lint/Typecheck/Build regardless (no `package.json`, no `deno.json`).
last_run: 2026-09-04
last_result: no suite
coverage_note: 0 of parent ENG-019's 7 acceptance criteria apply to this ticket's own diff — every one requires ENG-038 (brand-portal actions, the dispatcher, or broadcast-unsubscribe), none of which exist yet. This ticket's own criterion (the three tables/constraints/indexes/cron job exist exactly as designed) is verified by direct query against both a disposable local replica and the live linked project, not by execution of an application-level test — see Acceptance coverage and Not automated.
---

# Test plan — broadcast campaigns schema and dispatch cron (ENG-037)

## Scope note

`ENG-019`'s PRD carries 7 acceptance criteria (`agents/product-manager/specs/
ENG-019-restaurant-marketing-broadcasts.md`, `## Acceptance criteria`), all
read directly this pass rather than assumed from the ticket's own paraphrase.
Every one of them exercises behaviour this ticket does not build: composing
and sending a campaign (AC1/AC2), audience scoping at read time (AC3),
redemption reporting (AC4), send logging (AC5), the unsubscribe path (AC6),
and cross-tenant rejection (AC7) all require `ENG-038`'s brand-portal
actions, dispatcher, and unsubscribe endpoint. This ticket's own Outcome
section states this plainly ("inert until `ENG-038`"), and it's confirmed
rather than assumed: `grep -rln "broadcast_campaign" supabase/functions/`
returns zero hits — nothing in this repo reads or writes these tables yet.

Round 1 of both gates on this ticket — code review passed clean on this
round (`agents/principal-engineer/reviews/ENG-037.md`, same diff, same hop),
so nothing here was discarded per `code-review-gate/SKILL.md` step 9.

## Acceptance coverage

| AC | Criterion | Test | Level | Status |
|---|---|---|---|---|
| 1–7 (all of parent `ENG-019`) | Composing/sending a campaign, audience scoping, ROI reporting, send logging, unsubscribe, cross-tenant rejection | N/A | — | N/A — every one requires `ENG-038` application code that does not exist yet; inventing a test against code that isn't there would test something adjacent, not this diff |
| this ticket's own | `broadcast_campaigns`, `broadcast_campaign_steps`, `broadcast_campaign_recipients` exist with the exact columns, constraints, indexes, RLS state, and trigger the design specifies; `broadcast-dispatch-tick` is registered on the correct schedule | Two independent checks, both executed this pass: (1) disposable local Postgres replica, migration applied unmodified, every table/index/constraint/trigger/cron-job confirmed by direct query, constraint rejection confirmed by an actual failing insert, trigger confirmed by an actual `UPDATE` moving `updated_at`, rollback actually executed and confirmed clean — `agents/database/migrations/ENG-037-*.md`; (2) live linked project (`bmnmnejwdxbcqinqkwko`), schema-only dump, independently re-run this hop (not reused from the build hop's own account) — confirmed zero `broadcast_campaign*` relations exist pre-migration (no collision), and every FK target (`restaurants.id`, `offers.id`, `customers.id`, `orders.promos`, `communication_log`'s column set, `communication_templates`' content-column types) matches what this migration assumes | inspection + executed DDL against a real Postgres instance | pass |

## Failure paths

No runtime failure path exists yet for any of the three tables or the cron
job — nothing reads or writes them until `ENG-038` ships (see the grep
above). The template's standard failure-path rows (invalid input, downstream
timeout, unauthorised caller, concurrent write) all presuppose a code path
that exercises the change; none does here. The one failure mode that *is*
reachable today — the cron tick firing against a `broadcast-dispatch`
function that doesn't exist and a Vault secret that doesn't exist either —
is a deliberate, documented no-op (401 from a null bearer, against empty
tables with nothing claimable) per the design's own Rollout order, not a
gap. Every other failure-path row is deferred to `ENG-038`'s own test plan,
where these tables first become reachable.

## Regression risk

Additive-only, three `create table if not exists`, no existing table
altered. Independently re-verified this pass, not carried from the build or
review hop's own account:

- `grep -rln "broadcast_campaign" supabase/functions/` — zero hits anywhere
  in the function tree; nothing reads or writes these tables.
- Migration timestamp (`20260904140000`) sorts after every other on-disk
  migration (`ls supabase/migrations/ | sort | tail -5`) — no ordering
  collision.
- Live schema dump confirms no `broadcast_campaign*` relation exists before
  this migration runs — a clean create, not a silent overwrite of something
  already there.
- Both new `pg_cron`/`pg_net` extension activations are `if not exists`
  against extensions already live on the linked project (confirmed via the
  same dump) — no new dependency, no activation risk.

Zero blast radius on existing behaviour: no existing caller reads or writes
any of these three tables today, and none is required to until `ENG-038`
ships.

## Not automated

Schema correctness verified by direct query against two real Postgres
instances (a disposable local replica and, independently this hop, the live
linked project's own schema), not by an executed application-level test, for
one reason: **no application code exists yet to exercise any of these
tables** — this is the correct state per the design's own rollout order
(migration lands before `ENG-038`'s functions deploy), not a gap this ticket
could close alone. This is a materially stronger evidence class than this
board's own prior precedent on the identical shape of gap
(`ENG-031`/`ENG-007`/`ENG-011`/`ENG-013`, all recorded "no Docker, psql, or
Supabase CLI on this host" — this host has both, and the migration was
actually run and queried, not just read back).

A live end-to-end insert against a real customer-facing flow is out of scope
regardless — `engineering-standards.md`: no test touches production, and
none of the queries run this pass touched anything beyond schema metadata
and two narrowly-scoped, non-identifying read-only samples (already recorded
in `agents/database/migrations/ENG-037-*.md`).

## Result

- **Run:** no suite command applies to a pure-DDL diff on a project with no
  test/lint/typecheck/build command at all (`config/projects.md`'s Commands
  table — every cell empty for `aiorders-api`). In its place: the disposable
  local-replica run (database's own build-hop work, re-read and independently
  cross-checked this pass) plus a fresh live-schema dump run directly by
  this hop, described above.
- **Outcome:** both checks pass — every table, index, constraint, trigger,
  and the cron job's schedule confirmed present with the intended shape;
  rollback confirmed clean.
- **Bugs filed:** none. No open P0/P1 anywhere on this board
  (`agents/qa/bugs/_index.md`: one open item, `BUG-001`, P2, unrelated
  project area).
- **Verdict: PASS.** None of the quality gate's own fail conditions
  (`agents/qa/agent.md` → "The quality gate") are met: there is no suite to
  be red, no acceptance criterion of this ticket's own lacks a passing
  check (the parent's 7 don't apply to this diff, and this ticket's own
  criterion is covered by two independent direct-query checks), this isn't
  a bug fix missing a regression test, and no P0/P1 bug is open. Continues
  past this gate — code review also passed this round
  (`agents/principal-engineer/reviews/ENG-037.md`), which additionally
  named two non-blocking, non-QA-owned schema findings (missing index,
  missing cross-column constraint) that don't affect this verdict.
