# Proposals

Things the department thinks are worth building and is **not allowed to build
until the approver says so.** Agent-originated findings land here rather than
shaping straight into tickets: no id, no board row, nothing sequenced.

See `schedules/eng_build_loop.md` step 3.

## How this works

| | |
|---|---|
| **Who writes here** | Any agent — QA, security, devops, the architect, the EM |
| **What becomes a ticket** | Only a proposal the approver has approved |
| **How they see it** | One batched G1 in the weekly report — never a per-item ping |
| **What happens to silence** | Nothing. It stays listed, is re-surfaced weekly, and expires after 30 days |
| **The one bypass** | A **P0 on a registered project** — production down or actively exploitable, with real users — goes straight to a ticket |

Silence is not approval, and an unapproved proposal is not a rejection. Expiry
is the terminus, chosen deliberately over a queue that only grows.

## Open

| Filed | By | Project | What | Why it matters | Size |
|---|---|---|---|---|---|
| 2026-08-25 | product-manager | aiorders | `agents/critic/agent.md` doesn't exist at the department or instance level, though `skills/prd-writer/SKILL.md` step 8b requires every G1 item to carry a dissent pass from it before reaching the approver. Confirmed absent at both `departments/engineering/agents/critic/` and this instance's `agents/critic/` — not a config gap, the agent itself was never ported. | Every G1 raised since the 2026-08-22 life-os port (including this pass's own ENG-002 G1) has gone to the approver with no counter-case attached — `docs/engineering-team.md` describes the critic seat as already wired, which is stale. Either port the agent or drop the step 8b reference; leaving both in place means the gap repeats silently on every G1. | S |
| 2026-08-25 | eng-manager | aiorders | `lib/eng-notify.sh` has two separate correctness bugs, both confirmed by reading the full script and by this pass's own live call. **(1) No channel dispatch:** it posts unconditionally to Slack via `SLACK_WEBHOOK_URL`, with no reference anywhere to `config/config.yaml` or a `telegram`/`slack`/`none` branch, even though `departments/engineering/config/conventions.yaml` documents exactly that seam ("the instance's config decides where that goes") and this instance's own `config/config.yaml` sets `approver.notify: telegram`. **(2) A variable collision breaks `stall` and `nudge`:** the script's own `MODE` (set from `$1` — `raise`/`nudge`/`stall`) is silently overwritten a few lines later when it sources `eng-env.sh`, which sources `$BUSINESS_OS_ROOT/.env` — and that file defines an unrelated `MODE=active` (the sabbath/retreat/quiet pause switch, same variable name, different purpose). Confirmed live: this pass's own `eng-notify.sh raise ...` call logged `sent: active`, not `sent: raise`. Since `$MODE` is never actually `stall` or `nudge` after the clobber, `if [ "$MODE" = "stall" ]` (line 92) can never match — `eng-notify.sh stall` currently falls through to the generic item-lookup, finds no item (stall passes none), logs `no such item:`, and exits 0 having sent nothing — and `[ "$MODE" = "nudge" ]` (line 130) can never match either, so a 24h nudge is sent with no `_Still waiting_` prefix, indistinguishable from a fresh raise. **Reconfirmed live a second time, 2026-09-04, this `scheduled` pass:** both 24h nudges sent this pass (`ENG-027`'s rescope G1, `ENG-029`'s P0 incident notice) logged `sent: active`, not `sent: nudge`, in `traces/eng-notify-2026-09-04.log` — identical clobber, ten days after the original finding, still unfixed. **Reconfirmed a third time, 2026-09-05, this `scheduled` pass:** the 24h nudge sent this pass (`ENG-016`'s continue-to-Piece-2 question) logged `sent: active`, not `sent: nudge`, in `traces/eng-notify-2026-09-05.log` — identical clobber, eleven days after the original finding, still unfixed. Not re-filed as a new row; this is the same bug, same root cause, same fix. | The gate-notification mechanism is the one thing standing between a decision and it "just sitting there" (the script's own stated purpose), and the department's own runaway-guard design (`schedules/eng_build_loop.md` step 7) depends on the stall alert specifically to make "the board has stopped" a state change someone is told about rather than a silent freeze. Bug (2) means that alert has never once fired, on any instance running this script, regardless of channel. Bug (1) compounds it: if Harry is watching Telegram (per his own config, and the working Telegram integration already used elsewhere in this repo for the Reddit/marketing side) rather than a Slack `#life-os` channel, then even the raise/nudge messages that *do* send successfully — including this pass's own `ENG-003` raise — may not be reaching him where he's actually looking. Fix (2) is a five-minute rename (the clobbered local variable just needs a name that doesn't collide with the sourced `.env`'s `MODE`); fix (1) is the larger piece — real channel dispatch, and for `telegram` specifically, reply-routing back into `approve`/`reject`/`changed` decisions, which nothing here has built yet. | M |
| 2026-08-26 | eng-manager | restaurant-portal | `restaurant-portal/.env` is tracked in git and not `.gitignore`d — confirmed this pass via `git ls-files .env` (tracked) and `git check-ignore -v .env` (not ignored) in the department's own worktree. Holds `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_SUPABASE_PROJECT_ID`, `VITE_SUPABASE_PUBLISHABLE_KEY`, `VITE_OPENWEATHER_API_KEY` (key names only — values not inspected beyond confirming they're set). Discovered incidentally during `ENG-002`'s build hop, same class of gap as `ENG-003` (`config-site-builder`/`aiorders-admin-hub`) — a fourth of the five registered repos now showing it. | All five keys are `VITE_`-prefixed, so — same reasoning as `ENG-003` — they're already bundled into the shipped client build regardless of git tracking; the real exposure isn't materially increased by tracking, so this is a hygiene/history gap rather than a fresh leak. Worth the same fix shape (untrack, `.gitignore`, `.env.example`) rather than waiting for a fifth repo to surface it independently. Not fixed inline — self-discovered, so it's a proposal per `schedules/eng_build_loop.md` step 3, not a drive-by fix. | S |
| 2026-08-26 | eng-manager | aiorders | Third occurrence of the pattern `observations.md` flagged twice already (2026-08-25 ~15:00, 2026-08-26 10:13:33) — a `watch` fire launches a full pass that sweeps all three inboxes and finds nothing new. This pass traced the mechanism precisely in `lib/eng-trigger.sh`: `commit_watch_fingerprint()` (line 424) only ever writes `.watch-seen` when `$EVENT = "watch"` (line 428, `[ "$EVENT" = "watch" ] \|\| return 0`), and `WATCH_FP` is only computed for that same case (lines 1582-1586). So a gate item raised by any *other* event type — `continue`, `scheduled`, `decision` — changes the three inboxes' top-level fingerprint but never updates `.watch-seen`. The next `watch` fire's dedup check (line 404) then always sees a change, because it compares against a fingerprint that's stale by however long it's been since the last *watch*-typed pass, not the last pass of any kind. Confirmed live this pass: `.watch-seen` was stamped 10:20 (the last successful watch pass); `continue ENG-002` raised `2026-08-26-eng002-merge-request.md` into `inbox/` at 11:01, already fully processing it (raised, notified, board-updated); the very next fire was `watch`, and it found nothing left to do — the same shape as both prior observations, now with a cause instead of a guess. | This isn't the "transient mid-write state" theory the second observation guessed at — it is guaranteed to recur, once per gate item any non-`watch` pass raises into a watched inbox, for as long as `watch` stays wired on an instance. All three occurrences so far trace to exactly this. Per `observations.md`'s own rule ("two data points; a proposal if it recurs"), this is that proposal. | S — `WATCH_FP`'s computation and `commit_watch_fingerprint`'s guard both key off `$EVENT = "watch"` specifically; computing and committing the fingerprint after every successful pass regardless of event type looks like the natural fix, though sizing that precisely is for whoever picks this up. |

| 2026-08-26 | eng-manager | aiorders | A "control center" dashboard action set `ENG-002`'s `state:` directly from `blocked` to `shipped` — bypassing `inbox/2026-08-26-eng002-merge-request.md`'s `## Decision` field entirely (still literally "Filled in by the approver." when this pass found it) and bypassing `lib/eng-gate-check.sh`'s ancestry check (the ticket's own log said so explicitly: "ancestry not consulted"). This pass only caught it because a `scheduled` sweep happens to read every ticket file directly rather than trusting the board index or inbox state — it cross-checked the ticket's `state:` against its own open gate item and found them disagreeing (ticket said done, inbox said still waiting). Nothing in `schedules/eng_build_loop.md` currently names this cross-check as a step; it happened this time because the pass was already re-deriving `ENG-002`'s status for other reasons. | If the approver's control center becomes the normal way work gets acted on (merging on GitHub directly, flipping a ticket's state from a dashboard) rather than an occasional shortcut, a gate item can sit open indefinitely — never nudged past its 24h mark only by luck (in this case it hadn't crossed the threshold yet), never moved to `_handled/`, never journaled — while the ticket it belongs to has already moved on. `schedules/eng_build_loop.md` step 4 (gate returns) reads `inbox/` for answered items; it has no corresponding step that reads each in-flight ticket's own `state:` and checks whether it has already outrun an inbox item that's supposedly still open. This pass did that by hand for one ticket; a repeatable version would be a short addition to step 4 or step 8 (dead-end sweep): for every open gate item, confirm the ticket it names is still actually at the state the item describes, and if not, treat the ticket's own state as the answer. | S — the check itself is cheap (read the ticket's `state:` for every open inbox item's `ticket:` field, compare against what the item implies); sizing the exact step and wording is for whoever picks this up. |
| 2026-08-27 | eng-manager (filed by hand, interactive session — not a department pass) | aiorders | A fourth variant of the pattern the two rows above already cover, caught before it could recur rather than after: on 2026-08-26 the `decision 2026-08-25-eng003-g1-scope.md` pass ran 518s and did the entire close-out (dropped `ENG-003`, closed its PRD, journaled the decision, updated the board) before dying exit 1 on its last step, the post-pass gate check, when the account hit its monthly spend limit (`traces/eng-loop-2026-08-26.log`). Per `lib/eng-trigger.sh`'s own `pass_never_started` classifier (line ~702), a 518s pass with real output is correctly NOT treated as "never started" — `handle_failed_pass` (line 604) correctly spent an attempt and re-queued it as `2 decision 2026-08-25-eng003-g1-scope.md` in `traces/.pending`, exactly as designed. Separately, and before that retry could fire, the approver finished that identical close-out by hand in an interactive session (commit `06f54cb`) — `2026-08-25-eng003-g1-scope.md` was already in `_handled/`, `ENG-003` already `dropped`. Nothing reconciles a hand-completed close-out against a still-queued automated retry for the same event, so the queued retry was about to fire at the next `eng-loop` pass, find nothing left to decide, and spend a full `claude -p` launch (plus whatever tokens the session burns discovering that) for zero output. Removed the stale line from `.pending` by hand this time (safe: a transient queue file under `traces/`), rather than letting it fire. | Same root shape as the two rows above — work getting done outside the channel that's supposed to consume the triggering event, so the event outlives the work — but a different trigger (a failed-and-requeued retry, not a live pass's own edit or a dashboard action) and a different cost (tokens burned on a guaranteed no-op launch, not a stale gate item). Distinct enough from both to need its own fix rather than folding into either: neither existing row's proposed check (cross-referencing a ticket's `state:` against an open inbox item, or fixing `.watch-seen`'s fingerprint timing) touches `.pending`'s retry queue at all. Worth a fix now rather than waiting for a third occurrence — a hand-sync racing a queued retry is exactly the kind of thing that recurs whenever the account hits its spend limit mid-pass and the approver picks up the slack by hand in the meantime, which the account did do here and is not unlikely to do again. | S — a cheap bash precondition in `eng-trigger.sh`, before it launches `claude -p` for a `decision` event specifically: if the named gate-item file is no longer in `inbox/` (already moved to `_handled/`), log it and consume the event without a launch, zero tokens spent. Same shape as the existing `pass_never_started` guard — a deterministic check ahead of the expensive step, not a model call. Not implemented in this pass: it touches `eng-trigger.sh`'s core event-validated dispatch path, which deserves the approver's sign-off before a hand-edit changes it. |
| 2026-08-29 | eng-manager | aiorders-api | Third occurrence of the same host limitation `observations.md` set an explicit threshold for during `ENG-007`'s recovery pass ("worth one [a proposal]... a third time"): this host has no Docker daemon, no `psql`, no `supabase` CLI, so no ticket touching `aiorders-api`'s schema has ever had its migration dry-run against a live Postgres — named in `ENG-007`'s and now `ENG-011`'s database migration docs independently. `ENG-011`'s own `continue` pass found a partial fix already available and used it: the Supabase MCP connection wired into this session reaches the real `aiorders-api` project (`bmnmnejwdxbcqinqkwko`) directly, and its read-only queries (`information_schema`, `pg_get_functiondef`, `pg_depend`) cost nothing and carry no risk — used this pass to independently confirm the migration's schema assumptions and rule out catalog-level dependents, closing the "is this safe" half of the gap for free. The "does the exact DDL statement actually execute" half is still open: doing that for real needs either a Supabase branch (`create_branch`, gated behind `confirm_cost` — a real recurring charge) or applying directly to production ahead of review — and this pass correctly declined to do either unilaterally, since spending money and writing to a live business's database are both actions that need the approver first, not a verification shortcut a background pass takes on its own judgement. | Every future ticket that touches `aiorders-api`'s schema hits this identical wall and carries the identical gap forward, one more time, unless something changes. Two independent, cheap fixes exist and don't require solving the host's Docker problem at all: (1) formally adopt the Supabase MCP's read-only queries as this repo's standard schema-verification step, replacing "no live Postgres reachable" with "read-only verified, write-verification gap named" in every future migration doc — a wording and habit change, not new infrastructure; (2) pre-approve a small monthly budget (e.g. one throwaway branch-hour per data-touching ticket) so a devops-role pass can actually dry-run a migration's DDL when the ticket's own risk profile warrants it, rather than every migration on this project carrying the same named gap indefinitely. | S — (1) is a doc/habit change costing nothing; (2) needs the approver's explicit budget sign-off before any pass spends against it, which is exactly why this is a proposal and not a thing this pass just did. |
| 2026-08-29 | architect | aiorders-admin-hub | Found during `ENG-015` design, filed to `agents/eng-manager/inbox/` as a P1 finding (`2026-08-29-restaurant-detail-write-partner-exposure.md`), routed here per step 3 (P1 doesn't qualify for the P0-only ticket carve-out): `aiorders-api`'s `admin-portal/handlers/restaurants.ts` — `updateRestaurant()` (PUT `/admin-portal/restaurants/:id`) and `updateBrandOwner()` (PUT `.../brand-owner`) — both use `adminSupabase` (service-role) unconditionally, no role check, no ownership check. Same defect shape `ENG-015` is fixing in this file's `getRestaurants()`/`getRestaurantById()`, but on the **write** path instead of read. Confirmed reachable, not hypothetical: `aiorders-admin-hub`'s `RestaurantDetails.tsx` and `Activation.tsx` both call this PUT endpoint, gated only by the generic `hasAdminAccess()` — neither carries the partner-specific block `/influencers` and `/billing` already have. **Correction, 2026-09-03, `continue ENG-015` build pass: `updateRestaurant()` is no longer open — drop it from this row's scope.** The 2026-08-31 design (written two days after this row, before this row was revisited) reasoned past the "no full audit" framing below specifically for this one function: `updateRestaurant()` shares the *same resource* (`restaurants`) and the *same defect* AC2 already asks this exact PRD to close ("enforces the same brand scoping itself... not just a UI filter"), not a different page found by auditing — a deliberate, logged scope decision (see the design's own Approach section), not scope creep. This build pass shipped it: `updateRestaurant()` now runs the same `isStaff`/`getPartnerBrandIds` ownership check as the read path, on branch `fix/ENG-015-agency-reseller-brand-scoping` (`aiorders-api@b6b3024`). Only `updateBrandOwner()` remains open from this row — same function, same finding, as the 2026-08-31 row below; the two rows are now fully overlapping, not merely related (see `observations.md`, 2026-09-01, for the overlap already flagged before this correction narrowed it further). | A `partner-admin`/`partner-user` who has or can guess another agency's `restaurant.id` can write to (or reassign the brand owner of) that restaurant's record — cross-tenant write access, same severity class and root cause as `ENG-015`'s two confirmed defects, one file over. Not folded into `ENG-015` inline: that ticket's own G1-approved scope and non-goals explicitly named "a full audit of every admin-portal page for partner-role exposure" out of bounds ("anything else found later during real use is a new bug, not silently folded in here"). Rated P1 not P0 — a workaround exists (don't hand restaurant ids to untrusted partners), practically unenforceable but short of live exploitation, so the P0-carve-out (step 3) doesn't apply and this waits for a batched G1 like any other agent-originated finding. **Narrowed by the correction above to `updateBrandOwner()` alone** — the write-side exposure on `updateRestaurant()` itself is closed as of this pass. | ~~S–M~~ **S**, narrowed to `updateBrandOwner()` alone — `ENG-015`'s now-shipped `updateRestaurant()` fix (`restaurants.ts`, this same file) is a direct, working template to copy, not just a design doc's proposed shape. |
| 2026-08-29 | architect | aiorders-api | Found while designing `ENG-023` (the PRD directed this design to model its own new write action on `brand-portal/catering.ts`'s `update_catering_request` as a confirmed-correct pattern). Read closely, that function's access check is correct, but its write itself is not: `case 'update_catering_request'` fetches the row, verifies access via the properly-ordered `verifyRestaurantAccess(existing.restaurant_id, supabase, user)` call, then does `.from('catering').update(updateData).eq('id', id)` — passing the client's raw request-body `updateData` straight into `.update()` with no field allow-list. A caller who already holds legitimate access to at least one catering row (any authenticated brand/restaurant manager) can include extra keys in that same payload, including `restaurant_id`, and reassign the row to a restaurant they were never granted access to, or overwrite any other column the table has (`archived`, `source`, `status`, `full_name`, …) beyond whatever the UI form intends to send. `ENG-023`'s own new `update_feedback` action was designed with an explicit `status`/`notes` allow-list specifically to avoid copying this — see that design's Alternatives #2 and Risks. | Cross-tenant data reassignment/corruption via an authenticated user's own legitimate write path, not a read-exposure bug — same general defect family as `ENG-022` (broken access check) and the `ENG-015`-adjacent partner-write finding two rows above, but a third distinct shape: a *correct* access check guarding an *unconstrained* write. Doesn't meet the P0 carve-out (step 3): `restaurant-portal`/`aiorders-api` isn't internal-lane, but exploiting this needs an already-authenticated actor's deliberate misuse of a payload field, not an open unauthenticated hole, so it waits for a batched G1 like any other agent-originated finding. | S — mirror `ENG-023`'s own fix shape: replace the blind spread with an explicit allow-list of the fields `catering`'s update form actually sends (`status`, `archived`, and whichever subset of the enquiry fields the edit UI exposes — confirming the exact set is part of sizing this, not decided here). |
| 2026-08-29 | principal-engineer | aiorders-api | Found during `ENG-008` round-2 code review: `admin-portal`'s per-handler admin/sub-admin access check (`hasFoodswipeAccess`/`hasInfluencerAdminAccess`/`hasLoyaltyConfigAccess`) is now duplicated across three handler files, written on three different tickets the same day, and has quietly forked into two incompatible conventions. `foodswipe.ts` (`ENG-013`) and `influencers.ts` (`ENG-008`) both dereference `userProfile.role`/`auth.user.profile` directly with an untyped `AuthenticatedRequest { user: any; supabase: any; adminSupabase: any }` — throws if the profile is ever null/undefined. `loyalty-config.ts` (`ENG-007`) instead takes a typed, nullable parameter and uses `profile?.role` throughout, added specifically so a missing profile denies access instead of crashing. Confirmed byte-for-byte: `foodswipe.ts`'s and `influencers.ts`'s `AuthenticatedRequest` interfaces are identical; `deno lint` flags the same 4 `no-explicit-any` hits on both. | Not blocking either ticket individually — each matched its own nearest same-day precedent, which is the correct per-ticket call when the codebase already disagrees with itself. But the fork means the *next* handler has two conflicting "established patterns" to copy from, and the older/plainer one is winning 2-to-1 by file count despite being the less safe of the two. Left alone, a fourth handler is more likely to copy the crash-on-null shape than the defensive one, purely by majority precedent. | S — extract one shared, typed helper (`hasAdminPortalAccess(profile, roles)` or similar) that all three handlers import, using `loyalty-config.ts`'s null-safe shape as the baseline since it's the one with a documented reason for existing; update the two others to match. No behaviour change for any already-authenticated caller — this is a consistency/robustness fix, not a new access rule. |
| 2026-08-30 | product-manager | aiorders-api | Filed as `agents/qa/bugs/BUG-001-platform-analytics-cron-401.md` (P2): the `platform-analytics` hourly cron (`pg_cron` job `platform-analytics-hourly`, `0 * * * *`) has returned 401 on every run since 2026-08-30T01:00 UTC (7/7 as of this check), rejected before the handler's own code executes — a gateway/platform-level auth check, not an application bug in the function itself. Found while independently verifying `ENG-011`'s health-signal acceptance criterion against live production data (a read-only Supabase MCP check plus a direct unauthenticated probe of a sibling endpoint), not part of that ticket's own diff. | The Cloudflare KV analytics cache this cron maintains — which `ENG-011`'s newly-shipped health column reads — stops refreshing for as long as this persists; every cached value is correct as of its last successful write (2026-08-30T00:00:06Z) and nothing is lost, but the signal grows staler every hour until fixed. Admin-facing only; orders/checkout confirmed unaffected. | S — likely a cron-stored credential or a project-level "verify JWT" setting that changed around 01:00 UTC; root-causing it needs someone who can read the Supabase function/cron auth config directly (dashboard or CLI), which this pass's tools can't reach. |
| 2026-08-30 | eng-manager | aiorders-api | `continue ENG-013` (this pass) went to write the test file round 1's code review asked for and found it already written, correct, and pushed — `foodswipe.test.ts`, commit `c95b25b`, same automation identity (`businesspilotcare-gif`) as the ticket's own recorded build commit, 17 test cases hand-traced against the live handler and confirmed to cover exactly the three gaps round 1 named. But nothing in tracked state knew this: not the ticket's own log, not the board index, not `business-os`'s own `git log` (confirmed `main` 0/0 with `origin/main`), not either of the two dated trace logs on this host. Root cause, per the existing 2026-08-29 row below: this instance runs on two hosts and `traces/` is `.gitignore`d and host-local, so a pass that ran on the other host, did the work, and pushed the code commit leaves nothing on this host if it died (or never committed business-os) before its own ticket-log update. | Distinct from the row below, not a duplicate: that one is about a *dropped-event incident item* not carrying enough detail to diagnose cross-host; this is about a pass that actually **finished its real work correctly** and still left zero trace anywhere tracked — no incident, no log line, nothing to even signal a second look was needed. Caught only because this pass happened to read the worktree's own `git log` before writing anything. A less careful pass would have written a second, possibly-conflicting test file over already-correct work, or duplicated a hop for nothing. Fix: before a build hop assumes a ticket at `building` still needs code, check the ticket's own recorded commit hash(es) against `git log {hash}..origin/{branch} --oneline` on each linked repo — cheap, local, no API call, and it would have surfaced this in one command instead of a multi-step investigation. **Third occurrence, 2026-09-04, `continue ENG-033` fix-round-2 pass, and a materially different variant this proposed fix would not have caught:** `traces/eng-loop-2026-09-04.log` shows a `continue (ENG-033)` session launch at `00:27:23`, then stops — 18 lines total, no `pass end` ever written, `traces/.hops-2026-09-04-ENG-033` still reading `2`, the same count already present at that session's own start, meaning it never reached the wrapper's own hop-increment. The session had done real work in the meantime — `catering-request/validation.ts` and `index.test.ts` (mtimes `00:30:11`–`00:30:59`) plus a matching edit to `index.ts`, exactly what round 2's review asked for — but none of it was committed. A `git log`-against-recorded-hash check assumes a commit exists to check; here there was none yet, only an uncommitted working tree, so the fix needs to widen to a working-tree check (`git status --porcelain` on each linked repo's worktree, not only `git log` against its recorded hash) before a build hop starts writing new code. Full evidence: `ENG-033`'s own board-file log, 2026-09-04 fix-round-2 entry. | S — one `git log` call per linked repo, added to the top of the build hop before it starts writing anything; sizing exactly where (build hop vs. pre-pass gate-check) is for whoever picks this up. |
| 2026-08-29 | eng-manager | aiorders | Filed while investigating `inbox/_handled/2026-08-28-eng-events-dropped.md` (the approver answered that incident report "recheck the request and report back how to fix" this pass). Could not actually recheck the underlying failure: `traces/` is `.gitignore`d and host-local, this instance now runs on two hosts (Mac, where the 10:42:17 incident happened, and Windows since `168cb89`), and the dropped event's own inbox item pointed at `traces/` by name rather than carrying any of the failure itself — a dead end from any host but the one that failed. Best reconstructable read, from `observations.md`'s same-day entries rather than the missing log itself: two *other* spend-limit deaths that day each recovered on their next retry, while this one failed both allowed attempts and was dropped — a pattern that fits a standing TCC/EPERM permission denial (doesn't clear between two scripted retries) better than the vendor spend limit (evidently cleared within a retry, twice, that same day) — reasoned, not confirmed. | The root cause needs a human at the Mac (System Settings → Privacy & Security) or the vendor billing dashboard, neither reachable from here — but the reason it's *undiagnosable remotely at all* is fixable: a dropped event's inbox item is the one artifact that travels between hosts and sessions, and today it carries a pointer to host-local evidence instead of any of the evidence itself. Whichever host or session next answers a dropped-event incident hits exactly this wall unless the item carries its own excerpt. | S — in `lib/eng-trigger.sh`'s drop path (`handle_failed_pass`'s final branch, the one that writes this style of item), embed the last few lines of the pass's own captured output and the classified cause (spend-limit signature / never-started / genuine failure) directly in the written item, instead of only "Check `traces/` for this pass's log." Same shape as the two `eng-trigger.sh` proposals above (cheap, mechanical, but touches the core failure-handling path — approver sign-off before a hand-edit changes it). |

| 2026-08-31 | eng-manager | aiorders-admin-hub | `ENG-008` round 1 code review found a real correctness bug in `Influencers.tsx` (`accepts_paid`/`accepts_barter` defaulted via `null ?? !barter_visit`, fabricating a value for the 51/306 rows where the preference was genuinely unset) and asked for a fix plus "a regression test... confirm neither checkbox pre-checks and neither is written unless the user checks it." The fix landed this pass (`continue ENG-008`): null now passes through the edit-form state unchanged instead of being coerced, and `handleSaveInfluencer` omits the field from the PATCH body entirely while it's still null. Verified by hand-tracing plus `npm run build`/`npm run lint` — the only checks this repo has. No automated regression test could be written: `aiorders-admin-hub` carries zero test infrastructure — no `vitest`/`jest`/`@testing-library` dependency, no `test` script, zero `*.test.*` files anywhere in the repo (confirmed this pass) — the same gap `config/projects.md` already names for this project. | Every future `aiorders-admin-hub` bug fix hits this identical wall and ships on manual build+lint verification alone, with no way to pin the fixed behavior against a regression. `restaurant-portal` already solved the identical problem for itself via `ENG-002` (a dedicated, approver-originated test-harness ticket); `aiorders-admin-hub` has not. | M — wiring `vitest` + `@testing-library/react` into an existing Vite project is bounded, known work, not exploratory; sized above XS because it also needs a `test` script added to `package.json` and to `config/projects.md`'s Commands table, not because the work itself is uncertain. |
| 2026-08-31 | eng-manager | aiorders | This `scheduled` sweep found a fourth distinct dispatch-loss shape, on **three tickets at once**: `ENG-014`, `ENG-015`, and `ENG-025` were each moved to `designed`/`owner: architect` on 2026-08-29 with a `chained: ENG-0XX` line claiming the design-hop fire, and `ENG-014`/`ENG-015` each got a *second* confirmation the same day, a repair pass that found the first fire never actually launched (its own process died first) and re-fired it, verifying fresh that `traces/.pending` carried the queued entry. Despite that: grepping every `traces/eng-loop-*.log` this instance has ever written for `pass start: continue (ENG-014\|ENG-015\|ENG-025)` — the exact format confirmed live against `ENG-008`'s and `ENG-013`'s own known-good runs earlier today — returns zero matches, for any of the three, ever. No design file exists for any of them at `agents/architect/designs/`. This is distinct from the already-tracked redundant-dispatch race (`observations.md`, 2026-08-26/27): that race is two events chasing a **completed** action and is merely wasteful; this is an event confirmed appended to `traces/.pending` that then never drains and never runs — the underlying work silently never happens at all, for two full days, on a `P1` security ticket (`ENG-015`, a live cross-tenant data exposure) among the three. Also notable: none of the three ever produced an `eng-events-dropped`-style incident item — the drop-notice mechanism (`handle_failed_pass`, `pass_never_started`) only fires for an event that reaches a launch attempt and fails or never-starts; an event that simply fails to survive between append and drain hits neither path, so today's safety net (the twice-daily — now four-times-daily — `scheduled` sweep rereading every ticket's own frontmatter and log) is currently the *only* thing that catches this, and it took two days. | If this can happen to a `continue` event, nothing rules out the same silent loss on `intake`/`decision`/`finding`/`watch` — the failure looks like it lives in the shared append/drain path (`queue_append`/`collapse_pending`/`drain_next`), not in anything `continue`-specific, though this pass did not read that code closely enough to name an exact line. Left as a proposal rather than a code fix for that reason: diagnosing which of append, collapse, or drain is actually losing the line needs a focused read of `lib/eng-trigger.sh`'s queue functions (and possibly deliberately reproducing it), not something to guess at from the trace evidence alone, and it touches the same core dispatch path the three existing queue/dispatch proposals above (2026-08-26 watch-fingerprint row, 2026-08-27 stale-retry row, 2026-08-29 dropped-event-diagnosability row) already flag as needing the approver's sign-off before a hand-edit changes it. | M — needs a read of `queue_append`/`collapse_pending`/`drain_next` (lines ~508-665) with fresh eyes for how a line could be appended and never drained, most plausibly a race between an append and a concurrent collapse/drain rewriting the file; sized above S because reproducing a race is inherently harder than the other queue proposals' mechanical fixes. |

| 2026-08-31 | architect | aiorders-api | While designing `ENG-015` (partner brand-scoping on `restaurants`), found `updateBrandOwner()` in the same file (`admin-portal/handlers/restaurants.ts`) uses the service-role client unconditionally with **no role or ownership check at all** — any of the four roles that pass the top-level `admin-portal` gate, including `partner-admin`/`partner-user`, can `PUT /admin-portal/restaurants/:id/brand-owner` and rewrite `profiles.name/email/phone` for **any** brand owner platform-wide, not just their own brand's. Confirmed by reading the function directly: `body` is passed straight to `.update()` with only a `profile_id` presence check, no `auth.user.profile.role` reference anywhere in the function. **See the 2026-08-29 row above, corrected 2026-09-03: it named this same function alongside `updateRestaurant()`, which `ENG-015` has since fixed — the two rows now fully overlap on `updateBrandOwner()`, treat as one item, not two.** | Distinct resource and failure mode from `ENG-015` itself (that ticket is about restaurant visibility/add-location; this is unrestricted cross-tenant write access to brand-owner contact info) — not implied by `ENG-015`'s approved PRD, so not folded into that fix. Same class of gap as the two `ENG-015` defects (a role added to the top-level gate, never propagated to a handler's own checks), on the same file, found for free while already reading it for the design pass — worth fixing before it's independently rediscovered as "a new bug" per that PRD's own non-goals disclaimer. | S — the same `isStaff`-plus-ownership-check shape `ENG-015`'s design already applies to this file's other three functions would cover this one too; sizing exactly (reuse `ENG-015`'s helpers once shipped, or duplicate them if this lands first) is for whoever picks it up. |
| 2026-09-01 | eng-manager | aiorders | `eng_build_loop.md` names what happens to an answered G1/G2/G3 or merge-request item (`decision:` moves it to `_handled/`), but the `incident` gate type — the shape `inbox/2026-08-30-eng-events-dropped.md` uses — carries no `decision:` field at all by design (it is meant to self-close once investigated) and the procedure never says where it goes after that. Four consecutive passes have now re-read this exact item and reached the identical "fully investigated, below P0, nothing to do" conclusion for zero new information each time: 2026-08-31 20:19 (`watch`), 2026-08-31 20:30 (`scheduled`), 2026-09-01 02:00 (`scheduled`, logged in `observations.md`), and 2026-09-01 09:30 (`scheduled`, this pass) — the exact fourth-pass threshold the 02:00 pass's own observation named as the point to escalate from another observation to a proposal. | Every future sweep that reads `inbox/` re-derives the same closed case from scratch, indefinitely, since nothing in the procedure grants any pass authority to move a no-`decision:`-field incident notice to `_handled/` once its own investigation is stale-checked and still holds. A one-line addition to step 4 or step 8b (e.g. "an incident item with no `decision:` field whose investigation note is unchanged and at least N days old may be moved to `_handled/` by the pass that reconfirms it still holds") would let one pass close this permanently instead of carrying it forward a fifth time. | S — doc-only change to `eng_build_loop.md`; no code, no new state. |
| 2026-09-01 | eng-manager | aiorders | This `watch` pass found `board/_index.md`'s In-flight table and "Waiting on the approver" section claiming `ENG-016` was `shaped`/`product-manager`, G1 "drafted, ready to raise" — while the ticket's own board file and its own PRD both read `awaiting-scope`/`approver`, G1 actually raised and notified `2026-08-29T23:13:49` (`inbox/2026-08-29-eng016-g1-scope.md`, still open). Root cause named in the merge commit's own note: the 09:30 pass's `git pull` (`e281c71`, "reconcile 26 diverged engineering-board files") kept a rival host's `_index.md` account wholesale, for consistency with several *other* tickets where that host's history really was the more current one — but for `ENG-016` specifically, that host had simply never learned the G1 was raised, so its narrative wasn't stale, it was missing an event outright. Confirmed genuine staleness rather than a legitimate re-open before touching anything: no `decision-journal.md` row for `ENG-016`, no reset in its own PRD `status:`. Consequence, not just cosmetic: the approver-facing WIP cap (2) read `2/2` when a ticket whose own file says `owner: approver` made it `3/2`, over cap — and the G1 itself went un-nudged for 2.5 days because every intervening pass checked the index's narrative rather than the ticket's own file. Corrected this pass (index rows, WIP accounting, nudge sent) — this proposal is about the merge process, not this one ticket. **Second occurrence, 2026-09-02, and this time no merge was involved**: the 09:30 `scheduled` pass on 2026-09-01 read `ENG-009`'s own ticket file, misread its `state:` as `ready`, and "corrected" `_index.md`'s In-flight row from `building` (correct) to `ready` (wrong) — the opposite direction from this row's original case, but the identical root cause. Five consecutive passes then dispatched off the wrong table value instead of the ticket's own file, for ~28 hours, concluding "WIP-capped, nothing to do" when the ticket was actually sitting on a chain that never landed a session. Confirmed via `git log -p`/`git show` at every intervening commit (`29af8f2`, `db8bf41`, `3881cc2`, `e281c71`, `HEAD`) — full evidence in `ENG-009`'s own board-file log, 2026-09-02 entry. Corrected this pass; chain re-fired. | `_index.md`'s In-flight table is a derived summary of per-ticket board files, exactly the kind of aggregate-vs-source-of-truth split this department already treats as load-bearing everywhere else (`lib/eng-gate-check.sh` reads receipt files, never frontmatter claims; the 08-31 pass corrected a stale hold-reason the same way). A cross-host reconciliation merge is one place that split gets resolved by keeping one branch's *prose* wholesale rather than re-deriving the summary from each ticket's own frontmatter — which is exactly backwards, since the index is the derived artifact and the per-ticket file is the source — but `ENG-009` proves it isn't the only place: an ordinary pass can independently misread a ticket file and "fix" the index to match the misread, with no merge anywhere in the chain. The fix therefore can't be scoped to post-merge reconciliation alone; the general rule needs to be that the table is never hand-edited *from belief*, only ever regenerated *from each ticket's own frontmatter*, on any pass that touches it. Silent until something cross-references the two, as happened twice now. | S — after any merge that touches `board/_index.md`, regenerate its In-flight table's `state`/`owner`/`priority` columns from each listed ticket's own frontmatter rather than keeping either side's version verbatim; the prose narrative sections can still take "prefer the more-current account," but the table shouldn't, since it's cheap to derive correctly rather than merge by hand. **Given the second occurrence, worth widening the fix to any pass, not just merges**: treat a disagreement between the table and a ticket's own `state:` as always resolved in the ticket file's favor, mechanically, rather than trusting either account's prose. |
| 2026-09-01 | eng-manager | aiorders | The approver's own reply on `inbox/_handled/2026-09-01-eng-events-dropped.md` (verbatim, sic: "If the monhtly limit is hit, then do not retry tasks rather check that the limit is reset then only it makes sense to retry tasks instead of just retying tasks for no reason") asks for a behavior `lib/eng-trigger.sh`'s retry path doesn't have: today, `pass_never_started` classifies a vendor-limit signature, refunds the hop, re-queues at the same attempt, and arms a back-off window purely on elapsed time and attempt count (`schedules/eng_build_loop.md`'s "Runaway guard" / back-off sections) — nothing anywhere checks whether the monthly limit itself has actually cleared before the next retry fires. | A retry that fires before the limit resets repeats the exact no-op `ENG-016` was built to stop charging a life for, just one layer further out: the back-off window bounds *how often* it retries, not *whether retrying could possibly succeed yet*. Distinct from the three already-open `eng-trigger.sh` proposals (watch-fingerprint timing, stale-retry-vs-hand-sync, dropped-event diagnosability) — none of them touch the back-off/retry-eligibility path itself. | S–M — needs a way to check the vendor limit's actual reset state (a billing-period boundary is computable locally if the limit resets calendar-monthly; whether a live check exists depends on what the account/billing API this instance already talks to can expose) before `handle_failed_pass`'s vendor-limit branch re-queues; sizing the exact mechanism is for whoever picks this up, and it touches the same core dispatch path the other three `eng-trigger.sh` proposals already flag as needing sign-off before a hand-edit changes it. |
| 2026-09-02 | eng-manager | aiorders | This `scheduled` pass, about to nudge `ENG-026`'s readback question on the face-value reading of its `notified: 2026-09-01T10:03:26` field (>24h old against current UTC), cross-checked that field against `traces/eng-notify-2026-09-01.log`'s own freshly-generated local-time entry for the same raise (`[10:03:26] sent: active 2026-09-01-eng026-visibility-toggle-question.md`) and against this host's live `date`/`date -u` offset (confirmed this pass: local is PDT, UTC = local + 7h). The two are textually identical, which they cannot honestly be if one is UTC and the other is local — proving `notified:` for this item actually holds local time, mislabeled in a UTC-shaped ISO string. `ENG-016`'s and `ENG-007`'s `notified:`/`nudged:` timestamps show the identical pattern (log time equals frontmatter time exactly, no 7h gap). All three are `agent: product-manager` items. Both merge-request items (`ENG-008`/`ENG-013`, `agent: eng-manager`) show the correct behavior: frontmatter time is exactly 7h ahead of the log's local time, i.e. genuine UTC. Recomputed under the corrected reading, `ENG-026` is ~23h31m past its true raise time, not yet 24h — this pass did not nudge it on that basis (see this pass's own board entry). **Correction, 2026-09-02, `continue ENG-009` pass:** the claim above about the two merge-request items is wrong, and this time re-checked directly rather than carried forward. `ENG-013`'s `notified: 2026-08-31T11:05:16` matches its own trace-log line (`traces/eng-notify-2026-08-31.log`: `[11:05:16] sent: active 2026-08-31-eng013-merge-request.md`) exactly; `ENG-008`'s `notified: 2026-08-31T11:15:29` matches its line (`[11:15:29] sent: ...`) exactly too. Zero gap on both, not 7h — the identical local-time-mislabeled-as-UTC pattern the PM-agent items show, not a divergent "correct" path. There is no second, already-right code path to bring the first one in line with. | Every nudge-threshold decision this department makes reads `notified:`/`nudged:` at face value and assumes UTC (`schedules/eng_build_loop.md` step 7, and every prior pass's own arithmetic). That assumption is currently wrong by a fixed +7h (this host's PDT offset — DST-dependent, so the gap won't stay a constant 7h year-round) on **every** gate item this department raises, not only PM-raised ones (see the What column's 2026-09-02 correction) — which can flip a threshold decision either direction depending on how close to 24h the true elapsed time is, as it did here. Left unfixed, this recurs on every future G1/readback/continue-sequence/merge-request item, silently, since nothing currently cross-checks a gate item's stamped time against anything else. | S — find and fix the one shared code path every agent's pass uses to stamp `notified:`/`nudged:` (likely a `date` call missing `-u`, invoked the same way regardless of which agent is writing the frontmatter) rather than assuming a second, already-correct path exists to match against; a repo-wide grep for the exact stamping command should land on a single call site. |
| 2026-09-02 | principal-engineer | aiorders | Found while reviewing `ENG-009` round 1: `ENG-009`'s branch (both repos) forked from `ENG-008`'s branch on 2026-08-30, and `ENG-008` fixed a real bug on each repo the next day (2026-08-31, `aiorders-api@57f8c4b`, `aiorders-admin-hub@63be255`) — `ENG-009`'s branch was never rebased to pick either fix up, so it still ships the pre-fix code verbatim (confirmed: `git merge-base --is-ancestor {fix} {ENG-009-tip}` is false on both repos; `Influencers.tsx:139-140` still carries the exact null-coalescing bug `ENG-008`'s own round-1 review found). Four sweep passes read this board in the two days between the fix landing and this review without catching it, because merge detection (`eng_build_loop.md` step 5) diffs a ticket's branch against `origin/{default branch}` only, and code review's own inputs (`skills/code-review-gate/SKILL.md`) name "the full diff on the branch" and "the design," never "a sibling ticket this one was deliberately sequenced/branched behind." | Any ticket built as a stack on top of an unmerged sibling (this board's own established pattern for file-adjacent work — `ENG-009`/`ENG-010` behind `ENG-008`, explicitly, by design) carries the identical exposure for as long as the sibling stays unmerged and gets touched again: a later fix on the base ticket never automatically reaches the ticket stacked on it, and nothing currently checks for that staleness anywhere in the loop. This one cost two days of silent drift before a review happened to compare branch points directly instead of only against `main`. | S — add one check, either to merge detection or to code review's own steps: for a ticket whose design/log names a sibling as its branch base, run `git merge-base --is-ancestor {sibling's current tip} {this ticket's tip}` and flag it stale if false, before reviewing the diff for anything else. Cheap, local, no API call — same shape as the existing ancestry check merge detection already runs, just pointed at a sibling instead of `main`. |

| 2026-09-02 | eng-manager | aiorders | This `watch` pass discovered that six inbox items — five of them decided a full day earlier (2026-09-01, ~09:37–10:14 local) — had been sitting answered on a different host/checkout, invisible to this Mac's own passes (which correctly reported `decision:` blank at 09:30, 15:30, 17:52, and 20:30 today) until an interactive `git pull`-style merge (`1b72b26`, 21:27, outside any build-loop pass) finally reconciled the two histories. Two of the six were merge-request "changed" verdicts on `ENG-008`/`ENG-013` — real product decisions that sat un-actioned for over 24 hours through no fault of any pass, purely because nothing pulls business-os's own git state on a schedule the way the loop pulls `aiorders-api`/`aiorders-admin-hub` for merge detection. | Every mechanism this department relies on to react quickly (event-driven passes, `lib/eng-notify.sh`'s notify/nudge timing, the approver-facing WIP cap's own accuracy) assumes the local inbox reflects reality. It doesn't, whenever the approver or another host answers a gate item somewhere this Mac's checkout hasn't pulled from yet — and nothing currently surfaces that gap; a pass just quietly reports "still waiting" when the true state is "already answered, sitting on a shelf." Cross-host git sync for business-os itself is currently ad hoc (an interactive pull, whenever someone happens to run one) while the two build-loop-owned product repos get a scheduled fetch every `scheduled` pass. | S–M — the cheapest fix is folding a `git fetch`+fast-forward-or-report-conflict step for business-os itself into the existing `scheduled` cadence (four times daily), same spirit as the merge-detection fetch already does for the product repos; sizing the conflict-handling path (this evening's own merge needed real reconciliation, not a clean fast-forward) is for whoever picks this up. Separate from, but related to, the still-open question of whether a pass should also *commit* business-os (`[[project-buildloop-instance-repo-commit-gap]]`) — this proposal is only about *pulling* more often, not about committing. |
| 2026-09-03 | eng-manager | aiorders | `ENG-026`'s 2026-09-02 rescope (`intake→shaped→awaiting-scope`, narrowing a four-capability request to just the visibility-toggle piece) wrote a brand-new board file (`ENG-026-foodswipe-channel-visibility.md`) instead of editing the original ticket file in place, leaving both live with `id: ENG-026` simultaneously — the original frozen at `intake` since 2026-09-01. `lib/eng-gate-check.sh` has no id-uniqueness check across board files (confirmed by reading it), so nothing mechanical caught this; a whole-board dead-end sweep found it by inspection. The original file also carried a `priority: now` stamp (commit `a862607`, partial bookkeeping ahead of full gate processing — the approver's field, not this pass's to interpret) that never made it into the surviving file, and sat unresolved until this sweep flagged it in the live ticket's log. | Any future rescope that forks a new file instead of editing in place risks silently losing non-prose frontmatter (priority, severity, blocked_on, anything the prose doesn't restate), with no mechanical check to catch the resulting id collision. `ENG-016`'s own rescope did this correctly (same file, same id) the same night — the pattern to enforce already exists on this board, it just isn't written down or checked anywhere. | S — two independent fixes, either alone would have caught this: (1) add an id-uniqueness check to `lib/eng-gate-check.sh` (glob `agents/eng-manager/board/ENG-*.md`, grep each file's `^id:`, fail on any duplicate); (2) write down the "same file, same id" rule explicitly wherever rescoping is specified (`prd-writer/SKILL.md`, or beside `ENG-016`'s own precedent) rather than leaving it as implicit precedent one ticket log has to be found and read to learn. |

| 2026-09-03 | security | aiorders-api | Found during `ENG-022`'s security gate, one function away from the bug that ticket just fixed: `verifyRestaurantAccess` (`supabase/functions/brand-portal/utils.ts:27-37`, unchanged by `ENG-022`'s diff) lets any caller whose `profiles.role`/`additional_roles` includes `admin`, `sub-admin`, `partner-admin`, or `partner-user` bypass restaurant-level scoping entirely — `hasAccess: true` for any restaurant, no further check. Not asserted as a defect: an admin console plausibly wants exactly this for `admin`/`sub-admin`. But `partner-admin`/`partner-user` bypassing tenant scoping *platform-wide* (not just their own partner's brands) is a materially different, broader grant than "admin sees everything," and nothing in the code or the design distinguishes why a partner role gets the same blanket bypass as a platform admin. | Not confirmed exploitable and not blocking `ENG-022`'s gate — this function is explicitly out of that ticket's scope (PRD non-goals: "`verifyRestaurantAccess` itself... is correct"). Worth a deliberate look precisely because this ticket's own root cause was an *assumed*-correct access check elsewhere in the same file; the same assumption is currently resting on this bypass too, unexamined. | S — read what `partner-admin`/`partner-user` are actually meant to see (likely scoped to their own agency's brands, per the `ENG-015`/partner-write-exposure proposals already on this list) and either narrow the bypass to `admin`/`sub-admin` only or confirm in writing why partner roles need the same blanket grant. |

| 2026-09-03 | principal-engineer | aiorders | Found while running `ENG-024`'s fast-lane combined review (`agents/principal-engineer/reviews/ENG-024.md`): that ticket's backfill migration reached this review with no `database`-agent sign-off at all. Traced why, not just noticed: `schema-change/SKILL.md`'s only stated trigger is "a design with `touches_data: true`" (fired by the architect), but the fast lane's own path (`intake → building → in-review → shipped → verified`, `definition-of-done.md`) skips the architect's design state entirely — so on a fast-lane ticket, nothing anywhere ever fires that trigger, regardless of whether the build hop ends up writing a real migration. `definition-of-done.md`'s fast-lane row names exactly three things folded into principal-engineer's own combined gate ("review + suite run + OWASP") and migration is deliberately not a fourth — matching `principal-engineer/agent.md`'s own `never_touches: ... the security, quality, or migration gates (other agents own those)" — so this review correctly did not attempt to hold the migration gate itself, only assessed it informally against `schema-change`'s 7 stated failure conditions as a sanity check (low risk on this specific migration: not destructive, not irreversible, no schema/code coexistence question, one soft miss on runtime estimate/batching that matches this repo's own only backfill precedent). `release-runner/SKILL.md` step 2 ("a missing verdict is a fail, not an assumption") is the documented backstop that should still catch this before `shipped` if the risk read here is ever wrong on a future ticket. | A fast-lane ticket is explicitly allowed to touch data via a non-schema backfill (the lane's exclusion list names `schema` and `data deletion`, not "data" generally) — so this isn't a one-off; it's a standing gap for every future fast-lane ticket whose build hop decides a backfill is needed after shaping already committed to the lane, same as `ENG-024`'s did. Today the only thing that would catch a genuinely risky version of this (a large-table unbatched backfill, a non-idempotent transform) is `release-runner`'s step 2 check at `ready-to-ship` — late, and `instructed` rather than `enforced` per `eng_build_loop.md`'s own enforced-vs-instructed distinction, so nothing mechanical guarantees it fires. | S — either (a) have the fast-lane build hop itself trigger `schema-change`'s steps 2/6/7/10 (numbers, runtime estimate, batching, gate) whenever it writes a migration file, regardless of lane, or (b) explicitly document that fast-lane migrations are exempted from the database gate by design and rely on `release-runner` step 2 as the sole check — either is a real decision, not a gap to leave implicit; sizing the exact mechanism is for whoever picks this up. |
| 2026-09-03 | eng-manager | aiorders-api | `ENG-024`'s own build hop (fixing `restaurant-portal-onboarding`'s `createRestaurant` insert to set `show_in_marketplace: true`) confirmed the PRD's Risk note: `restaurants.show_in_marketplace` has no `DEFAULT` set in any tracked migration across all five repos — the column was added outside migration history (likely the Supabase dashboard) — so a future insert path that forgets this field the same way `createRestaurant` did would reintroduce today's exact bug silently. Fixing it at the column level (`ALTER TABLE restaurants ALTER COLUMN show_in_marketplace SET DEFAULT true`) closes that class of bug once, rather than per-insert-site. Not built as part of `ENG-024`: `definition-of-done.md`'s fast-lane exclusion list names `schema` explicitly, and this ticket was scoped fast lane on the PRD's own "no schema change (column already exists)" cost line — an `ALTER TABLE` would retroactively invalidate that lane choice, and `restaurant-claims`' insert (the one other known write path, deliberately `approved: false`) is unaffected either way since marketplace visibility already requires *both* flags true. | Prevents a third insert path from silently reintroducing the same visibility gap `ENG-024` just fixed on the second one — the column's own default should carry the safe behavior, not every call site's memory of it. Low urgency: no known insert path is currently missing it once `ENG-024` ships. | XS — one migration, `ALTER TABLE restaurants ALTER COLUMN show_in_marketplace SET DEFAULT true;`; no data change, no code change. Full lane (schema), so it needs its own ticket and G1 regardless of size. |
| 2026-09-03 | architect | aiorders-api | Found while designing `ENG-026` (channel-visibility flags): `admin-portal/handlers/restaurants.ts`'s `updateRestaurant()` (`PUT /admin-portal/restaurants/:id`) destructures `{ brand_owner, ...updates }` from the request body and passes `updates` straight to `.update(updates)` with **no field allow-list at all** — confirmed by reading the function directly. **Distinct from the already-tracked finding on this same function** (`proposals.md`, 2026-08-29, corrected 2026-09-03): that row is about *who* may call it — an ownership/role check, which `ENG-015` has since added to this exact function (unmerged as of this filing). This row is about *what fields* a caller can write once authorized — the ownership fix constrains the caller, not the payload, so even a correctly-scoped staff/partner user can still include arbitrary extra keys in the PUT body and overwrite any column on the row, including `cw_token`, `cw_api_key`, `stripe_account_id`. Same shape as the already-proposed `update_catering_request` gap (2026-08-29, architect, aiorders-api row above — "a correct access check guarding an unconstrained write") — a different function, same pattern, on a table that also holds this codebase's CloudWaitress/Stripe integration secrets. Newly relevant, not just newly noticed: `ENG-026`'s own three new flags (`has_order_food`/`has_dine_in`/`has_catering`) will ride this exact unconstrained path once shipped — legitimate new fields joining an already-open set, not a new hole this ticket creates. | An authorized staff/partner user (once `ENG-015`'s ownership fix merges, scoped correctly to their own restaurants) can still overwrite fields no form intends to expose on this endpoint, on a table whose columns include live payment/ordering-integration credentials — accidental or deliberate. Not confirmed exploited; not blocking `ENG-026`, which only adds three more fields to an already-unconstrained write and does not widen who can reach it. | S — mirror the fix shape already proposed for `update_catering_request`: replace the blind spread with an explicit allow-list of the fields the admin-hub edit form actually sends (the flags this ticket adds, the six existing `RestaurantDetails.tsx` capability booleans, and whichever subset of contact/display fields the form exposes — confirming the exact set is part of sizing this, not decided here). |
| 2026-09-03 | architect | aiorders-api | Found while designing `ENG-019` (restaurant marketing broadcasts): `outgoing-communications/services/sms.ts` has no real SMS provider wired — `createSMSService()` always returns `MockSMSService`; the `twilio`/`messagebird`/`vonage` branches are commented-out `TODO`s that log a warning and fall back to the mock regardless of `SMS_PROVIDER`. This is platform-wide, not specific to any one ticket: every existing trigger that claims to send SMS (`welcome_offer`, `every_order`, `first_order`, plus whatever `communication_templates.sms_enabled` templates restaurants have already turned on) has been logging fabricated `sms_mock_...` message IDs as `status: 'sent'` in `communication_log` this whole time, with nothing ever actually delivered. **Correction, 2026-09-04, `continue ENG-038` build pass, empirically verified rather than re-derived:** the "logs `status: 'sent'` with a fake message id" claim above is wrong for a different, independent reason found while building `ENG-038`'s own SMS-sending path. All three existing call sites (`sendWelcomeOffer`/`sendEveryOrderOffer`/`sendFirstOrderOffer`, `outgoing-communications/actors/consumers.ts:351,561,790`) call `sendSMS({to, body: ...})`, but `services/sms.ts`'s real `SMSData` interface takes `message`, not `body` — confirmed by running `sendSMS({to, body: 'x'})` directly against this tree (`deno run`, this pass): it throws `TypeError: Cannot read properties of undefined (reading 'length')` inside `MockSMSService.sendSMS`, caught by `sendSMS`'s own try/catch, so the actual live behavior is `smsResult = {success: false, error: "Cannot read properties of undefined (reading 'length')"}` on every attempt — which each of the three callers then correctly logs as `communication_log.status = 'failed'`, not `'sent'`. The customer never receives the SMS and the failure is visible in `communication_log`, just under a confusing internal error message rather than silently as "sent". `ENG-038`'s own new `broadcast_message` consumer action calls `sendSMS` with the correct `message` field and does not have this defect. **Correction, 2026-09-05, `continue ENG-039` release-readiness hop: that exemption is the whole story for broadcasts, and it points the other way from "milder than first stated" below.** Re-read `sendBroadcastMessage`'s two `sendSMS` call sites directly (`outgoing-communications/actors/consumers.ts:1079`) — both pass `{ to, message: smsText }`, the correct field name — so a broadcast SMS step never hits the `body`-vs-`message` `TypeError` the correction above found on the three legacy triggers. It reaches `MockSMSService.sendSMS` cleanly, which unconditionally returns `{success: true, messageId: 'sms_mock_...'}`; `resolveRecipient` (`broadcast-dispatch/dispatch.ts:246`) then calls `markRecipient(..., 'sent')`. Confirmed live this pass: `SMS_PROVIDER` is not among this project's 34 provisioned edge-function secrets (`supabase secrets list --project-ref bmnmnejwdxbcqinqkwko`), and `broadcast_campaigns`/`broadcast_campaign_recipients` both hold 0 rows (`supabase db query --linked`) — nobody has sent one yet, so nobody has been shown a false "sent" count yet either. Full trace: `agents/devops/notebook/2026-09-05-release-readiness-log.md`. | Every past and future feature that checks a box for "SMS support" — including `ENG-019`'s own acceptance criteria — is building against a channel that doesn't exist yet in production. **The correction above narrowed the exposure for the three pre-existing triggers only** (loud, logged `'failed'`, not silent) — **it does not apply to broadcasts.** `ENG-039` (shipping this pass) is the first and only UI that lets an owner actually configure and send an SMS-channel campaign, and that path is the one this file's own second correction already (if not explicitly) exempted from the loud-failure behavior: every SMS recipient on a real campaign will show `status: 'sent'` in `ENG-039`'s own report, having delivered nothing, with no error anywhere to notice. This is now the more urgent of the two framings in this row, not the settled one. | M — wiring one real provider (Twilio is the only one with a named-but-stubbed class) behind the existing `SMSService` interface is bounded, known work; sizing above S because it needs a real vendor account/credential and a security pass on the new dependency (`engineering-standards.md`'s dependency rule), not because the code shape is uncertain. **A separate, independent XS fix exists underneath this one**: rename `body` → `message` at the three existing `sendSMS(...)` call sites so the mock (and, once wired, a real provider) actually receives the composed content instead of throwing — mechanical, no design work, worth doing regardless of when/whether the M-sized provider work happens. |
| 2026-09-03 | architect | aiorders-api | Found while designing `ENG-019`: the `customers` table already carries structured per-channel consent (`consent_sms`/`consent_email: { consent: boolean, consent_at, channel?, source?, method? }`, written by `crm/customers.ts` and populated on signup — e.g. `cloudwaitress.ts`'s `createOrder`-adjacent customer-creation call sets both `consent: true` at order time) — but nothing in the actual send path checks it. Confirmed by reading every consumer-facing send handler in `outgoing-communications/actors/consumers.ts` and every marketing job in `autopilot/marketing/*.ts`: zero references to `consent_sms`/`consent_email` anywhere in either directory. Every automated send (welcome, first-order, every-order, birthday, and whatever custom `communication_templates` a restaurant has enabled) fires regardless of what the customer's own consent record says. | A real, structured opt-out signal already exists and is silently ignored platform-wide — not a missing data model, a missing read of one that's already populated. `ENG-019`'s own acceptance criterion 6 (unsubscribe path, opted-out customer excluded from every future send) can and will check this field for the *new* broadcast feature, but that only closes the gap for new sends — every existing reactive trigger keeps ignoring a customer who already said no through whatever `consent_sms: {consent: false}` route exists today (a checkout page, a future account settings page, etc.), which is a standing CASL/compliance exposure independent of this ticket. | M — thread a `consent_sms`/`consent_email` check into `outgoing-communications`' shared consumer dispatch (or each handler, if there's no shared entry point) before any `sendEmail`/`sendSMS` call; sizing above S because it touches every existing trigger handler, not one call site, and needs a decision on default behavior when the field has never been set (opt-in by default, matching `cloudwaitress.ts`'s own `consent: true` at order time, vs. fail-closed). |
| 2026-09-03 | qa | aiorders-api | `ENG-031`'s quality gate is the first ticket on this project whose diff has zero `.ts` files (one SQL migration only), so unlike `ENG-022`/`ENG-024` (same project, same day) there wasn't even an ad-hoc `deno check`/`deno test` to run informally. Confirmed this project has no repo-root test wiring at all: `config/projects.md`'s Commands table is empty across Test/Lint/Typecheck/Build for `aiorders-api`, and no `deno.json` exists at the repo root — though three individual functions (`brand-portal`, `restaurant-marketplace`, `ai-search-openrouter`) do carry their own, undiscoverable from the repo root and unregistered anywhere. Distinct from the already-open 2026-08-29 proposal (no live Postgres reachable to dry-run DDL): that gap is about executing against a *database*; this one is about there being no registered test *harness* at the language/tooling level for this project, full stop — the same distinction a 2026-08-31 `qa` notebook entry already drew for two superficially-similar `aiorders-admin-hub` gaps on `ENG-013`. **Updated 2026-09-07 (`ENG-049`):** a fourth per-function `deno.json` now exists (`loyalty-auto-complete`), and three of this ticket's four touched functions carry real, passing tests run this pass from their own directories (`cloudwaitress.test.ts` 8/8, `sweep.test.ts` 10/10, `loyalty.test.ts` 19/19) — the identical "undiscoverable from the repo root, unregistered anywhere" shape this row already names, not a new gap. Strengthens the case; doesn't change the ask. | `aiorders-admin-hub`'s identical shape was proposed 2026-08-31 after two tickets hit it, and `restaurant-portal` already solved its own version (`ENG-002`, shipped). `aiorders-api` — "highest blast radius of the set" per `config/projects.md`'s own description — is the only registered project with no path to this at all, not even an improvised one, and every future ticket on it with real logic (not just DDL) hits the same wall this one only avoided by having no code to check. | M — a root-level `deno.json` (or a documented convention pointing at each function's own) plus registering the real invocation in `config/projects.md`'s Commands table; the invocation itself is already known and working (`ENG-022`'s and `ENG-024`'s reviews independently hand-derived it — see `observations.md`, 2026-09-03), so this is wiring and documentation, not discovery. |
| 2026-09-03 | eng-manager | aiorders | Third occurrence of the same board-hygiene gap: a ticket is created (board file written, `chained:` fired) but `board/_index.md`'s In-flight table never gets a row for it and the `Next ID` counter never bumps past it. First occurrence 2026-09-02 (`ENG-026`'s counter alone, `observations.md`); second not separately logged before this filing; third is `ENG-035` (this pass's own dead-end-sweep fix — see that ticket's own board-file log and this pass's board-index entry), where the omission wasn't cosmetic: the ticket's own `chained: ENG-035` fire was lost (no hop file, no queue entry, no drop notice, ~9h silent), and because the ticket had no In-flight row, no board-reading pass had a place to notice it was stalled — it took the 02:00 `scheduled` sweep's whole-board mandate, not the board index itself, to surface it. All three occurrences share one root cause: nothing mechanical checks that every `agents/eng-manager/board/ENG-*.md` file has a matching In-flight row (or is accounted for as terminal in the prose below the table) and that `Next ID` exceeds every allocated id. | Per `eng_build_loop.md`'s own "three of the same means the rule doesn't fit reality" logic (stated for process exceptions, same principle applies here): a rule that depends on every pass remembering to update two places for one fact, by hand, has now failed three times, and the third time cost a P0 security ticket a lost chain for most of a day with nothing surfacing it. `lib/eng-gate-check.sh` already enforces other board-consistency facts (e.g. `blocked_from` presence) mechanically instead of by instruction. | S — extend `lib/eng-gate-check.sh` to glob `agents/eng-manager/board/ENG-*.md`, extract each `id:`, and fail if any id has no matching `_index.md` In-flight row and no terminal mention in the prose below it, or if `Next ID` is not strictly greater than the highest allocated id. Mechanical and cheap — same shape as the existing `blocked_from`-presence check. |
| 2026-09-03 | security | aiorders-api | Found during `ENG-032`'s security gate while independently verifying QA's negative-case reasoning (QA's plan cites "`ENG-022`'s own suite already covers `brand-portal`'s access-check call sites generally" as why no new negative-authz test was needed for that ticket). Checked directly against `origin/main`'s actual tree, not the claim: `ENG-022`'s 19 negative tests cover exactly 5 of `brand-portal`'s 10 handler files (`feedback`, `customers`, `hiring`, `website`, `offers`). `catering.ts` — the file `ENG-032`'s own rendered data (`selections`/`action_type`) flows through via `get_catering_requests` — is not one of the five, and no `catering.test.ts` exists anywhere in the directory. Not a defect: read `catering.ts` directly and confirmed all three of its actions (`get_catering_requests`, `create_catering_request`, `update_catering_request`) call `verifyRestaurantAccess` correctly and in the right order. `menus.ts`, `onlineOrders.ts`, and `profiles.ts` share the same no-test-file gap but weren't independently re-verified this pass — out of scope for what `ENG-032`'s own diff needed checked. | The code is correct today, but nothing regression-tests it, unlike 5 of its 9 siblings. Before `ENG-032`, `selections`/`action_type` existed in the DB (since `ENG-031`) but nothing in the UI read them, so a future access-check regression on this call site would have been lower-stakes; as of `ENG-032` shipping, the same call site's return value is rendered directly to the restaurant owner — exactly the condition under which a silent regression first becomes consequential. Not blocking `ENG-032`'s own gate (see `agents/security/reviews/ENG-032.md`, Findings #1) — that ticket adds no code to this repo and no authz-relevant code to test either way. | S — mirror `ENG-022`'s own shape (`feedback.test.ts` etc.): one `catering.test.ts` with a wrong-tenant negative case per action, three cases total. Confirming whether `menus.ts`/`onlineOrders.ts`/`profiles.ts` need the same treatment is part of sizing this, not decided here. |
| 2026-09-03 | eng-manager | aiorders | This `scheduled` pass's step 8 dead-end sweep found `ENG-019`'s `continue` chain silently incomplete: the 14:25:46 fire launched, ran, and exited 0, logging a normal-looking `chained: ENG-019` line, but the architect pass inside it had delegated the actual tech-design work to a background subagent that was still running past the session's own 600s internal budget and got terminated before writing anything — no design doc, no `links.design`, no board update, nothing in `*-eng-events-dropped.md` (it was never a failed or dropped event, just an exit-0 pass whose delegated work silently never finished). Full trace: `ENG-019`'s own board-file log, this pass's dated entry in `_index.md`. A sibling `continue ENG-020` pass caught the same gap by inspection hours earlier (`observations.md`) but correctly left it for this sweep rather than fixing a ticket outside its own contract. | Distinct from the already-open board-index-row gap (row above, 2026-09-03, `ENG-035`'s occurrence): that one is "a ticket exists but the index never learned about it," mechanically checkable from two files. This one is "a chain fired, ran, and reported success, but the work it was supposed to produce doesn't exist" — checkable only by reading the ticket's own downstream artifact (a design file, a receipt, whatever the state claims exists), not by anything in the log line itself. Every `chained:`-based health check this department runs (the dead-end sweep's simple version, `lib/eng-gate-check.sh`) currently trusts the log line at face value; this is the failure mode where that trust is wrong, and nothing bounds how long a ticket can sit silently stalled this way before a full whole-board sweep happens to read its actual artifacts instead of its log. | S–M — the cheapest mechanical version: extend `lib/eng-gate-check.sh` to flag a ticket sitting at `designed` (or any agent-owned state whose exit condition is "artifact X exists") whose named artifact link is blank past some age threshold since its last log entry: same shape as the existing `blocked_from`-presence check, generalized to "the state's own exit condition, not just the state name, must be satisfied." Sizing whether that generalizes cleanly across every agent-owned state (vs. just `designed`) is for whoever picks this up. |
| 2026-09-04 | security | aiorders-api | Found during `ENG-033`'s security gate while reading `catering-request/index.ts` in full for trust-boundary context, not part of that ticket's own diff: `index.ts:348-378` builds the owner-notification `message_html`/`message` via raw template-literal interpolation of `full_name`, `phone`, `email`, `event_type`, `event_location`, `event_timings`, `number_of_guests`, `normalizedDeliveryMethod`, and `requirements` — every one attacker-controlled, since `catering-request` is an intentionally unauthenticated public endpoint — with zero HTML-escaping. `send-notification/index.ts:142-162` forwards both fields verbatim to a third-party workflow webhook (Pabbly Connect), which is what actually emails/SMS/WhatsApps the restaurant owner. Confirmed by reading both files directly. `ENG-033`'s own two new fields (`action_type`/`selections`) are not included in either template, so that ticket's diff neither creates nor worsens this. | An unauthenticated caller can inject arbitrary HTML into a transactional "New Catering Request" notification a real restaurant owner receives — a phishing/content-spoofing vector (fake links, misleading formatting), reachable by anyone, zero cost, today. Bounded impact: most email clients strip `<script>`/inline event handlers, so this is HTML-formatting/link injection, not script execution or data exposure — genuinely Medium, not Critical. Not blocking `ENG-033`'s own gate (pre-existing, out of that diff) — see `agents/security/reviews/ENG-033.md`, Finding 1, and `agents/security/notebook/2026-09-04-findings.md`. | S — HTML-escape each interpolated value at the two `message_html` template-literal sites in `catering-request/index.ts` (a small `escapeHtml()` helper is enough); the plain-text `message` field needs no change. |

| 2026-09-04 | eng-manager | aiorders | This `scheduled` sweep's step-5 merge detection found `ENG-008` had actually been fully merged (both repos, into `main`) for roughly 3.5 hours before the first sweep even reported it — three consecutive prior sweeps (09:37Z, 09:45Z, 09:52Z) all wrongly read `aiorders-api` as "not merged." Root cause: `ENG-009` and `ENG-010` are deliberately stacked on `ENG-008`'s branch (`feat/ENG-008-influencer-admin-management`), and both of their PRs — configured with that branch as their *base*, not `main` — merged into it *after* `ENG-008`'s own PR had already separately merged to `main`. That moved the branch tip past what shipped, so `git merge-base --is-ancestor {branch-tip} origin/main` (the check `eng_build_loop.md` step 5 names) returns false for a ticket that has, in fact, fully shipped. The mirror-image failure hit `ENG-009`/`ENG-010` themselves in the same episode: their PRs show `MERGED` on GitHub, satisfying nothing — merging into a stacked base branch does not deliver the code to `main`, so a human (or a pass) reading the PR list sees green and reasonably assumes done. Full forensics: `ENG-008`'s, `ENG-009`'s, and `ENG-010`'s own board-file logs, this pass's dated entry in `_index.md`. Related to, but distinct from, the already-open 2026-09-02 sibling-staleness proposal above (principal-engineer): that one is about a stacked ticket's *content* going stale against an upstream fix; this one is about the *merge mechanism itself* silently failing to ship regardless of content, and doing so in both directions (a shipped base ticket reads as unmerged; a merged child ticket reads as shipped when it isn't). | Two distinct, compounding risks for every stacked-PR ticket pair on this board (`ENG-009`/`ENG-010` today, and any future ticket sequenced the same way): (1) a genuinely shipped ticket can sit `blocked` indefinitely because merge detection keeps checking a branch name that no longer means what it used to once a sibling merges into it, costing WIP-slot and approver-visibility accuracy; (2) a genuinely unshipped ticket can be mistaken for shipped — by a pass or by the approver — because GitHub's own PR state says `MERGED`, with no code in production and no signal anywhere that says otherwise. (2) is the more dangerous of the two: nothing currently distinguishes "merged to main" from "merged to some other base" anywhere this board surfaces PR status. | S — two independent fixes: (a) merge detection (`eng_build_loop.md` step 5) should check the ticket's own recorded commit (already in `branch:` frontmatter) or `gh pr view --json baseRefName,mergedAt` directly, instead of (or in addition to) the current branch's live tip, whenever that branch is also named as another ticket's stacking base; (b) any gate item or release record that reports a PR as "merged" should name its `baseRefName` explicitly, so "merged into main" and "merged into a feature branch" never collapse into the same word. Both are cheap, local, no new API surface beyond `gh` calls this loop already makes elsewhere. |

| 2026-09-04 | qa | config-site-builder | `ENG-034`'s quality gate is the first ticket on this project whose diff needed real logic coverage (a category-grouped dish picker plus a multi-condition enablement gate — the design's own Risks section calls it "the largest single piece of new logic in this ticket"). Confirmed fresh: no Test cell in `config/projects.md`'s Commands table, no `vitest`/`jest`/`@testing-library` dependency anywhere in `package.json`, no `test` npm script, zero `*.test.*`/`*.spec.*` files anywhere under `src/`. The design's own Risks section attributes this gap to `"ENG-002`'s tracked gap"` — checked, not assumed: `ENG-002` (`agents/eng-manager/board/ENG-002-restaurant-portal-smoke-test-harness.md`) is scoped to `restaurant-portal` only (`project: restaurant-portal`), and no open proposal named `config-site-builder` before this filing — `aiorders-api`'s (2026-09-03) and `aiorders-admin-hub`'s (2026-08-31) own versions of this same gap are both already proposed, but this repo's isn't. | Unlike `aiorders-api` (which at least has `deno test` built into its runtime for free), `config-site-builder` is a Vite/React project with no zero-config test path at all — standing one up means choosing and installing a runner (`vitest` is the natural fit for a Vite project) plus, likely, `@testing-library/react` for component-level assertions. `config-site-builder` is also the most actively-developed project on this board this week (`ENG-034` is its third ticket), so every future feature ticket here hits this same wall — this one only got past it via manual verification with reasons, which does not scale as a permanent substitute. | M — install + configure `vitest`, register the real command in `config/projects.md`'s Commands table, and write a first test against this ticket's own `CateringMenuSelector`/gate logic as the proof it works — same shape `ENG-002` and the open `aiorders-api` proposal both already take. |

| 2026-09-04 | eng-manager | aiorders | `config/conventions.yaml` → `ticket_log.entry.cap_lines: 20` (full reasoning routed to a `agents/{agent}/notebook/` file, only facts-and-pointers in the ticket log) has now been named as violated three separate times in `observations.md` (2026-09-03 ×2, 2026-09-04 ×1) without the drift stopping — most pointedly, `ENG-034`'s own build hop caught the pattern on `ENG-031`/`ENG-032`/`ENG-033` and wrote its own entry short as a self-correction, then that *same ticket's* next two hops (code review + quality, then security) both reverted to long-form entries anyway (each 90+ lines, both `pass` verdicts, neither in `over_cap_allowed_when`) before this release-readiness hop caught it again and moved its own reasoning to `agents/devops/notebook/2026-09-04-release-readiness-log.md`. The self-correction not even holding across hops on the *same* ticket is a stronger signal than the three prior observations individually showed: prose-only reminders (a rule in `conventions.yaml`, an observation naming the drift) are not propagating hop-to-hop even within one ticket's own lifecycle, let alone across tickets. | Every over-cap entry is a tax on every later pass that reads the ticket log in full (the exact cost `eng_build_loop.md` step 10's board-index rolling rule was written to avoid for `_index.md`) — and a rule three times named as violated with no mechanical check is, per `eng_build_loop.md` step 8b's own "third exception of the same kind" logic (same principle, different rule type), a signal the rule doesn't fit how passes actually work day-to-day, not that four separate hops were each unusually careless. `lib/eng-gate-check.sh` already enforces other ticket-log/board facts mechanically (e.g. `blocked_from` presence) instead of leaving them to instruction. | S — extend `lib/eng-gate-check.sh`'s existing `check_ticket()` to count lines per log entry (split on the `- \`YYYY-MM-DD\`` entry-start pattern already used throughout every board file) and fail when an entry exceeds `cap_lines: 20` with no `over_cap_allowed_when` marker ([gate_fail]/[incident]/[approver_answer_verbatim] — the marker itself would need defining, since today the exemption is judged by prose, not tagged) present in that entry. Same shape as the existing `blocked_from`-presence check. |

| 2026-09-04 | eng-manager | aiorders | `board/_index.md`'s header narrative (everything above `## In flight`) has grown to roughly 350 lines across many passes, each appending one more paragraph the same way this pass is about to — and a large share of it now documents tickets that are already terminal (`ENG-008`, `009`, `010`, `013`, `015`, `022`, `024`, `031`, `032`, `033`, `034` among others), which need no "recent narrative to resume a ticket" (`eng_build_loop.md` step 10's own stated purpose for what belongs here) since there is nothing left to resume. The file's dated `## {date} —` entries already have a mechanical keep-three rule for exactly this failure mode (`eng_build_loop.md` step 10, written after the 2026-08-12 incident in `eng_build_loop-rationale.md` §"Board index rolling"); the header prose above those entries has no equivalent and has never been rolled. | Every pass reads this file in full (step 10's own stated reason the dated-entries rule exists) — the header is now a bigger read than the three live dated entries combined, and it is pure tax: none of the terminal-ticket paragraphs are needed to resume anything, only to explain how something already `verified`/`dropped` got there, which is what each ticket's own board file and the decision journal already record. | S — no code change, a one-time edit: move every header paragraph that discusses only now-terminal tickets down to `_index-archive.md` (same destination the dated entries already roll to), leaving the header as the "currently 1/1, held by X" state plus whatever in-flight tickets still need context to resume. Whether to also add a mechanical size/age check (mirroring the dated-entries rule) or leave this as an occasional manual pass is for whoever picks this up to decide. |
| 2026-09-04 | principal-engineer | aiorders-api | Fourth occurrence of the same untracked-`deno.lock` pattern, this time three at once: `ENG-038`'s round-1 review (this filing) found `supabase/functions/brand-portal/deno.lock`, `broadcast-dispatch/deno.lock`, and `broadcast-unsubscribe/deno.lock` all untracked in the department's own `aiorders-api` worktree. The first is the same file `ENG-029`, `ENG-031`, and `ENG-037`'s own board-file logs each independently noticed and flagged — verbatim, `ENG-037`'s: "still untracked... now independently noticed by three passes... without being cleaned up or explained" — and the other two are new instances of the identical shape on the two brand-new function directories this ticket adds, meaning the pattern is actively spreading to every new `deno.json`-carrying function rather than shrinking. | Per `eng_build_loop.md`'s own "third exception of the same kind" logic (written for process exceptions, same principle applies to a repeatedly-renoticed hygiene gap): four independent passes noticing the identical file with zero action taken is a signal this needs a decision, not a fifth observation. Either `deno.lock` is meant to pin resolved `npm:`-specifier versions the same way any other lockfile in this instance's projects is committed (in which case it's a real, if small, gap — dependency resolution isn't pinned for any of these functions today), or it's meant to be environment-generated and untracked (in which case `.gitignore` should say so, once, rather than every review re-deriving that it's harmless). Cheap either way; the cost is entirely in re-noticing it. | XS — decide the intended convention (commit vs. ignore) for `deno.lock` under `supabase/functions/*/`, then either commit the existing files and note the step in the department's own function-scaffolding practice, or add `supabase/functions/*/deno.lock` to `.gitignore` — no code change either way. |

| 2026-09-04 | product-manager | aiorders | `acceptance-check/SKILL.md`'s own trigger ("a ticket enters state `shipped`") has not actually fired, with a written notebook entry, since `ENG-007`/`ENG-011` on 2026-08-30 (`agents/product-manager/notebook/2026-08-30-acceptance.md`, the last file in that directory before this date). Ten tickets shipped and reached `verified` in between — `ENG-008`, `009`, `010`, `013`, `015`, `022`, `024`, `031`, `032`, `033` — every one via step 5's merge detection, and every one's own board-file log describes the `shipped → verified` step as "receipt-confirmation and bookkeeping only" (re-reading the three gate verdicts, confirming no migration, flipping state) rather than the skill's own step 2 ("check against the live result... not against the test suite, not against the PR description, not against the engineer's summary"). `agents/eng-manager/config/exceptions.md` carries zero rows — this was never granted as an exception, at this rule or any other; per `eng_build_loop.md` step 8b's own "third exception of the same kind" logic, ten silent occurrences of the same skip is a strong signal either the rule needs updating to formally allow a lighter check for a step-5-detected merge with all upstream gates already `pass`, or the department needs to actually start running it again. This pass (`ENG-034`) ran the full check rather than add an eleventh — see `agents/product-manager/notebook/2026-09-04-acceptance.md` for what a from-scratch run looks like, including the honest substitute used where live/production access doesn't exist (reading the exact deployed source at the merged commit, same shape `ENG-007`/`ENG-011` already set). | "Merged" quietly becoming the finish line is exactly the failure this skill exists to prevent (its own stated Purpose) — and for ten consecutive ships it has been exactly that, undetected until this pass happened to read the notebook directory and notice the gap. Left alone, an eleventh, twelfth, and every future ship keeps skipping the one check that verifies the PRD's actual promise against the actual shipped thing rather than against upstream evidence that someone *intended* it to work — the receipts (review/QA/security) are real signal, but they are explicitly the proxies this skill's own step 2 says not to substitute for the live result. | S — decide, at the approver level, one of two things and write it down where the next ten tickets will actually see it: (a) reaffirm the skill as written and add a mechanical check (`lib/eng-gate-check.sh`, alongside its existing receipt checks) that a `verified` ticket has a same-or-later-dated file in `agents/product-manager/notebook/` naming it, so the skip is caught immediately rather than found by chance a tenth time; or (b) formally amend the skill to define what "check against the live result" means for a step-5-detected L1 merge with all upstream gates already `pass` (this instance's actual, repeated practice), so the shortcut becomes a documented reading of the rule instead of an unexamined drift from it. Either answer is better than the current state, where the written rule and the actual practice have quietly diverged for ten tickets running. |

| 2026-09-04 | devops | aiorders-api | `ENG-038`'s own release-readiness hop (this filing) found its migration gate never ran. The ticket's `building` hop (`owner: backend`) wrote a real schema migration itself — `supabase/migrations/20260904150000_broadcast_recipients_claimed_status.sql` (drops/recreates a check constraint to add a `claimed` status value, adds a new index on `broadcast_campaign_recipients.step_id`, adds a new check constraint on `broadcast_campaigns.scheduled_send_at`) — directly against `agents/backend/agent.md`'s own `never_touches: schema design or migrations (database owns those — you request, they design)`. No `agents/database/migrations/ENG-038-*.md` plan/receipt exists anywhere, and no board-file log entry attributes any hop to `database` — unlike every other migration on this board (`ENG-006` through `ENG-037`), each of which carries a `database`-authored plan doc with a rollback actually executed against a disposable Postgres replica before shipping. `ENG-038`'s own rollback exists only as a trailing SQL comment, never run. Code review and security both read the file for their own purposes (constraint legality; whether it closes an authz finding) but neither applies `database`'s own criteria (`agents/database/agent.md`: "Write the rollback, then run it... before the forward migration ships"). `conventions.yaml` names migration as one of five separate blocking machine gates; `skills/release-runner/SKILL.md` step 2 is the documented backstop ("a missing verdict is a fail, not an assumption — return the ticket to the gate that never ran") and is what caught this — confirmed the pre-pass `lib/eng-gate-check.sh` run (both scoped and whole-board) exited 0 clean despite the gap already existing. Related to, but a distinct root cause from, the 2026-09-03 principal-engineer row above (that one is fast-lane's design never triggering `schema-change/SKILL.md`; this is full-lane, where the trigger was available but nothing enforced that `backend` route the migration to `database` instead of writing it itself). | The backstop the 2026-09-03 proposal already named as the intended safety net has now actually fired, on a full-lane ticket, catching an untested, unreviewed schema change one hop before it would have shipped to a project with no CI/CD auto-deploy gate of its own to catch it a second time. Nothing mechanical prevents `backend`/`frontend` from writing a migration file directly and riding it through review/QA/security on their own incidental scrutiny alone. Two independent proposals now point at the same missing mechanical check from opposite lanes; a single fix likely closes both. | S — extend `lib/eng-gate-check.sh` to flag any ticket whose diff touches a project's own migrations directory (e.g. `supabase/migrations/*.sql`) with no matching `agents/database/migrations/{ENG-NNN}-*.md` receipt on disk before allowing `ready-to-ship`/`shipped` — same shape as the existing `blocked_from`-presence check. Whether this subsumes or should merge with the 2026-09-03 fast-lane proposal's own fix is for whoever picks this up. |

| 2026-09-05 | database | aiorders-api | `ENG-020`'s migration-verification pass, checking the design's own presumed index on `customers(restaurant_id, created_at)` against the live schema dump (`bmnmnejwdxbcqinqkwko`) rather than trusting the presumption: no such composite exists. What's live is `customers_restaurant_id_idx (restaurant_id)` alone, plus `idx_customers_analytics (restaurant_id, total_orders, total_spend)` — neither covers a `created_at` range filter. The sibling table, `orders`, does have the equivalent composite (`idx_orders_analytics_optimized`, confirmed present for both `ENG-020` and, independently, `ENG-037`'s own migration doc) — only `customers` is missing its half of the pair. | Every acquisition/attribution query scoped to a restaurant and a date range (this ticket's own `get_acquisition_breakdown`, and any future one shaped like it) can only index-scan on `restaurant_id` and then filter `created_at` row-by-row. Fine at today's per-restaurant customer-list sizes (no restaurant on this platform is large enough for this to show up as measurable latency yet), but it's the same class of gap this board has already named once for `orders` before `ENG-037` confirmed it wasn't actually missing there — better to add it once, deliberately, than have a future ticket's design keep presuming it's present. Per the design's own explicit instruction ("if either is missing, raises it as a separate ticket rather than adding an index inside this one"), not bundled into `ENG-020`'s own migration. | XS — `CREATE INDEX CONCURRENTLY idx_customers_restaurant_created_at ON customers (restaurant_id, created_at)`, matching `idx_orders_analytics_optimized`'s own shape. No design work needed; sizing is purely "when," not "how." |
| 2026-09-05 | qa | aiorders-api | `ENG-040`'s quality gate, writing the first test plan to touch `brand-portal/website.ts`'s *write* action: `website.test.ts`'s fake Supabase client has never mocked `.update()`/`.insert()`, for any key, since the file was created — confirmed by reading the harness directly (`FakeQuery` had only `select`/`eq`/`single`/`then`) and by the file's own test list, where `updateWebsiteContent`'s sole existing test is the access-denial case; no positive/success-path test exists for `catering`, `careers`, or (until this ticket) `faqs`. Closed for `faqs` only, this round: extended the fake with `update`/`insert` capture and added one test proving the insert branch receives exactly `{restaurant_id, faqs}`, mutation-checked by dropping `faqs` from `EDITABLE_PAGES` and confirming only that test went red. `catering`/`careers`' identical gap was not touched — pre-existing, not introduced by or blocking this XS ticket. Full detail: `agents/qa/test-plans/ENG-040.md`. | Two keys that already ship to real restaurant owners (`catering`, `careers`) have no automated evidence their write path actually persists what a caller sends — only that unauthorized callers are denied. A regression that silently dropped a field from the `updates` object, or swapped the update/insert branch condition, would pass this suite today. The harness gap is now half-closed (the plumbing to mock a write exists and has one working example to copy) rather than needing to be discovered and built from zero, which is most of the cost in a fresh instance of this same finding elsewhere in the codebase. | S — two tests, `catering.test.ts`-shape: mirror this round's new `updateWebsiteContent persists a faqs payload...` test for `catering` and `careers`, reusing the same `fakeSupabase` extension (already committed, `aiorders-api@104b057`) rather than building harness support again. |
| 2026-09-07 | eng-manager | aiorders | Third occurrence of the pattern `observations.md` already flagged twice (2026-09-06 row: discovered the skew, no wrong nudge yet; 2026-09-07 row: a near-miss, `ENG-028`/`ENG-042` correctly nudged with only ~65 seconds to spare) — this time an actual wrong action, not a near-miss. This `continue ENG-026` pass's own notify sweep read "current time" via `date -u` (UTC) and diffed it against four items' `notified:` frontmatter without converting — but cross-checking `traces/eng-notify-2026-09-06.log` proves those stamps are local PDT wall-clock time to the second (`notified: 2026-09-06T03:13:31` on `ENG-018`'s G1 matches that log's own `[03:13:31] sent: active ...eng018...` line exactly). Naive UTC-vs-local diffing read all four as 30-31 hours old; correct local-to-local math (nudges sent ~02:37:56-58 PDT on 2026-09-07, per this pass's own trace) shows `ENG-028`/`ENG-042` (`notified: 2026-09-06T02:28:29`) were genuinely ~24h09m — correctly due — but `ENG-018` (`notified: 2026-09-06T03:13:31`) was only ~23h24m and `ENG-043` (`notified: 2026-09-06T02:47:56`) only ~23h50m — both nudged 10-36 minutes before their true 24h mark. Not reverted: the Telegram nudge already sent cannot be unsent, and `nudged:` accurately records when the action happened; `ENG-026`'s own board-file log for this date carries the corrected math and the honest account rather than the original mistaken figures. | "Exactly one nudge, ever" assumes the one nudge lands at or after the real 24h mark, not up to ~40 minutes before it — a small miss here, but the same skew could land much further off on an item raised at a different hour, and the underlying cause (every pass re-deriving "current time" in whatever convention it individually picks — some UTC, some local, per the 2026-09-06 row's own two examples) guarantees this keeps recurring by chance rather than by a fixed rule. Third occurrence crosses this repo's own three-strike line (`eng_build_loop.md` step 8b; `proposals.md`'s own convention elsewhere on this board) from "worth a look" to "fix it." | S — either (a) have `lib/eng-notify.sh` itself stamp `notified:`/`nudged:` with an explicit UTC offset (`date -u '+%Y-%m-%dT%H:%M:%S%z'` or equivalent) so every future comparison is unambiguous regardless of which convention the comparing pass reaches for, with a one-time normalization note for the existing bare-local stamps already on disk; or (b) document, in `eng_build_loop.md` step 7 itself, that `notified:`/`nudged:` are local wall-clock and every age computation must convert to a common clock first. Either closes the ambiguity at its source instead of relying on each pass to remember it. |
| 2026-09-07 | backend | restaurant-marketplace | `ENG-045`'s own build hop (porting `getOpenState` to `aiorders-api/_shared/openingHours.ts`) found that `public.restaurants.opening_hours` (live query, `bmnmnejwdxbcqinqkwko`, two sampled rows) actually stores `{day, open, close, isClosed?, h24?}` objects — `_shared/googlePlaces.ts`'s own `OpeningHours` shape — not the `weekday_text` string list (`"Monday: 9:00 AM - 5:00 PM"`) `restaurant-marketplace`'s source `src/utils/openingHours.ts` (`origin/master`) is written to parse. Read directly: `toWeekdayLines`'s only array branch filters for `typeof line === 'string'`, so handed an array of `{day, open, close}` objects it filters everything out and returns `null` -- `getOpenState` then returns `{isOpen: null, label: null}` unconditionally. `RestaurantDetail.tsx` (per `ENG-026`'s own design doc, "wired only into the single-restaurant detail page") calls this client-side function directly on `restaurant.opening_hours` from this same backend. Not confirmed in a live browser this pass -- out of `aiorders-api`'s own worktree -- but the code path and the live data shape both point the same way. `aiorders-api`'s own copy of this parser (`_shared/openingHours.ts`, this ticket) now handles both shapes; `restaurant-marketplace`'s client-side original does not, and this ticket's diff is `aiorders-api`-only, so the frontend file is untouched. | If confirmed, the detail page's opening-hours/"is this open" display has likely never worked against real data -- silently, since fail-open on unparseable input (deliberate, ADR-010's own principle) means it degrades to "show nothing" rather than erroring, exactly the failure mode that hides in the UI instead of a log. | XS -- apply the same `toWeekdayLines` shape-adapter this ticket added to `aiorders-api/_shared/openingHours.ts` (recognize a `{day, open, close, isClosed?, h24?}` array and convert each entry to a `"Day: hours"` line before the existing string-line parsing) to `restaurant-marketplace/src/utils/openingHours.ts`'s own copy. Confirm first with one manual load of the detail page in a browser against a restaurant with real hours, before assuming the fix is needed. |
| 2026-09-07 | frontend | restaurant-marketplace | `ENG-047`'s own build hop found `npm run lint` cannot run at all: `.eslintrc.cjs` has an extra `]` closing the `rules` object one bracket too many (line 18), throwing `SyntaxError: Unexpected token ']'` before any file is linted. Confirmed pre-existing and untouched by this or any other ticket: `git diff origin/master -- .eslintrc.cjs` is empty from a fresh branch, and `git log -- .eslintrc.cjs` shows no commits since `380cd65` ("First push MVP") — this repo's very first commit. Also confirmed nothing has ever caught it: `.github/workflows/deploy-cf.yml` runs only `npm install` then `npm run deploy-cf` (`build` + `wrangler pages deploy`), never `lint`. Separately, in the same worktree: this repo has no `package-lock.json`, and it's listed in `.gitignore` by name (not merely absent) — `npm install` here resolves whatever the current registry offers within each dependency's declared range, not a pinned tree, which is the opposite of `engineering-standards.md`'s "Pin versions. Lockfiles are committed." Neither gap is this ticket's to fix (unrelated files, no drive-by refactor), and neither is new — both predate every ticket this board has shipped against this repo. | This registered L1 project has had a broken code-quality gate since its very first commit — `config/projects.md`'s own note that `restaurant-marketplace` is "the only repo here with a `typecheck` script" already implied `lint` carries real weight here, but it has silently checked nothing, ever, for `ENG-047`'s diff — the first engineering-department ticket ever filed against this repo (`ENG-026`, the only earlier one, is a work-breakdown container with no diff of its own) — or any human-authored commit before it. **Correction, this ticket's own quality gate:** the row as originally filed named `ENG-032`/`ENG-034` here; both are real tickets but neither touches this repo (`restaurant-portal` and `config-site-builder` respectively) — corrected in place rather than left to mislead the next reader, per `agents/qa/notebook/2026-09-07-coverage-gaps.md`. The missing lockfile compounds it: a future dependency bump on the registry can silently change what every future `npm install` resolves to, with no diff to review and no way to reproduce today's exact tree later. Neither has caused an incident yet, purely because nothing has been exploited or drifted badly enough to notice — same shape as the `.env`-tracking findings already on this list. | S — fix `.eslintrc.cjs`'s bracket (one-character diff, then actually run `npm run lint` once to see what a repo that's never been linted surfaces — likely more than zero findings, size that separately once seen); commit a fresh `package-lock.json` and drop the `.gitignore` line, same shape as any other repo's dependency pinning. |
| 2026-09-07 | qa | restaurant-marketplace | `ENG-047`'s quality gate is the first ticket on this project whose diff carries any real conditional logic (the meta-line growing-separator composition in `RestaurantCard.tsx`, plus depending on `RestaurantList.tsx`'s existing tab-reset/Clear-All effects already handling `openNow` correctly) rather than pure boilerplate threading. Confirmed fresh: `config/projects.md`'s Commands table carries an empty `Test` cell for `restaurant-marketplace` (only `Lint`/`Typecheck`/`Build` are registered), no `test` script or `vitest`/`jest`/`@testing-library` dependency anywhere in `package.json`, zero `*.test.*`/`*.spec.*` files anywhere under `src/`. Unlike its three siblings (`aiorders-admin-hub`, 2026-08-31; `aiorders-api`, 2026-09-03; `config-site-builder`, 2026-09-04), no open proposal already tracks this exact gap for `restaurant-marketplace` — only `restaurant-portal` has actually closed it (`ENG-002`, shipped). | Every future `restaurant-marketplace` ticket with real logic hits the same wall this one only got past via inspection plus an independent `typecheck`/`build` re-run (`agents/principal-engineer/reviews/ENG-047.md`) — the same substitute evidence class every sibling gap above already relies on, and it scales no better here than it did there. | M — same shape as the three sibling proposals: install and configure `vitest` (this repo already runs Vite, same as `config-site-builder`) plus `@testing-library/react` for the component layer, register the real command in `config/projects.md`'s Commands table, and write a first test against this ticket's own `RestaurantCard` status-render branch or `FilterBar`'s `openNow` wiring as the proof it works. |
| 2026-09-07 | principal-engineer | aiorders-api | Found while reviewing `ENG-048`'s new `credit_order_if_eligible` function (round 1, failed on this and one other finding — see the ticket's own board-file log and `agents/principal-engineer/notebook/2026-09-07-review-log.md`), then checked against the rest of this project rather than left as one function's own bug: `calculate_platform_analytics` (`20260217000001_platform_analytics_cron.sql`) and `get_acquisition_breakdown` (`20260905190000_add_acquisition_breakdown_rpc.sql`) each add only `GRANT EXECUTE ... TO service_role`, with no matching `REVOKE ... FROM PUBLIC` anywhere in this repo's migration history (checked: zero `revoke` statements exist at all, repo-wide). Confirmed live rather than assumed from generic Postgres defaults (`supabase db query --linked`, `pg_proc`/`pg_roles`/`has_function_privilege` — system-catalog read only, no row data): `anon` and `authenticated` **both currently have `EXECUTE`** on both functions, identically to `service_role`. The explicit grant is a no-op against them — Postgres's own default (`EXECUTE` to `PUBLIC` on function creation) is still in effect on this project and nothing has ever revoked it. | Both functions read as intended to be internal/system-only (cron-invoked, `service_role`-authenticated callers only, per their own migrations' framing) but are in fact directly callable today by any authenticated — and possibly anonymous — client of this live production project via `supabase.rpc(...)`, e.g. `get_acquisition_breakdown` (aggregate acquisition/attribution figures) and `calculate_platform_analytics` (whatever platform-wide analytics it computes) becoming readable outside their intended callers. Not confirmed exploited, and not independently deep-audited here — this proposal surfaces the pattern and the two named instances; assessing actual data sensitivity and any real exposure is `security`'s own call, not re-derived by this finding. Likely the same gap on every other function in this repo lacking an explicit `REVOKE`, i.e. most of them — worth a project-wide sweep, not a two-function patch. **Correction, `ENG-048`'s own round-1 fix (this date):** `revoke execute ... from public` alone was applied to `credit_order_if_eligible` and verified against a disposable replica of this project's own Postgres image (`supabase/postgres:15.8.1.073`) — `anon`/`authenticated` **still** had `EXECUTE` afterward. Root cause is one layer deeper than "PUBLIC's default is still in effect": `pg_default_acl` on this project's `public` schema carries an explicit default-privileges entry (grantor `postgres`, object type `f`) granting `EXECUTE` to `anon`/`authenticated`/`service_role` by name at function-creation time — a separate mechanism from the PUBLIC pseudo-grant, and one a `REVOKE ... FROM PUBLIC` cannot touch. Confirmed the fix that actually closes it: naming both roles directly, `revoke execute on function {name} from public, anon, authenticated;`. Corrected in place rather than left to mislead the next reader, per the migration plan doc and board-file log cited in `ENG-048`'s own entry above. **Superseded by `ENG-050` (2026-09-07):** security confirmed both functions actually live-reachable by `anon` against the real production project and filed the P0 carve-out ticket directly (`schedules/eng_build_loop.md` step 3) rather than leaving this to ride the weekly batched G1 — see the ticket and `agents/security/notebook/2026-09-07-findings.md`. This row stays as the historical record of the finding; no batched G1 needed for it. **Sizing decision, `ENG-050`'s own design pass (2026-09-07):** the architect scoped `ENG-050` to exactly the two functions security confirmed live-exploitable, matching its PRD's AC1–5 and its S/"an hour or two" estimate — the wider sweep stays open at this row, not folded into that ticket (`ENG-050`'s own design doc, "Out of scope"). Re-verified this pass, independently: grepped all five registered repos and confirmed no caller beyond `ENG-050`'s own two already-known `service_role` call sites exists for either of *its* two functions — irrelevant to the ~10 *other* functions this row is actually about, but confirms `ENG-050` itself isn't quietly narrower than believed. | S per function (`revoke execute on function {name} from public, anon, authenticated;` alongside the existing grant — **not** `from public` alone, confirmed insufficient on this project) once security confirms scope; **M** if scoped as the project-wide sweep the pattern actually calls for — sizing the full blast radius (which of this repo's ~12 functions are meant to be public vs. internal) is for whoever picks this up. |
| 2026-09-07 | principal-engineer | aiorders-api | Found while re-reviewing `ENG-048` round 2 (its own `credit_order_if_eligible` fix, passed — see the ticket's own board-file log and this date's review-log entry): `loyalty_ledger_entries`'s own comment claims "append-only, never updated or deleted," but no migration on this project revokes `UPDATE`/`DELETE` on it from `service_role` — enforced by convention only. `service_role` bypasses this project's RLS by default (confirmed on a disposable replica of this project's own image, `supabase/postgres:15.8.1.073`: `Bypass RLS` present on `service_role`, absent on `anon`/`authenticated`), so any future service-role code path — a mis-written correction on ticket 5 of this same sequence, or an unrelated handler reusing the service client — could mutate or delete a ledger row with no database-level guardrail. Checked this isn't unique to `ENG-048` before filing: `create_influencer_notes.sql` makes the identical "append-only" claim with the identical (non-)enforcement, and a repo-wide grep found zero `revoke update`/`revoke delete` statements anywhere in `supabase/migrations/`. A genuinely repo-wide pattern, not a gap this ticket introduced or made worse. | Not confirmed exploited, and no evidence anything has ever written to either table this way — this is a latent integrity gap, not an active one, and materially lower severity than this same review's own B1-shaped finding above (that one was reachable by any already-authenticated external client; this one only by the department's own future backend code). Worth surfacing precisely because this is "the department's first money-adjacent write path" (the PRD's own framing, already used twice on this ticket to justify over-enforcing): an audit trail that can be silently mutated by a future engineer's mistake, with nothing in the schema to stop it, is exactly the class of thing this role's own charter says belongs in the database, not only in a comment. | S per table (`revoke update, delete on {table} from public, anon, authenticated, service_role;`, leaving only the security-definer function's own insert path able to write — confirming no other legitimate direct-insert caller exists first, since dine-in earn per this ticket's own design is expected to insert directly as `service_role`) once whoever picks this up confirms the full set of "append-only" tables this should apply to; M if scoped as a project-wide sweep alongside the B1-shaped proposal above, since both stem from the same "constraints belong in the database, not in a comment" gap. |
| 2026-09-07 | principal-engineer | aiorders-api | Found while reviewing `ENG-049`'s two new `brand-portal` actions: the design's own `## Interfaces` section instructs `record_dine_in_earn` to fail access checks with "the same 403-shaped error every other brand-portal action already returns (AC17)" — read literally and checked, not assumed. Grepped the literal string `403` across the entire `brand-portal` function: it appears exactly once, at `index.ts:100`, for an unrelated API-key-scope check. Every `requireRestaurantAccess` failure across all six actions that call it (`customers.ts`, `website.ts`, `hiring.ts`, `broadcasts.ts`, `feedback.ts`, and now `loyalty.ts`) throws a bare `Error` that the function's own top-level `catch` (`index.ts:219`) turns into an **HTTP 500** with `{error: 'Internal server error', details: error.message}` — never a `403`. `ENG-049`'s own `loyalty.ts` matches this exactly, which is the correct call per-ticket (`engineering-standards.md`'s "reads like the code around it," and fixing the status code for one action alone would be a second, inconsistent convention) — this is the design's own label describing a pattern that was never literally true, not a code defect in this or any prior ticket. | Not confirmed to have caused any actual harm — every access denial does still fail closed (no data returned, an error surfaces), so there's no authorization bypass here, only a wire-level status code that doesn't match what six different design docs and this ticket's own AC17 have now called it. Worth fixing precisely because a future caller (a frontend, a monitoring dashboard, an on-call engineer skimming status codes) reasonably expects a `403` to mean "access denied" and a `500` to mean "the server broke" — today both land on the same code for every action in this file, and nothing distinguishes an access denial from a genuine internal error either in the HTTP layer or in any log a dashboard would alert on differently. | XS to correct the design template's own language (stop calling it "403-shaped" until it is one); S–M to actually differentiate status codes in `brand-portal/index.ts`'s top-level catch (map a `requireRestaurantAccess` denial to `403`, a validation `Error` to `400`, everything else to `500`) — a caller-visible behavior change across all six actions in this file, so scoping needs a decision on whether any existing caller depends on today's uniform `500`, not just a code change. |
| 2026-09-07 | devops | aiorders | `eng_build_loop.md` step 6 states, unambiguously, that "every ticket at `intake`, `shaped` or `awaiting-scope`... is the only place a *new* start is drawn from," yet this board's own history (`ENG-019` 2026-09-03, `ENG-020` and `ENG-021` 2026-09-05, `ENG-026` 2026-09-06 — all in `_index.md`'s own narrative, still readable there) has repeatedly picked a fresh dispatch from the `designed` state instead — a "held-for-slot pool" of "tickets with a completed design and no one-way door, deferred only by the cap" — whenever To-do held nothing startable. Confirmed this is a real, repeated pattern and not a one-off misreading: four separate dispatches across five days used the identical phrase and the identical reasoning. This `continue ENG-049` pass hit the same fork (`ENG-027`'s family fully parked, To-do's four occupants all genuinely blocked on an open G1/clarification) and chose the literal reading instead — raised `IDLE-2026-09-07.md` rather than drawing from `designed` — precisely because nothing in `eng_build_loop.md` itself (read in full this pass, not just the `continue` reading-map subset) sanctions the pool the way the board's own narrative describes it; it appears to be a convention the department invented and repeated, never written back into the schedule document itself. | Two different things are happening under one name, and neither is being tracked correctly. If the "held-for-slot pool" is right (a `designed` ticket with no G2 needed and no one-way door is functionally "ready, just capacity-blocked," not "waiting on a human"), it should be written into `eng_build_loop.md` step 6 itself so every future pass — including this one — applies it on purpose instead of by pattern-matching four prior board entries, and so raising "Nothing I can start" stops happening prematurely when work is in fact available. If it's wrong (To-do really is the only source, full stop), four prior dispatches started work the schedule document doesn't actually authorize, which is worth knowing regardless of whether any of them turned out badly. Either way, the department has been silently operating on a rule that exists only as repeated board-narrative precedent, not as written policy — exactly the gap step 8b's own three-strikes rule (already invoked twice above on unrelated patterns) exists to catch. | S — read `eng_build_loop.md` step 6 against the four cited dispatches and decide: either add an explicit "held-for-slot pool" clause (defining exactly which `designed` tickets qualify — no G2 needed, no one-way door, and presumably still ordered by `priority`/severity/id the same as To-do) or confirm the four prior dispatches were a process deviation and note it so a future `scheduled` sweep doesn't need to re-discover the same tension this pass did. |
| 2026-09-07 | eng-manager | aiorders | Third occurrence of the same root cause, each time a different symptom. **(1)** 2026-08-26, this file: a control-center action flipped `ENG-002` `blocked → shipped` directly, bypassing its own open merge-request item and `eng-gate-check.sh`'s ancestry check — caught by luck, because a `scheduled` sweep happened to cross-check that one ticket's state against its own inbox item. **(2)** 2026-09-07, `observations.md`, `ENG-048`'s release-readiness hop: the same class of dashboard flip (`ENG-045`/`ENG-046`/`ENG-047`, merge-detected) left `_index.md`'s In-flight table and "Waiting on the approver" narrative stale for all three, because nothing re-derives that hand-maintained text from the tickets it summarizes. **(3), this pass:** those same three dashboard flips (plus `ENG-044`'s, earlier) never fired `continue ENG-026` — a build-loop pass, not a dashboard write, is the only thing that ever fires the next hop, and a dashboard write skips that step entirely. Every `ENG-026` child reached `shipped` this way; the parent's own `ADR-003` closing hop (all four children settled, exemption satisfied) sat unfired with nothing coming until this `scheduled` sweep — the department's own safety net — happened to re-derive the family's state from scratch and notice. Root cause, named once instead of three times: the control center writes a ticket's `state:` directly and stops there; it never calls `lib/eng-trigger.sh continue {ticket-id}` the way every build-loop pass itself is required to when it leaves a ticket in an agent-owned state (step 9). **(4), same day, `ENG-026`'s own closing pass:** the parent's forced `ADR-003` recheck is what caught (3) at all — but none of the four children (`ENG-044`–`047`) had individually reached `verified` the way every sibling family's children had (`ENG-031`/`033`/`034`, `ENG-038`/`039`, `ENG-040`/`041` all show each child at `state: verified` on its own frontmatter, each via its own `acceptance-check` hop). Each sat at `shipped` with `acceptance-check/SKILL.md`'s own trigger never having fired on it at all — not even the shortcut "receipt-bookkeeping" version the 2026-09-04 row above already criticizes, since no pass ever touched them again to take even that shortcut. Closed this pass by folding each child's own AC slice into the parent's from-scratch walk (`agents/product-manager/notebook/2026-09-07-eng026-acceptance.md`) and flipping all four to `verified` directly, rather than leaving the gap standing. **A same-shaped ticket with no parent has no equivalent forcing function and would sit at `shipped`, unchecked, indefinitely** — this family's own gap self-healed only because `ADR-003`'s parent exemption happens to force exactly this recheck; a standalone ticket dashboard-flipped to `shipped` has no analogous safety net anywhere in the procedure. | Each individual instance was caught — twice by a `scheduled` sweep's own thoroughness, once by luck — so nothing has actually been lost yet. But the pattern is now three-for-three on "a dashboard write is functionally invisible to the loop's own event/chain machinery," and the specific consequence keeps escalating: a stale gate item, then stale display text, now a real (if bounded, ~hours-long) delay on a parent ticket's own terminal transition. The department's stated design goal is that chaining "reaches review in minutes instead of at the next scheduled pass" (`eng_build_loop.md`, "The chain"); every dashboard-driven transition quietly opts out of that guarantee and falls back to whatever the next `scheduled`/`watch` sweep happens to catch, which is exactly the cron-cadence behavior the event-driven design was built to replace. Fixing the one root cause (the control center firing `continue {ticket-id}` itself after any write, same as `lib/eng-trigger.sh` requires of every pass) would close all three symptoms at once rather than adding a bespoke cross-check for each new one that turns up. | M — the fix lives in the control-center codebase (`control-center/server.py` / the merge-detection action), not in this department's own scripts, so scoping it means finding where that write happens and adding the same trigger call `eng-trigger.sh` itself makes; sizing the actual change is for whoever owns that surface. |
| 2026-09-07 | eng-manager | aiorders | `observations.md`'s own 2026-09-07 row on this ("fourth consecutive pass ... worth a proposal ... if this recurs at this frequency") named its own trigger condition, and it just fired again, faster. Exact count from `traces/eng-loop-2026-09-07.log`'s `pass start`/`pass end` lines: **seven launches in just over one hour, of which five completed — and every one of those five reached the identical zero-information conclusion** ("`ENG-048`/`ENG-049` still open, Machine WIP `0/1`, the same four To-do tickets blocked on the same unanswered gate items, `IDLE-2026-09-07` still accurate"). In order: `scheduled (auto-drain)` `19:41:00`→`19:52:43` (succeeded, idle); `continue (ENG-027)` `19:52:46`→`19:54:18` (exit 1, genuine failure, re-queued one attempt older per the documented retry rule); `continue (ENG-027)` `19:59:21`→`19:59:26` (exit 1 in 3s, classified never-started and refunded, re-queued at the same attempt — both failure-handling paths worked exactly as designed, named only for completeness, not as a second finding); `continue (ENG-027)` `20:04:29`→`20:16:18` (succeeded, idle); `scheduled (auto-drain)` `20:16:21`→`20:27:11` (succeeded, idle); `continue (ENG-027)` `20:27:14`→`20:41:13` (succeeded, idle); and this `scheduled (launchd)` pass, `20:41:16`→ now (succeeded, idle) — the fifth consecutive successful pass, and seventh launch overall counting the two `continue` misfires, all inside one hour, each one independently re-running `git fetch`/`gh pr view` on two PRs, a whole-board frontmatter sweep, and `lib/eng-gate-check.sh`, to re-derive a conclusion the immediately preceding pass had already reached seconds to minutes earlier. No pass did anything wrong individually — each correctly avoided over-eager fallback and correctly declined to duplicate `IDLE-2026-09-07.md` — so this is a cost/design finding, not a correctness one. | This is the queue design working as documented (`eng_build_loop.md`, "never idle" / "one queue, oldest first, consumed only when a pass succeeds") producing a specific, measurable waste: a fully-idle department, with nothing externally actionable, still burns a full Claude session (multiple minutes of wall-clock, a `git fetch`+`gh pr view` round trip, a whole-board re-read) every time the queue happens to drain quickly, purely to re-confirm what the previous session already wrote down. At today's observed rate (seven launches/hour in this stretch, five of them full completed sweeps) this scales linearly with how fast the queue empties, which is the opposite of what a "safety net, not the engine" pass is supposed to cost. Nothing has been lost or misreported by any of these passes — the risk is pure spend, not correctness — but `observations.md`'s own three-strikes convention (already invoked twice elsewhere in this file) says a pattern that keeps re-triggering its own "worth a proposal if this recurs" clause should stop being re-logged as a fresh observation each time and get an actual fix decision instead. | M — needs a judgment call before it's a mechanical change, not just one: some cheap signal that nothing *external* changed (no new `inbox/` file, no `decision:` field flipped, no ticket frontmatter touched, no PR state changed since the last recorded idle confirmation's own timestamp) would let a pass short-circuit to a one-line log entry instead of a full sweep — but the check itself still has to do the two things that actually catch drift (a `git`/`gh` PR-state read and an inbox/frontmatter diff), since those are exactly what a "safety net" pass exists to catch and skipping them to save cost would defeat the purpose. The saving is in skipping the expensive part (the full board-narrative rewrite and re-derivation of already-known reasoning), not the cheap verification itself. Whoever picks this up decides where that line sits — plausibly a small addition to `lib/eng-trigger.sh` (compare a fingerprint of `inbox/`+in-flight ticket frontmatter against the value at the last idle confirmation, before ever launching `claude`) rather than inside the pass itself, since the whole point is not spending a session to find out nothing needs one. |
| 2026-09-08 | eng-manager | aiorders-api | Third occurrence of the identical repo-isolation slip on this same repo, despite two prior catches already on record in `observations.md` (both 2026-09-07): the 4th `continue ENG-027` idle-check ran its `git fetch`/`gh pr view` PR verification from `~/Documents/projects/aiorders/aiorders-api` — `config/projects.md`'s Working copies section: "the human's. Never touched by an agent" — instead of the department's own `~/Documents/projects/_eng/aiorders-api` worktree, self-caught and disclosed in the same pass; a second, later same-day pass repeated it, also caught and disclosed, that entry ending "Worth a look if a second pass is found making the same mistake." That tripwire has now fired: the 22nd `continue ENG-027` idle-check's own notebook (`agents/eng-manager/notebook/2026-09-08-eng027-continue-twentysecond-idle-check.md`, line 25) states its PR-state re-verification ran "from `~/Documents/projects/aiorders/aiorders-api`" — the wrong, human path again, with no isolation caveat this time, unlike checks 11/16/17 (which explicitly named and used the correct `_eng` worktree) and 18–20 (which sidestepped the question entirely via `gh pr view --repo owner/repo`, needing no local checkout at all). This 23rd check re-ran the same verification from the correct `_eng/aiorders-api` worktree and found no difference in outcome (`#21`/`#22` still both `OPEN`), so nothing on the board was affected by the slip itself. | Each individual occurrence is harmless on its own terms — `git fetch` only updates shared remote-tracking refs and touches no worktree's checked-out files/index/HEAD, and `gh pr view` is a pure API read, as both prior `observations.md` entries already established — so this is not a data-risk finding. What it does show is that "use the `_eng` worktree, not the human's checkout" is enforced by nothing except a session happening to remember it: two prior catches, both disclosed, apparently forgotten again 18 checks later. Nothing in `lib/eng-gate-check.sh` or anywhere else mechanical checks which directory a pass's git/gh commands actually ran in, so a less-harmless version of this same slip — on a repo that does carry live uncommitted WIP, per `config/projects.md`'s own notes on `aiorders-admin-hub` and `restaurant-marketplace` — would be equally invisible until an agent happened to run `git status` afterward and think to check, the way the first two occurrences did. Same shape as the two rows above: a real rule that exists only as narrative precedent scattered across notebooks nobody re-reads before acting, not as anything a pass is structurally pointed at. | S — likely mechanical rather than a judgment call, unlike the two rows above: either (a) a small `lib/` wrapper (e.g. `lib/eng-repo-check.sh {project}`) that resolves and `cd`s to the registered `_eng` worktree for a project, so a pass calls that instead of hand-typing a path from memory, or (b) standardize on the `--repo owner/repo` form checks 18–20 already used, which needs no local checkout at all and sidesteps the isolation question completely. Whoever picks this up should also check whether the same slip has happened on any other registered repo, since this proposal only confirms it on `aiorders-api`. |

## Approved

Moved here with its ticket id when the approver approves it. The row leaves
Open; it is never deleted.

| Filed | Approved | Ticket | Project | What |
|---|---|---|---|---|
