# Decision journal

What the approver approved, changed, or killed at a gate — and the pattern in
what they change. The PM and EM read this before framing the next gate.

| Date | Ticket | Gate | Answer | Approver's words | Interpretation |
|---|---|---|---|---|---|
| 2026-08-25 | ENG-002 | G1 scope | approved | `decision: approved` — no additional comment recorded in the gate item. | Approved as scoped: `restaurant-portal` only, smoke-level, `aiorders-api` and the other three frontends explicitly out. No pushback on the PM's readback or the one-repo scope — first data point that a tightly-scoped, single-repo G1 with a clear non-goals list passes without a round trip. |
| 2026-08-26 | ENG-002 | L1 merge request | merged (no written reply) | Merged PR #1 directly on GitHub rather than replying in `inbox/2026-08-26-eng002-merge-request.md`'s `## Decision` field — the item's own text offered this as valid ("Merge whenever suits you on GitHub directly"). A separate "control center" dashboard action also set the ticket's `state:` straight to `shipped` ahead of any build-loop pass, without going through the tracked gate item at all. | First data point on how a merge decision actually reaches this department when the approver acts on GitHub/the control center instead of the tracked channel: the outcome is still detectable (git ancestry) and still correct, but nothing here currently prompts a pass to reconcile the two on its own — this sweep found it by manually cross-checking ticket state against the open inbox item. Worth watching for a second occurrence; see the open proposal on this gap. |
| 2026-08-26 | ENG-001 | G3 (verification-ticket shape, `ADR-002`) | approved | `decision: approved` — `inbox/2026-08-26-eng001-g3-verification.md`'s `## Decision`, filled in directly by the approver this time (unlike `ENG-002`'s merge request); no additional comment recorded. `decided: 2026-08-27T05:05:01.598404+00:00`. | The first verification-only ticket's G3 was answered as a plain approve, no pushback and no "you didn't need to ask" framing. Per `ADR-002`'s own Review trigger, that specific phrasing is what would signal a lighter route is wanted for the *next* verification-ticket G3 — it isn't present here, so no proposal to relax the gate is warranted yet. Read as approval of the framing itself (confirm the record, not "ship to production"), not evidence either way on whether G3 should apply to this ticket shape at all. |
| 2026-08-26 | ENG-004 | G1 scope | approved | `decision: approved` — `inbox/2026-08-25-eng004-g1-scope.md`'s frontmatter (`decided: 2026-08-27T05:57:21.472123+00:00`) plus a second `## Decision` section appended below the original unedited placeholder; no additional comment. Third data point (after `ENG-002`'s GitHub merge and `ENG-001`'s G3) of the approver acting outside `lib/eng-notify.sh`'s reply channel — here by directly editing the gate item file rather than replying to a notification. | Approved as scoped: investigate first against the live Supabase project, remediation held for a separate pass once the authoritative repo is confirmed — no pushback on the investigate-then-remediate split or the `L` size. Growing pattern (three of the last four gate answers bypassed the tracked reply channel) worth watching for the existing `lib/eng-notify.sh` proposal (`proposals.md`, 2026-08-25, row 2): if the notify message itself isn't reaching the approver where they look, editing the file directly is the obvious workaround — plausible, not confirmed, so not asserted as the cause here. |
| 2026-08-26 | ENG-003 | G1 scope | rejected | "Drop this ticket do not  need to be done" — `inbox/2026-08-25-eng003-g1-scope.md`'s frontmatter (`decided: 2026-08-27T06:38:51.515614+00:00`) plus a second `## Decision` section appended below the original unedited placeholder, same hand-edit shape as `ENG-004`'s G1; no further reason given. Fourth data point (after `ENG-002`'s GitHub merge, `ENG-001`'s G3, `ENG-004`'s G1) of the approver acting outside `lib/eng-notify.sh`'s reply channel. | **First outright G1 rejection this department has recorded** — every prior G1 (`ENG-002`, `ENG-004`) was approved as scoped. No pushback on the readback or the recommendation's own split (ship the low-risk git-hygiene half now, hand the Maps-key referrer check and rotation decision back as explicit approver action items) — the whole ticket was declined instead, with no reason beyond the instruction itself. One reading, grounded in the PRD's own Problem section and labelled as interpretation, not confirmed: that section already noted the tracked `.env` values are "already public-by-design in the shipped Vite bundle regardless of git tracking," so the git-hygiene fix may have read as low-value on its own merits, separated from the Maps-key question it couldn't resolve anyway. Worth keeping as a prior data point for the still-open `restaurant-portal` `.env` proposal (`proposals.md`, 2026-08-26 row, same fix family) when that batch reaches the approver — not acted on now, since an unapproved proposal is a separate decision on its own timeline. |
| 2026-08-27 | ENG-004 | G3 (verification-ticket shape, `ADR-004`) | approved | `decision: approved` — `inbox/2026-08-27-eng004-g3-verification.md`'s frontmatter (`decided: 2026-08-27T18:03:06.296846+00:00`) plus a second `## Decision` section appended below the original unedited placeholder, same hand-edit shape as every gate answer on this instance except `ENG-002`'s GitHub merge; no additional comment. Answered roughly 92 seconds after `notified:` (11:01:34 local) was stamped — by far the fastest turnaround of any gate on this instance (`ENG-001`'s G3 took ~13 hours; this ticket's own G1 took about a day). Fifth data point of the approver acting outside `lib/eng-notify.sh`'s reply channel. | Second verification-only-ticket G3 answered as a plain approve, no pushback and no "you didn't need to ask" framing — per `ADR-004`'s own Review trigger, that specific phrasing is what would signal a lighter route is wanted for a future occurrence; still absent, so still no proposal to relax G3 for this ticket shape. The turnaround itself is the more notable fact: consistent with (not proof of) the already-open `lib/eng-notify.sh` proposal's hypothesis (`proposals.md`, 2026-08-25 row) that messages reach Slack rather than the Telegram the approver actually watches, forcing a direct file edit — but a reply this fast is equally consistent with the approver simply having the control center open live during this pass. Not asserted as confirmation either way. |
| 2026-08-27 | ENG-005 | G1 scope (the fork itself, not a plan) | approved, partially | `decision: approved` — `inbox/_handled/2026-08-27-eng005-g1-scope.md`'s frontmatter (`decided: 2026-08-27T18:03:50.514589+00:00`) plus a second `## Decision` section, same hand-edit shape as every gate on this instance but `ENG-002`'s merge. Verbatim answer: "wire it in". Sixth data point of the approver acting outside `lib/eng-notify.sh`'s reply channel. | This G1 asked two things in one reply — decide the fork, and if wiring in, name the route/surface "so acceptance criteria can be written against it." Only the first came back. Read as the fork genuinely settled (no reading of "wire it in" leaves that open) and the surface genuinely not — not as a terse approval of a default the department should infer. This PRD's own Non-goals named guessing the surface as out of bounds before the answer ever arrived, so the gap was carried forward as one investigated, evidence-backed follow-up question (`inbox/2026-08-27-eng005-g1-followup-surface.md`, recommending `RestaurantDetails.tsx`) rather than assumed. First occurrence on this instance of a G1 answer settling part of its own question and leaving a named, requested sub-detail open — worth watching for whether the next answer addresses both halves once asked this explicitly, or whether short answers to multi-part questions are this approver's normal register. |
| 2026-08-27 | ENG-005 | G1 scope follow-up (surface) | approved | `decision: approved` — `inbox/_handled/2026-08-27-eng005-g1-followup-surface.md`'s frontmatter (`decided: 2026-08-27T20:08:53.367622+00:00`) plus a second `## Decision` section, same hand-edit shape as every gate on this instance but `ENG-002`'s merge. Verbatim answer: "lets do RestaurantDetails.tsx". Seventh data point of the approver acting outside `lib/eng-notify.sh`'s reply channel. | Confirms the PM's evidence-backed recommendation exactly as offered, rather than naming a different surface — the first data point on this instance of an approver accepting a department recommendation on a question it was careful to frame as open, not a default. Closes both halves of ENG-005's G1 (fork + surface) in two separate answers roughly two hours apart, resolving the gap the row above flagged as worth watching. No pushback on the recommendation or its reasoning (the props-shape argument for why `RestaurantDetails.tsx` was the only fit). Ticket moved `awaiting-scope → designed → ready` the same pass, with no one-way door found at design — nothing here suggests the approver expected a G2 round trip for a change this size. |
| 2026-08-28 | ENG-005 | L1 merge request | merged | `decision: approved` — `inbox/_handled/2026-08-27-eng005-merge-request.md`'s frontmatter (`decided: 2026-08-28T00:13:09.817494+00:00`) plus a second `## Decision` section, same hand-edit shape as every gate on this instance but `ENG-002`'s merge. Verbatim answer: "merged". | Unlike `ENG-002`'s merge (approver bypassed the tracked item entirely and used the control center instead), this merge decision came back through the normal channel — the first L1 merge request on this instance answered that way. Not taken on the text alone: independently re-derived via git ancestry (`edf6947` merges `51cdb29` onto `origin/main`, no intervening commits), which confirmed the answer. The merge commit's own timestamp lands ~20s before `decided:`, consistent with merging and recording the decision in one sitting rather than two separate actions — a small but real data point that this approver's hand-edits can trail the actual action by seconds, not follow it later. No pushback on the recommendation. |
| 2026-08-28 | ENG-006 | G1 scope | approved, plus two riders | `decision: approved` — `inbox/_handled/2026-08-27-eng006-g1-scope.md`'s frontmatter (`decided: 2026-08-28T00:20:57.797718+00:00`) plus a `## Decision` section, same hand-edit shape as every gate on this instance but `ENG-002`'s merge. Verbatim: "Do create a PRD for the understanding you got for the frontend work in different portals so that the knowledge is not lost. We will discuss and execute later doesnt mean lose the knowledge and findings and learnings while building the backend. We do have a vendor with unlimited sms at a monthly fixed cost so thats managed." | Approved as scoped, with no pushback on the recommendation, the five-ticket shape, or the one-way-door flag — but the approval carried two unprompted instructions rather than a bare "approved," the first compound G1 answer on this instance where both halves were fully addressed rather than one going unanswered (contrast `ENG-005`'s G1, where only half came back). Read as: (1) preserve the frontend understanding already surfaced, explicitly *not* a scope change — "we will discuss and execute later doesnt mean lose the knowledge" reads as the approver anticipating this department might otherwise let that context evaporate, worth remembering as a general instinct (capture incidental findings even when their execution is deferred) rather than a one-off. (2) the SMS vendor cost question the PRD flagged is already closed on the business side — a pattern worth watching for: this approver sometimes has already-resolved-but-undocumented answers to a ticket's own flagged unknowns, so a Risks/Cost section's "not yet chosen" shouldn't be read as necessarily still open by the time it reaches a gate. |
| 2026-08-28 | ENG-006 | G2 one-way-door | approved | `decision: approved` — `inbox/_handled/2026-08-28-eng006-g2-oneway-door.md`'s frontmatter (`decided: 2026-08-28T20:09:06.151165+00:00`) plus a `## Decision` section, same hand-edit shape as every gate on this instance but `ENG-002`'s merge. Verbatim: "So the existing system will still continue to function where users at restaurant level would continue to get created and they should be mapped at login time or session time to new restaurants which you would do now with the one time migration. So if both the flows continue to work then everything is reversible. The user knows they have foodswipe identity and they also know that restaurants can continue to use their existing system where restaurant specific customers are getting created. Its the job of the new system to still surface their orders from all restaurants and show them one identity." | First G2 this department has raised, and the first data point on how this approver reasons about a one-way door specifically: not by disputing the schema-is-reversible argument the design offered, but by restating their own reversibility criterion in their own words — the legacy and platform flows must keep running side by side — which happens to already match what the design does (legacy `customers` untouched, additive-only). No pushback on the recommendation to proceed. Worth watching on the next one-way-door gate: this approver engages with the substance rather than rubber-stamping, but a design that's already conservative may be doing more of the work here than the gate process itself — not yet tested against a design the approver actually pushes back on. |
| 2026-08-28 | ENG-006 | L1 merge request | approved | `decision: approved` — `inbox/_handled/2026-08-28-eng006-merge-request.md`'s frontmatter (`decided: 2026-08-29T02:59:33.281266+00:00`) plus a `## Decision` section, same hand-edit shape as every gate on this instance but `ENG-002`'s merge. Verbatim: "approved" — no additional comment. Merge commit lands ~2m28s before `decided:`, same "merge then record in the same sitting" shape as `ENG-005`. | A hybrid of the two prior merge-decision shapes rather than a clean repeat of either: like `ENG-002`, a "control center" dashboard action flipped the ticket's `state:` straight from `blocked` to `shipped` ahead of any build-loop pass, bypassing the tracked item's own detection path; unlike `ENG-002` (no written reply at all, gate item left open indefinitely), a written "approved" reply also landed in the tracked channel minutes later. The bare "approved" (vs. `ENG-005`'s explicit "merged") is consistent with this approver's shortest replies correlating with the least-ambiguous questions — an L1 merge request only ever has one live question once the PR is already open. Second occurrence of the control-center-bypass gap first flagged at `ENG-002` (`proposals.md`, 2026-08-26 row); this pass found the gate item unprocessed the same way that one predicted, purely by cross-checking the ticket's own `state:` against the still-open inbox item rather than any mechanism the loop runs automatically — the open proposal's fix is still unimplemented and this is now two-for-two evidence it's needed. Addendum filed in `observations.md` rather than a new proposal row. |
| 2026-08-29 | ENG-007 | G1 scope | approved | `decision: approved` — `inbox/_handled/2026-08-28-eng007-g1-scope.md`'s frontmatter (`decided: 2026-08-29T07:15:41.687445+00:00`) plus a second `## Decision` section, same hand-edit shape as every gate on this instance but `ENG-002`'s merge; no additional comment. | Approved as scoped, no pushback on the readback, the recommendation, or the sequencing framing — ninth data point of this approver acting outside `lib/eng-notify.sh`'s reply channel, and this pass's own live call to that script (raising `ENG-007`'s G2, see below) finally surfaced *why* plainly rather than just failing silently: `SLACK_WEBHOOK_URL unset — cannot notify`, the channel-dispatch half of the two-bug `proposals.md` 2026-08-25 row, not the variable-collision half seen in earlier logged failures — same open bug, a cleaner failure signature this time, not a new finding. Real design work done against the live repo this same pass found a significant unplanned fact (a live third-party loyalty vendor, Walletly, unmentioned anywhere in the request or either PRD) and escalated it via a fresh G2 rather than proceeding straight to `ready` — worth watching whether this approver treats a discovery made *after* their G1 approval differently from a question raised *at* G1; no prior data point on this instance covers that case. |
| 2026-08-29 | ENG-007 | G2 one-way-door | approved | `decision: approved` — `inbox/_handled/2026-08-29-eng007-g2-walletly-conflict.md`'s frontmatter (`decided: 2026-08-29T08:10:30.599034+00:00`) plus a second `## Decision` section, same hand-edit shape as every gate on this instance but `ENG-002`'s merge. Verbatim: "Walletly is being retired/replaced." | Answers the discovery raised at G1-adjacent design time: picks option 1 of the three the gate explicitly offered (Walletly retiring, the native sequence is its intended replacement, proceed as originally scoped) rather than writing a free-form answer in their own words. First data point of this approver answering a one-way-door gate by selecting a named option instead of restating their own reasoning (contrast `ENG-006`'s G2, answered entirely in the approver's own words) — consistent with the earlier observation that this approver's shortest replies correlate with the least-ambiguous questions, and a three-way enumerated menu is about as unambiguous as a one-way-door question gets. Settles the boundary question before ticket 3 (the points ledger) is ever filed: no dual-system conflict to design around, so ticket 3 proceeds as originally scoped by `ENG-006`'s G1 once the sequence's auto-continuation files it. This event's own `watch` sweep caught the hand-edit fresh, ahead of a `decision` event already queued behind it for the same file (`traces/.pending`) — the mirror image of this same ticket's G1 a few minutes earlier, where the dedicated `decision` event drained first and the trailing `watch` found nothing left to do. |
| 2026-08-29 | — (incident, `ticket: unknown`) | incident report | approved | `decision: approved` — `inbox/_handled/2026-08-28-eng-events-dropped.md`'s frontmatter (`decided: 2026-08-29T08:27:47.038600+00:00`) plus a `## Decision` section, same hand-edit shape as every gate on this instance but `ENG-002`'s merge. Verbatim: "recheck the request and report back how to fix. fix if you can." | Not a ticket gate — first data point on how this approver responds to a raised *incident* rather than a scope/design/release question, and the answer is directive rather than a bare approve: investigate, report, and remediate if possible, not just acknowledge. Answered on the second read of the item (it sat unanswered, `notified:`-less, through several earlier passes that each correctly declined to re-raise it) rather than the first — worth remembering that a non-P0 item with no working notify channel can still get read and acted on once the approver happens to look at `inbox/` directly, so "never successfully notified" is not the same as "the approver will never see it." Response: rechecked as far as this instance's own architecture allows (`traces/` is host-local and `.gitignore`d, so the Mac-side failure that raised this incident is not reachable from a Windows session) and said so plainly rather than guessing at a false confirmation; offered a reasoned-not-confirmed hypothesis (TCC/EPERM over spend-limit, from the day's other two incidents' retry behavior); named the two human-only checks that would actually confirm it; and filed a proposal for the one piece of this that's fixable in code — dropped-event items carrying their own failure excerpt instead of a `traces/` pointer that only resolves on the host that failed. |
| 2026-08-29 | ENG-008 | G1 scope | approved | `decision: approved` — `inbox/_handled/2026-08-29-eng008-g1-scope.md`'s frontmatter (`decided: 2026-08-29T09:12:46.283064+00:00`), same hand-edit shape as every gate on this instance but `ENG-002`'s merge; no additional comment. | Approved as scoped, no pushback on the readback, the two-item split (admin-side data now, influencer-facing gating later), or the recommendation — the fourth G1 in a row this approver has approved without amendment when the PM split a multi-part raw request rather than building one oversized ticket (`ENG-005`, `ENG-006`, `ENG-007` before it), reinforcing that pattern rather than a one-off. Answered in under 6 minutes of `notified:` — among the fastest turnarounds recorded here, consistent with (not proof of) the approver having the control center open live during this window, same caveat noted at `ENG-004`'s G3. |
| 2026-08-29 | ENG-008 | intake-question (engagement source) | approved, answered directly | `decision: approved` — `inbox/_handled/2026-08-29-eng008-engagement-source-question.md`'s frontmatter (`decided: 2026-08-29T09:10:52.731201+00:00`). Verbatim: "i mean both reading a and reading B. reading A is something we can start with now so we know how active the particular influencer is. reading B is something our staff can update or later we can connect using some api from meta." | First standing (non-blocking, non-ticket) intake question this instance has raised, and the approver didn't pick one of the two offered readings — they wanted both, plus named the future path for reading B explicitly (a later Meta API connection) rather than leaving it implicit. Worth remembering as a general pattern: when a question offers "A or B" and both are cheap enough to build, this approver may prefer "both" over a forced choice — the question should say so if a genuine one-or-the-other trade-off exists next time, since this one didn't actually force a choice by its own design. Directly enabled filing `ENG-009` in the same pass. |
| 2026-08-29 | ENG-009 | G1 scope | approved, plus an unprompted new ask | `decision: approved` — `inbox/2026-08-29-eng009-g1-scope.md`'s frontmatter (`decided: 2026-08-29T09:20:42.679606+00:00`). Verbatim: "potentially add notes by the staff to collaborate on the influencer and see how their personality is or what they want/like or what conversations have happened with them off the platform. we want our community mangers(staff) to be able to more easily use these influencers for our restaurant campaings." | Approved as scoped, no pushback on the two-reading-both-wanted framing — but, like `ENG-006`'s G1, the approval carried unprompted content beyond a bare answer: a genuinely new feature (a staff notes/relationship log on the influencer record), not a rider on `ENG-009`'s own scope and not answerable by absorbing it into that ticket's acceptance criteria. Read as new business intake arriving through a gate-reply channel rather than the normal inbox — same class of thing `watch`'s own contract treats as new input when it shows up as a hand-edited file — not agent-invented scope, and not folded into `ENG-009` silently (`definition-of-done.md`'s "scope discovered outside G1 is never silently absorbed" applies even though the discoverer here was the approver, not an engineer). Shaped into `ENG-010` in the same pass, full readback run since the addendum did not itself carry testable acceptance criteria the way `ENG-009`'s own confirmed answer did. Second data point (after `ENG-006`) that this approver sometimes uses a G1 reply as a place to add a new idea, not only to answer the one it asked — worth watching for a third occurrence before treating it as a settled pattern. |
| 2026-08-29 | ENG-011 | G1 scope | approved | `decision: approved` — `inbox/_handled/2026-08-29-eng011-g1-scope.md`'s frontmatter (`decided: 2026-08-29T11:14:54.862156+00:00`), same hand-edit shape as every gate on this instance but `ENG-002`'s merge; no additional comment. Answered roughly 10 minutes after `notified:` was stamped, while the same `intake` pass that raised it was still running. | Approved as scoped, no pushback on the readback, the proposed stage taxonomy, or the proposed minimal health signal — tenth data point of this approver acting outside `lib/eng-notify.sh`'s reply channel, and the third G1 this pass has now seen answered mid-flight rather than on a later sweep (after `ENG-007`'s G1/G2 and `ENG-008`'s G1). Consistent with the growing pattern that this approver often has the control center open live during an active session rather than checking in on a fixed cadence. |
| 2026-08-29 | ENG-011 | intake-question (tickets source) | rejected, with a named reading | `decision: rejected` — `inbox/_handled/2026-08-29-eng011-tickets-source-question.md`'s frontmatter (`decided: 2026-08-29T11:16:32.000840+00:00`). Free text: "Reading A" — no other comment. | **Read as an interpretation, not a certainty, and flagged as such everywhere it's used.** Taken literally, `decision: rejected` and "Reading A" pull opposite ways: a rejection reads as "don't build this," but naming one of two fully-specified options reads as a selection. Went with the selection reading — a flat "no" doesn't usually come with a specific option named underneath it — and treated this as choosing Reading A (a minimal from-scratch ticket system) over Reading B (an external source), not as killing the idea. Most likely explanation: this item's own template only ever offered "Filled in by the approver," not an approve/reject choice, so `rejected` may be whatever this instance's reply channel defaults to for a free-text answer that doesn't fit a gate's normal approve/reject shape — a **first data point**, not yet a pattern; worth checking directly with the approver if a second occurrence of `decision:` disagreeing with its own free text shows up, rather than continuing to guess. Directly enabled filing `ENG-012` in the same pass. |
| 2026-08-29 | ENG-013 | G1 scope | approved | `decision: approved` — `inbox/_handled/2026-08-29-eng013-g1-scope.md`'s frontmatter (`decided: 2026-08-29T11:45:00.908943+00:00`); no additional comment. Answered roughly 6 minutes after `notified:` was stamped, while the same `intake` pass that raised it was still running. | Approved as scoped, no pushback on the readback, the proposed six-stage default, or the split-out standing question — eleventh data point of this approver acting outside `lib/eng-notify.sh`'s reply channel (still unfixed, `proposals.md` 2026-08-25 row), and another fast mid-pass turnaround consistent with the growing pattern that this approver often has the control center open live during an active session. Notably a **bare** approval on a ticket whose G1 explicitly split off a genuinely open, materially-swinging question (the pre-signup-leads standing question) rather than folding it in or guessing — the approver let the split stand without comment, some evidence (not yet a confirmed pattern) that this approver is comfortable with a G1 approving a bounded fix while a related, bigger question rides separately. |
| 2026-08-29 | ENG-010 | G1 scope | approved | `decision: approved` — `inbox/_handled/2026-08-29-eng010-g1-scope.md`'s frontmatter (`decided: 2026-08-29T10:49:55.456343+00:00`). Verbatim: "admin panel had Influencers section built/extend it there." | Approved as scoped, no pushback on the readback or the "last of the three influencer tickets" sequencing — twelfth data point of this approver acting outside `lib/eng-notify.sh`'s reply channel. The instruction itself ("extend it there") confirms rather than redirects the PRD's own proposed surface (the existing Influencers admin page), the same shape `ENG-005`'s "lets do RestaurantDetails.tsx" set: a short, confirming reply to a question the PM had already narrowed with a specific recommendation, not a new direction. |
| 2026-08-29 | ENG-012 | G1 scope | rejected | `decision: rejected` — `inbox/_handled/2026-08-29-eng012-g1-scope.md`'s frontmatter (`decided: 2026-08-29T11:46:47.872706+00:00`). Verbatim: "later." | Second outright G1 rejection this department has recorded (after `ENG-003`'s "Drop this ticket do not need to be done"). Read plainly — "later" names no alternative option the way `ENG-011`'s own tickets-question reply did ("Reading A" under a `rejected` header), so there is no reading-selection ambiguity here, just a deferral: not now, no reason given beyond the word itself. Ticket moved to `dropped` rather than left open, consistent with `ENG-003`'s precedent — a G1 answered `rejected` is a kill, not a hold, even when the free text implies the idea itself might return later. If it does, this department's own convention (per `ENG-003`) is that it re-enters as a fresh request rather than this ticket reopening on its own. |
| 2026-08-29 | ENG-013 | intake-question (pre-signup leads) | approved | `decision: approved` — `inbox/_handled/2026-08-29-eng013-presignup-leads-question.md`'s frontmatter (`decided: 2026-08-29T11:46:34.557123+00:00`). Verbatim: "Reading B. autopilot built to nurture these leads to next stages autpmatically and send them emails/sms to nurture." | Second standing (non-blocking, non-ticket) intake question this instance has raised (after `ENG-008`'s engagement-source question), answered the same way — a clear pick of one named reading plus unprompted mechanism detail (autopilot, email/SMS) beyond what the question asked. This answer's own "next step" (file a new ticket once answered) turned out already satisfied: an independently-run `intake` pass the same day reached the same request from a different raw input (the "no autopilot for sales staff/resellers" card) and filed `ENG-017` citing this same verbatim answer before this question was processed on its own. First data point of a standing question's answer arriving to find its own consequence already built by a parallel thread of work — worth remembering that two live intake threads on related topics can converge without either being told about the other, so checking for an existing ticket before filing is now demonstrated as necessary, not just theoretical. |
| 2026-08-29 | ENG-014 | G1 scope | approved | `decision: approved` — `inbox/_handled/2026-08-29-eng014-g1-scope.md`'s frontmatter (`decided: 2026-08-29T15:54:50.916162+00:00`); no additional comment. Found answered by a `watch` event pass, not processed until several passes later. | Bare approval of the full recommendation (restaurant self-service for QR codes/media, confirmed admin-only end to end) — no pushback on scope, the deferred website-settings item 2, or the admin-only-backend evidence. Consistent with this approver's established pattern of fast, unqualified approvals on tightly-scoped G1s with a clear recommendation and a named non-goal. |
| 2026-08-29 | ENG-015 | G1 scope | approved | `decision: approved` — `inbox/_handled/2026-08-29-eng015-g1-scope.md`'s frontmatter (`decided: 2026-08-29T16:12:24.708073+00:00`); no additional comment. Found answered by a `watch` event pass, not processed until several passes later. | Bare approval, read as informed consent to the PRD's one proposed default surfaced specifically for a tap (partner-created restaurants held for staff review, not auto-approved) — same reading this instance used for `ENG-013`'s six-stage default. Also a bare approval of the readback's correction that two of the four originally-named symptoms (Dashboard, Influencers) don't reproduce on this branch — no pushback, no "check again," suggesting this approver trusts a readback that names what it could *not* reproduce as much as what it confirmed. |
| 2026-08-29 | ENG-022 | incident (P0, informational) | approved | `decision: approved` — `inbox/_handled/2026-08-29-eng022-p0-incident.md`'s frontmatter (`decided: 2026-08-29T17:38:25.380622+00:00`); no additional comment, no `priority` set. | Item's own text framed this as "nothing to decide... filled in only if you want to set priority or have a question" — a bare approval with no `priority: now` and no question is read as acknowledgement of the interrupt, not an instruction to reorder the board. `ENG-022` continues at its own P0 severity's normal (fast) speed rather than jumping ahead of everything in flight. |
| 2026-08-29 | (none — department-wide) | incident (dropped events) | rejected | `decision: rejected` — `inbox/_handled/2026-08-29-eng-events-dropped.md`'s frontmatter (`decided: 2026-08-29T15:52:51.834010+00:00`); no additional comment. | No verbatim words beyond the bare rejection, so read plainly rather than over-interpreted: declines the item's own recommendation to investigate *this specific* dropped event (`08:40:57 scheduled schtasks`, exit status 3) further, not a rejection of the drop-and-notify mechanism itself. Not treated as covering the later, separate daily-hop-ceiling halt (next row) — different incident, different item, answered separately and later in the day. |
| 2026-08-29 | (none — department-wide) | incident (daily hop ceiling) | approved, with a direct instruction | `decision: approved` — `inbox/_handled/2026-08-29-eng-loop-halted.md`'s frontmatter (`decided: 2026-08-29T21:28:04.845356+00:00`). Verbatim: "I have already raised the limit, confirm if not raised then raise it mased on max_5x plan." | A direct instruction to verify, not a simple approve/reject — answered in the same pass with a concrete confirmation rather than a guess: `plan.tier: max_5x` was already correct on disk, but `read_plan_budget()`'s config path bug meant it was silently having no effect (every lookup fell through to the `pro`-tier 40-hop fallback); both the tier value and the path bug were already fixed, uncommitted, before this pass started, and this pass's own execution past hop 40 is live proof the fix now works. Nothing further raised — the approver's own belief that they'd already raised it was correct, the gap was entirely in the code path silently ignoring it. Worth remembering: this approver checks in on infrastructure-level department health (not just product tickets) and expects a verified answer, not a restated assumption. |
| 2026-08-29 | ENG-025 | G1 scope | approved | `decision: approved` — `inbox/_handled/2026-08-29-eng025-g1-scope.md`'s frontmatter (`decided: 2026-08-29T22:22:18.827452+00:00`); no additional comment. | Bare approval of the direct, already-scoped resolution of `ENG-023`'s own standing frequency question ("reading b, frequent is same restaurant over time") — no pushback on the recommendation, the `S` sizing, or the evidence that the existing `sub_type`/`nature` taxonomy already covers the grouping (overturning the standing question's own AI-categorization speculation). Consistent with this approver's established pattern of fast, unqualified approvals on tightly-scoped G1s with a clear recommendation and grounded evidence. |
| 2026-08-29 | ENG-023 | G1 scope | approved | `decision: approved` — `inbox/_handled/2026-08-29-eng023-g1-scope.md`'s frontmatter (`decided: 2026-08-29T23:38:32.834274+00:00`); no additional comment. Found answered by a `watch` event pass, not processed until a later pass. | Bare approval of the status-and-notes core, no pushback on the readback, the `ENG-022`-tenant-scoping dependency it named, or the decision to ship the confirmed core regardless of the still-separate frequency question (already split out as `ENG-025`). Same shape as `ENG-014`/`ENG-015`: a tightly-scoped G1 with a clear recommendation and grounded evidence, answered fast and bare. |
| 2026-08-30 | ENG-007 | L1 merge (no gate item ever raised) | merged (no written reply, no tracked item to reply to) | PR #4 opened and merged directly on GitHub, `mergedAt` `2026-08-30T02:38:08Z`, `mergedBy` the approver's own account — confirmed independently via `git merge-base --is-ancestor` (both `origin/loyalty-system`→`origin/main`) and `gh pr view 4`, not trusted from either signal alone. <!-- merge note: an earlier local-side entry for this row described a merge-request gate item that was raised but never answered ("no decision: field at all in inbox/_handled/2026-08-29-eng007-merge-request.md"); this later (2026-08-30) entry found no such item was ever raised in the first place — kept as the more recent, more thoroughly re-derived account. --> | Third occurrence of a direct GitHub merge (`ENG-002`, `ENG-006`, now this) and the first with *no* merge-request item ever open to bypass — this ticket's own item was never raised at all, blocked by a Saturday release-window hold that a same-day skill correction (L1 is exempt) had already made moot. Reads as this approver simply working from the branch/PR list on GitHub rather than waiting on this department's own inbox flow, consistent with their own words elsewhere ("I can check them on weekdays or weekends, my choice") — not a signal anything about the tracked channel is broken, just that it isn't the only channel they use. |
| 2026-08-30 | ENG-011 | L1 merge request | merged (no written reply) | `decision:` stays empty on `inbox/_handled/2026-08-29-eng011-merge-request.md` — never answered. Both PRs merged directly on GitHub instead, 40 seconds apart (`aiorders-api` #3 at `2026-08-30T00:12:50Z`, `aiorders-admin-hub` #3 at `2026-08-30T00:13:30Z`), exactly as the item's own text invited ("merge whenever suits you on GitHub directly"). Confirmed independently on both repos via git ancestry and `gh pr view` before treating the ticket as shippable. <!-- merge note: an earlier local-side entry for this row described this same gate item as answered ("decision: approved" plus trailing free-text "merged", decided 2026-08-30T01:43:13Z) via the tracked channel; this later (2026-08-30) entry, re-derived directly against the two PRs' own merge timestamps, found the field still empty and both merges done straight on GitHub instead — kept as the more recent, independently re-verified account. --> | Fourth occurrence of a direct GitHub merge and the first on a multi-repo ticket — both PRs merged back to back suggests this approver reviews a ticket's linked PRs as one unit, matching how the department already presents them (one merge-request item, both links, one line explicitly saying a reply isn't required). Same evening as `ENG-007`'s own out-of-band merge on a different repo — this approver worked through a batch of open PRs directly on GitHub rather than replying to either tracked item, the clearest data point yet that this is a standing preference, not an occasional shortcut. |
| 2026-09-01 | ENG-017 | G1 scope | approved, with a UI rider | `decision: approved` — `inbox/_handled/2026-08-29-eng017-g1-scope.md`'s frontmatter (`decided: 2026-09-01T13:59:15.473904+00:00`). Verbatim: "Build a new section on admin portal for autopilot/nurturing so we have flexibility to control this from the ui." | Same pattern already seen on `ENG-009`/`ENG-010`/`ENG-006`: this approver tends to add concrete implementation-shaping detail in a G1 reply rather than a bare approve/reject. Read as clarifying this ticket's own scope (the control surface must be a real admin-portal UI section) rather than new, separable scope, since a staff-settable stage already implied *some* UI — folded into the design rather than split into a new ticket, unlike `ENG-010`'s rider on `ENG-009` which named a materially different capability. Answered ~3 days after being raised (2026-08-29 23:13), the longest gap yet recorded for a G1 on this board — no reminder or pushback about the delay in the reply itself. |
| 2026-09-01 | (none — department-wide) | incident (dropped events) | approved, with a direct instruction | `decision: approved` — `inbox/_handled/2026-09-01-eng-events-dropped.md`'s frontmatter (`decided: 2026-09-01T16:23:42.685518+00:00`), plus a trailing hand-written line appended after the pass's own investigation footer (sic throughout): "If the monhtly limit is hit, then do not retry tasks rather check that the limit is reset then only it makes sense to retry tasks instead of just retying tasks for no reason." | Not a bare approval — a standing instruction about the department's own automated retry behavior, in the same "directive rather than acknowledgement" register as the 2026-08-29 daily-hop-ceiling row above. Read plainly: before the mechanical retry/back-off logic (`lib/eng-trigger.sh`'s two-attempt-then-drop path) fires a queued retry after a suspected monthly-spend-limit failure, it should first confirm the limit has actually reset rather than retrying on elapsed time or attempt count alone. Current behavior does neither — it retries by attempt count and time, with no check against the vendor's own limit state, which is exactly what this note objects to. Not implemented inline: it touches `eng-trigger.sh`'s core retry path, the same one three still-open proposals already flag as needing sign-off before a hand-edit changes it — filed as a proposal (`proposals.md`, this pass) rather than coded on the spot. |
| 2026-09-02 | (none — department-wide) | incident (dropped events) | rejected | `decision: rejected` — `inbox/_handled/2026-09-02-eng-events-dropped.md`'s frontmatter (`decided: 2026-09-03T02:18:12.195875+00:00`); no additional comment. | Read plainly, same convention as the 2026-08-29 rejected-dropped-events row: declines the item's own recommendation to investigate the day's five drops further, not a claim they didn't happen. One of the five named drops (`2026-08-30-eng-loop-halted.md`) was appended to this file at 19:45:51, roughly 27 minutes *after* this decision was already recorded — read as outside the rejection's own scope rather than covered by it, and checked on its own: already independently closed via the ordinary per-file sweep, not lost. All four other named drops re-confirmed still open with `decision:` blank, exactly where an unanswered gate should be. Nothing in this reply suggests dissatisfaction with the drop-and-notify mechanism itself, only with spending further investigation on this specific day's causes — consistent with this approver's established pattern of declining to re-litigate well-understood failure classes (monthly spend limit, TCC/EPERM) rather than treating every recurrence as new. |
| 2026-09-02 | ENG-026 | intake-question (channel-visibility toggle) | approved, as a full spec rather than a reading pick | `decision: approved` — `inbox/_handled/2026-09-01-eng026-visibility-toggle-question.md`'s frontmatter (`decided: 2026-09-03T00:58:19.957907+00:00`). Full text: a four-part memo naming exact field names, exact defaults, exact discovery/query semantics — reproduced in full on the item itself. | Third data point (after `ENG-009`'s engagement-source question and `ENG-013`'s presignup-leads question) of this approver answering a standing, non-blocking question with more than the question asked for — but the largest jump yet: not a sentence of unprompted detail, a complete implementation-ready specification, formatted like a memo to a team rather than a reply to a PM. Confirms Reading B (a real toggle is wanted) but supersedes it with exact mechanics neither original reading specified. Directly enabled writing `ENG-026`'s PRD and raising its G1 in the same pass without a second readback round — the answer left nothing to interpret. Worth watching: this is the second time (after `ENG-011`'s own raw request) this approver's input reads as if drafted with another AI tool's help rather than typed free-hand: structured headers, numbered sections, explicit field names. Doesn't change how it's treated (still read as the approver's own words), but worth naming as a pattern in case a future one contains an internal inconsistency that needs a question back rather than a straight adoption. |
| 2026-09-01 | (none — department-wide) | incident (gate-violation watch) | rejected | `decision: rejected` — `inbox/_handled/2026-09-01-eng-gate-violation-watch.md`'s frontmatter (`decided: 2026-09-01T16:38:16.887004+00:00`); no additional comment. Recorded on a different host/checkout; not read by any Mac-side pass until tonight's `1b72b26` merge. | Item's own file already carried a finished investigation (Windows-host BOM encoding, fixed by merge `e281c71`) before this reply landed — read as declining to reopen an already-closed, already-fixed finding, not as new information. Re-confirmed still true (`lib/eng-gate-check.sh` clean on every pass since) before archiving. |
| 2026-09-01 | (none — department-wide) | incident (dropped events, 08-30) | rejected | `decision: rejected` — `inbox/_handled/2026-08-30-eng-events-dropped.md`'s frontmatter (`decided: 2026-09-01T16:37:56.836389+00:00`); no additional comment. Same batch-reply window as the two rows immediately above/below (seconds apart) — read as one sitting, not three separate decisions. | Declines investigating a 69-entry, ~16-17h sustained Windows-host outage window — large in volume but the same already-understood failure class (self-healing polls, plus three `continue` chain-drops that all independently recovered via later sweeps, one of them `eng_build_loop.md`'s own named `ENG-009` example). Consistent with this approver's established pattern of declining to re-litigate a well-understood, now-fixed failure class regardless of how many log lines it produced. |
| 2026-09-01 | (none — department-wide) | incident (dropped events, 08-31) | rejected | `decision: rejected` — `inbox/_handled/2026-08-31-eng-events-dropped.md`'s frontmatter (`decided: 2026-09-01T16:38:59.834633+00:00`); no additional comment. Same batch-reply window as the two rows above. | Same reasoning as the 08-30 row — 48 entries, all routine polls, zero unique content lost, same root cause since fixed. |
| 2026-09-01 | ENG-008 | L1 merge request | changed | `decision: changed` — `inbox/_handled/2026-08-31-eng008-merge-request.md`'s frontmatter (`decided: 2026-09-01T16:44:07.981185+00:00`). Verbatim: "There is no need for a new column when we already have a column to signify the same intent of whether the influencer accepts barter or not." | First `changed` verdict this journal has recorded on a merge request (prior merge decisions have all been approve/reject/merged-on-GitHub). Read as objecting specifically to the new `accepts_barter` column as redundant with the pre-existing `barter_visit`, not to the ticket's other new column (`accepts_paid`, no pre-existing equivalent) or to the feature generally — a narrow, mechanical schema correction, not a scope change. Ticket sent back to `building` for the fix rather than merged. Recorded on a different host/checkout; only reached this Mac's copy via tonight's `1b72b26` merge, a full day after it was actually decided — worth watching whether cross-host sync lag on *decisions* (as opposed to code) becomes a recurring gap. |
| 2026-09-01 | ENG-013 | L1 merge request | changed | `decision: changed` — `inbox/_handled/2026-08-31-eng013-merge-request.md`'s frontmatter (`decided: 2026-09-01T17:13:54.293916+00:00`). Verbatim: "You added manual update of stage to the card what about the funnel stages itself on the page, if I want custom pipeline flow stages not just per card. This ticket was meant to allow custom pipeline stages for the whole foodswipe funnel, the stage updates per card can be manual or automatic." | Second `changed` verdict, same batch as `ENG-008`'s row above but a different shape: this one contradicts a named, proposed assumption from the ticket's own PRD (fixed six-stage set) that the G1 approved without comment. Read as evidence that a bare G1 approval doesn't guarantee the approver registered every flagged assumption, not as this readback process failing — the assumption *was* named; the approval just didn't engage with it. Genuinely ambiguous whether to ship the built piece and file the larger ask separately or hold for one combined ship — asked (`inbox/2026-09-02-eng013-stage-config-question.md`) rather than guessed, per this journal's own standing lesson from `ENG-011`'s "Reading A"/`rejected` ambiguity (don't keep guessing at a second occurrence of an unclear reply shape). |
| 2026-09-03 | (none — department-wide) | incident (dropped events, 09-01 evening) | rejected | `decision: rejected` — `inbox/_handled/2026-09-01-eng-events-dropped-b.md`'s frontmatter (`decided: 2026-09-03T02:18:09.271126+00:00`); no additional comment. Decided 3 seconds before the same-evening `2026-09-02-eng-events-dropped.md` rejection — one sitting, two files. `-b` suffix: this file's original name collided with an unrelated, already-closed 2026-09-01 incident of the same auto-generated filename — see `observations.md` for the near-miss this caused. | Two entries, one a dropped notification for `2026-08-30-eng-events-dropped.md` itself — moot, since this same pass independently found and processed that file regardless. Nothing lost; same reasoning as the evening's other dropped-events rejections. |
| 2026-09-03 | ENG-016 | G1 scope | changed | `decision: changed` — `inbox/2026-08-29-eng016-g1-scope.md`'s frontmatter (`decided: 2026-09-03T00:53:17.403384+00:00`). Not a correction or a rider: a complete replacement engineering spec (packaging/fulfillment model, tiered included-vs-charged pricing, per-item variant notes, an owner-side "Edit Quote" action, a three-stage pipeline matrix), reproduced in full on the item itself, reintroducing both things the original PRD named as non-goals (a catering-specific pricing model; owner-side quote editing). | Second data point, after `ENG-026`'s visibility-toggle answer, of this approver replying to a G1 with a complete implementation-ready document rather than a scope decision — but the first where the document contradicts the ticket's own prior non-goals rather than just adding detail, and the first that contains an internal inconsistency (the reply's own stage matrix names three target stages, its own enum-update instruction names two). Read as genuine intent to build the larger thing, not as scope creep to push back on — but the honest size of the whole document is `XL` against this department's own bar, so it was split rather than built whole or bounced outright: `L`-sized Piece 1 (structured order capture, no pricing) proceeds on a freshly re-raised G1; pricing (Piece 2) and owner-edit (Piece 3) are named but not filed, Piece 2 explicitly held on a named question (who maintains a per-restaurant price book) the reply itself never addressed. Worth watching whether this approver's rewrites keep arriving as complete specs rather than decisions — if so, this department's G1 format may be better framed as "confirm or replace" than "approve or reject" for this particular approver. |
| 2026-09-01 | ENG-007 (sequence) | intake-question | approved | `decision: approved` — `inbox/_handled/2026-08-30-eng007-continue-sequence-question.md`'s frontmatter (`decided: 2026-09-01T17:02:39.576746+00:00`). Full text: "yes." | Bare one-word answer to a question this department raised rather than the approver — asked because `ENG-007`'s own G1 approval never independently re-affirmed continuing `ENG-006`'s five-ticket sequence, per `acceptance-check/SKILL.md` step 6b's own bar for when to ask versus auto-file. Read plainly: continue the sequence, file ticket 3 (points ledger/earn API) next, same process as `ENG-007`. Actioned this pass (2026-09-03) rather than the day it was answered — the item itself said "answer when convenient" and named nothing in flight depending on it, so the ~38h gap between answer and action reflects this department's own gate-return ordering (oldest-first, one at a time) landing on a busier stretch of the board, not neglect. |
| 2026-09-03 | ENG-013 | stage-config question | approved | `decision: approved` — `inbox/_handled/2026-09-02-eng013-stage-config-question.md`'s frontmatter (`decided: 2026-09-03T15:23:36.496711+00:00`). Full text: "Reading a approved". | Reading A: ship the two already-built, already-gated PRs as-is; file custom pipeline-stage definitions as a new, separate ticket built on top. Read plainly, no hedge — the approver picked the narrower reading over holding both PRs for a combined ship, consistent with `ENG-011`'s own precedent of treating an independently useful, already-tested piece as worth shipping on its own rather than bundling it with unbuilt scope. Both PRs re-verified still open on GitHub (not merged by this answer itself) — the answer authorises the merge, it doesn't perform it. The new ticket (`ENG-028`, staff-configurable Foodswipe pipeline stage set) was filed the same pass per the decision's own explicit direction, not left for the approver to notice was missing — same carve-out this journal already has on file for `ENG-027`. Worth watching for a second occurrence of this exact shape (a `changed` merge-request reply naming a bigger ask, resolved by "ship the small thing, file the big thing") to see if it becomes this approver's standing preference over holding for one combined release. |
| 2026-09-03 | ENG-016 | G1 scope (rescope, Piece 1) | approved | `decision: approved` — `inbox/2026-09-02-eng016-g1-rescope.md`'s frontmatter (`decided: 2026-09-03T15:47:46.139489+00:00`). Full text: "Lets start with piece 1" (sic). | Approves Piece 1 exactly as split and scoped by the row above (structured order capture, itemized owner view, two automatic stages, no pricing) — the shortest reply yet to a G1 this large, and, unlike the `changed` answer that produced this split, a plain approval with no added detail. Silent on both riders this G1 carried (the 3-vs-2 stage-count resolution; the fulfillment-value remap question) — read as accepting each as proposed, not as overlooked, since both were named plainly in the recommendation the approval responds to. "Start with" read literally: confirms the recommended build order without pre-authorizing Pieces 2 or 3, which stay unfiled; Piece 2 still explicitly waits on a named answer for who maintains each restaurant's price book. Third consecutive G1 this evening answered same-day (after `ENG-013`'s stage-config question and, earlier, the visibility-toggle question) — worth watching whether same-day turnaround holds as the open-item count grows now that the approver-WIP cap no longer throttles how many are open at once. |
| 2026-09-03 | ENG-026 | G1 scope | approved | `decision: approved` — `inbox/2026-09-02-eng026-g1-scope.md`'s frontmatter (`decided: 2026-09-03T15:51:04.400168+00:00`). No additional comment recorded. | Bare approval of the recommendation exactly as scoped: channel-visibility toggles and capability-based discovery only, the other three bundled capabilities (operational status engine, smart filters, promo badges) deferred as separate future tickets. Silent on requirement 6's proposed default (staff-set via `aiorders-admin-hub`, not restaurant self-service) despite the readback's explicit "correct this if wrong" — read as accepting it as proposed, same convention this journal already applies to an unremarked-on rider (`ENG-016`'s row above, `ENG-013`'s stage-count assumption before it). Fourth G1/gate item this evening answered same-day, all bare or near-bare approvals except `ENG-016`'s `changed` — continuing to look like this approver's standing pattern for a tightly-scoped G1 with a clear non-goals list, per this journal's first-ever entry (`ENG-002`). Requirement 7's rollout/backfill question was named as deliberately unresolved at PRD stage and stays open — the architect inherits it, not a second G1. |
| 2026-09-03 | ENG-019 | G1 scope | approved | `decision: approved` — `inbox/2026-09-03-eng019-g1-scope.md`'s frontmatter (`decided: 2026-09-03T15:52:30.648626+00:00`). No additional comment recorded. | Bare approval of the recommendation exactly as scoped: one-time and drip broadcasts, all-customers/inactive-for-N-days audience only, coupon-code redemption/revenue as the ROI mechanic, owner-authored content, owner/manager access only. Silent on every item in the readback's "Assumed, correctable here" list — read as accepting each as proposed, same convention this journal already applies to an unremarked-on assumption (`ENG-026`'s row above, `ENG-016`'s unremarked riders, `ENG-013`'s stage-count assumption). Fifth G1/gate item this evening answered same-day, continuing this approver's standing pattern for a tightly-scoped G1 with a clear non-goals list. The PRD's own Risks — durable scheduling/drip infrastructure, whether the existing send services need changes for a chosen-audience fan-out, and the CASL consent posture — were named as the architect's to resolve, not this gate's; none are a second G1, all stay open into `designed`. |
| 2026-09-03 | ENG-020 | G1 scope | approved | `decision: approved` — `inbox/2026-09-03-eng020-g1-scope.md`'s frontmatter (`decided: 2026-09-03T15:53:14.495206+00:00`). No additional comment recorded. | Bare approval of the recommendation exactly as scoped: a per-restaurant traffic-source/revenue breakdown on the brand dashboard, reusing attribution data already captured on every customer row. Clarity integration, a true ROI ratio, isolating AI-SEO specifically from organic traffic, and a staff-facing all-restaurants rollup all named as later, separate work, not dropped. Silent on every item in the readback's "Assumed, correctable here" list — read as accepting each as proposed, same convention this journal already applies to an unremarked-on assumption (`ENG-019`'s row above, `ENG-026`'s requirement 6, `ENG-016`'s unremarked riders). Sixth G1/gate item this evening answered same-day, continuing this approver's standing pattern for a tightly-scoped G1 with a clear non-goals list — the third of three sibling G1s (`ENG-019`, `ENG-020`, `ENG-021`) raised together this pass after the stale approver-WIP-cap correction, and the second of those three answered so far. The PRD's own Risks — attribution honesty, cross-domain attribution completeness, PIPEDA/Law 25 exposure, no historical baseline, and small-restaurant noise — were named as the architect's to resolve, not this gate's; none are a second G1, all stay open into `designed`. |
| 2026-09-03 | ENG-021 | G1 scope | approved | `decision: approved` — `inbox/_handled/2026-09-03-eng021-g1-scope.md`'s frontmatter (`decided: 2026-09-03T15:54:34.623417+00:00`). No additional comment recorded. | Bare approval of the recommendation exactly as scoped: customer questions surfaced on the brand portal plus a self-service FAQ editor writing the same `restaurant_website.faqs` table the bot already reads from — scoring answer quality, clustering questions, a staff-facing admin-hub mirror, and any change to the chat bar's own runtime behavior all named as later, separate work, not dropped. Silent on every item in the readback's "Assumed, correctable here" list — read as accepting each as proposed, same convention this journal already applies to an unremarked-on assumption (`ENG-020`'s row above, `ENG-026`'s requirement 6, `ENG-016`'s unremarked riders). Seventh G1/gate item this evening answered same-day, continuing this approver's standing pattern for a tightly-scoped G1 with a clear non-goals list — the last of the three sibling G1s (`ENG-019`, `ENG-020`, `ENG-021`) raised together after the stale approver-WIP-cap correction, all three now answered. The PRD's own Risks — PII in free-text customer questions, the unconfirmed RLS assumption on `restaurant_website`, and the unknown retention window/query volume — were named as the architect's to resolve, not this gate's; none are a second G1, all stay open into `designed`. This pass also recovered from an earlier same-event attempt that hit the session's rate limit mid-write (see `ENG-021`'s own board log): the ticket's frontmatter/log entry had already been written before the crash, everything downstream of it (this journal entry, the PRD, the gate item's move to `_handled/`, the board index, and the actual chain-fire) had not, and is completed by this pass. |
| 2026-09-03 | ENG-027 | G1 scope | changed | `decision: changed` — `inbox/_handled/2026-09-03-eng027-g1-scope.md`'s frontmatter (`decided: 2026-09-03T16:00:32.878229+00:00`). Full text, both clauses: "Accrual at fulfillment,  have ticket completed as autocompleted after x hours if not cancelled or deleted." (sic, double space). Not a rewrite in the `ENG-016` mould and not a rejection: a two-clause scope change that takes the fork this ticket's own G1 named explicitly ("If you want accrual on fulfilment instead, that signal has to be built first and this ticket goes from `M` to `L`"), picks the second branch, and then supplies its own mechanism for the missing signal rather than accepting the stated cost. Silent on the G1's one rider (what the earn % applies to). | **The first `changed` answer this board has received that made a ticket cheaper to get right than the G1 that prompted it — by being wrong in a way that forced a re-check.** Re-verifying the fork's premise before rescoping found that this department's own last G1 was mistaken: it read "the handler discards every event except `order_new`" as "there is no completion signal," when AIOrders' own webhook registration (`AIORDERS_WEBHOOK`, `cloudwaitress-middleware/handlers/restaurant.ts` line 6) already subscribes to `order_completed_updated`/`order_cancelled_updated`/`order_cancel`/`order_update_status` and delivers them to production, where line 238 throws them away. The signal doesn't need building; it needs un-ignoring. Read as genuine intent, not as a scope-creep push-back — and sized `L` on its own re-derived merits (first write-after-insert on `orders` in the codebase's history, plus a scheduled sweep, plus a moved accrual trigger), deliberately neither the old `M` nor the old warning's `L`-by-assertion, and short of the `XL` that would have sent it back to be split. What's genuinely new and worth watching, though, is the *shape* of the answer: this approver supplied an implementation mechanism ("autocompleted after x hours") for a capability gap the G1 had priced, and that mechanism contains an unfilled placeholder ("x hours") and a condition that is **vacuous against the current code** — nothing can mark an order cancelled or deleted, so "if not cancelled or deleted" is always true and a literal build would credit every order on a timer. Third data point, after `ENG-026` and `ENG-016`, of this approver answering a scope gate with implementation detail rather than a scope decision — but the first where the detail is *underspecified* (a placeholder number) and *unsatisfiable* (a condition with no signal behind it) rather than over-specified. Handled by naming both plainly on a fresh G1 with a proposed default (24h) and a named resolution (stop discarding the cancellation events) rather than by picking a number and building the clause as decorative. If this pattern holds, the useful adaptation is probably not a format change but a habit: when this approver names a mechanism, verify the mechanism's own preconditions against live code before costing it — this pass's own last G1 is the counter-example. |
