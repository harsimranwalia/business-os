# Observations

"While I was in there, I noticed..." — the thing a good engineer mentions on the
way past. Not a bug, not a ticket, not a request. Cheap to file, no obligation
on anyone, and worth more in aggregate than any single one is alone.

## Rules

- Any agent may append. No permission, no owner, no reply expected.
- One line each, newest last, under the Ledger below.
- An observation is not work. If it needs doing it is a bug or a proposal —
  file it as one instead of writing it here and hoping.
- Nothing reads this on a pass. It is read by the weekly report and by a human.

## Format

`| {date} | {agent} | {project} | {what you noticed} |`

## Ledger

| Date | By | Project | Observation |
|---|---|---|---|
| 2026-08-24 | eng-manager | aiorders | `board/_index.md` carries no "next ID" counter, though `config/templates/ticket.md` says IDs are never reused and the next one lives there — worth adding before ENG-002 gets allocated. |
| 2026-08-25 | product-manager | aiorders | Three approver-filed requests in `inbox/requests/` (`a4-poster-generator-unwired`, `admin-hub-migration-history`, `aiorders-env-hygiene`, all `received: 2026-08-23`) are still unshaped two days later — out of scope for this `continue ENG-001` pass, so left untouched, but worth flagging: `watch` isn't wired on this instance yet (`eng_build_loop.md`'s own note), so nothing but the twice-daily scheduled sweep would otherwise find them. None is P0, but `aiorders-env-hygiene` names a live cost exposure (an unrestricted Google Maps API key) that's worth not leaving to chance. |
| 2026-08-25 | product-manager | aiorders | `config/definition-of-done.md`'s "Ticket states" table lists `intake`'s owner as `eng-manager`, contradicting the paragraph directly beneath it ("`intake` and `shaped` are owned by the **product-manager**... corrected 2026-07-27"), the ticket template's own `owner: product-manager` default, and `agents/product-manager/agent.md`'s description of intake as the PM's job. Went with the prose + template + agent.md (unanimous) over the stale table cell for this pass; the table row itself is still wrong on disk. |
| 2026-08-25 | product-manager | aiorders | Shaping `ENG-003` (env hygiene) hit a ticket-schema gap: the work genuinely spans three repos (`config-site-builder`, `aiorders-admin-hub`, `restaurant-marketplace`), but `config/templates/ticket.md` models `project:`, `branch:` and `links.pr` as singular. Scoped the ticket's primary `project:` to `config-site-builder` (the repo with the actually-tracked secret) and named the other two repos' fixes explicitly in the PRD rather than inventing a multi-project ticket shape unilaterally — left for whoever picks this up at `building` to resolve the branch/PR mechanics. |
| 2026-08-25 | product-manager | aiorders | The architect's blind reading on `ENG-004` (admin-hub migration history) caught that `config/projects.md`'s registry records each repo's stack/deploy-target/autonomy but not which Supabase *project* it's linked to — so "is `aiorders-admin-hub` on the same database as `aiorders-api`" (the actual question ENG-004 needs to answer) isn't answerable from the registry as it stands today. |
| 2026-08-25 | eng-manager | aiorders | The first `scheduled (manual-unblock)` attempt at this pass (three full request-readbacks plus board updates in one session) hit the 1800s pass timeout and was killed mid-edit; the retry (this pass) picked it up from the on-disk state and found the substantive work already complete and consistent — only the board index's In-flight table rows for the three new tickets were missing. Worth a look if intake-heavy scheduled sweeps keep running long: either split business intake across more passes, or the timeout has room to grow — not filed as a proposal since one data point isn't a pattern yet. |
| 2026-08-25 | eng-manager | aiorders | This `watch` pass found `traces/.pending` carrying `1 scheduled instances/aiorders/engineering/agents/eng-manager/board/ENG-001-register-repos-and-prove-the-loop.md` — a `scheduled` event whose context is a ticket board file path. Both plist generators in `lib/eng-schedule.sh` invoke `scheduled` with context `launchd`, and the one manually-fired `scheduled` pass logged today used `manual-unblock`; this entry matches neither. Harmless today — `eng-trigger.sh` gives `scheduled`/`watch` an empty `TICKET_ID` regardless of context — but worth tracing to its source if it recurs. |
| 2026-08-26 | eng-manager | aiorders | Second occurrence of the row above, same exact shape (`traces/eng-loop-2026-08-26.log`: `draining queued event: scheduled (instances/aiorders/engineering/agents/eng-manager/board/ENG-001-register-repos-and-prove-the-loop.md)`) — this pass's own launch. This corrects the earlier row's assumption, rather than confirming it: `TICKET_ID=$(echo "$CONTEXT" \| grep -oE '[A-Z]{2,4}-[0-9]+' \| head -1)` in `eng-trigger.sh` runs unconditionally on `CONTEXT` regardless of `EVENT`, so a ticket-path context does NOT leave `TICKET_ID` empty for a `scheduled` fire — `traces/.hops-2026-08-26-ENG-001` read `1` after this pass launched, i.e. it was charged against `ENG-001`'s own per-ticket budget rather than running ticket-less. Minor in impact (one hop against an 8/day budget) but the "harmless" claim in the row above is now known wrong. Still not traced to a root cause — worth a proposal if a third occurrence shows up. |
| 2026-08-26 | eng-manager | aiorders | Second time in two days a `watch` fire cleared `watch_fingerprint`'s top-level de-noise check, launched a full session (this pass, 10:13:33), and found nothing new in any of the three watched inboxes — both `agents/*/inbox/` empty, and `inbox/`'s only two files (`ENG-003`, `ENG-004` G1s) unchanged since yesterday and already fully accounted for by the `scheduled` pass that finished 20 seconds earlier. First occurrence was yesterday 15:00–15:05 (same shape: queued behind a long pass, nothing left to do). Didn't trace which top-level file/mtime/size actually differed from the committed `.watch-seen` value either time — worth checking whether `commit_watch_fingerprint` is capturing a transient state (e.g. mid-write on a gate item) rather than the settled one, since that would mean a busy afternoon burns a full session and a hop for zero output each time. Two data points; a proposal if it recurs. |
| 2026-08-26 | eng-manager | aiorders | A `decision` event (context: `2026-08-25-eng004-g1-scope.md`) drained immediately after a `watch` pass that had already fully processed the same hand-edited gate answer (`traces/eng-loop-2026-08-26.log`: `pass end: watch (exit 0, 669s)` → `draining queued event: decision (2026-08-25-eng004-g1-scope.md)`, no gap) — a full session and a hop spent finding nothing left to do. Root mechanism: the approver's hand-edit changes a file inside a watched `inbox/`, so both the poll-detected `decision` path and the raw `watch` path queue their own event for the one change, and the queue's dedup only collapses identical `<event> <context>` lines — `decision (2026-08-25-eng004-g1-scope.md)` and `watch (launchd)` never match each other. Different mechanism from the row above (that's watch-after-non-watch, stale `.watch-seen`; this is a `decision` fire and a `watch` fire racing on the same edit, both legitimately queued). One data point — a proposal if it recurs. |
| 2026-08-26 | eng-manager | aiorders | Addendum to the open `.watch-seen` proposal (`proposals.md`, 2026-08-26), not a new occurrence to recount: that proposal's fix ("commit the fingerprint after every event type, not just `watch`") won't cover the fifth occurrence this pass hit. Read `lib/eng-trigger.sh` directly rather than guessing — `WATCH_FP` is captured *before* the pass launches (line 1583, deliberate, so mid-pass arrivals still look new next time) and `commit_watch_fingerprint` (line 424) writes that unchanged pre-launch value on any clean exit (line 1892-1893). The pass that triggered this fire (pid 73496) *was* `watch`-typed and *did* exit 0, so it committed fine — but it had just moved `2026-08-25-eng004-g1-scope.md` into `_handled/` as part of its own work, so the fingerprint it committed was already stale relative to the `inbox/` it left behind. Any `watch` pass that actually processes a gate item does this same move, so it isn't a corner case — it guarantees one wasted `watch` fire after every productive one. Whoever picks up the open proposal needs to widen the fix to cover self-modifying `watch` passes too, not just non-`watch` event types — full mechanism and citations on the board's dated entry for this pass. |
| 2026-08-26 | architect | aiorders-api | `ENG-004`'s `designed` investigation found `_eng/aiorders-api`'s own worktree branch (`eng/base`) has diverged from `origin/main` by dozens of commits — its HEAD predates a same-day migration consolidation (2026-08-24) that `origin/main` already carries. Read `origin/main` directly instead of trusting the worktree's files; a future ticket that trusts `_eng/aiorders-api`'s working tree at face value for this repo specifically would reach stale conclusions. Not filed as a proposal — one occurrence, and the fix (read `origin/main`, not the worktree, when investigating repo state) is now on record in `ADR-003`/this ticket's design rather than a process gap needing a rule change yet. |
| 2026-08-26 | architect | aiorders-admin-hub | `supabase/config.toml` still declares 20 `[functions.*]` stanzas for edge functions that no longer live in this repo (moved to `aiorders-api` in an earlier, already-closed consolidation) — harmless (nothing reads the stale stanzas; `project_id` is still valid) but orphaned. Unrelated to `ENG-004`'s migrations scope, left untouched. |
| 2026-08-26 | architect | aiorders-admin-hub | `ENG-004`'s entire subject matter — moving migration ownership from `aiorders-admin-hub` to `aiorders-api` — turned out to already be done: the approver committed it directly to both repos' `main`, in two matched pairs seconds apart, on 2026-08-24, one day after filing the request and before the ticket ever reached `shaped`. `ADR-004` names what a second occurrence of this specific pattern (the approver resolving a ticket's own subject matter directly, mid-flight) would mean — flagged here as the first data point rather than re-argued there. |
| 2026-08-27 | eng-manager | aiorders | This `scheduled` sweep's pre-pass `git status` showed uncommitted changes sitting in the department's own tree — `departments/engineering/lib/eng-schedule.sh` and `departments/engineering/schedules/eng_weekly_report.md` modified, `departments/engineering/lib/eng-report.sh` untracked — none of them touched by this pass. That directory is documented as shared and read-only from an instance's perspective, so finding it mid-edit was unexpected; not investigated deeper since none of the three is a file `eng_build_loop.md`'s own procedure reads (`eng-gate-check.sh`, `eng-trigger.sh`, and the schedule doc itself are untouched), and the changes look like separate, deliberate work on the `eng_weekly_report` schedule rather than damage. Worth a look by whoever owns that work, to confirm it's intentional and get it committed. |
| 2026-08-27 | security | aiorders-api | `ENG-004`'s `in-security` content read (`agents/security/reviews/ENG-004.md`) of `supabase/migrations/20250814065606_3be00703-...sql` found its own comment ("recreate the view without SECURITY DEFINER — default is SECURITY INVOKER") likely backwards: a plain `CREATE VIEW` with no `WITH (security_invoker = true)` defaults to owner-rights (RLS-bypassing) execution on any Postgres version that supports the option, not invoker-rights — so the drop-and-recreate may not have changed `restaurants_public`'s actual RLS-bypass posture at all. Not filed against `ENG-004` — the PRD's own non-goals exclude auditing whether the six files' policy is still correct today, only whether the consolidation preserved it unmodified (it did, confirmed by blob hash). No DB credential is available to check Supabase's advisor output and confirm live. Worth a look if a future ticket touches `restaurants_public` or gets DB read access. |
| 2026-08-27 | eng-manager | aiorders | This `decision` event (context: `2026-08-27-eng004-g3-verification.md`) drained and launched a full pass after the ticket it names had already reached `verified` — the G3 answer was actually consumed inline by the preceding `continue ENG-004` pass (answered ~92s after `notified:`, while that pass was still running; its own board entry names the same timestamp), which advanced `ENG-004` `ready-to-ship → awaiting-release → shipped → verified` in that session, two pass-slots before this queued event drained (`traces/eng-loop-2026-08-27.log`: `pass end: watch (exit 0, 674s)` → `queue: collapsed 1 duplicate event(s)` → `draining queued event: decision (2026-08-27-eng004-g3-verification.md)`). Verified rather than assumed before concluding no-op: gate item already sat unedited in `inbox/_handled/`, `decision-journal.md` row 13 already recorded this exact decision/timestamp, `ENG-004`'s own ticket log already ended in a proper terminal `chained: none`, and `lib/eng-gate-check.sh` (scoped `ENG-004` and whole-board) both exited 0. No ticket-log or board-index edit made — no new fact for either to carry, and this ticket's log is not touched again per its own terminal state. Same root shape as the 2026-08-26 row above (`decision` draining after `watch` had already processed the same ticket's G1) and the 2026-08-27 proposals.md row (hand-edit racing a queued retry) — work landing through a channel other than the one queued to consume the triggering event, so the event outlives the work. Another corroborating occurrence of that open proposal's target fix (`eng-trigger.sh`: skip the launch if the named gate item is already in `_handled/`), which would have caught this before the hop was charged (`day 10/40`). `traces/.pending` still carries `1 decision 2026-08-27-eng005-g1-scope.md` — the same file the immediately-preceding `watch` pass already moved to `_handled/` — likely one more occurrence about to land the same way; not acted on here, out of scope for a `decision` event naming `ENG-004`. |
| 2026-08-27 | eng-manager | aiorders | This `decision` event (context: `2026-08-27-eng005-g1-scope.md`) is the occurrence the row above named in advance: `traces/eng-loop-2026-08-27.log` shows `pass end: decision (exit 0, 401s)` (that same `ENG-004` G3 pass) → `draining queued event: decision (2026-08-27-eng005-g1-scope.md)` → `pass start: decision (2026-08-27-eng005-g1-scope.md) [day 11/40 charged]`, no gap. Verified rather than assumed before concluding no-op: `inbox/_handled/2026-08-27-eng005-g1-scope.md` unedited since the earlier `watch` pass (`decision: approved`, `decided: 2026-08-27T18:03:50.514589+00:00`, "wire it in"); `decision-journal.md` row 14 already records it; `ENG-005`'s own ticket log already ends at `awaiting-scope` with a proper `chained: none` (owner: approver — the chaining guard's ordinary reason); the follow-up that same `watch` pass raised (`inbox/2026-08-27-eng005-g1-followup-surface.md`) carries no `decision:` field — genuinely unanswered, and a different item from the one this event named regardless; `lib/eng-gate-check.sh`, scoped `ENG-005` and whole-board, both exit 0 clean. No ticket-log or board-index edit made, same reasoning as the row above — no new fact for either to carry, though unlike `ENG-004` this ticket is not terminal, so its log simply stays open for whichever pass next has one. Closer to the 2026-08-26 row's shape than the row directly above's: a `decision` racing a `watch` pass on the identical file, not a `continue` answering inline. Fifth occurrence overall of the still-open, still-unimplemented `proposals.md` (2026-08-27 row) fix — three of the five are dated 2026-08-27. `traces/.pending` now holds `1 watch launchd`, unrelated in shape; whether it finds new work depends on whether the approver has answered the still-open follow-up by the time it fires, which this pass has no way to predict. |
| 2026-08-27 | eng-manager | aiorders | This `watch (launchd)` event is the one the row above left unresolved (`traces/eng-loop-2026-08-27.log`: `pass end: decision (exit 0, 509s)` → `draining queued event: watch (launchd)`, no gap) — a third queued event this morning tracing to the same single hand-edit of `2026-08-27-eng005-g1-scope.md`. Verified rather than assumed before concluding no-op: all three watched inboxes swept fresh — `agents/product-manager/inbox/` and `agents/eng-manager/inbox/` hold only `.gitkeep`, `inbox/requests/` is empty, and `inbox/`'s one live item (`2026-08-27-eng005-g1-followup-surface.md`) was read directly and carries no `decision:` field — genuinely still unanswered, not just assumed so from the narrative. `ENG-005`'s own ticket log and the board index already end in the correct state with a valid `chained: none`; `lib/eng-gate-check.sh`, scoped `ENG-005` and whole-board, both exit 0 clean pre-pass. No ticket-log or board-index edit made — no new fact for either to carry. Sixth occurrence overall of the still-open `proposals.md` fix (2026-08-25 row, restated 2026-08-26 and twice on 2026-08-27); three separate queued events (one `watch`, two `decision`) have now drained against this one hand-edit alone, each spending a full session and a hop to confirm the same nothing. |
| 2026-08-27 | product-manager | aiorders | Shaping `ENG-006` found no `_handled/` convention under `agents/product-manager/inbox/` — every prior shaped request had arrived via the top-level `inbox/requests/` → `inbox/_handled/` path instead. Created `agents/product-manager/inbox/_handled/` and moved the processed request there, mirroring the top-level naming rather than the EM's `_processed/` (which is specific to the technical-intake proposal pipeline, a different flow). One data point, not yet a pattern — worth checking for consistency if a second request ever lands directly in this inbox. |
| 2026-08-27 | eng-manager | aiorders | Seventh occurrence overall of the open `proposals.md` race, but the first via a new path — `intake` racing `watch`, not `decision` racing `watch`. Swept all three inboxes fresh: `agents/eng-manager/inbox/` and `inbox/requests/` empty; `inbox/`'s three items (`2026-08-27-eng004-g3-verification.md`, `2026-08-27-eng005-g1-followup-surface.md`, `2026-08-27-eng005-g1-scope.md`) all sit in `_handled/` with `decision: approved` frontmatter spot-checked directly against the board's own account — matches, not assumed. `agents/product-manager/inbox/` held one new file, `2026-08-27-i-want-to-be-able-to-give-the-customers-points-for-online-or.md` (a large multi-repo loyalty-points request) — genuinely unshaped (no PRD, no ticket; board still shows `Next ID: ENG-006`), but its own frontmatter reads `via: control-center`, and `traces/.pending` already carried a correctly-typed `1 intake .../2026-08-27-i-want-to-be-able-to-give-the-customers-points-for-online-or.md` line for that exact file before this pass reached the lock — the control center's own dispatch had already queued the right event. This event's own definition (given in this pass's prompt) scopes `watch` to a change that did *not* come through the control center; the frontmatter says this one did, so shaping the request here would both be doing `intake`'s job under the wrong contract and guarantee the queued `intake` fire finds nothing left to do later — the same waste the six prior rows document, just with this pass on the other side of the race (the one that would leave nothing, not the one finding nothing). Left the request untouched for the queued `intake` event. `ENG-005` (only in-flight ticket) already carries a valid `chained: ENG-005` from the immediately-preceding `decision` pass — nothing broken to resume. No ticket-log or board-index edit made — no new fact for either to carry. Widens the open proposal's scope: its proposed fix (skip the launch if a *gate item* is already in `_handled/`) doesn't cover this variant, since the racing context here is a fresh PM-inbox request that never lived in `_handled/` at all — whoever picks that fix up should also have a `watch` fire skip business-intake work whose context file already carries `via: control-center` with a matching `intake` line already queued, not just already-`_handled/` gate items. |
| 2026-08-27 | eng-manager | aiorders-admin-hub | The `continue ENG-005` session the preceding `decision` pass chained ran to completion in the git sense — implemented the wiring, committed (`51cdb29`), pushed to `origin` — then ended without touching the ticket's board file or log at all: `state` was still `ready`, the log's last line was still the prior pass's own `chained: ENG-005`, and only `traces/.hops-2026-08-27-ENG-005` reading `2` hinted a first dispatch had run. Distinct from the seven rows above (all "an event drained and found nothing left to do"): this is the opposite failure — real, correct work that existed on disk and in git with zero trace in either place this department actually reads. Recovered only because this pass's own habit (`git status`/`git log` the worktree before any build hop, per `config/projects.md`'s "uncommitted changes means a previous pass died" rule) happens to also surface *committed-but-unrecorded* work, which that rule was not written for. Worth a look if it recurs: nothing today distinguishes "the chain never fired" from "the chain fired, did the work, and died before writing the receipt" except a pass choosing to check. |
| 2026-08-27 | eng-manager | aiorders | Eighth occurrence of the open `proposals.md` race (2026-08-27 row, "skip the launch if the gate item is already in `_handled/`") — a `watch (launchd)` event drained immediately behind the `continue ENG-005` pass that had just taken the ticket to `ready-to-ship` and chained its own next `continue ENG-005` (`traces/eng-loop-2026-08-27.log`: `pass end: continue (exit 0, 1046s)` → `draining queued event: watch (launchd)`, no gap). Verified rather than assumed before concluding no-op: all three watched inboxes swept fresh — `agents/eng-manager/inbox/` holds only `.gitkeep`, `agents/product-manager/inbox/` holds only `.gitkeep` plus an already-`_handled/` file, `inbox/requests/` is empty, and `inbox/`'s one live item (`2026-08-27-eng006-g1-scope.md`) was read directly — `decision:`/`decided:` both still empty, genuinely unanswered and already correctly `notified: 2026-08-27T13:47:31` by the earlier `intake ENG-006` pass, well under the 24h nudge threshold. `ENG-005`'s and `ENG-006`'s own ticket logs both already end in valid, accounted-for states (`chained: ENG-005` and `chained: none` respectively); `lib/eng-gate-check.sh`, scoped and whole-board, exits 0 clean. No ticket-log or board-index edit made — no new fact for either to carry, and re-firing a chain for either ticket here would either duplicate the `continue ENG-005` already queued behind this pass (`traces/.pending`) or wrongly chain a ticket waiting on the approver. `traces/.pending` still holds `1 scheduled launchd` ahead of that fresh `continue ENG-005` — unrelated to this fire, not acted on here. |
| 2026-08-27 | eng-manager | aiorders | Ninth occurrence of the open `proposals.md` race (2026-08-25 row, "skip the launch if the gate item is already in `_handled/`") — a `watch (launchd)` event drained shortly behind the `continue ENG-005` pass immediately above, which raised the merge-request gate and moved the ticket `ready-to-ship → blocked`. Verified rather than assumed before concluding no-op: all three watched inboxes swept fresh — `agents/eng-manager/inbox/` and `agents/product-manager/inbox/` hold only `.gitkeep` (plus the latter's already-`_handled/` file), `inbox/requests/` holds only `.gitkeep`, and `inbox/`'s two live items were both read directly — `2026-08-27-eng006-g1-scope.md` (`notified: 13:47:31`) and `2026-08-27-eng005-merge-request.md` (`notified: 16:03:58`) — both `decision:`/`decided:` still empty, both well under the 24h nudge threshold. `ENG-005` and `ENG-006` already end their own logs in valid, accounted-for states (`chained: none`, owner `approver`, in both cases) — nothing to resume. `lib/eng-gate-check.sh`, whole-board (this event names no ticket to scope to): exit 0 clean. Also confirmed via `ps` on the pid held in `traces/.loop.lock` that it's this pass's own top-of-chain orchestrator (fired as `scheduled launchd`, which per the queue design drained the older pending `watch launchd` event first) — not a second concurrent instance. No ticket-log or board-index edit made — no new fact for either to carry. |
| 2026-08-28 | eng-manager | aiorders | `lib/eng-trigger.sh`'s per-ticket hop-budget file is named `.hops-$(date '+%Y-%m-%d')-{TICKET_ID}` (lines 884, 1496) — a fresh file every midnight, counter effectively back to 0. `schedules/eng_build_loop.md`'s own Cadence section states the opposite: "the day's counter clears at midnight where a ticket's does not," implying the per-ticket `max_hops_per_ticket` cap is meant to persist across days. Found while checking `ENG-005`'s hop budget before dispatching this pass (`traces/.hops-2026-08-27-ENG-005` read `3`; today's `.hops-2026-08-28-ENG-005` didn't exist yet) — harmless to this ticket either way (nowhere near the 8/day `pro`-tier cap under either reading), so not chased further. Worth a look if a ticket ever bounces across a midnight boundary: under the code as written, a ticket that hit 8 hops yesterday gets a clean 8 more today, which is not what the prose says the guard is for. |
| 2026-08-28 | eng-manager | aiorders | A new failure shape, distinct from the duplicate-event race this file documents repeatedly (two queued events, one finding nothing left because the other already did it): a pass can crash **mid-edit-sequence within a single gate response** and leave on-disk artifacts partially updated and self-contradictory, rather than either fully done or untouched. This `decision (2026-08-27-eng006-g1-scope.md)` pass found `ENG-006`'s PRD already flipped to `status: designed` with a full `## Decision` section claiming a companion doc was "Done" — written by an earlier `watch` pass that then died on the account's monthly spend limit right after those edits, before touching the ticket board, the design, or the gate item itself (`inbox/2026-08-28-eng-events-dropped.md`'s 10:42:17 drop entry is that same pass's failed retry). The claim was false at the time this pass read it — the file didn't exist. Caught only because this pass checked the claim against the filesystem instead of trusting the PRD's own text. Neither existing open proposal (the `_handled/`-precondition fix, or the `.watch-seen` fingerprint timing fix) touches this — both assume a pass either fully lands its work or doesn't start it; this is the case where a pass lands *some* of it and a later pass has to tell which parts by checking each artifact rather than trusting any single file's self-report. Not filed as a proposal yet — one occurrence; per this file's own rule, a second one is what would make it worth a process fix rather than a recovery habit (verify every claim of "done" against the filesystem before building on it, already this department's general practice, just not yet tested against a genuinely partial write before now). |
| 2026-08-28 | eng-manager | aiorders | Another corroborating occurrence of the open `.watch-seen` fingerprint-timing race (`proposals.md`, 2026-08-26 row): a `watch (launchd)` fire drained immediately behind the `scheduled` pass that had just closed `ENG-006`'s G2 and chained its own `continue ENG-006`. Same exact mechanism as the prior rows — `commit_watch_fingerprint()` only runs for `$EVENT = "watch"`, so the `scheduled` pass's edits changed the three inboxes' top-level contents without ever updating `.watch-seen`, guaranteeing this fire would see "something changed." Verified rather than assumed: all three inboxes swept fresh, nothing postdated the `scheduled` pass; `ENG-006`'s log already ended validly with `chained: ENG-006`. No new mechanism, no ticket touched — filed for the count, not for new content. Also separately confirmed `lib/eng-gate-check.sh`'s default `ROOT` resolves from the script's own path (`departments/engineering`), not cwd, so it fails closed (exit 2, "board directory not found") unless `ENG_ROOT` is pinned to the instance root — `eng-trigger.sh` already does this (line 1298); worth remembering for any manual/interactive invocation of the check outside the trigger script. |
| 2026-08-28 | eng-manager | aiorders | Tenth occurrence of the open `proposals.md` race (2026-08-27 row, "skip the launch if the gate item is already in `_handled/`") — this `decision (2026-08-28-eng006-g2-oneway-door.md)` event drained behind both the `scheduled` pass that actually answered `ENG-006`'s G2 and the `watch` pass immediately after it (`traces/eng-loop-2026-08-28.log`: `pass end: watch (exit 0, 370s)` → `draining queued event: decision (2026-08-28-eng006-g2-oneway-door.md)`, no gap; `[day 7/40 charged]`) — exactly the duplicate-fire shape the `scheduled` pass's own board entry and this ticket's own log predicted in advance, before it happened. Verified rather than assumed before concluding no-op: `inbox/_handled/2026-08-28-eng006-g2-oneway-door.md` carries the processed footer, unedited since the `scheduled` pass; `decision-journal.md`'s `ENG-006` G2 row already records the same decision and timestamp; `ENG-006`'s own ticket log already ends at `ready` with a valid `chained: ENG-006`; `lib/eng-gate-check.sh`, scoped `ENG-006` and whole-board, both exit 0 clean. Also confirmed via `ps` that the pid holding `traces/.loop.lock` (17867, elapsed 40:54) is this same queue-draining orchestrator working through `scheduled → watch → decision` in one lock hold, not a second concurrent instance — same check the 2026-08-27 Ninth-occurrence row used rather than assuming it. No ticket-log or board-index edit made — no new fact for either to carry, and re-chaining here would duplicate the `continue ENG-006` the `scheduled` pass already queued (`traces/.pending` holds exactly `1 continue ENG-006`, unaffected by this pass). |
| 2026-08-28 | eng-manager | aiorders | Deeper cause underneath the open `.watch-seen` proposal (`proposals.md`, 2026-08-26 row) and its self-modifying-pass addendum above: `watch_fingerprint()` (`lib/eng-trigger.sh:366`) hashes one `eng_stamp "$f"` line per matching file, but `eng_stamp()` itself (`lib/eng-env.sh:185`) is `date '+%Y-%m-%dT%H:%M:%S%z'` — it ignores its `$1` argument entirely and returns the current wall-clock time, never the file's path, mtime, or content (contrast `eng_mtime()` immediately above it, which correctly `stat`s `$1`). Verified empirically this pass rather than read alone: called the real function twice, 2s apart, against identical unchanged inbox contents — two different SHA-1s (`cba0b32b...` vs `55b6fbd9...`). So even the open proposal's fix (commit after every event type, not just `watch`) would not close this: any two computations of a genuinely-unchanged inbox more than about a second apart already produce different fingerprints, because the hash is a function of wall-clock call time and file *count*, never file identity. The one case that stays stable is all three inboxes empty (zero `eng_stamp` calls, hash of empty input, `da39a3ee...`) — which is exactly the value an earlier entry above found stamped, before `inbox/` started holding a file continuously from today's 10:42 incident onward. Whoever picks up the open proposal needs to fix `eng_stamp()` (or stop routing `watch_fingerprint()` through it) in addition to widening the commit condition — the commit-timing fix alone would still misfire on nearly every non-empty sweep. |
