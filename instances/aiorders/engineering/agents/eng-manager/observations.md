# Observations

"While I was in there, I noticed..." — the thing a good engineer mentions on the
way past. Not a bug, not a ticket, not a request. Cheap to file, no obligation
on anyone, and worth more in aggregate than any single one is alone.

## Rules

- Any agent may append. No permission, no owner, no reply expected.
- One line each, newest last, under the Ledger below.
- An observation is not work. If it needs doing it is a bug or a proposal —
  file it as one instead of writing it here and hoping.
- Nothing reads this on a pass. It is read by the weekly report and by a human.

## Format

`| {date} | {agent} | {project} | {what you noticed} |`

## Ledger

| Date | By | Project | Observation |
|---|---|---|---|
| 2026-08-24 | eng-manager | aiorders | `board/_index.md` carries no "next ID" counter, though `config/templates/ticket.md` says IDs are never reused and the next one lives there — worth adding before ENG-002 gets allocated. |
| 2026-08-25 | product-manager | aiorders | Three approver-filed requests in `inbox/requests/` (`a4-poster-generator-unwired`, `admin-hub-migration-history`, `aiorders-env-hygiene`, all `received: 2026-08-23`) are still unshaped two days later — out of scope for this `continue ENG-001` pass, so left untouched, but worth flagging: `watch` isn't wired on this instance yet (`eng_build_loop.md`'s own note), so nothing but the twice-daily scheduled sweep would otherwise find them. None is P0, but `aiorders-env-hygiene` names a live cost exposure (an unrestricted Google Maps API key) that's worth not leaving to chance. |
| 2026-08-25 | product-manager | aiorders | `config/definition-of-done.md`'s "Ticket states" table lists `intake`'s owner as `eng-manager`, contradicting the paragraph directly beneath it ("`intake` and `shaped` are owned by the **product-manager**... corrected 2026-07-27"), the ticket template's own `owner: product-manager` default, and `agents/product-manager/agent.md`'s description of intake as the PM's job. Went with the prose + template + agent.md (unanimous) over the stale table cell for this pass; the table row itself is still wrong on disk. |
| 2026-08-25 | product-manager | aiorders | Shaping `ENG-003` (env hygiene) hit a ticket-schema gap: the work genuinely spans three repos (`config-site-builder`, `aiorders-admin-hub`, `restaurant-marketplace`), but `config/templates/ticket.md` models `project:`, `branch:` and `links.pr` as singular. Scoped the ticket's primary `project:` to `config-site-builder` (the repo with the actually-tracked secret) and named the other two repos' fixes explicitly in the PRD rather than inventing a multi-project ticket shape unilaterally — left for whoever picks this up at `building` to resolve the branch/PR mechanics. |
| 2026-08-25 | product-manager | aiorders | The architect's blind reading on `ENG-004` (admin-hub migration history) caught that `config/projects.md`'s registry records each repo's stack/deploy-target/autonomy but not which Supabase *project* it's linked to — so "is `aiorders-admin-hub` on the same database as `aiorders-api`" (the actual question ENG-004 needs to answer) isn't answerable from the registry as it stands today. |
| 2026-08-25 | eng-manager | aiorders | The first `scheduled (manual-unblock)` attempt at this pass (three full request-readbacks plus board updates in one session) hit the 1800s pass timeout and was killed mid-edit; the retry (this pass) picked it up from the on-disk state and found the substantive work already complete and consistent — only the board index's In-flight table rows for the three new tickets were missing. Worth a look if intake-heavy scheduled sweeps keep running long: either split business intake across more passes, or the timeout has room to grow — not filed as a proposal since one data point isn't a pattern yet. |
| 2026-08-25 | eng-manager | aiorders | This `watch` pass found `traces/.pending` carrying `1 scheduled instances/aiorders/engineering/agents/eng-manager/board/ENG-001-register-repos-and-prove-the-loop.md` — a `scheduled` event whose context is a ticket board file path. Both plist generators in `lib/eng-schedule.sh` invoke `scheduled` with context `launchd`, and the one manually-fired `scheduled` pass logged today used `manual-unblock`; this entry matches neither. Harmless today — `eng-trigger.sh` gives `scheduled`/`watch` an empty `TICKET_ID` regardless of context — but worth tracing to its source if it recurs. |
| 2026-08-26 | eng-manager | aiorders | Second occurrence of the row above, same exact shape (`traces/eng-loop-2026-08-26.log`: `draining queued event: scheduled (instances/aiorders/engineering/agents/eng-manager/board/ENG-001-register-repos-and-prove-the-loop.md)`) — this pass's own launch. This corrects the earlier row's assumption, rather than confirming it: `TICKET_ID=$(echo "$CONTEXT" \| grep -oE '[A-Z]{2,4}-[0-9]+' \| head -1)` in `eng-trigger.sh` runs unconditionally on `CONTEXT` regardless of `EVENT`, so a ticket-path context does NOT leave `TICKET_ID` empty for a `scheduled` fire — `traces/.hops-2026-08-26-ENG-001` read `1` after this pass launched, i.e. it was charged against `ENG-001`'s own per-ticket budget rather than running ticket-less. Minor in impact (one hop against an 8/day budget) but the "harmless" claim in the row above is now known wrong. Still not traced to a root cause — worth a proposal if a third occurrence shows up. |
| 2026-08-26 | eng-manager | aiorders | Second time in two days a `watch` fire cleared `watch_fingerprint`'s top-level de-noise check, launched a full session (this pass, 10:13:33), and found nothing new in any of the three watched inboxes — both `agents/*/inbox/` empty, and `inbox/`'s only two files (`ENG-003`, `ENG-004` G1s) unchanged since yesterday and already fully accounted for by the `scheduled` pass that finished 20 seconds earlier. First occurrence was yesterday 15:00–15:05 (same shape: queued behind a long pass, nothing left to do). Didn't trace which top-level file/mtime/size actually differed from the committed `.watch-seen` value either time — worth checking whether `commit_watch_fingerprint` is capturing a transient state (e.g. mid-write on a gate item) rather than the settled one, since that would mean a busy afternoon burns a full session and a hop for zero output each time. Two data points; a proposal if it recurs. |
| 2026-08-26 | eng-manager | aiorders | A `decision` event (context: `2026-08-25-eng004-g1-scope.md`) drained immediately after a `watch` pass that had already fully processed the same hand-edited gate answer (`traces/eng-loop-2026-08-26.log`: `pass end: watch (exit 0, 669s)` → `draining queued event: decision (2026-08-25-eng004-g1-scope.md)`, no gap) — a full session and a hop spent finding nothing left to do. Root mechanism: the approver's hand-edit changes a file inside a watched `inbox/`, so both the poll-detected `decision` path and the raw `watch` path queue their own event for the one change, and the queue's dedup only collapses identical `<event> <context>` lines — `decision (2026-08-25-eng004-g1-scope.md)` and `watch (launchd)` never match each other. Different mechanism from the row above (that's watch-after-non-watch, stale `.watch-seen`; this is a `decision` fire and a `watch` fire racing on the same edit, both legitimately queued). One data point — a proposal if it recurs. |
| 2026-08-26 | eng-manager | aiorders | Addendum to the open `.watch-seen` proposal (`proposals.md`, 2026-08-26), not a new occurrence to recount: that proposal's fix ("commit the fingerprint after every event type, not just `watch`") won't cover the fifth occurrence this pass hit. Read `lib/eng-trigger.sh` directly rather than guessing — `WATCH_FP` is captured *before* the pass launches (line 1583, deliberate, so mid-pass arrivals still look new next time) and `commit_watch_fingerprint` (line 424) writes that unchanged pre-launch value on any clean exit (line 1892-1893). The pass that triggered this fire (pid 73496) *was* `watch`-typed and *did* exit 0, so it committed fine — but it had just moved `2026-08-25-eng004-g1-scope.md` into `_handled/` as part of its own work, so the fingerprint it committed was already stale relative to the `inbox/` it left behind. Any `watch` pass that actually processes a gate item does this same move, so it isn't a corner case — it guarantees one wasted `watch` fire after every productive one. Whoever picks up the open proposal needs to widen the fix to cover self-modifying `watch` passes too, not just non-`watch` event types — full mechanism and citations on the board's dated entry for this pass. |
| 2026-08-26 | architect | aiorders-api | `ENG-004`'s `designed` investigation found `_eng/aiorders-api`'s own worktree branch (`eng/base`) has diverged from `origin/main` by dozens of commits — its HEAD predates a same-day migration consolidation (2026-08-24) that `origin/main` already carries. Read `origin/main` directly instead of trusting the worktree's files; a future ticket that trusts `_eng/aiorders-api`'s working tree at face value for this repo specifically would reach stale conclusions. Not filed as a proposal — one occurrence, and the fix (read `origin/main`, not the worktree, when investigating repo state) is now on record in `ADR-003`/this ticket's design rather than a process gap needing a rule change yet. |
| 2026-08-26 | architect | aiorders-admin-hub | `supabase/config.toml` still declares 20 `[functions.*]` stanzas for edge functions that no longer live in this repo (moved to `aiorders-api` in an earlier, already-closed consolidation) — harmless (nothing reads the stale stanzas; `project_id` is still valid) but orphaned. Unrelated to `ENG-004`'s migrations scope, left untouched. |
| 2026-08-26 | architect | aiorders-admin-hub | `ENG-004`'s entire subject matter — moving migration ownership from `aiorders-admin-hub` to `aiorders-api` — turned out to already be done: the approver committed it directly to both repos' `main`, in two matched pairs seconds apart, on 2026-08-24, one day after filing the request and before the ticket ever reached `shaped`. `ADR-004` names what a second occurrence of this specific pattern (the approver resolving a ticket's own subject matter directly, mid-flight) would mean — flagged here as the first data point rather than re-argued there. |
| 2026-08-27 | eng-manager | aiorders | This `scheduled` sweep's pre-pass `git status` showed uncommitted changes sitting in the department's own tree — `departments/engineering/lib/eng-schedule.sh` and `departments/engineering/schedules/eng_weekly_report.md` modified, `departments/engineering/lib/eng-report.sh` untracked — none of them touched by this pass. That directory is documented as shared and read-only from an instance's perspective, so finding it mid-edit was unexpected; not investigated deeper since none of the three is a file `eng_build_loop.md`'s own procedure reads (`eng-gate-check.sh`, `eng-trigger.sh`, and the schedule doc itself are untouched), and the changes look like separate, deliberate work on the `eng_weekly_report` schedule rather than damage. Worth a look by whoever owns that work, to confirm it's intentional and get it committed. |
| 2026-08-27 | security | aiorders-api | `ENG-004`'s `in-security` content read (`agents/security/reviews/ENG-004.md`) of `supabase/migrations/20250814065606_3be00703-...sql` found its own comment ("recreate the view without SECURITY DEFINER — default is SECURITY INVOKER") likely backwards: a plain `CREATE VIEW` with no `WITH (security_invoker = true)` defaults to owner-rights (RLS-bypassing) execution on any Postgres version that supports the option, not invoker-rights — so the drop-and-recreate may not have changed `restaurants_public`'s actual RLS-bypass posture at all. Not filed against `ENG-004` — the PRD's own non-goals exclude auditing whether the six files' policy is still correct today, only whether the consolidation preserved it unmodified (it did, confirmed by blob hash). No DB credential is available to check Supabase's advisor output and confirm live. Worth a look if a future ticket touches `restaurants_public` or gets DB read access. |
| 2026-08-27 | eng-manager | aiorders | This `decision` event (context: `2026-08-27-eng004-g3-verification.md`) drained and launched a full pass after the ticket it names had already reached `verified` — the G3 answer was actually consumed inline by the preceding `continue ENG-004` pass (answered ~92s after `notified:`, while that pass was still running; its own board entry names the same timestamp), which advanced `ENG-004` `ready-to-ship → awaiting-release → shipped → verified` in that session, two pass-slots before this queued event drained (`traces/eng-loop-2026-08-27.log`: `pass end: watch (exit 0, 674s)` → `queue: collapsed 1 duplicate event(s)` → `draining queued event: decision (2026-08-27-eng004-g3-verification.md)`). Verified rather than assumed before concluding no-op: gate item already sat unedited in `inbox/_handled/`, `decision-journal.md` row 13 already recorded this exact decision/timestamp, `ENG-004`'s own ticket log already ended in a proper terminal `chained: none`, and `lib/eng-gate-check.sh` (scoped `ENG-004` and whole-board) both exited 0. No ticket-log or board-index edit made — no new fact for either to carry, and this ticket's log is not touched again per its own terminal state. Same root shape as the 2026-08-26 row above (`decision` draining after `watch` had already processed the same ticket's G1) and the 2026-08-27 proposals.md row (hand-edit racing a queued retry) — work landing through a channel other than the one queued to consume the triggering event, so the event outlives the work. Another corroborating occurrence of that open proposal's target fix (`eng-trigger.sh`: skip the launch if the named gate item is already in `_handled/`), which would have caught this before the hop was charged (`day 10/40`). `traces/.pending` still carries `1 decision 2026-08-27-eng005-g1-scope.md` — the same file the immediately-preceding `watch` pass already moved to `_handled/` — likely one more occurrence about to land the same way; not acted on here, out of scope for a `decision` event naming `ENG-004`. |
| 2026-08-27 | eng-manager | aiorders | This `decision` event (context: `2026-08-27-eng005-g1-scope.md`) is the occurrence the row above named in advance: `traces/eng-loop-2026-08-27.log` shows `pass end: decision (exit 0, 401s)` (that same `ENG-004` G3 pass) → `draining queued event: decision (2026-08-27-eng005-g1-scope.md)` → `pass start: decision (2026-08-27-eng005-g1-scope.md) [day 11/40 charged]`, no gap. Verified rather than assumed before concluding no-op: `inbox/_handled/2026-08-27-eng005-g1-scope.md` unedited since the earlier `watch` pass (`decision: approved`, `decided: 2026-08-27T18:03:50.514589+00:00`, "wire it in"); `decision-journal.md` row 14 already records it; `ENG-005`'s own ticket log already ends at `awaiting-scope` with a proper `chained: none` (owner: approver — the chaining guard's ordinary reason); the follow-up that same `watch` pass raised (`inbox/2026-08-27-eng005-g1-followup-surface.md`) carries no `decision:` field — genuinely unanswered, and a different item from the one this event named regardless; `lib/eng-gate-check.sh`, scoped `ENG-005` and whole-board, both exit 0 clean. No ticket-log or board-index edit made, same reasoning as the row above — no new fact for either to carry, though unlike `ENG-004` this ticket is not terminal, so its log simply stays open for whichever pass next has one. Closer to the 2026-08-26 row's shape than the row directly above's: a `decision` racing a `watch` pass on the identical file, not a `continue` answering inline. Fifth occurrence overall of the still-open, still-unimplemented `proposals.md` (2026-08-27 row) fix — three of the five are dated 2026-08-27. `traces/.pending` now holds `1 watch launchd`, unrelated in shape; whether it finds new work depends on whether the approver has answered the still-open follow-up by the time it fires, which this pass has no way to predict. |
| 2026-08-29 | architect | aiorders-api | This `continue ENG-025` pass's design work incidentally confirmed, by reading the code directly rather than trusting the PRD's wording, that `brand-portal/feedback.ts`'s `getFeedback` calls `verifyRestaurantAccess(supabase, user.id, restaurant_id)` — wrong argument order against `utils.ts`'s real `(restaurantId, supabase, user, options?)` signature — and then checks the returned object's truthiness (`if (!hasAccess)`, an object literal, always truthy) instead of `.hasAccess`, so the check can never actually deny access. Same bug class `ENG-022` ("Fix broken restaurant-scoped access check on 5 brand-portal handlers") is already scoped to fix; not confirmed here whether `feedback.ts` is already one of that ticket's named 5 or would be a 6th — worth a check by whoever builds `ENG-022`, since this file wasn't in this pass's own scope to fix. |
| 2026-08-29 | eng-manager | aiorders-api | This `continue ENG-025` pass found `ENG-007`'s own ticket file already at `state: shipped` (`blocked → shipped`, logged as "control center, merge detected... recorded on Harry's say-so; ancestry not consulted") while `board/_index.md`'s header and In-flight table still show it `blocked` — the transition happened outside any build-loop pass, by a human dashboard action, and `inbox/2026-08-29-eng007-merge-request.md` still carries no `decision:` field to match. Same shape as the `ENG-011` staleness `ENG-007`'s own pass logged earlier today. Not reconciled here — out of scope for a ticket-scoped `continue ENG-025` pass; worth a look by whichever pass next sweeps the whole board. |
| 2026-08-27 | eng-manager | aiorders | This `watch (launchd)` event is the one the row above left unresolved (`traces/eng-loop-2026-08-27.log`: `pass end: decision (exit 0, 509s)` → `draining queued event: watch (launchd)`, no gap) — a third queued event this morning tracing to the same single hand-edit of `2026-08-27-eng005-g1-scope.md`. Verified rather than assumed before concluding no-op: all three watched inboxes swept fresh — `agents/product-manager/inbox/` and `agents/eng-manager/inbox/` hold only `.gitkeep`, `inbox/requests/` is empty, and `inbox/`'s one live item (`2026-08-27-eng005-g1-followup-surface.md`) was read directly and carries no `decision:` field — genuinely still unanswered, not just assumed so from the narrative. `ENG-005`'s own ticket log and the board index already end in the correct state with a valid `chained: none`; `lib/eng-gate-check.sh`, scoped `ENG-005` and whole-board, both exit 0 clean pre-pass. No ticket-log or board-index edit made — no new fact for either to carry. Sixth occurrence overall of the still-open `proposals.md` fix (2026-08-25 row, restated 2026-08-26 and twice on 2026-08-27); three separate queued events (one `watch`, two `decision`) have now drained against this one hand-edit alone, each spending a full session and a hop to confirm the same nothing. |
| 2026-08-27 | product-manager | aiorders | Shaping `ENG-006` found no `_handled/` convention under `agents/product-manager/inbox/` — every prior shaped request had arrived via the top-level `inbox/requests/` → `inbox/_handled/` path instead. Created `agents/product-manager/inbox/_handled/` and moved the processed request there, mirroring the top-level naming rather than the EM's `_processed/` (which is specific to the technical-intake proposal pipeline, a different flow). One data point, not yet a pattern — worth checking for consistency if a second request ever lands directly in this inbox. |
| 2026-08-27 | eng-manager | aiorders | Seventh occurrence overall of the open `proposals.md` race, but the first via a new path — `intake` racing `watch`, not `decision` racing `watch`. Swept all three inboxes fresh: `agents/eng-manager/inbox/` and `inbox/requests/` empty; `inbox/`'s three items (`2026-08-27-eng004-g3-verification.md`, `2026-08-27-eng005-g1-followup-surface.md`, `2026-08-27-eng005-g1-scope.md`) all sit in `_handled/` with `decision: approved` frontmatter spot-checked directly against the board's own account — matches, not assumed. `agents/product-manager/inbox/` held one new file, `2026-08-27-i-want-to-be-able-to-give-the-customers-points-for-online-or.md` (a large multi-repo loyalty-points request) — genuinely unshaped (no PRD, no ticket; board still shows `Next ID: ENG-006`), but its own frontmatter reads `via: control-center`, and `traces/.pending` already carried a correctly-typed `1 intake .../2026-08-27-i-want-to-be-able-to-give-the-customers-points-for-online-or.md` line for that exact file before this pass reached the lock — the control center's own dispatch had already queued the right event. This event's own definition (given in this pass's prompt) scopes `watch` to a change that did *not* come through the control center; the frontmatter says this one did, so shaping the request here would both be doing `intake`'s job under the wrong contract and guarantee the queued `intake` fire finds nothing left to do later — the same waste the six prior rows document, just with this pass on the other side of the race (the one that would leave nothing, not the one finding nothing). Left the request untouched for the queued `intake` event. `ENG-005` (only in-flight ticket) already carries a valid `chained: ENG-005` from the immediately-preceding `decision` pass — nothing broken to resume. No ticket-log or board-index edit made — no new fact for either to carry. Widens the open proposal's scope: its proposed fix (skip the launch if a *gate item* is already in `_handled/`) doesn't cover this variant, since the racing context here is a fresh PM-inbox request that never lived in `_handled/` at all — whoever picks that fix up should also have a `watch` fire skip business-intake work whose context file already carries `via: control-center` with a matching `intake` line already queued, not just already-`_handled/` gate items. |
| 2026-08-27 | eng-manager | aiorders-admin-hub | The `continue ENG-005` session the preceding `decision` pass chained ran to completion in the git sense — implemented the wiring, committed (`51cdb29`), pushed to `origin` — then ended without touching the ticket's board file or log at all: `state` was still `ready`, the log's last line was still the prior pass's own `chained: ENG-005`, and only `traces/.hops-2026-08-27-ENG-005` reading `2` hinted a first dispatch had run. Distinct from the seven rows above (all "an event drained and found nothing left to do"): this is the opposite failure — real, correct work that existed on disk and in git with zero trace in either place this department actually reads. Recovered only because this pass's own habit (`git status`/`git log` the worktree before any build hop, per `config/projects.md`'s "uncommitted changes means a previous pass died" rule) happens to also surface *committed-but-unrecorded* work, which that rule was not written for. Worth a look if it recurs: nothing today distinguishes "the chain never fired" from "the chain fired, did the work, and died before writing the receipt" except a pass choosing to check. |
| 2026-08-27 | eng-manager | aiorders | Eighth occurrence of the open `proposals.md` race (2026-08-27 row, "skip the launch if the gate item is already in `_handled/`") — a `watch (launchd)` event drained immediately behind the `continue ENG-005` pass that had just taken the ticket to `ready-to-ship` and chained its own next `continue ENG-005` (`traces/eng-loop-2026-08-27.log`: `pass end: continue (exit 0, 1046s)` → `draining queued event: watch (launchd)`, no gap). Verified rather than assumed before concluding no-op: all three watched inboxes swept fresh — `agents/eng-manager/inbox/` holds only `.gitkeep`, `agents/product-manager/inbox/` holds only `.gitkeep` plus an already-`_handled/` file, `inbox/requests/` is empty, and `inbox/`'s one live item (`2026-08-27-eng006-g1-scope.md`) was read directly — `decision:`/`decided:` both still empty, genuinely unanswered and already correctly `notified: 2026-08-27T13:47:31` by the earlier `intake ENG-006` pass, well under the 24h nudge threshold. `ENG-005`'s and `ENG-006`'s own ticket logs both already end in valid, accounted-for states (`chained: ENG-005` and `chained: none` respectively); `lib/eng-gate-check.sh`, scoped and whole-board, exits 0 clean. No ticket-log or board-index edit made — no new fact for either to carry, and re-firing a chain for either ticket here would either duplicate the `continue ENG-005` already queued behind this pass (`traces/.pending`) or wrongly chain a ticket waiting on the approver. `traces/.pending` still holds `1 scheduled launchd` ahead of that fresh `continue ENG-005` — unrelated to this fire, not acted on here. |
| 2026-08-27 | eng-manager | aiorders | Ninth occurrence of the open `proposals.md` race (2026-08-25 row, "skip the launch if the gate item is already in `_handled/`") — a `watch (launchd)` event drained shortly behind the `continue ENG-005` pass immediately above, which raised the merge-request gate and moved the ticket `ready-to-ship → blocked`. Verified rather than assumed before concluding no-op: all three watched inboxes swept fresh — `agents/eng-manager/inbox/` and `agents/product-manager/inbox/` hold only `.gitkeep` (plus the latter's already-`_handled/` file), `inbox/requests/` holds only `.gitkeep`, and `inbox/`'s two live items were both read directly — `2026-08-27-eng006-g1-scope.md` (`notified: 13:47:31`) and `2026-08-27-eng005-merge-request.md` (`notified: 16:03:58`) — both `decision:`/`decided:` still empty, both well under the 24h nudge threshold. `ENG-005` and `ENG-006` already end their own logs in valid, accounted-for states (`chained: none`, owner `approver`, in both cases) — nothing to resume. `lib/eng-gate-check.sh`, whole-board (this event names no ticket to scope to): exit 0 clean. Also confirmed via `ps` on the pid held in `traces/.loop.lock` that it's this pass's own top-of-chain orchestrator (fired as `scheduled launchd`, which per the queue design drained the older pending `watch launchd` event first) — not a second concurrent instance. No ticket-log or board-index edit made — no new fact for either to carry. |
| 2026-08-28 | eng-manager | aiorders | `lib/eng-trigger.sh`'s per-ticket hop-budget file is named `.hops-$(date '+%Y-%m-%d')-{TICKET_ID}` (lines 884, 1496) — a fresh file every midnight, counter effectively back to 0. `schedules/eng_build_loop.md`'s own Cadence section states the opposite: "the day's counter clears at midnight where a ticket's does not," implying the per-ticket `max_hops_per_ticket` cap is meant to persist across days. Found while checking `ENG-005`'s hop budget before dispatching this pass (`traces/.hops-2026-08-27-ENG-005` read `3`; today's `.hops-2026-08-28-ENG-005` didn't exist yet) — harmless to this ticket either way (nowhere near the 8/day `pro`-tier cap under either reading), so not chased further. Worth a look if a ticket ever bounces across a midnight boundary: under the code as written, a ticket that hit 8 hops yesterday gets a clean 8 more today, which is not what the prose says the guard is for. |
| 2026-08-28 | eng-manager | aiorders | A new failure shape, distinct from the duplicate-event race this file documents repeatedly (two queued events, one finding nothing left because the other already did it): a pass can crash **mid-edit-sequence within a single gate response** and leave on-disk artifacts partially updated and self-contradictory, rather than either fully done or untouched. This `decision (2026-08-27-eng006-g1-scope.md)` pass found `ENG-006`'s PRD already flipped to `status: designed` with a full `## Decision` section claiming a companion doc was "Done" — written by an earlier `watch` pass that then died on the account's monthly spend limit right after those edits, before touching the ticket board, the design, or the gate item itself (`inbox/2026-08-28-eng-events-dropped.md`'s 10:42:17 drop entry is that same pass's failed retry). The claim was false at the time this pass read it — the file didn't exist. Caught only because this pass checked the claim against the filesystem instead of trusting the PRD's own text. Neither existing open proposal (the `_handled/`-precondition fix, or the `.watch-seen` fingerprint timing fix) touches this — both assume a pass either fully lands its work or doesn't start it; this is the case where a pass lands *some* of it and a later pass has to tell which parts by checking each artifact rather than trusting any single file's self-report. Not filed as a proposal yet — one occurrence; per this file's own rule, a second one is what would make it worth a process fix rather than a recovery habit (verify every claim of "done" against the filesystem before building on it, already this department's general practice, just not yet tested against a genuinely partial write before now). |
| 2026-08-28 | eng-manager | aiorders | Another corroborating occurrence of the open `.watch-seen` fingerprint-timing race (`proposals.md`, 2026-08-26 row): a `watch (launchd)` fire drained immediately behind the `scheduled` pass that had just closed `ENG-006`'s G2 and chained its own `continue ENG-006`. Same exact mechanism as the prior rows — `commit_watch_fingerprint()` only runs for `$EVENT = "watch"`, so the `scheduled` pass's edits changed the three inboxes' top-level contents without ever updating `.watch-seen`, guaranteeing this fire would see "something changed." Verified rather than assumed: all three inboxes swept fresh, nothing postdated the `scheduled` pass; `ENG-006`'s log already ended validly with `chained: ENG-006`. No new mechanism, no ticket touched — filed for the count, not for new content. Also separately confirmed `lib/eng-gate-check.sh`'s default `ROOT` resolves from the script's own path (`departments/engineering`), not cwd, so it fails closed (exit 2, "board directory not found") unless `ENG_ROOT` is pinned to the instance root — `eng-trigger.sh` already does this (line 1298); worth remembering for any manual/interactive invocation of the check outside the trigger script. |
| 2026-08-28 | eng-manager | aiorders | Tenth occurrence of the open `proposals.md` race (2026-08-27 row, "skip the launch if the gate item is already in `_handled/`") — this `decision (2026-08-28-eng006-g2-oneway-door.md)` event drained behind both the `scheduled` pass that actually answered `ENG-006`'s G2 and the `watch` pass immediately after it (`traces/eng-loop-2026-08-28.log`: `pass end: watch (exit 0, 370s)` → `draining queued event: decision (2026-08-28-eng006-g2-oneway-door.md)`, no gap; `[day 7/40 charged]`) — exactly the duplicate-fire shape the `scheduled` pass's own board entry and this ticket's own log predicted in advance, before it happened. Verified rather than assumed before concluding no-op: `inbox/_handled/2026-08-28-eng006-g2-oneway-door.md` carries the processed footer, unedited since the `scheduled` pass; `decision-journal.md`'s `ENG-006` G2 row already records the same decision and timestamp; `ENG-006`'s own ticket log already ends at `ready` with a valid `chained: ENG-006`; `lib/eng-gate-check.sh`, scoped `ENG-006` and whole-board, both exit 0 clean. Also confirmed via `ps` that the pid holding `traces/.loop.lock` (17867, elapsed 40:54) is this same queue-draining orchestrator working through `scheduled → watch → decision` in one lock hold, not a second concurrent instance — same check the 2026-08-27 Ninth-occurrence row used rather than assuming it. No ticket-log or board-index edit made — no new fact for either to carry, and re-chaining here would duplicate the `continue ENG-006` the `scheduled` pass already queued (`traces/.pending` holds exactly `1 continue ENG-006`, unaffected by this pass). |
| 2026-08-28 | eng-manager | aiorders | Deeper cause underneath the open `.watch-seen` proposal (`proposals.md`, 2026-08-26 row) and its self-modifying-pass addendum above: `watch_fingerprint()` (`lib/eng-trigger.sh:366`) hashes one `eng_stamp "$f"` line per matching file, but `eng_stamp()` itself (`lib/eng-env.sh:185`) is `date '+%Y-%m-%dT%H:%M:%S%z'` — it ignores its `$1` argument entirely and returns the current wall-clock time, never the file's path, mtime, or content (contrast `eng_mtime()` immediately above it, which correctly `stat`s `$1`). Verified empirically this pass rather than read alone: called the real function twice, 2s apart, against identical unchanged inbox contents — two different SHA-1s (`cba0b32b...` vs `55b6fbd9...`). So even the open proposal's fix (commit after every event type, not just `watch`) would not close this: any two computations of a genuinely-unchanged inbox more than about a second apart already produce different fingerprints, because the hash is a function of wall-clock call time and file *count*, never file identity. The one case that stays stable is all three inboxes empty (zero `eng_stamp` calls, hash of empty input, `da39a3ee...`) — which is exactly the value an earlier entry above found stamped, before `inbox/` started holding a file continuously from today's 10:42 incident onward. Whoever picks up the open proposal needs to fix `eng_stamp()` (or stop routing `watch_fingerprint()` through it) in addition to widening the commit condition — the commit-timing fix alone would still misfire on nearly every non-empty sweep. |
| 2026-08-28 | eng-manager | aiorders | Second occurrence of the control-center-bypass gap first flagged at `ENG-002` (`proposals.md`, 2026-08-26 row): this `decision (2026-08-28-eng006-merge-request.md)` event found a "control center" dashboard action had already flipped `ENG-006`'s `state:` straight from `blocked` to `shipped` ahead of any build-loop pass, the same mechanism `ENG-002` hit — but this time the tracked gate item also got a written "approved" reply, ~2m28s after the merge, rather than being bypassed outright with no reply at all. That's a new wrinkle worth naming: the proposal's own framing ("a gate item can sit open indefinitely... never moved to `_handled/`, never journaled") assumed the failure mode was an unanswered item; here the item *was* answered, and still sat unprocessed until a pass happened to notice the ticket had already outrun it. So the gap isn't just "answers can arrive outside the tracked channel" — it's that nothing checks an in-flight gate item's named ticket against that ticket's own `state:` regardless of whether or how the item eventually gets answered. Two-for-two now on the proposal's own fix (check every open gate item's ticket against its actual state) being the thing that would have caught this automatically instead of by a pass manually cross-referencing. Not re-proposed — same open row, corroborated. |
| 2026-08-28 | eng-manager | aiorders-api | A `continue ENG-006` event drained at 21:02:49 with no traceable origin in the loop's own mechanism — distinct from every occurrence of the duplicate-queued-event race logged above, which is always two events the loop itself legitimately queued for the same underlying change. This one doesn't fit that shape: `traces/eng-loop-2026-08-28.log` has no `continue — queued as pending` line anywhere after the `ready-to-ship → blocked` pass consumed the last real chain at 14:38:21, and no pass since (`decision` at 20:12:38, `watch` at 20:24:32, `scheduled` at 20:35:43) recorded `chained: ENG-006` — all three correctly logged `chained: none` against a ticket already or newly terminal. The fire lands at 21:02:49, 27 minutes after the `scheduled` pass's own `pass end` line, with the lock free and nothing queued in between — meaning it reached the lock and drained its own freshly-appended line rather than an older one left waiting. That points to a direct, external invocation of `eng-trigger.sh continue ENG-006` (a human or a dashboard action), not a bug in the queue/chain mechanism. A concrete, plausible source surfaced mid-pass: commit `3c3dcd0` ("ENG-006: verify against production," Harsimran, `2026-08-28T21:09:07-07:00`) landed inside this same window — the approver ran `supabase db push` and `supabase functions deploy platform-customer-auth` directly against production and updated the release record accordingly, "outside this department's own L1 workflow." Recorded as circumstantial rather than confirmed — nothing ties the commit to the fire directly, only the overlapping ticket and time window. Not filed as a proposal — the design already allows firing the trigger directly, and nothing here indicates a process fault, only one spent hop and one no-op session. Worth a second look only if it recurs against a ticket that's been terminal a while, since a real pattern there might mean something is re-firing on its own rather than a one-off check-in. |
| 2026-08-28 | eng-manager | aiorders-api | The release record committed in `3c3dcd0` (`agents/devops/releases/2026-08-28-aiorders-api-ENG-006.md`) has its frontmatter and body pulling in different directions: `environment:`/`health_check:` now say the migration and edge function are confirmed deployed to production (direct `supabase db push`/`functions deploy`, CLI-confirmed), but the `## Deploy`, `## Verification`, `## Health note`, and `## Follow-ups` prose is unedited and still says the opposite ("not established that a live Supabase deploy has happened yet," "Health checks: not run"). Not reconciled here — doing so honestly needs either the Supabase access this department has never had, or trusting the frontmatter's claim outright, and every gate on this ticket so far has declined to do the latter. Left for whoever next touches this ticket's records. Same general shape as the earlier "partially-updated, self-contradictory artifact" row above (2026-08-28), this time from an out-of-band human edit rather than a crashed pass. |
| 2026-08-28 | product-manager | aiorders | Second monthly-spend-limit death today, both mid-pass rather than idle: the 08:35 `watch` pass editing `ENG-006`'s PRD Decision section, and a `watch` fire at 21:33 processing this same `ENG-007` intake, right after spawning the blind architect-reading subagent. Both classified correctly by the trigger as real failures (charged, not refunded — each ran well past the 60s never-started threshold despite the vendor-limit signature in the output), and both retried cleanly on the next fire per the two-attempt design. Not filed as a proposal — the retry/classification mechanism is working exactly as `eng_build_loop.md` describes, and two data points in one day is thin. Worth a look if a third shows up: `traces/costs-MacBook-Pro.jsonl` would be the place to check whether spend is concentrated in a few oversized passes (e.g. readback subagents with no model/token cap) rather than evenly spread. |
| 2026-08-28 | product-manager | aiorders | Eleventh occurrence of the open `proposals.md` race, and the second confirmed instance of the `intake`-racing-`watch` path first identified at the Seventh occurrence (2026-08-27) — this time from the opposite side. There, the racing request carried `via: control-center`, so `watch`'s own contract (act only on a change that did *not* come through the control center) correctly told it to defer, leaving the work for the already-correctly-queued `intake` event. Here, `2026-08-28-eng006-sequence-item-2.md` carries `via: eng-006-sequence-continuation`, not control-center, so `watch` correctly did the work directly — and it's the separately-queued `intake` event for the identical file (this pass) that drained second and found nothing left. `traces/eng-loop-2026-08-28.log`: `pass end: watch (exit 0, 878s)` → `queue: collapsed 1 duplicate event(s)` → `draining queued event: intake (2026-08-28-eng006-sequence-item-2.md)`, no gap — and `ENG-007`'s own ticket log (written by that `watch` pass) had already named this exact queued `intake` event and predicted it would no-op, before this pass ever ran. Verified rather than assumed: all three inboxes swept fresh (`agents/product-manager/inbox/` and `agents/eng-manager/inbox/` hold only `.gitkeep`/already-`_handled/` items, both unchanged since before today; `inbox/requests/` empty, the source card already in `inbox/_handled/`; `inbox/`'s other live item, `2026-08-28-eng-events-dropped.md`, is the already-committed, already-tracked incident card, unrelated to this ticket); `inbox/2026-08-28-eng007-g1-scope.md` read directly — `decision:`/`decided:` both still empty, correctly `notified: 2026-08-28T22:15:21`, well under the 24h nudge threshold; `ENG-007`'s ticket log already ends validly (`chained: none`, owner `approver`, at `awaiting-scope`); `decision-journal.md` carries nothing for `ENG-007` yet, correctly, since its G1 is unanswered; `lib/eng-gate-check.sh`, scoped `ENG-007` and whole-board, both exit 0 clean. No ticket-log or board-index edit made — no new fact for either to carry. `traces/.pending` now holds one fresh, unrelated `1 watch launchd` fire — not acted on here. |
| 2026-08-28 | eng-manager | aiorders | First confirmed end-to-end pass on the Windows port (`168cb89`, landed 23:34:44, one minute before this pass's own drain). Two mechanisms confirmed working as designed, both for the first time on this host: the pre-lock quiet-mode pause switch (`eng-trigger.sh` "the pause switch") queued a `watch (schtasks)` fire without launching at 23:33:59 while `MODE=quiet`, rather than dropping it or launching anyway; and once `MODE` cleared, this pass's own `scheduled (manualtest)` fire reached the lock, collapsed 3 duplicate queued event(s), drained, and launched `claude` successfully via `/c/Users/jerryai/AppData/Local/Microsoft/WinGet/Links/claude` — the `$ENG_CLAUDE_BIN`/PATH fallback the same commit added, not a hardcoded macOS path. Three more fires (`watch schtasks` at 23:35:40 and 23:42:05, `scheduled schtasks` once) queued behind this pass while it held the lock — roughly the documented 5-minute watch-poll cadence, not a busy loop; left unactioned for the next pass to drain, per design. Not filed as a proposal — everything observed matched the design intent from the port's own commit message, nothing to fix. |
| 2026-08-28 | eng-manager | aiorders | Second Windows-port confirmation, completing what the row above left open: that pass verified `scheduled (manualtest)` end-to-end but only queued the `watch (schtasks)` fires it saw arrive. This pass **is** one of those — drained at 23:44:24, launched 23:44:38 — and it completed cleanly: all three watched inboxes swept, both live `inbox/` items read fresh and correctly left untouched (neither new), mode check and pre/post gate-check all clean. So the 5-minute Task Scheduler poll path is now confirmed working on both sides — queuing during quiet mode (prior row) and actually draining and finishing a real pass (this one). `traces/.pending` still held one `scheduled schtasks` and one `watch schtasks` fire, queued behind this pass while it ran — left for the next pass, same as before. Not filed as a proposal — nothing observed deviated from design. |
| 2026-08-29 | architect | aiorders-api | Third Windows-port first: the first pass needing an actual project git worktree, not just the loop's own repo. `config/projects.md` claims "all five worktrees already exist," true only for the 2026-08-23 Mac verification — `$ENG_WORKTREES` (`C:/Users/jerryai/Documents/_eng`, per `lib/eng-env.sh`) existed as a bare directory on this host but held none of the five. Created `aiorders-api`'s with the same commands `lib/eng-setup.sh` runs (not the full script — only this one project was needed). The other four registered projects will hit the same gap the first time a ticket needs to design or build against them; worth running `lib/eng-setup.sh --apply` for real before that happens rather than recreating worktrees one ticket at a time, though not urgent enough to block on. A short host-specific note was added to `projects.md` rather than rewriting its table. |
| 2026-08-29 | architect | aiorders-api | `ENG-006`'s own design doc (2026-08-28) states this repo has "no schema in version control at all" — that was accurate when written and is now stale. Commit `5b3bac2` ("Consolidate remaining migrations from aiorders-admin-hub"), landed one commit before `ENG-006`'s own, added a real `supabase/migrations/` history (21 files). Whoever next designs against this repo can and should read real migrations rather than reverse-engineering schema from edge-function code, the way `ENG-006` had to. |
| 2026-08-29 | architect | aiorders-api | While designing `ENG-007`, found a live, documented, actively-maintained third-party loyalty integration (`external-integrations/handlers/walletly.ts`, proxies to `api.walletly.ai` for point balances and brand rewards) that neither the approver's original loyalty request nor `knowledge/business-profile.md` mentions anywhere. Escalated via a new G2 (`inbox/2026-08-29-eng007-g2-walletly-conflict.md`) rather than decided or silently carried forward, since the real exposure is ticket 3 (the points ledger) building a second competing points system, not this ticket. Cross-referencing here for whoever files ticket 3 or 4 next, in case the G2 is still unanswered by then. |
| 2026-08-29 | eng-manager | aiorders | This pass's own live `lib/eng-notify.sh raise` call (for `ENG-007`'s new G2) logged a plainer failure than earlier entries: `SLACK_WEBHOOK_URL unset — cannot notify`, not the `sent: active` mislabeling quoted in `ENG-006`'s own G1/G2 log entries. Same open bug (`proposals.md`, 2026-08-25 row, bug 1 — no channel dispatch, posts unconditionally to Slack regardless of `config.yaml`'s `approver.notify: telegram`), not a new one — just the plain-and-simple face of it when the Slack webhook happens to be unset entirely, versus whatever state produced the mislabeled-but-attempted send seen before. Consistent with, not new evidence for, the existing proposal. |
| 2026-08-29 | eng-manager | aiorders | Twelfth occurrence of the open `proposals.md` race (filed 2026-08-27, interactively — not by a department pass — proposing `eng-trigger.sh` skip the launch when a `decision` event's named gate item is already in `_handled/`; two earlier rows here, Ninth and Eleventh, cited this as the "2026-08-25 row" — checked `proposals.md` directly this time rather than trusting those citations, and that date is wrong, it's the 2026-08-27 row filed after `ENG-003`'s hand-synced close-out). This `decision (2026-08-28-eng007-g1-scope.md)` event drained immediately behind the `watch (schtasks)` pass that had already fully processed the same hand-edited G1 (`traces/eng-loop-2026-08-29.log`: `pass end: watch (exit 0, 1357s)` → `queue: collapsed 4 duplicate event(s)` → `draining queued event: decision (2026-08-28-eng007-g1-scope.md)`, no gap; `[day 3/40 charged]`). Verified rather than assumed before concluding no-op: `inbox/_handled/2026-08-28-eng007-g1-scope.md` carries the processed footer, unedited since the `watch` pass; `decision-journal.md`'s last row already records this exact decision and timestamp; `ENG-007`'s own ticket log already ends at `awaiting-decision` with a valid `chained: none` (owner `approver`); its new G2 (`inbox/2026-08-29-eng007-g2-walletly-conflict.md`) re-read fresh this pass and still genuinely unanswered — `decision:`/`decided:` both empty; `lib/eng-gate-check.sh`, scoped `ENG-007` and whole-board, both exit 0 clean. Also confirmed via the lock file and `ps` that `traces/.loop.lock/pid` (`1067`) is the same trigger process that ran the `watch` pass, now showing argv `decision 2026-08-28-eng007-g1-scope.md` — it re-execs itself per drained queue item rather than spawning a fresh one, so this is one held lock working through `watch → decision`, not a second concurrent instance (sharper confirmation than prior rows' generic same-pid check). No ticket-log or board-index edit made — no new fact for either to carry. `traces/.pending` now holds two collapsible `1 watch schtasks` lines behind this pass — the 5-minute poll firing again while this pass ran; not acted on here. |
| 2026-08-29 | eng-manager | aiorders | A `watch (schtasks)` pass processing `ENG-007`'s G2 (Walletly answered, ticket advanced to `ready`) found its own initial three-inbox sweep had gone stale mid-pass: five new files landed in `agents/product-manager/inbox/` between that sweep and this note, all `source: approver`, `via: control-center`, received within an 8-minute window (08:14–08:22) — a batch filing, not five independent events. Content: admin-panel influencer-board visibility/editing, admin-panel client-stage/health filtering, FoodSwipe sales-funnel pipeline stages, brand-portal QR/media/site-timing self-service, and admin-portal readiness for agency/reseller users. Read all five to confirm none is P0 (none is — all are real but non-emergency functionality gaps) before leaving them untouched for their own dedicated `intake` events rather than shaping them under this `watch` pass's narrower contract. Worth watching for two things: whether `agents/product-manager/inbox/` reads as clean at pass-start should be re-verified immediately before a pass exits, not just at the top of the sweep, on any pass that runs long enough for new filings to land mid-flight; and that five requests arriving at once will contend hard for the approver-facing WIP cap of 2 once their `intake` events start shaping them into G1s — likely worth the EM watching this specific batch through the cap over the next few passes rather than assuming they will clear it as easily as ENG-007's single-ticket sequence has so far. |
| 2026-08-29 | eng-manager | aiorders-admin-hub | Re-read the same five-item batch fresh this pass (nothing yet processed — all still sitting unshaped, their dedicated `intake` events still queued in `traces/.pending`) and want to sharpen, not overturn, the prior row's "all non-emergency functionality gaps" read: `2026-08-29-admin-portal-is-not-optimized-for-new-agency-users-resellers.md` is the approver's own words, verbatim — "security issue if they go to /restaurant they can see all locations," "they dashboard data is super admin data not just their brands," and "same for other tabs like influencer they are able to see all or user." That's a cross-tenant authorization boundary gap on `aiorders-admin-hub` (registered, **L1**, real deploy target — `config/projects.md`), not a UI polish item, and reads as materially more severe than the other four in this same batch. Still correctly not a P0-immediate-ticket case — the approver already knows (they reported it), so there's no notification gap to close, and business intake carries no proposal-batching gate to bypass in the first place — but worth whoever's `intake` pass shapes this one giving it real severity (not routine `P3`) rather than batch-processing all five identically by arrival order. Not escalated further here: forcing full PM readback/G1 into this `watch` pass would duplicate the already-queued dedicated `intake` event for this exact file and cut the same corner the last two passes deliberately declined to cut. |
| 2026-08-29 | product-manager | aiorders-api | Shaping `ENG-008` (influencer board admin management) needed to know whether an influencer-facing concept exists at all before sizing it — the architect's blind reading (per `skills/request-readback/SKILL.md`) correctly refused to assume it did, naming a possible net-new auth/identity class as the request's biggest one-way door. Checked `aiorders-api`'s `origin/main` directly rather than asking the approver a question the repo could answer: it already has `supabase/functions/restaurant-influencer-campaigns/` (with an `influencer-invitations.ts` handler and its own `utils/auth.ts`), `supabase/functions/outgoing-communications/actors/influencers.ts`, and `supabase/functions/migrate-influencer-images/` — a real, live influencer/campaign concept, not net-new. Worth flagging for whoever designs `ENG-008`'s item 2 (influencer-facing opportunity visibility + apply gating, not yet filed): the existing surface reads as invitation-based (staff/restaurant invites a specific influencer to a specific campaign), which is a different access pattern from "influencer browses all opportunities and applies," so item 2 may be a new access pattern layered on top rather than a small edit to what's there. Not investigated further than filenames — deeper reading is a design-stage task, out of scope for this `intake` pass. |
| 2026-08-29 | eng-manager | aiorders | Thirteenth occurrence of the open `proposals.md` race (2026-08-27 row, filed by hand — `eng-trigger.sh` should skip the launch when a `decision` event's named gate item is already in `_handled/`) — the mirror image of the Twelfth occurrence two rows up, this time on the G2 rather than the G1. This `decision (2026-08-29-eng007-g2-walletly-conflict.md)` event drained at 01:43:01 behind two intervening passes that had already made it a no-op: an earlier `watch (schtasks)` pass actually answered this G2 (Walletly retiring, `ENG-007` advanced `awaiting-decision → ready`, `chained: ENG-007` fired), and a second, unrelated `watch (schtasks)` pass ran after it and correctly left `ENG-007` untouched while resolving the separate events-dropped incident (`traces/eng-loop-2026-08-29.log`: `pass end: watch (exit 0, 1020s)` at 01:42:47 → `queue: collapsed 2 duplicate event(s)` → `draining queued event: decision (2026-08-29-eng007-g2-walletly-conflict.md)`, both 01:43:01; `[day 6/40 charged]`). Verified rather than assumed before concluding no-op: `inbox/_handled/2026-08-29-eng007-g2-walletly-conflict.md` carries the processed footer, unedited since the `watch` pass that answered it; `decision-journal.md` row 21 already records this exact decision and timestamp; `ENG-007`'s own ticket log already ends at `ready` with a valid `chained: ENG-007`, and `traces/.pending` confirms that chain is genuinely still queued unconsumed — re-chaining here would duplicate it, not fill a gap; `lib/eng-gate-check.sh`, scoped `ENG-007` and whole-board, both exit 0 clean. Also confirmed via `ps` that `traces/.loop.lock/pid` (`1067`, a bash process started 00:38:52) is the same trigger orchestrator working through the queue, and the `claude` process running this pass (pid `15229`, started 01:43:16) is the only one live — not a second concurrent instance. No ticket-log or board-index edit made — no new fact for either to carry. `traces/.pending` still holds `1 decision 2026-08-28-eng-events-dropped.md`, an incident already resolved by the intervening `watch` pass named above — likely a fourteenth occurrence about to land the same way; not acted on here, out of scope for a `decision` event naming `ENG-007`. |
| 2026-08-29 | product-manager | aiorders-admin-hub | Shaping `ENG-011` (client stage/health visibility on the Brands page) found the Brands page (`src/pages/Brands.tsx`) already carries `onboarding_step` (number, written by a real onboarding wizard) and `is_active` (boolean, set at brand creation) on its `Brand`/`Restaurant` interfaces — neither surfaced or labeled on the page today, and neither a stage-name mapping nor a "client" flag exists anywhere. Searched both `aiorders-api` and `aiorders-admin-hub` for `stage`, `lifecycle`, `health`, `last_order`, `churn`, `ticket`, `support` — no health signal and no ticket/support table or concept exists anywhere in either repo. Grounded the PRD's proposed stage taxonomy and health signal in this evidence rather than inventing one, and turned "does a ticket system exist to extend" from a guess into a confirmed no, which is most of why "tickets" became a standing question instead of a third guess. |
| 2026-08-29 | product-manager | aiorders | While verifying approval-cap/approver-WIP numbers fresh before raising `ENG-011`'s G1 (per this instance's own "verify rather than assume" practice), found `inbox/2026-08-29-eng009-g1-scope.md` and `inbox/2026-08-29-eng010-g1-scope.md` both carrying `decision: approved` (09:20:42 and 10:49:55) but still sitting in `inbox/`, not `_handled/` — their tickets (`ENG-009`, `ENG-010`) still read `state: awaiting-scope`, `owner: approver` on disk, and `board/_index.md`'s cached cap numbers predate both. Same family as the open `proposals.md` race (2026-08-27 row, now well past a dozen occurrences), a new pair though — these were never raced by a second queued event finding nothing to do; they simply have no pass yet that consumed the answer at all. Left untouched (dead-end-sweep/decision-event work, out of scope for an `intake` event on an unrelated ticket) but treated as closed rather than open for this pass's own cap arithmetic, consistent with how this board's own index has always treated an answered gate item ("off the list" the moment it's answered, e.g. `ENG-007`'s G2) rather than waiting on the mechanical `state:` field to catch up. Worth a dead-end sweep picking these two up soon — `ENG-009` in particular has sat answered for roughly 1h45m as of this pass. |
| 2026-08-29 | eng-manager | aiorders | While rolling `board/_index.md`'s dated entries per the keep-three rule (already one over the limit before this pass's own new entry), found the oldest of the four existing entries already sitting, verbatim, at the top of `_index-archive.md` — a genuine duplicate, confirmed by direct text comparison before touching anything rather than assumed from the matching headings. Reads as an earlier roll that copied the entry into the archive but crashed or was interrupted before removing it from the live file. Same general failure family as the single already-documented "partially-updated, self-contradictory artifact" occurrence (this file, 2026-08-28 — a pass crashing mid-sequence between writes on a PRD's `## Decision` section), though a different specific artifact (a board-index roll, not a PRD edit) — worth treating as a second, corroborating data point for that same underlying risk class (a multi-file write left half-done by a crash) rather than a new, unrelated bug. Fixed here: archived the next-oldest entry for real, dropped the stale duplicate from the live file instead of archiving it a second time. |
| 2026-08-29 | architect | aiorders-api | Designing `ENG-011`'s health signal, checked whether cheap internal order data actually exists before trusting the PRD's "no new cost" claim — `get-online-orders` proxies live to a third-party vendor (CloudWaitress, `cw_api_key`/`cw_restaurant_id`) for some paths, which could have meant no internal order history to derive health from at all. Found the opposite: `20260217000001_platform_analytics_cron.sql` defines a real internal `orders` table and an hourly cron (`calculate_platform_analytics()`) already aggregating `total_orders`/`total_order_value` per restaurant into Cloudflare KV, confirmed unconsumed by any admin-hub UI today (only referenced in a generated Supabase types file). Worth flagging for whoever next needs restaurant-level activity data on this instance: this pipeline already exists and is the cheaper source, before building a new one. |
| 2026-08-29 | product-manager | aiorders | `ENG-011`'s standing "tickets" question came back with `decision: rejected` in the frontmatter but a free-text answer ("Reading A") naming one of the two options the question itself offered — the two signals point opposite ways taken literally. Read as a selection, not a rejection (see `decision-journal.md` and the gate item's own processed footer for the full reasoning), on the theory that this item's template never offered an approve/reject choice to begin with, so `rejected` may just be this instance's reply channel's default for a free-text answer to a non-gate item. First occurrence — worth asking the approver directly, rather than continuing to infer, if a second gate or standing question shows the same `decision:`-vs-free-text mismatch. |
| 2026-08-29 | product-manager | aiorders-admin-hub | Shaping `ENG-013` (Foodswipe funnel stage control) found the page (`FoodswipeListings.tsx`) and its backend handler (`admin-portal/handlers/foodswipe.ts`) have zero write path of any kind — no click/edit affordance, no mutation branch, `classifyStage()` a pure read-only function over existing columns. Confirmed no staff-assignable status concept (`assigned_to`, `lead_status`, `crm_stage`, `sales_stage`, `contact_status`, `owner_id`) exists anywhere in `aiorders-api`. One field looked like a candidate — `claim_status`, fetched by this same handler — but is dead: written once to a constant (`'pending'`), read nowhere. Grounded the PRD's proposed defaults in this evidence rather than guessing, same practice `ENG-011` established for "tickets." |
| 2026-08-29 | product-manager | aiorders | "Foodswipe" names two unrelated concepts across this instance's current work: `ENG-013`'s restaurant self-signup funnel (`profiles.source = 'foodswipe'`, an admin-panel kanban) and `ENG-006`'s already-shipped cross-restaurant *consumer* loyalty identity (a "foodswipe customer" identified by phone/OTP across restaurants). Same brand name, no technical relationship — worth flagging so a future request or ticket naming "foodswipe" isn't assumed to mean the other one. |
| 2026-08-29 | architect | aiorders-admin-hub | `ENG-013`'s blind architect reading guessed, unprompted and before seeing any code, that "sales" and "onboarding" staff might need two different stage vocabularies covering two different phases (pre-sale and post-sale). Checked against `FoodswipeListings.tsx`/`foodswipe.ts` and found it true: all six existing stages are post-signup — nothing anywhere in this system tracks a restaurant before it signs up via Foodswipe. Split out as a standing, non-blocking question (`inbox/2026-08-29-eng013-presignup-leads-question.md`) rather than assumed either way. Worth noting as a data point that this instance's blind second-reading step is catching real, code-confirmable gaps, not just noise. |
| 2026-08-29 | architect | aiorders-api | Designing `ENG-013`'s stage-override column, searched every migration under `supabase/migrations/` for `profiles` before proposing to alter it: six hits, all RLS policies or later references, none a `CREATE TABLE` — `profiles`' own base table isn't in this repo's tracked migration history at all. Second, corroborating occurrence of the 2026-08-26 finding for this same repo (`ENG-004`'s design: "no schema in version control at all," later found stale once `5b3bac2` added 21 migration files — evidently that consolidation still didn't capture `profiles`' own origin). Not blocking (an `ALTER TABLE` doesn't need the original `CREATE TABLE`), but worth a look if a future ticket needs `profiles`' full column list or constraints — reading the live handler code remains the only way to get it, not the migration history. |
| 2026-08-29 | product-manager | aiorders-api | Shaping `ENG-014`, read `url-shortener/index.ts` directly rather than trusting the frontend's admin-only gate: `verifyAdminAccess` checks `profile.role === 'admin'` exactly, stricter than the four-role `adminRoles` list the `brand-portal` function itself uses elsewhere. Any future ticket that needs this function reachable by a broader admin set (sub-admin, partner-admin) will hit the same wall `ENG-014` did. QR images themselves come from a free public API (`api.qrserver.com`, no key, no published pricing) — worth knowing before anyone assumes a paid QR vendor needs onboarding. |
| 2026-08-29 | product-manager | aiorders | The answered-but-unprocessed inbox backlog first noted 2026-08-29 (`ENG-009`/`ENG-010`) is now four items deep (`ENG-012`'s G1, rejected; `ENG-013`'s standing question, approved, both newly found answered this pass) and four consecutive `intake` passes old without a `decision` event or dead-end sweep picking any of them up. Each individual pass has correctly left them alone (out of scope for an unrelated `intake` event) and correctly re-verified them fresh rather than trusting the cached header, so nothing here is a misread — but the backlog itself isn't self-clearing, and `SLACK_WEBHOOK_URL` being unset on every raise this whole day (see `traces/eng-notify-2026-08-29.log`) is a plausible reason `watch`'s poll isn't the approver's usual path to answering these. Flagged as a pattern worth a dead-end sweep or a `decision` event, not (yet) a proposal — the underlying cause is still a guess, not confirmed. |
| 2026-08-29 | eng-manager | aiorders-api | This `continue ENG-007` pass found the ticket's own board file still reading `ready` while four complete, dated, mutually-consistent gate receipts (migration, code review, QA, security — all referencing commit `2aec86f` on `loyalty-system`) already sat on disk with pass verdicts. Unlike the two prior partially-updated-artifact occurrences (a PRD `## Decision` section, a board-index roll — both 2026-08-28/29 rows above), here the entire downstream chain (build through security) was finished and self-consistent; only the ticket's own state/log bookkeeping lagged. Third data point for the same underlying risk class (a multi-step write left half-done by a crash), and the furthest-progressed one yet — worth a proposal if a fourth occurrence lands, since the pattern now spans three different artifact types. |
| 2026-08-29 | eng-manager | aiorders-api | `deno` is now genuinely installed and working on this Windows host (`2.9.6`, at `/c/Users/jerryai/AppData/Roaming/npm/deno`, installed via `npm install -g deno`) — the database migration doc's own in-progress fallback attempt (Docker unavailable, `npm install -g deno` tried "as a fallback real-verification path") resolved successfully sometime after it was written. Confirmed by running `deno test`/`check`/`lint` for real against `ENG-007`'s handler this pass, 44/44 passing. Future passes on this host can reach for `deno` directly rather than trying Docker first. |
| 2026-08-29 | eng-manager | aiorders-api | Docker Desktop still does not bring its daemon up within a bounded wait on this Windows host (`docker info`, 8s timeout, no response) — second occurrence after the one the `ENG-007` database migration doc already named. Not yet a proposal (two data points, same host, same session lineage), but worth one if a future pass needs live-Postgres verification and hits the same wall a third time. |
| 2026-08-29 | eng-manager | aiorders | This `continue ENG-007` pass's own trace-log tail showed a `continue (ENG-007)` fire queued as "pass in flight" around the time this session was already running, which read at first like a second, concurrent writer on the same ticket. Checked rather than assumed: `traces/.loop.lock/pid` (`26107`) came back "no tasks found" under a plain `tasklist //FI "PID eq 26107"` — but `traces/.pass-out.26107` was mirroring this very session's own narration in real time, proving the lock's recorded PID *is* this session's own launching wrapper, not an orphan. `tasklist`'s native-PID lookup simply doesn't resolve an MSYS/Git-Bash-space PID the way it would a real Windows one. Not confirmed either way whether `lib/eng-trigger.sh`'s own stale-lock liveness check (the one that logged `lock is 6907s old but PID 1782 is alive — not stealing` earlier in today's trace) uses a method vulnerable to the same mismatch, or something MSYS-aware that doesn't have this problem — worth someone actually reading that check's implementation, since a false "not alive" there would let a second pass steal a live lock and write concurrently with a still-running one, which is exactly the corruption the lock exists to prevent. |
| 2026-08-29 | product-manager | aiorders-api | Shaping `ENG-015` (agency/reseller brand scoping) found a precise, single-file root cause for the confirmed data leak: `admin-portal/handlers/restaurants.ts`'s `getRestaurants()` always queries with the service-role client (`adminSupabase`), unconditionally — no role check of any kind, unlike its sibling `brands.ts` handler, which already branches (service-role only for `role === 'admin'`, the caller's own RLS-scoped client otherwise). The partner role clearly propagated to `brands.ts` and to the top-level `admin-portal` auth gate, but never to `restaurants.ts`. Worth checking any other `admin-portal` handler that follows the same `adminSupabase`-by-default pattern before assuming `brands.ts`'s role-branch is the norm rather than the exception. |
| 2026-08-29 | product-manager | aiorders-admin-hub | Same `ENG-015` shaping: three of the four symptoms named in the approver's raw request (Dashboard, Influencers, and — reading "or user" as `/users` — that page too) turned out, checked directly against `ProtectedRoute.tsx`/`Dashboard.tsx`/`AppSidebar.tsx`/`users.ts`, to already be fully blocked for `partner-admin`/`partner-user` today — not leaking, not scoped, just outright denied. Only the Restaurants page (read) and the restaurant-creation write path were confirmed still open. Reads as a page-by-page pattern: whenever partner access to a page became a problem, the fix applied so far has been "deny that role entirely" rather than "scope it to their own brand" — cheaper in the moment, but it's why the approver now has agency/reseller users who can log in and find most of the portal simply refuses them, `/restaurants` still leaks everyone's data, and they can't add their own locations. Worth naming as a pattern if a future ticket touches partner access again: the fix this instance actually wants is scoping, not another outright block. |
| 2026-08-29 | product-manager | aiorders-admin-hub | Corrected my own mid-investigation misread while shaping `ENG-015`, logged rather than silently fixed: read `AppSidebar.tsx`'s `item.url === '/influencers' && (hasRole('partner-admin') \|\| hasRole('partner-user'))` conditional in isolation first and took it to mean partner roles could reach `/influencers`. Reading the branch's actual body (`return null`) and then `ProtectedRoute.tsx` in full showed the opposite — both hide and deny it. This instance's own practice of verifying a claim against the filesystem before building on it applies just as much to a PM's own first-pass inference mid-investigation as to a prior pass's artifact or a stale board index; noting the near-miss since it would have shaped a materially wrong PRD (fixing a page that isn't broken) if the second file hadn't been read before writing anything. |
| 2026-08-29 | eng-manager | aiorders | This `decision` event (context `2026-08-28-eng-events-dropped.md`) drained from `traces/.pending` after a `watch`/`schtasks` pass had already fully processed the same file — investigated, proposal filed, journaled. Confirmed fresh rather than trusted (re-read the file end to end: no content past its "Processed" footer); no-op, mirror image of `ENG-007`'s G2 case where the dedicated `decision` event drained first and a trailing `watch` found nothing left. While confirming, checked `traces/.pending` directly: the answered-but-unprocessed backlog the board header has flagged for five consecutive passes (`ENG-009`/`ENG-010`/`ENG-012`'s G1s, `ENG-013`'s presignup question, among others) already has its own `decision` event individually queued — not missing or dropped, each just waiting behind roughly a dozen other queued events (mostly `intake`) for its own turn to drain. Some of those queued lines (e.g. `ENG-008`, `ENG-011`, `ENG-013`'s G1) will likely find themselves already resolved too, same shape as this one, since other event types have been reading `inbox/` fresh and processing them first. |
| 2026-08-29 | eng-manager | aiorders | This `watch`/`schtasks` pass (the next drain after the row above) swept all three top-level watched inboxes against `watch_fingerprint()`'s own scope (maxdepth 1, no dotfiles) and found every file already accounted for: the 7 unshaped `agents/product-manager/inbox/` requests each already carry their own queued `intake` event in `traces/.pending`; `inbox/`'s four answered-but-unprocessed items (`ENG-009`/`ENG-010`/`ENG-012`'s G1s, `ENG-013`'s presignup question) each already carry their own queued `decision` event; and the two newest `inbox/` files (`ENG-014`'s and `ENG-015`'s G1s) are open gates already raised and notified by their own intake passes, genuinely awaiting the approver rather than missed. The fingerprint change that fired this event is fully explained by that same churn (files arriving/leaving the top level as those other passes ran), not by anything new. Confirms the prediction in the row directly above. No file unaccounted for, no ticket touched, no board change, nothing to chain — `agents/eng-manager/inbox/` is empty. |
| 2026-08-29 | product-manager | aiorders-api | Shaping `ENG-016` (catering quote generator) found the existing `catering-request` function already creates a CRM customer record and notifies the **restaurant owner** on every submission, but sends the **customer** nothing beyond the form's own static "we'll contact you" success text — confirmed by reading the function end to end, not inferred from the form alone. Worth remembering for any future catering-adjacent ticket: the owner-side notification path is solid and already in production; the customer-side gap is total, not partial. |
| 2026-08-29 | product-manager | aiorders-api | Same `ENG-016` shaping: `aiorders-api`'s `autopilot`/`outgoing-communications` system (DB-trigger-initiated, Cloudflare-Queue-backed, per-restaurant `communication_templates` with email/SMS toggles, a `communication_log`) is real, shipped, and already sends customer-facing email/SMS today — but only for customer-lifecycle events (welcome/first-order/every-order/birthday/winback). Nothing catering-related touches it yet. Worth flagging for whoever next needs to send a restaurant-customer a templated message for any reason: this engine exists and is the cheaper path before building a new send mechanism. |
| 2026-08-29 | product-manager | aiorders-portal | Same `ENG-016` shaping: `restaurant-portal`'s catering kanban (`CateringKanban.tsx`, `StatusUpdateModal.tsx`, `CateringDetailModal.tsx`) hardcodes the same five status strings independently in three files, with no server-side enum behind `catering.status` — confirmed by reading all three plus the `brand-portal/catering.ts` backend handler. Adding a new status value is a frontend-only, additive change, not a migration; worth knowing before any future ticket touching this pipeline assumes otherwise. |
| 2026-08-29 | product-manager | aiorders | The answered-but-unprocessed inbox backlog first noted 2026-08-25 (`ENG-009`/`ENG-010`) is unchanged again this pass — `ENG-009`, `ENG-010`, `ENG-012`'s G1s and `ENG-013`'s standing question all still sit in `inbox/` rather than `_handled/`, now six consecutive `intake`/`watch` passes deep without a dead-end sweep or `decision` event clearing them. Correctly left alone again (out of scope for this `intake` event, itself about an unrelated ticket) and correctly re-verified fresh rather than trusted — but the pattern itself is now old enough that a scheduled or dead-end pass finding it a seventh time without acting would be worth a proposal rather than another re-verification. |
| 2026-08-29 | product-manager | aiorders | Shaping `ENG-017` found this new inbox request ("no autopilot on admin panel for our sales staff/resellers... autopilot nurturing... based on stages update") substantially restates `ENG-013`'s own presignup-leads standing question (`inbox/2026-08-29-eng013-presignup-leads-question.md`), already `decision: approved` with explicit verbatim direction ("autopilot built to nurture these leads to next stages autpmatically and send them emails/sms to nurture") but still unprocessed, part of the backlog in the row above. Not touched or closed by this pass — that item belongs to `ENG-013`'s own lifecycle — but used as confirming evidence for `ENG-017`'s core mechanism. **Recommend whichever `decision`/dead-end-sweep pass next reaches that standing question point it at `ENG-017` instead of shaping a second, duplicate ticket from it.** |
| 2026-08-29 | product-manager | aiorders-api | Same `ENG-017` shaping: confirmed the `leads` table (website "become a client" form submissions) has no stage/status column and no consent flag of any kind — `aiorders-website/index.ts`'s `saveLead` captures only contact/business fields, unlike the catering-request flow, which explicitly sets `consent_sms`/`consent_email: true`. Also confirmed the existing `autopilot`/`outgoing-communications` engine's `communication_templates`/`trigger_type` model is hard-scoped to `restaurant_id` and a closed set of customer-lifecycle triggers — a presignup lead (no restaurant, not a customer) can't be enrolled through it as-is, so a lead-nurture engine reuses the underlying send *services* but needs a parallel trigger/template *data model*, not a drop-in extension. The `outgoing-communications` router's `actor: 'admin'` path exists in name but all three of its handlers are unimplemented `TODO` stubs — not working prior art. Worth remembering for any future ticket that assumes "autopilot" is a single, generically reusable system: the send layer is generic, the trigger/enrollment layer is not. |
| 2026-08-29 | product-manager | aiorders-admin-hub | Shaping `ENG-018` (sales demonstration account) searched all five repos for any existing demo/sandbox concept before proposing a net-new one — the only hit, `config-site-builder/public/config/demo-restaurant.json`, is a static SEO/config fixture for the site-generation pipeline, not a loggable-into account. No `is_demo` flag, no seed script, no isolation of outbound sends or analytics exists anywhere. Combined with `ENG-011`'s prior finding of a live `platform_analytics_cron` aggregating orders/revenue platform-wide per restaurant, this is why `ENG-018`'s PRD treats excluding demo activity from real sends and real analytics as acceptance criteria rather than an implementation nicety — a demo that leaks into either would be worse than no demo. |
| 2026-08-29 | product-manager | aiorders-portal | Shaping `ENG-019` (brand-portal marketing broadcasts) found "campaign" already names a real, shipped, unrelated feature on this exact portal: `pages/campaigns/*`/`services/campaignService.ts` is entirely about inviting influencers to visit and post (`influencer_campaigns` table, `restaurant-influencer-campaigns` edge function) — not customer messaging. Same shape as the earlier-noted "Foodswipe" collision (2026-08-29 row, above): one word naming two unrelated concepts on this codebase. `ENG-019`'s PRD proposes a different label ("Broadcasts") for the new capability specifically to avoid it — worth checking for a third occurrence before assuming any noun on this codebase means only one thing. |
| 2026-08-29 | product-manager | aiorders-portal | Same `ENG-019` shaping: the brand portal's `Automations` page (`pages/autopilot/Automations.tsx`) is real, restaurant-facing self-service already — stats, an offer-based flow list, custom templates, a send-history tab — but every trigger type it supports (`TriggerType` in `types/autopilot.ts`: catering lead, feedback, welcome, order completed, abandoned cart, birthday) is reactive, fired by a customer-lifecycle event. Confirmed no manual, scheduled, mass, or multi-step-drip concept exists anywhere in that data model or its backend (`outgoing-communications` actors). Worth remembering for any future ticket that assumes "autopilot" already covers owner-initiated sending: the reactive engine and an owner-initiated one are different mechanisms that happen to share a send layer, same relationship `ENG-017` already found between lead-nurture and the customer-lifecycle engine. |
| 2026-08-29 | product-manager | aiorders-api | Same `ENG-019` shaping: `outgoing-communications/actors/brands.ts`'s `sendPerformanceReport`/`sendMonthlySummary` actions are real routed actions, called from a real `processScheduledReports` batch path, but both bodies are unimplemented — literally `// TODO: Implement ... email template and logic`, returning `notificationsSent: 0` unconditionally. The platform already intended owner-facing performance reporting once and never finished it. Worth checking before any future ticket assumes either function sends anything today, and worth a look as a possible reuse target (or cautionary precedent) for `ENG-019`'s own proposed campaign-reporting UI. |
| 2026-08-29 | product-manager | aiorders-api | Shaping `ENG-020` (AI SEO / marketing ROI tracking) found real, wired customer-attribution data already being captured and going nowhere: five separate entry points (`website-submissions/customer-signup.ts`, `email-signup.ts`, `update-customer-tracking.ts`, `catering-request/index.ts`, `crm/customers.ts`) all write `utm_source`/`utm_medium`/`utm_campaign`/`first_touch_source`/`last_touch_source`/`first_referrer` onto the `customers` row, and `autopilot/marketing/welcome.ts` already reads `first_touch_source` to branch its own send logic — but nothing in `restaurant-portal` (the brand portal an owner logs into) ever reads those columns back out. A request that reads as "we have no tracking" is, on this codebase, "we have tracking with no report" — worth remembering before any future ticket in this area assumes the gap is data capture rather than surfacing. |
| 2026-08-29 | product-manager | aiorders-portal | Same `ENG-020` shaping: `restaurant-portal/src/pages/analytics/Index.tsx` — the page literally named "Analytics" in the brand portal's nav — is entirely mock/hardcoded data about influencer-campaign performance (views, applications, saves, Instagram/TikTok/YouTube split), not connected to any live query. An owner has no way to know this from the UI; it reads as real. Distinct from `ENG-019`'s already-noted "Campaigns" naming collision (same portal, different page) — worth a look in its own right, and worth checking before any future ticket assumes "Analytics" already shows something real. |
| 2026-08-29 | product-manager | aiorders | Fourth occurrence this session of the per-host worktree registry gap (`config/projects.md`'s "all five worktrees already exist" is a stale, Mac-only verification from 2026-08-23) — this time for `config-site-builder`, needed to shape `ENG-020`. Created it the same way the prior three occurrences did (`git worktree add -b eng/base`, one project rather than the full `lib/eng-setup.sh --apply`). Now hit for `aiorders-api`, `restaurant-portal`, and `config-site-builder` on this Windows host; only `aiorders-admin-hub` and `restaurant-marketplace` remain unconfirmed here. Worth running the full setup script for real rather than continuing to absorb this one ticket at a time, though still not urgent enough to block on. |
| 2026-08-29 | eng-manager | aiorders | The answered-but-unprocessed inbox backlog tracked across at least six prior rows above (`ENG-009`/`ENG-010`/`ENG-012`'s G1s, `ENG-013`'s presignup question) is cleared as of this `scheduled` pass: `ENG-009`/`ENG-010` advanced through design to `ready`, `ENG-012` dropped, `ENG-013`'s question closed against the already-filed `ENG-017` rather than duplicated. Confirms this thread's own repeated recommendation (a dead-end sweep or `decision` event would clear it) rather than needing a further proposal. |
| 2026-08-29 | architect | aiorders-admin-hub | Two unverified cross-references caught designing `ENG-009`/`ENG-010` in this same pass: `ENG-008`'s design doc claimed a premise-correction note had been left on `ENG-009`'s own ticket file (it hadn't — checked directly, nothing there); `ENG-010`'s ticket claimed its central risk rested on "`user_id` on the influencer record, per `ENG-008`'s own design evidence" (`ENG-008`'s design never mentions `user_id`). Both underlying claims turned out substantively true once checked against live code by a different route (engagement/followers really are already displayed; influencers really do authenticate, via `profiles.role='influencer'`, not a column on `influencers`) — the citations were wrong, not the conclusions, which is the more dangerous shape since the error only surfaces if someone actually reopens the source. Worth naming as its own pattern, distinct from the three-times-named crash-recovery gap: treat "per X's own evidence" as a prompt to open X, not a reason to skip it. |
| 2026-08-29 | architect | aiorders-api | Designing `ENG-010`, found that reusing `admin-portal`'s shared router-level gate (`authenticate()`, `admin-portal/index.ts` line 94) unmodified would have been wrong for this specific resource: its allowlist (`admin`, `sub-admin`, `partner-admin`, `partner-user`) grants every route under it to agency/reseller roles too, not just internal staff — fine for existing routes, but `ENG-010`'s own G1 already defaulted brand/agency/reseller visibility on influencer notes to no. Added a narrower in-handler role check rather than silently relying on a gate that doesn't match the PRD's own stated default. **Not re-checked, and worth someone doing so:** `ENG-008` and `ENG-009` reuse this same blanket gate for influencer preference/rating/collaboration/engagement data, and neither PRD states whether partner-admin/partner-user access to that data is intended or an oversight — `ENG-015`, elsewhere on this board, already found this exact role gaining unintended access on other pages by the same "shared gate, nobody checked" mechanism. Not fixed here (would be scope creep on two already-approved G1s); flagged for whoever builds or reviews `ENG-008`/`ENG-009`. |
| 2026-08-29 | eng-manager | aiorders | Dead-end sweep found `ENG-008` chained (`chained: ENG-008`, trigger fired, logged) but no `continue (ENG-008)` pass ever ran — absent from both `traces/.pending` and every `pass start:` line in today's full loop log; no branch or code exists in either worktree. Distinct from the three-times-named "design done, log/state not updated" crash shape (`ENG-006`/`ENG-007`/`ENG-008`'s own earlier entry): this is "chain recorded, the fire itself produced no pass at all" — a fourth, related but different crash-recovery gap. Most likely cause, reasoned not confirmed: the immediate drain-on-append raced the `continue (ENG-007)` retry already holding the lock at that same moment (independent evidence: that session's own log shows it checking for a concurrent writer around 05:50). Re-fired this pass (queued cleanly behind this pass's own held lock, confirmed via the trigger's own "pass in flight" log line — no race this time); full detail on `ENG-008`'s own ticket log. Two related-but-distinct chain-loss shapes on this board now (this, and the `eng-events-dropped` incident's own double-failed `watch launchd`) — worth a proposal if a fifth variant shows up, per this board's own "third exception of the same kind" convention. |
| 2026-08-29 | eng-manager | aiorders | Machine WIP is now 6/6 (at cap, not over) after this pass advanced `ENG-009`/`ENG-010` to `ready`: `ENG-007` (ready-to-ship), `ENG-008`/`ENG-009`/`ENG-010`/`ENG-011`/`ENG-013` (ready). No further ticket can enter `ready` until one of these six clears, regardless of approver-facing WIP or approval-cap headroom — worth checking first thing next pass rather than rediscovering it mid-dispatch. |
| 2026-08-29 | architect | aiorders-portal | Third occurrence of this Windows host's stale-worktree-registry gap (first two: `aiorders-api` and, implicitly, `aiorders-admin-hub`, both 2026-08-29): `$ENG_WORKTREES` held only two of the five registered projects before this pass, missing `restaurant-portal`. Created it the same way as the prior occurrence (`git worktree add -b eng/base`, one project, not the full `lib/eng-setup.sh`). Worth running `lib/eng-setup.sh --apply` for real before a fourth ticket hits this the same way — `config-site-builder` and `restaurant-marketplace` are the two registered projects still unverified on this host. |
| 2026-08-29 | eng-manager | aiorders-admin-hub | The `decision (2026-08-29-eng008-engagement-source-question.md)` fire turned out to be a duplicate-queued-event no-op — the live `intake` pass that raised the question had already consumed the approver's hand-edit and shaped `ENG-009` from it before this queued copy reached the lock (confirmed via `traces/eng-loop-2026-08-29.log`'s own `queue: collapsed 1 duplicate event(s)` line). The next item now at the front of `traces/.pending` — `decision 2026-08-29-eng008-g1-scope.md` — is the same shape: that G1 was already closed in the same pass that carried `ENG-008` to `ready`. Flagging so whoever handles that fire can confirm-and-close quickly rather than re-deriving what this pass just re-derived. |
| 2026-08-29 | eng-manager | aiorders | That same `decision (2026-08-29-eng008-engagement-source-question.md)` fire's first attempt ran 541s doing real work, then the `claude` process's own Bun runtime segfaulted (`exit 127` — crash report in `traces/eng-loop-2026-08-29.log`), so the harness re-queued it as attempt 2/2 instead of treating it as consumed. A new failure shape, distinct from the four already-catalogued crash-recovery gaps on this board (design/log desync, chain-recorded-but-no-pass): here every substantive write survived intact, and only the final, already-redundant verification step was lost — attempt 2 re-ran it for real and got the same clean result attempt 1 had reported unconfirmed. Cost one of this event's two retry attempts to a runtime bug unrelated to anything this department controls; worth watching for a second occurrence before treating it as more than a one-off. |
| 2026-08-29 | product-manager | aiorders | This `intake` pass (shaping `ENG-021`) checked `ENG-014`'s and `ENG-015`'s G1s fresh from `inbox/` for cap purposes and found **both now answered** (`decision: approved`, decided 15:54:50 and 16:12:24) — but neither ticket's own frontmatter has moved past `state: awaiting-scope, owner: approver` yet, so the cached board header's "both unanswered" is now stale. Out of scope for this event's own narrower contract to process (that's a `decision` event's job, and one appears to already be in flight independently for each) — treated conservatively as still occupying both approver-facing WIP slots for this pass's own G1 decision, and flagged here so the next `decision`/`watch`/`scheduled` pass doesn't have to rediscover it. |
| 2026-08-29 | product-manager | aiorders | `board/_index.md` line 73 (the `ENG-012` terminal note in the In-flight narrative) still reads "the 2026-08-29 `scheduled` sweep below" — that sweep was rolled to `_index-archive.md` by an earlier pass and the cross-reference was never updated. Fixed the same dangling reference where it blocked this pass's own edit (the "Waiting on the approver" section); left this second, unrelated occurrence alone rather than widen this `intake` event's scope for a one-line wording fix. |
| 2026-08-29 | product-manager | aiorders | `ENG-014`/`ENG-015`'s G1s are still sitting answered-but-unprocessed as of this `intake` pass (shaping `ENG-023`/`ENG-022`) — now confirmed unchanged across at least three consecutive passes today (`ENG-021`'s, and now this one). No `decision`/`watch`/`scheduled` pass has cleared either despite each apparently believing one was "already independently in flight." Worth someone actually checking whether that belief is true rather than every pass re-deferring on the same assumption. |
| 2026-08-29 | product-manager | aiorders | `lib/eng-notify.sh raise` on this pass's two new inbox items (`ENG-022`'s P0 incident, `ENG-023`'s frequency question) both exited 0 but logged `SLACK_WEBHOOK_URL unset — cannot notify` (`traces/eng-notify-2026-08-29.log`) — the same gap `proposals.md`'s 2026-08-25 eng-manager row already named (no channel dispatch, no `telegram` branch even though `config/config.yaml` sets `approver.notify: telegram`). Confirming it again here because this time it silently no-op'd a P0 incident notice specifically, not just a routine gate item — the item is still on disk and reachable via the control center, so nothing was lost, but the one channel `security-baseline.md` promises for "reaches the approver directly" isn't actually reaching anyone on this instance yet. Not a new proposal, an increment on the existing one — worth weighing that when the weekly batch prioritizes which proposals to approve first. |
| 2026-08-29 | product-manager | aiorders-api | Shaping `ENG-024` (onboarded restaurants missing from marketplace search) found a second insert site with the identical omission: `aiorders-api/supabase/functions/restaurant-claims/index.ts`'s own restaurant insert (the separate "claim your restaurant" flow) also never sets `show_in_marketplace` — but it sets `approved: false` by design, pending manual review, so unlike `ENG-024`'s onboarding path this may already be a non-issue if the claims-approval step (wherever a pending claim gets approved) sets the flag at that point. Not traced that far — genuinely unverified, not a confirmed second bug, so left as an observation rather than folded into `ENG-024` or written up as its own proposal. Worth a look by whoever next touches the claims-approval flow. |
| 2026-08-29 | eng-manager | aiorders | `ENG-010`'s own board-index entry (`_index.md`, written by the `scheduled` pass that processed its G1) asserted the duplicate-queued-event shape was "already logged three times on this board today (`ENG-008`'s two gate items, then `ENG-009`'s G1, **all now archived**)" — but `ENG-009`'s dated entry was in fact still live in `_index.md` at that point, and remained so until this pass's own keep-three roll archived it for real. Narrative outran actual state by one roll; harmless here since this pass's own text avoided repeating the claim and the roll now makes it true, but the same "checked fresh, not trusted" discipline this instance applies to tickets and inbox items apparently doesn't always extend to a pass's own prose about the board it's editing. |
| 2026-08-29 | eng-manager | aiorders-api | `ENG-011`'s `continue` pass (the one that recovered its unrecorded build) found the Supabase MCP connection available to this session reaches the real `aiorders-api` project (`bmnmnejwdxbcqinqkwko`) directly — `list_projects`/`get_project` confirm it's the same project id `config/projects.md` registers, `ACTIVE_HEALTHY`. Read-only queries (`execute_sql` with a `SELECT`, `list_migrations`) cost nothing and touch nothing, and closed a real verification gap this pass (see the ticket's own log and the database migration doc's addendum) that Docker/psql/`supabase` CLI have never been able to close on this host. Not used for anything beyond read-only this pass, and not proposed as a full fix — `create_branch`/`apply_migration` both write and the former costs money — but worth any future data-touching ticket knowing this path exists before assuming "no live Postgres" the way the last two tickets both did. |
| 2026-08-29 | eng-manager | aiorders | This `continue ENG-013` pass found three files already modified, uncommitted, on disk at pass start, none touched by this pass: `departments/engineering/agents/eng-manager/config.yaml` (`plan.tier: pro → max_5x`), `departments/engineering/lib/eng-trigger.sh` (`read_plan_budget()`'s config path fixed from `$ROOT/agents/eng-manager/config.yaml`, which never existed, to `$ENG_DEPT/agents/eng-manager/config.yaml`), and `instances/aiorders/engineering/config/config.yaml` (`wip.machine_limit: 6 → 12`, matching `max_5x`'s table value) — all three coherent with each other and in-progress, not broken, so left exactly as found and not committed alongside this ticket's own unrelated changes. Consequence worth flagging: `board/_index.md`'s header still reads "Machine WIP 6... Currently 6/6 — at cap" — that cached number is now stale against the live (if uncommitted) config, and whoever next does a board-wide sweep should recompute it against the real cap rather than trust the header. |
| 2026-08-29 | eng-manager | aiorders-api | The `continue ENG-007` pass that found this ticket at `ready-to-ship` on a Saturday is the first time `releases.block_weekends` has actually held a ticket back on this instance — every prior `ready-to-ship` → PR-open on this board (`ENG-002`, `ENG-004`, `ENG-005`, `ENG-006`) landed inside the window on a weekday, so the guard had only ever been checked-and-passed, never checked-and-blocked, before now. Consistent with `ENG-006`'s own precedent (Friday, 14:40 PDT, inside the window) and this pass's own prompt (which names "a closed release window" as a chaining exclusion alongside the approver), the PR-open step itself — not just the eventual merge — is treated as the release-window-gated action. No PR opened; ticket holds at `ready-to-ship` until the window reopens Monday. Worth watching for whether a scheduled or chained pass actually picks this back up then, since this is also the guard's first real test of that recovery path. |
| 2026-08-29 | product-manager | aiorders | This `watch` pass edited `inbox/2026-08-29-eng-loop-halted.md` twice, and between the two edits the file changed on disk underneath it — the approver answered it live (`decided: 2026-08-29T21:28:04...+00:00`, a direct instruction to confirm/raise the hop budget) while this pass was still mid-write on the same file. The edit tool surfaced this as a stale-read warning rather than silently overwriting the new content, which is what made it possible to notice and fold the live answer into the same pass instead of clobbering it or answering it a pass late. First direct evidence (rather than inferred from timestamps after the fact, as in earlier entries) that this approver has the control center open and answers gate items in real time while a pass is running — worth remembering that a long-running pass should re-read a gate item immediately before its final edit to it, not just at the start. |
| 2026-08-29 | product-manager | restaurant-portal | Shaping `ENG-025` from `ENG-023`'s own standing frequency-question found that `restaurant_feedback` already carries populated, structured `type`/`sub_type`/`nature` columns (live schema + data check, Supabase project `bmnmnejwdxbcqinqkwko`) — 30 distinct real categories in use, largest 640 rows. The standing question itself had guessed Reading B "likely means AI-assisted categorization" of free text; it doesn't, because the categorization already happened at submission time. Worth any future feedback-adjacent ticket checking this table's actual columns before assuming free-text analysis is needed — the assumption was reasonable from the request text alone and wrong against the live schema. |
| 2026-08-29 | eng-manager | aiorders-api | Building `ENG-008`'s step-6b artifact-enumeration grep (over `accepts_paid`, `accepts_barter`, `staff_rating`, `collaboration_count`, `handlers/influencers`) surfaced a real bug none of the three local self-tests would have caught: `ENG-009`'s design doc (sibling ticket, same handler, sequenced to build after this one) had already read `admin-portal/index.ts` and recorded its CORS `Access-Control-Allow-Methods` as missing `PATCH` — exactly the method this ticket's own approved design specifies for its new endpoint. `deno check`, `npm run lint`, and `npm run build` all stay green on a CORS misconfiguration, since none of them issue a real cross-origin request; only a live browser preflight would have surfaced it, most likely after ship. Fixed in the same build hop (widened the allow-list rather than switching to `PUT`, per the two options `ENG-009`'s doc itself named). Concrete evidence for why step 6b runs on the build hop and not only at review. |
| 2026-08-29 | eng-manager | aiorders | This `decision` event (context `2026-08-29-eng-events-dropped.md`) had already been popped off `traces/.pending` to launch this pass — the pending queue shows no trace of it — and a `watch`/`schtasks` pass had already fully processed the same file before this dedicated event reached the lock: read the rejection plainly (declines further investigation of that one dropped event specifically, not the drop/notify mechanism itself), took no further action per the approver's answer, and journaled it. Verified fresh rather than trusted before concluding no-op: the file sits unedited in `inbox/_handled/` with its "Processed" footer intact since that `watch` pass; `decision-journal.md` already carries this exact decision and timestamp; there is no `ticket:` this item belongs to (`ticket: unknown` — a department-level incident, not a ticket gate), so nothing to advance; `lib/eng-gate-check.sh` (whole-board, this instance) exits 0 clean. No ticket-log or board-index edit made — no new fact for either to carry. Another occurrence of the still-open, still-unimplemented `proposals.md` fix (2026-08-27 row, filed by hand — skip the launch when a `decision` event's named gate item is already in `_handled/`). |
| 2026-08-29 | eng-manager | restaurant-portal | This `decision` event (context `2026-08-29-eng014-g1-scope.md`) is the same race as the row above, but with a wrinkle the existing `proposals.md` fix ("skip the launch when the named gate item is already in `_handled/`") wouldn't catch: the `watch`/`schtasks` pass that won the race processed the gate correctly and wrote `chained: ENG-014` (and, on the sibling ticket, `chained: ENG-015`) into the ticket log — but its own process (pid 36150) died before that recorded fire actually reached the trigger, so both tickets sat at `designed`/`architect` with a chain record on disk and nothing queued behind it. `traces/eng-loop-2026-08-29.log` has no `continue (ENG-014)` or `continue (ENG-015)` anywhere, and neither was in `traces/.pending`. If the open proposal's fix skips the launch whenever the gate item is already handled, it would skip past this exact failure mode too — a genuinely stalled ticket, not a genuine no-op — unless it also checks that a state sitting in an agent-owned state actually has a live or queued chain behind it, not just a log line claiming one. Repaired `ENG-014`'s chain this pass (re-fired, confirmed on the queue); `ENG-015` carries the identical gap, untouched here (out of scope — this event names `ENG-014` only), for whoever next touches it. |
| 2026-08-29 | eng-manager | aiorders | This `decision (2026-08-29-eng015-g1-scope.md)` event is the pickup the row above flagged in advance — repaired `ENG-015`'s identical broken chain (re-fired `continue ENG-015`, confirmed queued behind this still-running pass). Separately, while in `agents/eng-manager/board/`, found `_index-archive.md.tmp.4632.31c9ee9459a2` — an untracked, 5,027-line stale temp file (mtime 14:52) whose content is an outdated archive snapshot (top entry `ENG-012`, several rolls behind the current top of `ENG-013`/now `ENG-014`), consistent with an atomic-write temp file left behind by an earlier pass's crashed or interrupted edit to `_index-archive.md` — same crash-artifact family as the "partially-updated, self-contradictory artifact" rows above, just a leftover write buffer rather than a half-applied edit. Not deleted — out of scope for this event's own ticket-scoped contract, and file cleanup on a shared board directory deserves its own deliberate pass rather than a side-effect of an unrelated chain repair. Worth a dead-end sweep clearing it. |
| 2026-08-29 | eng-manager | aiorders | A new variant of the "claimed done, verify against the filesystem" lesson (rows above, 2026-08-28): the `eng-loop-halted` incident-repair pass's own log entry states "Action taken: committing the three already-fixed files... alongside this pass's own changes," but a fresh `git status` at the start of this `scheduled` pass showed all three files (`departments/engineering/agents/eng-manager/config.yaml`, `departments/engineering/lib/eng-trigger.sh`, `instances/aiorders/engineering/config/config.yaml`) still modified and uncommitted, on top of several more passes' worth of also-uncommitted work (`continue ENG-008`, `continue ENG-013`, the `ENG-014`/`ENG-015` chain repairs, `ENG-022`/`ENG-023` incident processing) — nothing since `a143d9b` had actually landed. Distinct from every prior occurrence of this lesson: those were a pass trusting a stale or partially-written *artifact*; this is a pass's own narrated *action* (a git commit) that most likely never executed at all, with no error surfaced anywhere to say so. This pass committed the accumulated legitimate work itself (verified against each ticket's own log before including it) rather than leaving it to accumulate further. Worth a look if it recurs: nothing today distinguishes "the commit ran and this log line is accurate" from "the pass died or skipped the step before reaching it" except a future pass choosing to run `git status` rather than trust the narration — same shape as the standing `chained:` record check in `eng_build_loop.md` step 8, just for `git commit` instead of the trigger fire. |
| 2026-08-29 | architect | aiorders-api | Designing `ENG-022`'s fix found `brand-portal/`'s 9 handler files already split into two unrelated, pre-existing error-response conventions for the exact same "access denied" case — `feedback.ts`/`customers.ts`/`hiring.ts`/`website.ts` throw a plain `Error` (reaching `index.ts`'s generic catch as HTTP 500 for `customers.ts`/`feedback.ts` specifically, since neither has a local `try/catch`), while `offers.ts`/`catering.ts`/`menus.ts`/`restaurants.ts` return `{success:false, error}` at HTTP 200. Predates this ticket and isn't caused by the bug being fixed — noted so a future ticket doesn't mistake the split for something `ENG-022` introduced, and so whoever eventually looks at `restaurant-portal`'s handling of `get_feedback` specifically knows its denial path is dead code today and about to become live at a 500 rather than a 200. |
| 2026-08-29 | eng-manager | aiorders-api | Another occurrence of the open `proposals.md` race (2026-08-27 row, filed by hand — skip the launch when a `decision` event's named gate item is already in `_handled/`): this `decision (2026-08-29-eng022-p0-incident.md)` event drained immediately behind the `continue ENG-022` design pass (`traces/eng-loop-2026-08-29.log`: `pass end: continue (exit 0, 1286s)` → `queue: collapsed 5 duplicate event(s)` → `draining queued event: decision (2026-08-29-eng022-p0-incident.md)`, no gap). Verified fresh rather than trusted: the incident item's own "Processed" footer (written by an earlier `watch`/`schtasks` pass) already closed it out, `decision-journal.md` row 34 already records the decision, and — unlike the `ENG-014`/`ENG-015` broken-chain variant of this same race — `ENG-022`'s own `chained: ENG-022` from the immediately-preceding pass is confirmed still genuinely queued in `traces/.pending`, not lost. No ticket-log state change beyond recording the verification; no re-chain (would duplicate the live one). `lib/eng-gate-check.sh`, whole-board: exit 0 clean. |
| 2026-08-29 | eng-manager | restaurant-portal | Another occurrence of the same open `proposals.md` race (2026-08-27 row) — this `decision (2026-08-29-eng023-frequency-question.md)` event drained well behind two intervening passes that had already consumed it: a `watch`/`schtasks` pass that shaped the answer into `ENG-025` and raised its G1, and a later `scheduled`/`schtasks` pass that closed that G1 out (`ENG-025` → `designed`, journaled) and separately raised `ENG-023`'s own unrelated G1 from the capacity that freed. Verified fresh rather than trusted: the item's own "Processed" footer is intact in `inbox/_handled/`; `decision-journal.md` row 37 records `ENG-025`'s G1 approval, whose interpretation cites this exact answer ("reading b, frequent is same restaurant over time"); `ENG-025`'s `chained: ENG-025` is confirmed still genuinely queued in `traces/.pending`, not lost; `ENG-023`'s own G1 (`inbox/2026-08-29-eng023-g1-scope.md`) is correctly still unanswered and was never blocked by this question in the first place (its own text says so); `ENG-023`'s ticket log already ends validly at `awaiting-scope` (`chained: none`, owner `approver`); `lib/eng-gate-check.sh`, scoped `ENG-023` and whole-board, both exit 0 clean. No ticket-log or board-index edit made — no new fact for either to carry. |
| 2026-08-29 | eng-manager | aiorders-api | This `continue ENG-024` pass found the ticket at `shaped`, correctly unable to start `building` — machine WIP was already 6/1 (over cap) at the moment the immediately-preceding `intake` pass shaped it and fired `chained: ENG-024`. That chain decision reasoned only about human gates ("no gate to wait at, G1 auto-skipped"); it never checked the machine-WIP cap, one of the loop's own explicit do-not-chain conditions ("held by a cap (WIP or approvals)"). The cap was already known full — the board's own header named `ENG-014` through `ENG-025` as held by it before this ticket was even created — so the hold was foreseeable, not a race against other passes like the rows above. Cost: one full session and one hop to re-derive a hold that was knowable at shaping time. Worth widening step 9's chain check (or the PM's own shaping step) to consult the machine-WIP count before firing `continue` on a freshly-shaped To-do-column ticket, not just whether a human gate is open — otherwise every ticket shaped while WIP is full pays this same wasted hop. One occurrence — a proposal if it recurs. |
| 2026-08-29 | eng-manager | aiorders | This `continue ENG-011` pass's own prior log entry (written earlier the same day, at the `in-security → ready-to-ship` transition) had explicitly left the release-window question open for this hop to decide ("flagged here so the next hop doesn't have to rediscover it, not decided here"). By the time this hop ran, `skills/release-runner/SKILL.md` had already been corrected (same day, same board's `_index.md` shows it among today's modified files) to state plainly that step 1's window check is L2/L3-only and never applies to L1 — both this ticket's projects are L1. Worth naming for a future pass: when a ticket's own log and the current skill file disagree on policy (not on ticket state), the skill file is the live truth and wins — a stale ticket-log note shouldn't be re-litigated as if the question were still open. No proposal needed; this is exactly what re-reading the current skill before acting is for, and it worked as intended here. |
| 2026-08-29 | principal-engineer | aiorders-api | `ENG-013`'s code review round 1 is the first code-review failure recorded on this board (automatic-failure #10 — a new authz-gated write path, `foodswipe.ts`'s `setStageOverride`/`resetStageOverride`, shipped with zero test coverage, against direct in-repo precedent from `ENG-007`/`ENG-011`). One data point, not a pattern — worth revisiting `speed.first_pass_gate_rate` once enough rounds exist for the weekly report to compute it meaningfully. |
| 2026-08-29 | eng-manager | aiorders | This `decision` event (context `2026-08-29-eng-loop-halted.md`) drained well after the item was already fully closed. Verified fresh rather than trusted, given this exact item's own history (the earlier "claimed done" row above): `decision-journal.md` row 36 already records the decision and its verified answer; the three files the item names (`departments/engineering/agents/eng-manager/config.yaml`, `departments/engineering/lib/eng-trigger.sh`, `instances/aiorders/engineering/config/config.yaml`) are this time genuinely committed — `git log --follow` on the item and `git show --stat` on `10f9d95` both confirm the recovery pass actually landed them, matching the diffs the item's own text describes; `lib/eng-gate-check.sh`, whole-board, exits 0 clean. No ticket (`ticket: unknown`, department-level incident) — no ticket-log or board-index edit made, no chain fired. Another occurrence of the same still-open `proposals.md` race (2026-08-27 row: skip the launch when a `decision` event's named gate item is already in `_handled/`) — now recurred often enough across today alone that it's the majority of this instance's `decision`-event volume; not re-proposed, the existing S-sized fix already covers it and is waiting on the batch. |
| 2026-08-29 | eng-manager | restaurant-portal | Another occurrence of the same open `proposals.md` race (2026-08-27 row) — this `decision (2026-08-29-eng025-g1-scope.md)` event drained well behind the `scheduled`/`schtasks` pass that had already closed this exact G1 out (`ENG-025` `awaiting-scope → designed`, `owner → architect`, `chained: ENG-025`) while sweeping the whole board, the direct sequel to this same lineage's own row above (`ENG-023`'s frequency question, which that G1 answers). Verified fresh rather than trusted: the item's "Processed" footer is intact in `inbox/_handled/`; `decision-journal.md` row 37 records this exact decision and timestamp; `ENG-025`'s `chained: ENG-025` is confirmed still genuinely queued in `traces/.pending` (behind `ENG-014`'s and `ENG-015`'s own queued continues, not lost or collapsed); `lib/eng-gate-check.sh`, scoped `ENG-025` and whole-board, both exit 0 clean. No ticket-log or board-index edit made — no new fact for either to carry, and re-chaining here would duplicate the live one. Not re-proposed — same still-open fix, already batched. |
| 2026-08-29 | architect | restaurant-portal | `continue ENG-014` finished this ticket's design (no one-way door, no G2) with machine WIP already known at 5/1 from a fresh `_index.md` read, so it left `owner: eng-manager` and recorded `chained: none — held by cap` rather than firing the next hop — the reverse of the `continue ENG-024` row above, where the prior pass chained *without* checking WIP and paid a wasted hop when the next one found the same cap. Same risk category that row already flagged as "a proposal if it recurs" (step 9's chain check not consulting machine-WIP before firing `continue`); this pass is a data point that checking the cap before chaining already works when done, not a new failure — worth weighing in if that proposal is written up, since it shows the fix is cheap (one board read) rather than needing new tooling. |
| 2026-08-29 | architect | aiorders-api | Designing `ENG-015`'s fix found `public.brands` — not just `brands.partner_id` — entirely absent from `aiorders-api`'s tracked migration history (grepped the whole `supabase/migrations/` tree for `partner_id` and for any `CREATE TABLE`/`CREATE POLICY` naming `brands`: zero hits either way), a wider version of the untracked-schema gap `ENG-015`'s own PRD already named for `profiles`. Consequence named in the design rather than chased further: whether the Brands page (`getAllBrands()`, already branching to the RLS-scoped client for non-admin roles, same pattern this design extends to `restaurants.ts`) actually scopes a partner to their own brands can't be confirmed from code, since `brands`' own RLS is exactly the untracked history in question. Not investigated live (no DB credential from here) and not this ticket's problem — the PRD names Restaurants, not Brands, as the leaking list — but the same shape of gap in the same table family, worth knowing before any future ticket opens broader partner access on the Brands page itself. |
| 2026-08-29 | eng-manager | aiorders | This `continue ENG-007` pass, while reading `inbox/2026-08-29-eng011-merge-request.md` purely as a formatting precedent for `ENG-007`'s own merge request, found it already carries `decision: approved` and a trailing "merged" line — apparently answered and resolved since `board/_index.md` was last written (which still shows it as the sole, unanswered occupant of both the approver-facing WIP and approval-cap counts). Not verified or processed here — out of scope for a `continue` event naming `ENG-007` specifically. Worth a dead-end/board-staleness sweep reconciling it and the board header's counts, since if `ENG-011` is in fact fully resolved, both caps have more headroom than the header currently states. |
| 2026-08-29 | eng-manager | aiorders-api | `ENG-013`'s round-1 test gap, closed this pass (`foodswipe.test.ts`, `hasFoodswipeAccess`), hit the exact same `&&`-short-circuit-loses-declared-`boolean`-type bug row 134 above already logged for `ENG-008`'s `hasInfluencerAdminAccess` — same day, same shape, same fix (`Boolean(...)` wrap), no production impact either time (both call sites only check truthiness). Two occurrences of this specific pattern now, both surfaced only once a test finally exercised the "no matching role, no `additional_roles` key at all" case neither engineer wrote a test for at first pass. Worth a lint rule (or a line in `engineering-standards.md`) if a third `hasXAccess`-shaped function in this codebase makes the same mistake — `allowedRoles.includes(x) \|\| (y && y.some(...))` silently typed as `boolean \| undefined` rather than caught at compile time (`userProfile: any` erases the check). |
| 2026-08-29 | eng-manager | aiorders-api | Writing `ENG-008`'s round-1 fix (`influencers.test.ts`) caught a real bug the review itself hadn't named: `hasInfluencerAdminAccess` is declared to return `boolean` but actually returned `undefined` for any profile with no `additional_roles` key at all (the `&&` short-circuit on `userProfile.additional_roles && ...` yields `undefined`, not `false`, when that key is absent) — a strict `assertEquals(..., false)` failed where a loose falsy check would have passed silently. No production impact (the one call site only ever does `if (!hasInfluencerAdminAccess(...))`, and `!undefined === !false`), but a real contract violation caught only because the test asserted the exact declared return type rather than truthiness. Fixed in the same hop (wrapped in `Boolean(...)`) since the function was already being touched for the `export` this test needed — not filed separately. Worth noting as a concrete case for `ENG-013`/`ENG-008`'s own shared observation (rows above) on promoting "new handler needs a colocated test file" to a standing item: this is the second time today a test written specifically to close a review gap found something the review's own read-through had missed. |
| 2026-08-29 | eng-manager | aiorders | This `decision` event (context: `2026-08-29-eng023-g1-scope.md`) drained after a `watch` pass (context `schtasks`) had already fully processed the same answered gate — the same recurring shape this board has logged repeatedly (2026-08-26; twice on 2026-08-27; again today at row 109 for `ENG-008`'s engagement-source-question). Verified rather than assumed before concluding no-op: `inbox/_handled/2026-08-29-eng023-g1-scope.md` carries the approver's answer (`decision: approved`, `decided: 2026-08-29T23:38:32.834274+00:00`) and that `watch` pass's own processed footer; `ENG-023`'s ticket log already ends `awaiting-scope → designed` with `chained: ENG-023`, and the record is accurate, not stale — `traces/.pending` still carries `1 continue ENG-023` (second in line, behind `1 watch schtasks`), so the chain fire is real and outstanding, not lost; `decision-journal.md` row 38 already records the decision; `lib/eng-gate-check.sh`, whole-board, exits 0 clean. No ticket-log or board-index edit made, same reasoning this board used on 2026-08-27: no new fact for either to carry, and re-writing an already-accurate `chained:` record would be noise. Did not re-fire `continue ENG-023` — it is already queued; firing it again would only duplicate an entry the queue's own dedup would have to collapse. At least the sixth occurrence of the still-open `proposals.md` (2026-08-27) fix (skip the `eng-trigger.sh` launch when the named gate item is already in `_handled/`), now spanning three separate days. |
| 2026-08-29 | eng-manager | aiorders | Closing the loop row 138 (and `ENG-025`'s own log, same day) flagged and deliberately left open: this `watch (schtasks)` pass reconciled both `ENG-011` (`blocked → shipped`, its merge request found answered and still sitting in `inbox/`) and `ENG-007` (already `state: shipped` via an earlier control-center bypass, but with no independent ancestry check, no release record, and its own gate item never answered through any channel at all). Both confirmed merged via git ancestry in this department's own worktrees, not taken on say-so. Board header's WIP/approval-cap counts were stale against both tickets' true state going into this pass; corrected in the same edit as the In-flight table. |
| 2026-08-29 | eng-manager | aiorders-api | Verifying `ENG-007`/`ENG-011`'s deploys found more live than either ticket's own gate receipts claimed: read-only Supabase MCP queries show both migrations (`20260829130000_restaurant_loyalty_configs`, `20260829190000_add_last_order_at_to_platform_analytics`) already applied to `bmnmnejwdxbcqinqkwko`, and the `admin-portal` edge function redeployed at `2026-08-30T02:47:37Z` (version 115, from the approver's own checkout) carrying **both** tickets' handler code in one event — neither ticket's own migration doc anticipated the other's deploy landing in the same push. Worth remembering for any future ticket touching `admin-portal`: a "not yet deployed" gap named at `ready-to-ship` can already be closed by the time a later pass checks, if the approver bundles deploys rather than doing one per ticket. |
| 2026-08-29 | architect | aiorders-admin-hub | `ENG-011`'s reconciliation found a GitHub Actions Cloudflare Pages workflow (`.github/workflows/deploy-cf.yml`) now exists on this repo — added by the approver directly (`698b7c1`, `ceb9552`, both `Co-Authored-By: Claude Sonnet 5`, a separate Claude Code session from this department's own) two commits after `ENG-011`'s own PR merged, with the first run failing on a missing `environment: Production` scope and the second fixing it. This is the first registered project on this board to gain real CI/CD mid-flight; every prior release record's "no CI/CD exists on this repo" line is now stale for `aiorders-admin-hub` specifically (still true for `aiorders-api`) — worth checking fresh rather than assumed on the next ticket that touches this repo. |
| 2026-08-29 | eng-manager | aiorders-admin-hub | This `decision` event (context: `2026-08-29-eng011-merge-request.md`) drained well behind two intervening passes that had already closed it out: the `watch (schtasks)` pass that reconciled `ENG-011` `blocked → shipped` (21:19:36–21:52:16, verified merged via git ancestry in both repos plus live-deploy confirmation via Supabase MCP and GitHub Actions run status), and `continue ENG-023`'s design pass after it (ended 22:09:43) — this event only reached the front of `traces/.pending` after both (`traces/eng-loop-2026-08-29.log`: `pass end: continue (exit 0, 942s)` → `queue: collapsed 4 duplicate event(s)` → `draining queued event: decision (2026-08-29-eng011-merge-request.md)`, `[day 69/200 charged]`). Verified fresh rather than trusted: `inbox/_handled/2026-08-29-eng011-merge-request.md` carries the processed footer, unedited since the `watch` pass; `decision-journal.md` row 39 already records this exact decision (`decided: 2026-08-30T01:43:13.118048+00:00`) and timestamp; `ENG-011`'s own ticket log already ends at `shipped` with a valid `chained: ENG-011`, and `traces/.pending` confirms that chain is genuinely still queued unconsumed (behind `continue ENG-008`, `scheduled schtasks`, `continue ENG-013`, `watch schtasks`) — re-firing it here would duplicate it, not fill a gap; `lib/eng-gate-check.sh`, scoped `ENG-011` and whole-board, both exit 0 clean. No ticket-log or board-index edit made, same reasoning this board has used repeatedly today — no new fact for either to carry, and an already-accurate `chained:` record doesn't need rewriting. Another occurrence of the same still-open `proposals.md` race (2026-08-27 row: skip the `eng-trigger.sh` launch when a `decision` event's named gate item is already in `_handled/`) — not re-proposed, the existing S-sized fix already covers it and is waiting on the batch. |
| 2026-08-29 | principal-engineer | aiorders-api | `ENG-008`'s round-2 review independently reproduced every self-reported number from the building pass and round 1 rather than trusting the log: `deno test` gave the identical 17/17; `npm run lint` on `aiorders-admin-hub` gave the identical 150 pre-existing errors, 0 new; and reverting the round's own `Boolean(...)` regression fix by hand reproduced the exact 16/17 failure the log described. Zero discrepancies found across three independent checks — worth one line as a positive data point on this board's own self-report reliability so far, since most of this file's entries are about things that turned out to be wrong or stale. |
| 2026-08-29 | eng-manager | aiorders | This `scheduled (schtasks)` pass's own launch (22:52:30) sat under sustained load — five `watch schtasks` polls and this pass's own later `continue ENG-009` fire all arrived while this pass held the lock, per `traces/eng-loop-2026-08-29.log` ("lock is Ns old but PID 1909 is alive — not stealing" / "pass in flight, queued as pending"), stacking `traces/.pending` to eleven lines. Confirmed by direct read that this is healthy, not stuck: every one of those fires correctly deferred to the live lock rather than racing it, and the FIFO-with-collapse queue design means they'll drain in order once this pass exits. Worth recording as a positive data point (first time this file has directly observed the queue under this much simultaneous backlog) rather than chased as a bug — nothing here deviates from `eng_build_loop.md`'s own description of the mechanism. |
| 2026-08-29 | eng-manager | aiorders | This pass found `board/_index.md`'s header and "Waiting on the approver" section still framed around a "3, across all gates" approval cap that `config/config.yaml` records as removed 2026-08-29 at the approver's own request — stale board narrative outliving a same-day config change. Corrected in the live board only; historical ticket-log entries that correctly cite the cap as it stood when written were deliberately left alone (append-only). Worth a check on `docs/`/`schedules/` copies outside this instance's own board if the same stale phrasing shows up somewhere a future pass trusts at face value. |
| 2026-08-29 | principal-engineer | aiorders-api | `ENG-013`'s round-2 review independently re-ran every self-reported number from the two prior passes and found one real (if non-blocking) mismatch, not the zero found on `ENG-008`'s equivalent round-2 check (row above): the log's whole-tree `deno check admin-portal/handlers/*.ts` note said the 17 pre-existing errors were "all in `users.ts`" — re-run fresh with each error's file location isolated, the actual split is `auth.ts` (4), `partners.ts` (4), `users.ts` (9). The total (17) and the claim that actually mattered (zero in the files this ticket touched) both still held, so nothing downstream broke, but it's a reminder that a self-reported aside can be wrong even when the headline number and the load-bearing claim both check out — worth spot-checking the specifics, not just the total, on any future "N pre-existing, none of them mine" claim. |
| 2026-08-30 | product-manager | aiorders-admin-hub | `ENG-011`'s acceptance-check (the first to run on this board) had no browser access on this host, same standing gap the release record already named for monitoring dashboards. Substituted: reading deployed source at the merged commit via `git show origin/main:<path>` (no worktree disruption to the other tickets currently checked out in `_eng`), sampling real production rows through the read-only Supabase MCP connection, and one live unauthenticated `curl` against the actual production endpoint to close a negative-auth criterion QA had left unverified. Caught a real, live issue this way (`BUG-001`) that no earlier gate could have, since it only exists in production, not in the diff. Not a full click-through, and every criterion's verdict says so rather than rounding up — but worth reusing as the default substitute the next time acceptance-check hits the same no-browser wall; a second occurrence would make it a proposal to formalize, per this board's own convention. |
| 2026-08-30 | product-manager | aiorders-api | `ENG-007`'s acceptance-check hit a variant of `ENG-011`'s no-browser gap, one step harder: this ticket has no user path at all (backend-admin-only, no frontend anywhere in the sequence), and its acceptance criteria are almost entirely insert-time DB-trigger behavior — exactly the branch the migration doc, QA, and the release record all separately named as their own unclosed "no live Postgres dry run" gap. Closed most of it without a live write: introspected the *deployed* `pg_proc`/`pg_trigger`/`information_schema` state (not git source) and confirmed it byte-for-byte identical to the design, then fetched the deployed edge-function bundle and confirmed the actual shipped `deriveCurrentConfig`/`validateLoyaltyConfigInput`/`mapLoyaltyConfigInsertError` logic matches what QA unit-tested against source. Deliberately did not attempt a live insert-and-rollback test to close the remaining gap for real: this table holds live per-restaurant financial configuration with a near-term real reader (ticket 3, the points ledger), and there was no independent confirmation that the Supabase MCP's `execute_sql` runs a multi-statement script as one atomic, rollback-safe session — a botched rollback would silently plant a fake rate on a real restaurant with no frontend to surface the mistake. Worth reusing on the next trigger-heavy, no-frontend ticket: deployed-artifact introspection plus hand-tracing closes most of a "no live DB" gap safely; a genuine live-write test on production financial data needs either a confirmed-safe transactional tool or an explicit approver sign-off first, not an assumption either way. |
| 2026-08-30 | product-manager | aiorders-api | `ENG-007`'s acceptance-check found `skills/acceptance-check/SKILL.md` step 6b and `eng_build_loop.md` step 3's narrative describing the same mechanism differently, and it's load-bearing, not cosmetic. `eng_build_loop.md` step 3 reads as: the *original* sequence-proposing ticket's G1 ("the G1 answer on it affirms proceeding with the whole shape") licenses auto-filing every later item in the chain. `SKILL.md` 6b's literal text requires *this* ticket's *own* G1 to independently re-affirm the sequence, and gives a worked example of a bare "approved" (context mentioned, question never asked) failing that bar. `ENG-007`'s own G1 is exactly that worked example — a plain approval that never itself asked "continue the sequence?" — so 6b did not fire, and a standing question went to the approver instead (`inbox/2026-08-30-eng007-continue-sequence-question.md`) rather than auto-filing ticket 3. Followed the skill's literal text over the loop doc's narrative, since the skill is the actual step-by-step procedure and its own worked example anticipates this exact case. Not filed as a proposal yet — one occurrence, but it will recur verbatim at ticket 3's own eventual acceptance-check (and 4's, and 5's) unless whoever files ticket 3's G1 writes it to explicitly ask the continuation question rather than merely mention it for context, which is the actual fix, not loosening 6b's bar. Worth a proposal if the next ticket in this sequence hits the same wall. |
| 2026-08-30 | eng-manager | aiorders-admin-hub | `ENG-008` reached `ready-to-ship` (security pass, release-readiness confirmed) while approver-facing WIP sat genuinely 2/2 (`ENG-016`, `ENG-017`, both still-open G1s). Held rather than opening its L1 PR, since that would be a third concurrent approver item over the stated cap — `eng_build_loop.md`'s Guards section names an L1 PR awaiting merge as one of the three things counted against this cap and gives no carve-out for a ticket already in flight before the cap filled (unlike machine WIP, which explicitly does grandfather that case). First time this board has checked the cap at the `ready-to-ship → PR-open` boundary rather than at intake/G1 time — the loop doc mostly discusses this cap in the context of new starts; worth watching whether this becomes a recurring hold point once more tickets reach `ready-to-ship` concurrently. |
| 2026-08-30 | eng-manager | aiorders-api | `ENG-009`'s build hop found its own architect design's Interfaces section specifying an extension to "`GET /admin-portal/influencers`" (a list endpoint) that does not exist — the design was written against `ENG-008`'s own *design* doc, before `ENG-008` actually built a **per-id**, frontend-uncalled `GET /admin-portal/influencers/{id}` instead. Same category of gap as `ENG-008`'s own PUT-vs-PATCH catch against this ticket's design (row above, 2026-08-29), just discovered from the other side — a build hop finding its *own* design stale against a sibling's *shipped* code rather than the reverse. Resolved locally (a new `GET /admin-portal/influencers/activity` route, no cross-file instruction conflict per the step 6b grep), but this is now two same-week occurrences of "a design doc extending ticket X is only checked against X's design, never X's actual diff." Worth a proposal if a third one shows up: the architect re-reading the dependency's real diff (not just its design doc) when a ticket explicitly extends another might be cheaper than absorbing the drift at build time every time. |
| 2026-08-29 | eng-manager | aiorders | This `watch` pass's own first two `git status` checks, moments apart with no action from the pass in between, disagreed: the first showed `agents/eng-manager/board/_index.md`, `_index-archive.md`, and `observations.md` as unmerged (literal `<<<<<<< Updated upstream` / `>>>>>>> Stashed changes` conflict markers on disk in `_index.md`, from `stash@{0}: On main: local instance state before marketing port pull`); the second, run to double-check before editing, showed a clean tree matching `HEAD` (`ad4c6c4`), with `git reflog` confirming an intervening `reset: moving to HEAD` right after a `pull --ff-only` — someone or something else backed the conflicted pop out concurrently while this pass was mid-sweep. The stash itself is still intact in `git stash list`, untouched by whatever did the reset — reads as a deliberate "abandon this pop, keep the stash for later" recovery, not a crash. Not this pass's to resolve (unrelated to the `watch` event's own inbox-sweep scope, and second-guessing a concurrent hand-recovery is more likely to destroy work than help); proceeded once the tree was confirmed clean and re-ran the pre-pass gate check fresh rather than trust the first one. Worth remembering as a live example that this instance's board files can change under a pass mid-run from outside the build loop entirely (not just the approver answering a gate, per the `eng-loop-halted` row above, but direct git operations) — a pass that reads once at the top and never re-checks before its own final writes is the exposure. |
| 2026-08-30 | eng-manager | aiorders | This `scheduled` sweep found two tickets (`ENG-007`, `ENG-011`) fully merged on GitHub — three-plus hours before this pass ran, in `ENG-007`'s case with no gate item ever having been raised to bypass at all — sitting undetected through one failed `continue ENG-023` attempt (02:13) and one that read files but never checked git state before dying (09:31–13:00). Merge detection runs every pass, but only a pass that actually starts gets to run it, and a `continue` event's own contract scopes it to the one named ticket. A `scheduled`/`watch` sweep is the only event type that checks *every* blocked/ready-to-ship ticket's merge status regardless of which ticket a failing `continue` happened to be stuck on — today is a concrete case of a safety-net pass catching exactly what its own description promises ("a PR merged on github.com") and nothing narrower would have. |
| 2026-08-30 | eng-manager | aiorders | Fifth and sixth data points (after `ENG-002`, `ENG-006` twice, now `ENG-007`/`ENG-011`) that this approver merges L1 PRs directly on GitHub rather than replying through the tracked gate-item channel — both merged the same night, `ENG-011`'s two PRs 40 seconds apart. The still-open `proposals.md` fix for this gap (2026-08-26 row: detect a state/gate-item mismatch without waiting for a `decision` event to drain first) is now six-for-six evidence it's needed — every occurrence so far has still been caught, but only because a `scheduled`/`watch` sweep eventually ran, never because anything watches for it directly. |
| 2026-08-30 | eng-manager | aiorders | Another occurrence of the open `.watch-seen` fingerprint-timing proposal (2026-08-26 row): this `watch (launchd)` fire drained at 21:13:03, two seconds after the immediately-preceding `scheduled` pass released the lock at 21:13:01. That `scheduled` pass had spent 5.4 hours touching all three watched inboxes — moving several gate items to `_handled/`, appending the investigation section to `inbox/2026-08-30-eng-events-dropped.md`, and re-firing `continue ENG-008`/`ENG-013`/`ENG-023` as its own chains — none of which runs `commit_watch_fingerprint()` (guarded to `$EVENT = watch` only), so this fire inherited a changed fingerprint with zero unprocessed work behind it. Verified fresh rather than trusted before concluding no-op: `agents/product-manager/inbox/` and `agents/eng-manager/inbox/` hold only `.gitkeep`; `inbox/`'s sole item is the day's still-open running incident log (correctly not yet in `_handled/` — the day isn't over and it's designed to accumulate further drops); `traces/.pending` already carries the three re-fired chains (`continue ENG-008`, `ENG-013`, `ENG-023`) queued behind this pass; `lib/eng-gate-check.sh` scoped `ENG-023` and whole-board both exit 0. No ticket-log or board-index edit made — no new fact for either to carry. |
| 2026-08-30 | principal-engineer | aiorders-api | `ENG-008`'s code review round 1 failed on automatic-failure #10 (`influencers.ts`'s new admin-gated `PATCH` path, zero test coverage) — the identical shape that failed `ENG-013`'s round 1 one day earlier, same repo, same `admin-portal/handlers/` file family (row 130). Two occurrences now, not yet the third `skills/code-review-gate/SKILL.md` step 10 asks for before promoting to `engineering-standards.md`, but both are on tickets built the same week against the same missing precedent — worth pre-empting rather than waiting for a third ticket to repeat it: whoever writes the next `admin-portal` handler should be pointed at `loyalty-config.test.ts` before building, not after failing review. <!-- merge note: one copy of this entry (local/HEAD) dated the event 2026-08-29 ("same day" as ENG-013's round 1); the other copy (remote) dated it 2026-08-30 ("one day earlier" than ENG-013). Kept the more recent (2026-08-30) date; content is the remote copy's wording. --> |
| 2026-08-30 | principal-engineer | aiorders | Reviewing `ENG-008` found its frontmatter has never carried `time_estimate`/`time_spent`/`time_remaining`, despite both `agents/eng-manager/config/definition-of-done.md` ("Time tracking and scope changes") and `config/templates/ticket.md` calling for all three from `building` onward. Not fixed in this pass (out of scope for a review-gate hop, and fabricating numbers after the fact would be worse than leaving them blank) — worth checking whether this is ticket-specific or board-wide before it becomes a control-center dashboard gap on more than one ticket. |
| 2026-08-31 | architect | restaurant-portal | `designed` conflates two different sub-states and this pass's own investigation shows it: `ENG-023`'s prior log entry (2026-08-29) cited `ENG-014`/`ENG-015` as precedent for "a completed design correctly held at `designed` by the machine WIP cap," but neither has a file in `agents/architect/designs/` — they're sitting at `designed` un-designed, same as `ENG-023` was until this pass, not cap-held-after-completion. Harmless today since neither sub-state is currently gated differently, but a future pass picking "which `designed` ticket to fast-track the moment the cap clears" needs to check for the actual design file, not just the state name — the frontmatter alone can't tell the two apart. |
| 2026-08-31 | eng-manager | aiorders-api | A variant of the cross-host untracked-work pattern (`ENG-013`, 2026-08-30 row): `continue ENG-008` found a commit already on the branch (`dc6972a`, `influencers.test.ts`) that `ENG-013`'s own precedent (accept-if-verified) would say to trust once confirmed correct. It was correct as far as it went, but *incomplete* against what round 1's review actually asked for — it covered admin/sub-admin/wrong-role and every field validation, but not the explicitly-named "missing/undefined profile" case, and no test proved the field allowlist actually strips an unauthorized field from a mixed body. Hand-tracing the found commit against the review's own findings (not just against the handler) caught the gap before chaining past it as done. Worth naming as its own nuance: verifying a found-but-undocumented commit means checking it against the finding it was supposed to close, not only checking that what's there is internally correct. |
| 2026-08-31 | qa | aiorders-api | `ENG-013`'s round-2 combined review+quality hop needed to run `deno test` on `foodswipe.test.ts` for the actual quality gate (not just an investigating pass) and hit the same "no deno on this Mac host" wall three prior entries on this ticket already named. `proposals.md`'s 2026-08-29 row (eng-manager) proposes adopting the Supabase MCP connection as a standard substitute-verification path, and this ticket's own 2026-08-30 log entry treated that proposal as covering this gap too ("same root cause, same open fix"). It doesn't: the MCP connection reaches the live database for read-only schema/catalog checks, which has nothing to do with executing a Deno test file — there is no substitute execution path for that at all on this host, only hand-tracing. Not filing a third near-duplicate proposal for this ticket alone (the department's own cap on inventing work about its own machinery applies to proposals piling up too, not only tickets) — flagging so whoever next writes a proposal in this area scopes it to both gaps explicitly rather than assuming one fix covers both. |
| 2026-08-31 | eng-manager | aiorders | Confirms and extends the row directly above (architect, `ENG-023`): `ENG-014` and `ENG-015` didn't just miss having a design file — grepping every `traces/eng-loop-*.log` this instance has ever written for `pass start: continue (ENG-014\|ENG-015\|ENG-025)` (format confirmed live against `ENG-008`/`ENG-013` today) returns zero matches for all three, ever, despite each having its own `chained: ENG-0XX` log line and `ENG-014`/`ENG-015` each having a *second*, independently-verified "confirmed queued" repair entry from 2026-08-29. The queued fire simply never drained into a real pass, for two days, on three tickets at once, including a `P1` security ticket. No `eng-events-dropped` incident was ever raised for any of the three — that mechanism only triggers on a launch that fails or never-starts, and this loss happens earlier, between append and drain. Filed as a proposal (`proposals.md`, this pass) rather than fixed inline — it's the core dispatch path, not a ticket-shaped change. Repaired by hand this pass: `continue ENG-015`/`continue ENG-025` re-fired and confirmed on `traces/.pending`; `continue ENG-014` was already there and left alone rather than double-queued. |
| 2026-08-31 | architect | restaurant-portal | Closing the loop on this same day's own `ENG-023` observation above (the two sub-states `designed` conflates): `ENG-014`'s design is now actually written (`agents/architect/designs/ENG-014-restaurant-qr-media-self-service.md`), so it's genuinely cap-held-after-completion as of this pass, not un-designed. `ENG-015` is untouched and remains in the un-designed sub-state — still indistinguishable from the completed kind by frontmatter alone until its own `continue` pass runs. |
| 2026-08-31 | architect | aiorders-admin-hub | Designing `ENG-014` found the same "list existing shortened URL by matching `destination_url`, else create one" logic hand-rolled independently three times in `aiorders-admin-hub` (`utils/qrUtils.ts`, `BagInsertGenerator.tsx`, `A4PosterGenerator.tsx`), each building its own copy of the destination-URL string. `ENG-014`'s design adds a fourth independent implementation of the same construction (server-side, restaurant-scoped, in `url-shortener`) rather than consolidating the existing three — in scope for what this ticket's acceptance criteria actually need, but worth a look if the URL scheme under `/links/{restaurantId}` ever changes, since four call sites would need to move in lockstep with nothing enforcing that today. |
| 2026-08-31 | architect | restaurant-portal | Closing the loop for the third and last of the three tickets the 2026-08-31 `scheduled` sweep found sitting at `designed` *un-designed* (row above, and the `ENG-014` "closing the loop" row before it): `ENG-025`'s design is now actually written (`agents/architect/designs/ENG-025-feedback-recurring-issues.md`), so all three tickets that dispatch gap affected are now genuinely in the cap-held-after-completion sub-state, none still un-designed. No new dispatch-bug finding — the existing `proposals.md` row (2026-08-31, eng-manager) already covers all three and is waiting on the approver's sign-off before the core dispatch path itself gets touched. |
| 2026-08-31 | eng-manager | aiorders | `ENG-013`'s release-readiness hop ran `lib/eng-notify.sh raise` on its new merge-request item and it sent cleanly (`traces/eng-notify-2026-08-31.log`: `sent: active`) — no error, in direct contrast to `ENG-011`'s recorded `SLACK_WEBHOOK_URL unset` gap at this identical step two days earlier. Not investigated further (out of this event's own narrow contract); worth a corroborating data point for whoever next looks at that channel's reliability, since it suggests the gap may be intermittent or already resolved rather than a standing failure. |
| 2026-08-31 | eng-manager | aiorders-admin-hub | This `scheduled` sweep found `ENG-009`/`ENG-010` still citing their original sequencing hold ("pending `ENG-008` reaching `in-review` or later") hours after `ENG-008` satisfied it and moved on to `blocked`, awaiting the approver's merge — the *conclusion* (stay at `ready`) was still correct, now for a different reason (machine WIP 2/1, over cap), but every intervening `continue ENG-008`/`ENG-013` pass was scoped only to its own ticket and had no reason to revisit either sibling's stated rationale. General shape: whenever ticket B is held pending a condition on ticket A, and A later satisfies that condition while under a narrowly-scoped event contract, only a whole-board sweep re-checks whether B's own stated reason is still the *live* one or has been quietly superseded by something else that happens to reach the same conclusion. Corrected in both tickets' own logs this pass. One occurrence — a proposal if this recurs on a future sequenced pair. |
| 2026-09-01 | eng-manager | aiorders | `inbox/2026-08-30-eng-events-dropped.md` is still sitting in `inbox/`, not `_handled/`. The 2026-08-30 entry above reasoned it correctly stayed open because "the day isn't over" — that day is now two calendar days past with zero further drops appended, and the file's own body already declares itself fully investigated and "not further gated" (no `decision:` field was ever added; unlike the 2026-08-28/2026-08-29 siblings, it never needed one). Conclusion unchanged — nothing to escalate, it's below the P0 bar — but the stated reason for it remaining in `inbox/` rather than `_handled/` has gone stale, same shape as the `ENG-009`/`ENG-010` hold-reason row above, just on an inbox item rather than a ticket. Not moved to `_handled/` this pass: nowhere in `eng_build_loop.md` says a self-closed (no-`decision:`) incident notice gets filed there, so this is flagged rather than acted on unilaterally. Third consecutive pass (20:19 `watch`, 20:30 `scheduled`, this one) to re-read this same closed item and reach the same conclusion — worth a proposal for an explicit filing rule if a fourth pass does the same. |
| 2026-09-01 | eng-manager | aiorders | A `git pull` mid-pass (this same `scheduled` sweep) fast-forwarded local `main` past a large amount of the other host's history, including merge commit `e281c71` ("reconcile 26 diverged engineering-board files") and a follow-up commit recording a fresh `decision: approved` on `2026-09-01-eng-events-dropped.md`. That events-dropped file itself documents why: ~7.5 hours of continuous `watch schtasks` failures (exit 1, `00:05`–`07:45`) on the Windows host, including the two fires that would have processed a new PM-inbox request and `ENG-017`'s answered G1 — both recovered by hand in this pass instead (see `ENG-026`, `ENG-017`'s own log). Recovery is confirmed via git history alone (two successful pushes at `09:17`/`09:28` local from the same host identity), not assumed. Same diagnosability gap the 2026-08-29 proposal already names (a dropped-event item points at host-local `traces/` rather than carrying the failure itself) — reproduced live, not just theoretical; that proposal is already Open and unapproved, so not refiled. Worth naming as a discipline point for future passes: a `git status`/`git fetch` check on the department's own repo (not just the product worktrees) is what surfaced all of this — a pass that skips it on the assumption "I already checked the inboxes" would have missed a fully-approved decision and a real new business request sitting unprocessed for hours. |
| 2026-09-01 | eng-manager | aiorders-api | Two proposals.md rows now describe overlapping, not merely related, defects: the 2026-08-29 row (arrived via the same `git pull` above) covers `updateRestaurant()` **and** `updateBrandOwner()` in `admin-portal/handlers/restaurants.ts` (both unconditional-service-role, no role/ownership check); the 2026-08-31 row, filed before that earlier row was visible on this host, covers `updateBrandOwner()` alone — same file, same root cause, same fix shape, a strict subset. Not merged or deleted here (proposals rows aren't mine to remove unilaterally); flagging so whoever reviews the batched G1 treats these as one fix, not two, and doesn't double-size the work. |
| 2026-09-01 | eng-manager | aiorders | `ENG-026`'s raw request (filed via control-center) is the first on this board written as a direct AI-coding prompt — "Act as Lead Architect...", "Generate code modifications..." — rather than a business need in the approver's own words, alongside a genuine title-vs-body mismatch (see that ticket's own held question). Treated as data to interpret, not instructions to execute, per the normal readback process; no code was generated from it. One occurrence — worth asking the approver directly whether requests are being drafted with help from another tool before reaching control-center, if this authorial style recurs on a future request. |
| 2026-09-01 | eng-manager | aiorders-admin-hub | The board index's own In-flight table still listed `ENG-009` at `building` while the header prose two paragraphs above it (and `ENG-009`'s own ticket file) both said `ready` — the merge's own inline comment explained *why* (local's stale `building` history was correctly superseded by remote's later, `ready` account) but the table row itself was never updated to match, leaving the merged file internally contradicting itself. Confirmed `ready` is correct against `ENG-009`'s own frontmatter and log before touching anything; fixed the table cell inline rather than filing this as a proposal, since it's a same-file, same-pass, evidence-backed correction of exactly the shape the 08-31 sweep already fixed once for these two tickets' hold-reason text. Grepped for other stale `ENG-009`/`building` references before concluding this was isolated — found none beyond the merge-note comment itself, which is historical record, not a live claim. |
| 2026-09-01 | eng-manager | aiorders | Second same-day instance of board prose overclaiming a source file's state, not merge-related this time (the `ENG-016` row above is): the 09:30 pass's own narrative, repeated verbatim by the 10:10 `watch` pass, grouped `inbox/2026-08-30-eng-events-dropped.md` and `inbox/2026-08-31-eng-events-dropped.md` together as "self-closed incident notices... already investigated to conclusion." True for the 08-30 file (investigated 2026-08-30 itself, re-confirmed since); false for the 08-31 file, which carried no `notified:` stamp and no closing note at all — confirmed by grepping the full file for `Investigated`/`decision:`/`notified:`/`nudged:` and getting zero matches. Read closely instead of trusted this pass: all 48 of its drops are `watch`/`scheduled` (no unique payload lost), and read together with the 09-01 sibling the real outage ran `12:46:01` (08-31) through `07:45:58` (09-01) — about 19 hours, not the ~7.5 hours previously measured from the 09-01 file alone. Closed out in place (investigation note appended to the file itself, board corrected) rather than filed as a fresh gate — not a decision the approver needs to make. |
| 2026-09-02 | eng-manager | aiorders | `inbox/2026-08-29-eng016-g1-scope.md` (`ENG-016`'s G1) has no `decision:` key in its frontmatter at all — every other gate item checked this pass (`ENG-007`/`ENG-008`/`ENG-013`/`ENG-026`, and per the 2026-08-29 row above, `ENG-009`/`ENG-010`) carries `decision:` blank until answered, so a pass can find "still open" with one grep across `inbox/*.md`. This file has to be opened and read to the bottom to learn the same thing, since the key isn't there to even read as blank. Not a live problem today — this pass and every prior one reads it directly rather than relying on the grep shortcut for it specifically — but it means that shortcut is silently incomplete, and would misreport this item as answered-or-nonexistent rather than open if anyone ever trusted it alone. One file, apparently hand-authored before whatever gave later gate items their `decision:` key; not fixed here since editing a live, already-notified gate item isn't this pass's call to make. Worth a proposal if a second gate item is ever found missing the same key. |
| 2026-09-02 | eng-manager | aiorders | Rebasing `ENG-009` onto `ENG-008`'s fix commits (round-1 review finding, same date) split into two different outcomes the review's own text treated as symmetric risk. `aiorders-admin-hub`'s flagged hunk (`handleSaveInfluencer`'s body literal, ENG-008 deleting two lines ENG-009's insertion sits beside) — the one the review explicitly called "not a clean auto-resolve," "needs to apply both changes by hand" — merged automatically, correctly, via git's default `ort` 3-way strategy; verified by reading the resulting code directly rather than trusting either the review's prediction or the merge's silence. `aiorders-api`'s `influencers.test.ts`, never called out by the review at all, produced a real multi-hunk conflict: both commits append an independent, non-overlapping `Deno.test`/helper block right after the same anchor test, but the blocks share near-identical mock-Supabase-client boilerplate (`user: {...}, adminSupabase: { from() { return { update(patch) {...} } } } as unknown as AuthenticatedRequest`), and the line-based merge interleaved the two blocks' matching skeleton lines as if one were editing the other. Resolved by treating it as "keep both blocks whole and sequential," not by editing markers in place. Net lesson, opposite of what the review's own framing suggested: proximity/adjacency in a diff is a weak predictor of rebase risk; repeated structural boilerplate between two otherwise-independent additions (most likely in test files) is a better one, and isn't something `code-review-gate/SKILL.md`'s current checks would surface either way. |
| 2026-09-02 | eng-manager | aiorders | This 15:30 `scheduled` sweep, checking every in-flight ticket's own last `chained:` line for a broken chain (per this event's own remit), found six held tickets (`ENG-014`, `ENG-015`, `ENG-017`, `ENG-022`, `ENG-023`, `ENG-025`) whose most recent log entry still cites a superseded machine-WIP count ("4/1, over") from before `ENG-008`/`ENG-009`/`ENG-013` each left the counted `ready`..`ready-to-ship` range for `blocked` (2026-08-31 and today) — the true count is now 1/1, `ENG-010` alone. Conclusion unaffected in all six cases: each sits at `designed` or `shaped`, still one approver gate away from `ready`, and approver-facing WIP (4/2, over cap) independently bars raising any of those gates regardless of which machine-WIP number is cited — so nothing is actually mis-held, only the arithmetic in already-written prose is stale. `ENG-010` itself was the one ticket where the same class of stale citation *did* change the conclusion (both its sequencing hold and the machine-WIP hold it cited had actually cleared) — resumed and chained this pass, see its own board file. Not fixed inline across all six — no conclusion changes, and the load-bearing version of this drift (index vs. ticket-file disagreement) is already the open `proposals.md` (2026-09-01) fix; this is the same root cause showing up as stale prose within a ticket's own log rather than in the shared index. |
| 2026-09-02 | eng-manager | aiorders-api | `ENG-010`'s build hop (`continue`, resuming from the cleared hold the 15:30 sweep found) checked `ToolSearch` for a Supabase MCP connection before writing its migration, same as `ENG-008`'s and `ENG-009`'s own build hops each used to re-verify live schema state immediately beforehand — none was available this session. Fell back to static evidence only (migrations-directory listing, a repo-wide grep for any colliding table/column name, and the architect design's own same-week live-schema reading), named explicitly as narrower than precedent in `agents/database/migrations/ENG-010-influencer-relationship-notes.md` rather than presented as equivalent. Not filed as a proposal — may be this session's own tool configuration rather than a department-wide change, and the one ticket it affected still reached a `pass` verdict on its own (weaker but sufficient, additive-only) evidence. Worth a proposal if a future pass finds the same tool missing again. |
| 2026-09-02 | eng-manager | aiorders-api | `aiorders-api/CLAUDE.md` requires updating a changed function's entry in `supabase/functions/README.md` "in the same commit." `admin-portal`'s own entry never documented the `influencers`/`influencers/activity` routes `ENG-008`/`ENG-009` each added while modifying that same function — an omission in two prior tickets against their own target repo's stated convention, not something either ticket's board log flagged. Caught while adding `ENG-010`'s own new route to the same file and fixed in the same edit (full routes list, DB tables, and a Notes line on the narrower admin/sub-admin check) rather than compounding the gap with a third under-documented addition. No proposal needed — already fixed; flagging only so a future pass checking `README.md` currency elsewhere in this repo knows this class of drift is real, not hypothetical. |
| 2026-09-02 | principal-engineer | aiorders-admin-hub | `ENG-010`'s round-1 code review failed on a stale-response race in `Influencers.tsx`'s two new per-dialog note callbacks (`fetchNotes`/`handleAddNote`) — closing one influencer's dialog and opening another while a request is still in flight can display the wrong influencer's notes, with no error. Caught only by reading the new async code for ordering assumptions; no automated check could have, since this project's frontend carries no test harness at all. Second concrete data point for the standing `aiorders-admin-hub`-has-no-frontend-test-harness proposal (`proposals.md`, 2026-08-31, filed off `ENG-008`'s own unregression-testable fix) — that proposal argued the *next* bug on this repo would ship unguarded; this is that bug, on the very next ticket to touch this file. Not re-filed, same open row. |
| 2026-09-02 | eng-manager | aiorders-admin-hub | The fix hop for `ENG-010`'s round-1 finding (stale-response race in `Influencers.tsx`) found the review's own suggested example line (`selectedInfluencer?.id === influencerId`) would have read a closure-stale value and failed on every normal open, not just the race — `fetchNotes` is invoked synchronously from `openInfluencer` before React applies `setSelectedInfluencer`, so the `selectedInfluencer` a given call closes over is always the *previous* selection. Used a `useRef` set synchronously at click time instead, same intent, correct for React's render-timing model. Worth remembering generally: an example fix inside a review finding is a diagnosis of the bug, not necessarily a verified patch — retrace timing/ordering claims in example code the same way the finding itself was expected to, rather than transcribing it. |
| 2026-09-02 | principal-engineer | aiorders-api | `ENG-010`'s round-2 code review failed on a new table (`influencer_notes`) created with no RLS — see the ticket log and `agents/principal-engineer/notebook/2026-09-02-review-log.md` for the full finding. Neither the architect's design, the database migration gate, nor round 1's review asked whether a brand-new table needs RLS when the feature's own edge function always uses a service-role client; all three checked the edge-function-level authorization carefully and never looked one layer lower. This codebase already has a working precedent for this exact shape (`proxy_sessions`, `20250926000000_proxy_sessions_audit_logs.sql`) that nothing in this ticket's design or review pipeline consulted. First occurrence of this specific gap on this board — not promoted to `engineering-standards.md`'s automatic-failure list off one instance, but a strong candidate if a second new-table ticket misses it too. |
| 2026-09-02 | security | aiorders | `ENG-010`'s security gate needed the real per-ticket/per-day hop ceiling before proceeding and found `departments/engineering/agents/eng-manager/config.yaml` lines 488–489 (`max_hops_per_ticket: 8`, `max_hops_per_day: 40`, commented "← from plan.budgets[plan.tier]") stale against the live `plan.tier: max_5x` (line 416, corrected 2026-08-29), whose actual budget is 20/200. Read `lib/eng-trigger.sh`'s `read_plan_budget()` directly rather than trusting either number: it dynamically resolves through `plan.tier` via awk, so the real enforced ceiling is 20/200 and the stale comment has zero runtime effect — but it contradicts the value it claims to mirror, the exact comment-vs-code class `engineering-standards.md` already names as a defect elsewhere. This is a department template file, out of reach for an instance-scoped pass to fix directly (and gated through Fable per the constitution regardless); worth a correction next time anyone is in that file for another reason. |
| 2026-09-02 | security | aiorders | `ENG-010`'s security gate is the sixth consecutive pass on this ticket (build hop through this one) to find business-os itself sitting uncommitted rather than checkpointing per-pass the way every other ticket's own history on this board does — matches the open, previously-flagged gap (`[[project-buildloop-instance-repo-commit-gap]]` in this session's memory; a prior pass already asked the approver once rather than deciding unilaterally, not re-asked here). Independently re-verified the full uncommitted chain (all five prior hops) against the live `aiorders-api`/`aiorders-admin-hub` repos before relying on any of it for this gate's own verdict — every claim in the ticket log matched the actual diffs, commits, and test results exactly, nothing fabricated or drifted. Recorded here for the count, per that entry's own "don't assume silently" guidance, not to reopen the question. |
| 2026-09-02 | eng-manager | aiorders | `ENG-010`'s release-readiness hop found `skills/release-runner/SKILL.md`'s own Trace section (`traces/devops-{run-id}.json` — ticket, project, autonomy, gates verified, deploy duration, health result, rollback used, cost delta) has never actually been written on this instance: `find traces -iname "*devops*"` returns nothing, and neither `ENG-008`'s nor `ENG-009`'s own release-readiness hops produced one either, per their board-file entries. Not written for this ticket either, matching rather than breaking the established (if skill-non-compliant) precedent — inventing the format unilaterally on a third occurrence felt like more scope than this event's own narrow contract called for. Worth a proposal if a future pass wants this trace to actually exist. |
| 2026-09-02 | eng-manager | aiorders | The 20:30 `scheduled` sweep found `board/_index.md`'s own `Next ID: ENG-026` counter stale — `ENG-026` has been an allocated ticket on this same board since earlier the same day, so the true next free id was `ENG-027`. Corrected inline while already editing that file for the archive roll. One occurrence, not chased further — worth a look if a future ticket allocation ever trusts this line without cross-checking the board's actual highest id. |
| 2026-09-02 | eng-manager | aiorders | A ~22:00 `watch` pass moving an answered `2026-09-01-eng-events-dropped.md` into `inbox/_handled/` silently overwrote a different, already-closed incident report that happened to share the exact same auto-generated filename (both raised 2026-09-01; the date-only naming convention has no collision guard). Caught immediately — the destination showed `git status`'s " M" (modified-tracked) instead of the expected "??" (new) — and fixed in the same pass: original restored from `git show HEAD:...`, the new one re-filed as `...-b.md`, cross-references corrected. No content actually lost, but only because it happened to be git-tracked already; an untracked collision would have been unrecoverable. Worth a look if the pattern recurs: either check the destination doesn't already exist before an inbox→`_handled` move, or give incident filenames a collision-proof suffix (a counter or full timestamp) at creation time instead of just a date. |
| 2026-09-02 | principal-engineer | aiorders | `ENG-008`'s round-3 review (the merge-request fix hop, `accepts_barter` dropped in favor of reusing `barter_visit`) found `ENG-009`'s branch stale against it — `git merge-base --is-ancestor` on both repos' fix commits returns false. **This is the second occurrence today of the exact gap `ENG-009`'s own round-1 review already found and proposed a fix for** (`proposals.md`, 2026-09-02, `principal-engineer`, sized `S`): `ENG-009` was rebased onto `ENG-008`'s round-2 tip specifically to fix the first occurrence, passed its own round 2, and then `ENG-008` moved again (round 3) after that rebase — so the same ticket pair drifted stale a second time in one day, purely because `ENG-008` kept getting corrected after `ENG-009` had already synced against it once. Not fixed here (rebasing `ENG-009` is a different ticket's build work, and it's already `blocked`/`blocked_on: approver` with an open merge request — out of this event's own contract); cross-referenced into both tickets' own logs instead. Not filed as a second proposal — the existing one already covers this shape and would have caught this occurrence too. Flagging the count explicitly: two occurrences, same day, same ticket pair, before the existing proposal has even reached a G1 — worth weighing that when the batched proposal G1 comes up, since `eng_build_loop.md`'s own third-occurrence rule (step 8b, written for exceptions but the same logic applies) says two of the same kind is the point to stop treating it as case-by-case. |
| 2026-09-03 | eng-manager | business-os | Mid-pass, `git log` showed four new commits (`ee63abf`, `8d83eab` predecessors aside — the new ones were `7a2ba52`, `53d4db9`, `43a1f0e`) this pass never made — another active session (this instance had five concurrent interactive `business-os-*` sessions running at the time, per `ListAgents`) committed tonight's accumulated uncommitted backlog in clean, semantic, per-ticket commits, this pass's own mid-flight `ENG-016` rescope work included. Checked carefully rather than assumed benign: `git status`/`git diff` against the new `HEAD` showed the committed content matched this pass's own on-disk state exactly (one line of later drift, from an edit made after the external commit), and nothing this pass had written was reverted, conflicted, or partially captured. First direct evidence, not just standing-default reasoning, on the open question in `[[project-buildloop-instance-repo-commit-gap]]`: business-os commits are apparently happening, just out-of-band from build-loop passes, via an interactive session rather than this loop. Doesn't resolve whether a *pass* should self-commit — if anything it's a reason for a pass not to (a second concurrent committer raises the odds of a genuine race, exactly what `git status` before any destructive op exists to catch) — but it's real signal worth keeping for whoever eventually asks the approver. |
| 2026-09-03 | eng-manager | aiorders | Found during this whole-board `scheduled` sweep: `ENG-026` had two live board files sharing the same id — `ENG-026-foodswipe-channel-visibility.md` (the current, gated ticket) and `ENG-026-foodswipe-multichannel-filters-and-promo-engine.md` (the pre-rescope original, frozen at `intake` since 2026-09-01, carrying an unresolved `priority: now` stamp). The 2026-09-02 rescope that produced the current file wrote a new file instead of editing the original in place, unlike `ENG-016`'s equivalent split the same night. Retired the old file — content folded into the live ticket's log, full original still in git history; see that ticket's own 2026-09-03 log entry for the full account. Proposal filed for the systemic gap. |
| 2026-09-03 | eng-manager | aiorders-api | `ENG-022`'s build hop ran `deno check` against `brand-portal/` — apparently for the first time ever (no CI wiring exists, already a named gap in `config/projects.md`). 9 of the 19 pre-existing errors were `TS2345`: `feedback.ts`'s and `offers.ts`'s 8 broken call sites passing the `SupabaseClient` where `verifyRestaurantAccess`'s first parameter expects a restaurant-id string — the exact wrong-argument-order bug this ticket fixes, already flagged by the compiler and never once looked at. All nine cleared by this ticket's own fix and confirmed against the original tree (`git stash`) before attributing them; the remaining 10 (implicit-`any` catch params, one implicit-`any` callback, a supabase-js generic-select quirk in `website.ts`) are pre-existing and untouched, outside this ticket's own scope. Concrete evidence for the existing gap, not a new one — not re-proposed. |
| 2026-09-03 | eng-manager | aiorders-api | Running `deno check`/`deno test` in `brand-portal/` for `ENG-022`'s self-test generated a `deno.lock` — the first one anywhere in this repo (checked every function directory), never previously tracked or gitignored either way. Left untracked rather than committed: whether this repo wants Deno lockfiles at all is a repo-wide tooling decision one bug-fix ticket shouldn't make unilaterally, and the design scoped CI/tooling wiring out. Worth a look next time Deno tooling is deliberately wired in. |
| 2026-09-03 | eng-manager | aiorders-admin-hub | This `scheduled` sweep's own merge detection re-confirmed the 2026-09-02 finding directly above (`ENG-009` stale against `ENG-008`'s round-3 fix) and found the gap one layer further out: the finding had reached `ENG-009`'s own board-file log but never propagated to `inbox/2026-09-02-eng009-merge-request.md` — the artifact the approver actually reads — which still said `recommendation: merge` and "merge cleanly" until this pass corrected it (and cross-referenced `ENG-010`'s merge request, which inherits the same stale base). Worth naming as its own shape, distinct from the sibling-staleness gap itself: even when a pass *does* catch and log a cross-ticket finding, nothing currently carries it forward into an already-raised gate item sitting in `inbox/`. Not filed as a new proposal — this instance is fixed, and the general fix (checking a gate item's own claims against its ticket's current log before treating it as current) would fold naturally into whoever eventually picks up the standing sibling-staleness proposal, not a separate mechanism. |
| 2026-09-03 | principal-engineer | aiorders-api | `ENG-015`'s round-1 code review is the **third** occurrence this week of automatic-failure #10 on the `admin-portal/handlers/` family (`ENG-013` round 1, 2026-08-29; `ENG-008` round 1, 2026-08-30 — both explicitly logged as "not yet a third, waiting for one more" in `agents/principal-engineer/notebook/`). `skills/code-review-gate/SKILL.md` step 10's promotion threshold is now crossed. Not actioned directly: the target file, `engineering-standards.md`, lives in the read-only department tree, out of reach for an instance-scoped pass — same situation `ENG-010`'s 2026-09-02 security-gate entry hit on a different department file. Worth the promotion (an explicit standing item: "every new/changed access-control branch in `admin-portal/handlers/` needs a colocated `*.test.ts`, per `brands.test.ts`/`loyalty-config.test.ts`") next time anyone with department-tree write access is in that file. |
| 2026-09-03 | eng-manager | aiorders | Reading `config/config.yaml` for `ENG-015`'s review hop found `wip.approver_limit: unlimited` (the file's own comment: raised 2026-09-02 by the approver, no-cap). Every board entry since — including tonight's own header recomputation — has kept treating the limit as 2 and computing/enforcing an `8/2, over cap` state ("nothing new may start needing the approver until enough of these eight clear"). Not investigated or resolved here — outside this narrowly-scoped `continue ENG-015` pass's own contract, and this ticket's own hop neither raises nor touches an approver-facing gate either way — but worth a look: if the config is the live value, the board may be withholding G1s (`ENG-019`–`ENG-021`) it no longer needs to. |
| 2026-09-03 | eng-manager | aiorders-api | `ENG-015`'s round-1-fix build hop self-test hit a flaky failure running `admin-portal/handlers/`'s full test directory together: `brands.test.ts`'s "deriveHealth: exactly 14 days ago is still active (boundary is inclusive)" failed once (77/78), then passed 12/12 twice in isolation and 78/78 on a second full-directory run with no code change in between. `brands.ts`/`brands.test.ts` are untouched by this ticket's diff (`git diff --stat origin/main` empty on both) — a pre-existing wall-clock-boundary sensitivity in a test comparing against `Date.now()`, not a regression. Not fixed — different file, outside this hop's own scope — but worth a look if it recurs: a literal boundary compared against the live clock at test-definition time will occasionally lose a race against however long the rest of the suite takes to run. |
| 2026-09-03 | principal-engineer | aiorders-api | `ENG-015`'s round-2 review found a partner `PUT` containing only restricted fields (e.g. `{approved: true}` alone) strips to an empty `updates = {}` object, which the real `supabase-js`/PostgREST client has never been exercised against on this codebase (no live Postgres reachable this or any prior hop) — unclear whether an empty PATCH body 200s as a no-op or errors. Not blocking: either outcome preserves the security property this ticket exists to enforce, and the only realistic trigger is an exploit-shaped request rather than a normal edit. Worth a look whenever this repo gets a live/staging DB connection back — full reasoning in the review receipt, `agents/principal-engineer/reviews/ENG-015.md`. |
| 2026-09-03 | qa | aiorders-admin-hub | `ENG-015`'s QA pass is the second ticket blocked from full acceptance-criteria coverage by the standing `aiorders-admin-hub`-has-no-test-infrastructure gap (`proposals.md`, 2026-08-31 — `ENG-008`'s round-1 regression test was the first), and the first where the untestable piece is a security enforcement boundary (an RLS `WITH CHECK` plus its frontend counterpart) rather than a read-path UI behaviour. Not re-filed as a new proposal — same open row, cross-referenced. Full reasoning and the static trace performed in place of live execution: `agents/qa/test-plans/ENG-015.md`, `agents/qa/notebook/2026-09-03-coverage-gaps.md`. Worth weighing the severity bump (UI-only → security-relevant) next time the batched proposal G1 comes up. |
| 2026-09-03 | eng-manager | aiorders | Closing the loop on the row directly above (same day, earlier hop): the `wip.approver_limit: unlimited` reading was correct, and this pass's own dispatch step acted on it — `ENG-019`/`ENG-020`/`ENG-021` were exactly the three G1s it predicted were being needlessly withheld, all three complete with no material divergence, all three raised this pass. Gap between flag and fix: same day, roughly two hours across at least one intervening `scheduled` pass that read the config header narrative rather than the config file itself and re-asserted the stale `2`/"over cap" framing before this pass corrected it. Worth noting as a pattern, not just this one instance: a correct observation sitting unactioned is functionally identical to one never filed until something else forces a fresh read of the primary source rather than the board's own cached narrative of it. |
| 2026-09-03 | eng-manager | aiorders-api | `ENG-024`'s build hop hit a `deno check`/`deno test` byonm/`node_modules` resolution failure even when run from the target function's own directory — the workaround `ENG-022`'s review-gate log names (run from the function's own directory, not the repo root) wasn't sufficient on this host. Root cause: an unrelated `~/package.json` + `~/node_modules` at the home directory, which Deno's package.json auto-discovery walks up and finds above the repo root regardless of which subdirectory the command runs from. Fix: `DENO_NO_PACKAGE_JSON=1 deno check --node-modules-dir=none` (same env/flag combination for `deno test`). Worth folding into `config/projects.md`'s Commands section as the real `aiorders-api` invocation once this repo gets its own `deno.json` (`ENG-002`-style test-harness gap already tracked there) — until then, the next pass running Deno tooling on this repo should use this invocation rather than rediscovering the same failure. |
| 2026-09-03 | product-manager | aiorders | Re-checking dispatch order while processing `ENG-016`'s rescope G1 (`decision` event pass), `board/_index.md`'s In-flight table reads `priority:` blank or `next` for `ENG-019`/`ENG-020`/`ENG-021`/`ENG-026`/`ENG-027`, but each ticket's own frontmatter, read fresh, says `priority: now`. Not corrected here — out of this event's own "advance only the ticket it belongs to" scope, and the same root cause the open 2026-09-01 proposal on this file already names (the table needs regenerating from each ticket's own frontmatter, not hand-kept). Worth a look next time anything touches `_index.md`'s table wholesale. |
| 2026-09-03 | eng-manager | aiorders | Firing this pass's own `continue ENG-016` chain found `traces/.pending` 16 events deep (5 collapsed duplicates already drained ahead of it) — `scheduled auto-drain`, `watch launchd` ×3, four `decision` G1s (`ENG-026`/`019`/`020`/`021`), `continue ENG-024`, `decision ENG-027`, five `scheduled` entries whose `context` is a board-file path rather than the usual `launchd`/`auto-drain`/`manual`, and `scheduled launchd`, with `continue ENG-016` landing last. Not investigated — queue depth is reference territory (`eng_build_loop.md`'s own "Cadence"/"The chain" sections), not a normal pass's job, and this pass's own daily counter read `7/200 charged` so nothing here looks like a runaway. Worth a look by whoever next reads the queue: the five board-file-path `scheduled` contexts don't match this department's usual context vocabulary and are worth confirming they're not a symptom of something misfiling `watch` events as `scheduled`. |
| 2026-09-03 | eng-manager | restaurant-marketplace | Processing `ENG-026`'s G1 approval found its PRD (`agents/product-manager/specs/ENG-026-foodswipe-channel-visibility.md`) had neither the frontmatter block (`ticket`/`project`/`status`/etc.) nor the `## Decision` section every sibling PRD carries per `templates/prd.md` — the 2026-09-02 `shaped` pass that wrote it skipped both, and nothing caught it since no gate reads a PRD's own structure. Fixed inline while processing this decision (both added, `status: approved`). Not filed as a proposal — first occurrence seen, fixed cheaply in the same edit the decision already required. Worth a look if a second PRD turns up missing either section. |
| 2026-09-03 | eng-manager | aiorders | This `decision` event pass on `ENG-021`'s G1 found the event had already been attempted once (`traces/eng-loop-2026-09-03.log`, `pass FAILED (exit 1, 379s)`, re-queued, then several `pass NEVER STARTED` rate-limit refunds): that first attempt got as far as writing the ticket's own frontmatter/log update — including prose claiming `decision-journal.md` was written, the PRD was updated, the gate item was moved to `inbox/_handled/`, and `continue ENG-021` was fired — before the session's rate limit cut it off, and none of those four things had actually happened (no `.hops-2026-09-03-ENG-021` trace, gate item still in `inbox/`, PRD untouched, no journal row). The narration was accurate as a description of what *should* happen, just written before it was true rather than after. This resumption verified each claim against the underlying files rather than trusting the prose, then completed the gap. Same structural shape as the already-known "mid-pass expiry" failure mode, different trigger (session/usage limit, not OAuth) — worth a look if a third instance turns up, since the risk is specific: an unresumed instance of this would leave a ticket log permanently narrating completed work that never happened. |
| 2026-09-03 | product-manager | aiorders-api | `ENG-027`'s first G1 declared "there is no order-completion signal in the system" from reading only the discarding side of the webhook path (`external-integrations/handlers/cloudwaitress.ts`'s `event !== 'order_new'` gate). Re-verifying for the rescope checked the other side — the webhook *registration* (`cloudwaitress-middleware/handlers/restaurant.ts`, `AIORDERS_WEBHOOK.events`) — and found AIOrders already subscribes to `order_completed_updated`/`order_cancelled_updated`/`order_cancel`; the signal exists and is discarded, which a check of the handler alone can't distinguish from "the vendor never sends this." A "no signal exists" claim is only as good as the weaker of the two checks — worth treating "we don't act on X" and "X never arrives" as separately-verified claims next time a PRD asserts an integration has no signal for something, rather than inferring the second from the first. |
| 2026-09-03 | architect | restaurant-marketplace | Designing `ENG-026` found this repo's department worktree (`~/Documents/projects/_eng/restaurant-marketplace`, branch `eng/base`) sitting 16 commits behind `origin/master` — and that gap isn't cosmetic: `origin/master` deleted this repo's entire `supabase/functions/*` tree on 2026-08-23 (`f733e68`, "now owned by aiorders-api"), so the stale worktree still shows a complete, plausible-looking backend (RPC caller, fallback query, handlers) that no longer exists on the live branch. A design or build pass that trusts this worktree's own checked-out state instead of fetching and diffing against `origin/master` first would misdirect every backend change into a repo that no longer ships one. Caught here only because this pass happened to cross-reference `aiorders-api`'s own copy of the same handler file before writing anything. Worth a general habit, not just this ticket: `git fetch origin` and a diff-stat against the default branch before trusting any `_eng/` worktree's checked-out state, especially on a repo whose deploy target is frontend-only and whose backend could plausibly have moved elsewhere. |
| 2026-09-03 | architect | aiorders-api | Designing `ENG-020` (marketing ROI/traffic-source reporting) found two of this file's own prior claims wrong once checked against `origin/main` directly. Row 100 (2026-08-29): `customers` has no `utm_source`/`utm_medium`/`utm_campaign` columns — every capture path folds those into a `first_touch`/`last_touch` object before `crm/customers.ts` persists it, and the separate `utm_data` object the same callers send is never referenced there and is dropped on the floor; the real columns are `first_touch_at/_source/_medium/_campaign`, `first_referrer`, `last_touch_at/_source`. Separately (not this file's own row, but the same shape the PRD and ticket Notes both repeated): `restaurant-portal/src/pages/analytics/Index.tsx`, the mock influencer-campaign page, is not reachable from the live app at all — absent from both `App.tsx` and `Sidebar.tsx` — so it's dead code, not a live owner-facing honesty problem. Neither correction changes any open ticket; noted for whoever next reads either claim at face value. |
| 2026-09-03 | architect | restaurant-portal | `ENG-019` (coupon-redemption revenue) and `ENG-020` (traffic-source revenue) were both approved the same evening and are both sitting at `designed` for the same portal — two independent revenue-attribution surfaces that will eventually need to agree on what "revenue" means (period, cancelled-order handling, guest orders) or an owner will notice two numbers that don't reconcile. Not a blocker for either ticket individually; worth someone's attention before both ship. |
| 2026-09-03 | architect | restaurant-portal | This `continue ENG-020` pass's design work cross-checked `ENG-019` for file collisions and found its own `continue` pass appears to have ended without writing `ENG-019`'s board update: the ticket log's last line is still `chained: ENG-019` with no follow-up entry, no design doc exists at `agents/architect/designs/ENG-019-restaurant-marketing-broadcasts.md`, and `traces/eng-loop-2026-09-03.log`'s last `ENG-019`-relevant line reads "I'll pick back up once that finishes — no need to poll in the meantime" (it had delegated the design to a background subagent after filing `ENG-029` as a byproduct, then the session log stops). Not resumed here — out of a `continue ENG-020` pass's own narrow contract (advance only the named ticket). Worth the dead-end sweep's own "broken chains" check picking this up if no later pass has already re-run `continue ENG-019` by then. |
| 2026-09-03 | architect | aiorders | Confirming today's "held at `designed`, owner stays `architect`" convention (established on `ENG-020`'s own design pass) before applying it to `ENG-021` found `ENG-014`'s board file (`agents/eng-manager/board/ENG-014-restaurant-qr-media-self-service.md`, last touched 2026-08-31) sitting at `state: designed`, `owner: eng-manager` — the design was completed and cap-held that day, same situation, but the older pass moved `owner` to `eng-manager` while leaving `state` at `designed`. That combination violates the ticket template's own rule ("`state` and `owner` move together. Changing one without the other is a bug in the pipeline, not a shortcut.") and is also what `ENG-020`'s pass implicitly treated as wrong by keeping `owner: architect` for every ticket in the same held-for-slot pool. Not corrected here — a different ticket, out of this `continue ENG-021` event's own narrow contract — but worth fixing (`owner: eng-manager → architect`, no state change) whenever `ENG-014` is next touched, so the two fields agree again. |
| 2026-09-03 | eng-manager | aiorders | `ENG-016`'s work-breakdown is the first decomposition this board has ever run (no prior ticket carries `parent:`), and `eng_build_loop.md`/`work-breakdown/SKILL.md` don't say whether the machine-WIP-1 cap counts a parent and its dispatched children separately or as one family. Read it as one family — a parent already occupying the slot, plus whichever children work-breakdown dispatches to `building`/`ready`, all count once — because the alternative makes the skill and the cap mutually impossible (the parent always already holds the only slot by the time work-breakdown runs, so no child could ever be dispatched). Also found `eng-gate-check.sh` cites the parent-receipts exemption as "ADR-003," but this instance's real `ADR-003` is a different, unrelated decision (migrations authority) — a citation-label mismatch in a department-template script, not a functional bug. Neither acted on further — full reasoning in `agents/eng-manager/notebook/2026-09-03-eng016-work-breakdown.md`. Worth the approver's eyes on the WIP reading specifically, since it's a first-precedent call on a cap they set directly. |
| 2026-09-03 | architect | aiorders-api | `ENG-029`'s own design pass (reading `autopilot/index.ts` in full to design its ownership-check fix) found a fourth authorization gap on this codebase this week, and it's a genuinely different bug class from the other three — filed separately as `ENG-035`. `ENG-022`/`ENG-029`/`ENG-030` are all "a real ownership-check primitive exists (or should) but is missing, defeated, or unwired"; `ENG-035` is "the code trusts a plain boolean the caller supplies in their own request body as proof of system identity" (`autopilot`'s `systemTriggered` flag, gating a real message-send action, checked *before* even the weak `apikey` gate the other 8 actions get). Two independent bug classes surfacing this densely in one week, both in `aiorders-api`, is worth the approver's or EM's attention as a pattern in its own right — `ENG-030`'s own PRD already flagged the first class (three instances) as worth a dedicated sweep if a fourth turned up; this is that fourth, but of the second class, not the first. Not re-filed as a proposal here — already a ticket (`ENG-035`), named here only for the cross-ticket pattern. |
| 2026-09-03 | eng-manager | aiorders | This `continue ENG-029` pass found its own ticket's In-flight row on `board/_index.md` stale (still `shaped`, hours after the ticket's own frontmatter and log moved to `designed`) and no dated board-index entry anywhere, live or archived, for the pass that actually moved it — that pass's own step 10 (Board update) appears to have never run. **Second instance of the identical shape**, not a new one: this file's own row directly above already logged it once, on `ENG-019`'s `continue` pass never writing its own board update after filing `ENG-029` itself as a P0 byproduct. Both instances share the same trigger — a design pass that discovers and fully files a P0 byproduct ticket (PRD, board ticket, incident notice, chain fire) mid-flight, then never returns to close out its own board entry. Plausible mechanism, not confirmed: the byproduct detour may be consuming the pass's remaining turns/context before it gets back to its own ticket's step 10. Fixed `ENG-029`'s own row this pass (the ticket this event was about); `ENG-035`'s row is a different ticket's gap, left alone, same precedent this file already set for the `ENG-014` owner/state mismatch. Two occurrences now — worth watching for a third per `eng_build_loop.md` step 8b's own "three of the same means the rule doesn't fit reality" logic, even though that line is written for exceptions specifically. |
| 2026-09-03 | architect | aiorders-api | `ENG-029`'s and `ENG-030`'s designs, written the same day, each independently commit to writing their own minimal stubbed `SupabaseClient` object for `Deno.test` (covering `.auth.getUser()` and the `.from().select().eq()...single()` chains `verifyRestaurantAccess`/`isPlatformAdmin` call) — `platform-customer-auth/handler.test.ts`'s own comment already names this repo as having "no precedent or infrastructure" for one. Neither ticket has reached `building` yet, so no actual duplication exists on disk today; worth extracting one shared stub into a test-helper module once whichever ticket builds first lands, rather than two directories growing slightly different stubs of the same shape. |
| 2026-09-03 | eng-manager | aiorders-api | This `continue ENG-031` pass found the `aiorders-api` worktree's branch named `feat/ENG-016-catering-quote-generator` (the parent ticket's slug) rather than `feat/ENG-031-catering-order-capture-migration` (this ticket's own, per `engineering-standards.md`'s `Branch: {type}/{ENG-NNN}-{slug}` convention) - left that way by the prior `continue ENG-031` pass that wrote the migration file and died before committing. No evidence it was a deliberate shared-branch call (nothing in the design or the work-breakdown notebook says so, and one wouldn't make sense given `ENG-033`'s dependency on `ENG-031` being *shipped*, not just built). Fixed via `git branch -m` (zero commits existed on the branch, so nothing was lost). Worth a look if a second parent/child decomposition ever produces the same slip - this is the first ticket-family this board has run through `building`. |
| 2026-09-03 | eng-manager | aiorders-api | The `aiorders-api` worktree still carries an untracked `supabase/functions/brand-portal/deno.lock`, unrelated to any ticket currently in flight (timestamped hours before `ENG-031`'s own migration file, and not part of that or any other diff). Independently noticed and left alone by both `ENG-029`'s design pass and this one, without being cleaned up or its origin explained - worth someone's attention before a third pass has to re-derive the same "not mine, leave it" conclusion. |
| 2026-09-03 | eng-manager | aiorders | `config/conventions.yaml` -> `ticket_log.entry.cap_lines: 20` (landed `660c849`, 2026-09-02 23:54, alongside the `eng-trigger.sh` checkpoint block this same pass's own prompt was generated from) has not been followed by any pass since it landed: `ENG-013` round 2, `ENG-015` rounds 1-2, `ENG-022`, `ENG-024`, `ENG-029`, `ENG-030`, and `ENG-031`'s own prior `building -> in-review` entry all wrote full-reasoning ticket-log entries well over the cap (the last, ~118 lines), with nothing moved to a notebook and no entry naming a notebook pointer. The checkpoint mechanism itself works correctly (this pass's own prompt was built from it), so the gap is pure non-compliance with the entry format, not a broken mechanism. This pass wrote its own `ENG-031` entry under the cap, reasoning moved to `agents/principal-engineer/notebook/2026-09-03-review-log.md` and `agents/qa/notebook/2026-09-03-coverage-gaps.md` - flagging so a pattern is visible before a third-plus occurrence normalizes it further. |
| 2026-09-03 | security | aiorders-api | This `continue ENG-031` pass's own security-gate hop initially wrote a ~100-line ticket-log entry before catching, from the row directly above, that `config/conventions.yaml` -> `ticket_log.entry.cap_lines: 20` applies here too (a `pass` verdict isn't in `over_cap_allowed_when`) - rewrote it down to a ~20-line facts-and-pointers entry before this pass exited, reasoning left in `agents/security/reviews/ENG-031.md` and `agents/security/notebook/2026-09-03-findings.md`. Separately: both this hop's and the immediately prior `in-review -> in-qa` hop's ticket-log entries claim to have fired `/bin/zsh departments/engineering/lib/eng-trigger.sh continue ENG-031` - a path relative to the instance working directory. Verified this pass: that exact relative path does not resolve from the instance engineering root (`test -f departments/engineering/lib/eng-trigger.sh` fails there; `../../../departments/engineering/...` or the absolute path both do resolve). Yet the chain visibly did fire each time - this pass exists because of it. This pass fired the absolute-path form instead (also what this event's own instructions specify verbatim), and left the mismatch unresolved rather than guessing: either the trigger mechanism invokes from a different cwd than an interactive pass sees, or the logged command text is boilerplate copied forward rather than the exact string actually run. Worth a look if a third ticket logs the same unresolvable path. |
| 2026-09-03 | devops | aiorders-api | This `continue ENG-031` pass's release-readiness hop re-checked the `aiorders-api` worktree before opening the PR and found the same untracked `supabase/functions/brand-portal/deno.lock` still sitting there, unrelated to `ENG-031`'s own diff - third independent pass to notice it (`ENG-029`'s design pass, this ticket's own `building` hop, now this one), still uncleaned and unexplained. Left alone again, same reasoning as both prior notices. Worth someone actually picking this up now that three separate passes have each re-derived "not mine, leave it" from scratch. |
| 2026-09-03 | devops | aiorders | This pass's own inbox sweep found `inbox/2026-09-03-eng035-p0-incident.md` (raised by the architect, `ticket: ENG-035`, informational/no-action-needed) alongside a real board ticket at `agents/eng-manager/board/ENG-035-autopilot-systemtriggered-auth-bypass.md` — but `board/_index.md`'s In-flight table has no `ENG-035` row anywhere, and its own header still reads "Next ID: ENG-035" (should be `ENG-036` if the ticket was actually allocated). Not investigated or fixed — out of this `continue ENG-031` event's own narrow contract, a different ticket's gap. This looks like a third occurrence of the exact pattern two rows above already flagged and asked to be watched for (a design pass files a P0 byproduct ticket, then never returns to write its own board-index update) — worth the dead-end sweep treating it as the third instance rather than a fresh one. |
| 2026-09-03 | devops | aiorders | Step 7's 24h notify-sweep, run against the whole inbox rather than scoped to `ENG-031` (the step is in the not-negotiable set and its own wording — "any item" — doesn't scope itself to the ticket a `continue` event names), found `ENG-022`'s merge request over 24h with no nudge and nudged it (see `ENG-031`'s own board-index entry, 2026-09-03, for the full accounting). Left un-nudged, deliberately: the three `gate: incident` P0 notices (`ENG-029`/`030`/`035`), each already carrying `recommendation: fix now — no action needed from you beyond awareness`. Step 7's literal text (`notified: older than 24h, no nudged:, no decision:`) doesn't exclude them, but nudging exists to chase an unanswered decision, and these three explicitly aren't asking for one — `decision:`/`decided:` sit empty by design, not by oversight. No written rule resolves this either way; flagging rather than deciding it unilaterally, since it changes how every future P0 incident notice gets treated, not just these three. |
| 2026-09-03 | frontend | restaurant-portal | Second occurrence of the parent-slug worktree-branch slip a 2026-09-03 row above flagged on `aiorders-api` (`ENG-031`'s `continue` pass, "worth a look if a second parent/child decomposition ever produces the same slip"): this `continue ENG-032` pass found `~/Documents/projects/_eng/restaurant-portal` on `feat/ENG-016-catering-quote-generator` (the parent's slug), 0 commits, clean tree. Fixed the same way — `git branch -m` to `feat/ENG-032-catering-portal-stages-and-itemized-view`, lossless. Two for two on this family's first two sub-tickets to reach `building`; worth treating as a real pattern in `work-breakdown`/`tech-design` (whichever step first touches a family's worktrees) rather than a per-repo coincidence, since `ENG-034` (`config-site-builder`) hasn't reached `building` yet and may show the same thing a third time. |
| 2026-09-03 | architect | aiorders-api | This `continue ENG-035` design pass found `ENG-036` (`outgoing-communications` shares `autopilot`'s exact `systemTriggered`-trusts-the-body bug shape) while reading a file `ENG-035`'s own fix required, one hop further out than `ENG-035` itself was found (`ENG-029`'s design pass found `ENG-035`; `ENG-035`'s design pass found `ENG-036`). Three of this week's four authorization gaps on this codebase were each found as a byproduct of designing the previous one, not through an assigned sweep — worth asking whether a dedicated pass across every edge function for this same client-trusted-flag shape would surface the rest at once rather than one hop at a time. |
| 2026-09-03 | architect | aiorders-api | Noticed only while researching `ENG-035`'s own trigger-authentication design (not chased, out of that ticket's scope): `supabase/migrations/20260217000001_platform_analytics_cron.sql`'s `pg_cron` job calls its own `platform-analytics` edge function via raw `net.http_post` with no `Authorization` header and no body-level flag either — the one confirmed *headerless* trigger convention in this repo, next to two confirmed examples that do carry one. Not a clear P0 on this pass's own limited read (no message send, no PII in the fire-and-forget call itself) — at most a compute-cost/abuse question, worth someone actually reading `platform-analytics/index.ts` rather than inferring severity from the caller side alone. |
| 2026-09-03 | principal-engineer | restaurant-portal | `ENG-032`'s round-1 review failed on automatic-failure #3 (missing regression test on the `CateringPageForm` save-path fix) — the first time this repo's `@testing-library/react`/`jsdom`/`vitest` install (`ENG-002`) has actually mattered to a verdict. The tooling has been there since `ENG-002`; no per-component test file existed yet on this repo before this round needed one. Not filed as a proposal — one occurrence, and the fix is the ordinary one (the next build hop writes the first test) rather than a process gap; worth a look if a second `restaurant-portal` ticket also ships a testable fix with nothing to extend. |
| 2026-09-03 | qa | config-site-builder / aiorders-api | Named directly by ADR-009's Consequences and the `ENG-016` design's own Risks section ("worth a line in QA's test plan and worth an observation; not fixed here"), filed on `ENG-032`'s quality-gate hop rather than left only in the test plan: `get-brand-website` resolves `catering: brand.metadata?.catering \|\| restaurantData.catering \|\| null`, so for any brand with brand-level catering content set, an owner flipping `orderFormEnabled` on through the `restaurant-portal` editor (`ENG-032`) produces no visible change on that brand's public catering page — enablement for those brands is a staff edit at brand level, silently. Pre-existing precedence (it already affects `heardAboutUsOptions` the same way) but newly consequential now that the object carries a functional switch rather than only copy. Nothing to fix on `ENG-032`'s own diff (the precedence lives in `get-brand-website`, untouched here); worth someone checking how many of aiorders's actual multi-location brands set brand-level catering content before `ENG-034` ships the public form this is meant to gate. |
| 2026-09-03 | qa | restaurant-portal | `CateringKanban.tsx` and `CateringDetailModal.tsx` both turned out cheaper to component-test than expected going in — both take their data as props (`requests`/`request`), no internal fetch, no context provider needed, so the "these probably need heavy supabase/router mocking" assumption (reasonable given both transitively or directly import the client) resolved to a one-line `vi.mock` stub never actually exercised in a read-only render path. Worth checking this shape — data-as-props, no internal fetch — before assuming a `restaurant-portal` component needs heavy test scaffolding; it may just need `render()` and a fixture. |
| 2026-09-03 | architect | aiorders-api | `ENG-036`'s design (reusing `ADR-016`'s service-role-bearer mechanism for a sibling `systemTriggered` bug) leaves two near-identical ~20-line `authorizeSystemTrigger` files once both it and `ENG-035` build — `autopilot/marketing/auth.ts` and `outgoing-communications/auth.ts`, same signature, same comparison, same credential (`ADR-017`). Deliberately not extracted to `_shared/` now: `ADR-016`'s own file doesn't exist in the tree yet (`ENG-035` still `designed`, not `building`), so sharing now would couple this ticket's build order to `ENG-035`'s branch for a duplicate with zero behavioral cost. Worth consolidating into `_shared/` once both have shipped, per `ADR-017`'s own Review trigger (extract on a third occurrence of this exact check, same "third exception" line `eng_build_loop.md` step 8b already draws elsewhere) — not urgent, nothing is broken by leaving two copies indefinitely. |
| 2026-09-03 | frontend | restaurant-portal | This `continue ENG-032` fix hop (round 1's missing regression test) found the prior `ready → building` hop's logged lint count — "63 pre-existing problems, confirmed zero new" — doesn't match reality: `npm run lint` on the untouched branch tip (`ab3fa4e`) shows 96, and a throwaway detached worktree at `origin/main` (symlinked `node_modules`, removed after) shows 96 too, so 96 is the real baseline and this ticket has always introduced zero new problems — same conclusion, different number. Most likely an eslint/plugin version or cache difference between that hop's run and this one, not a regression on any ticket. Not chased further (not blocking, re-deriving the exact cause isn't this hop's job) but worth a look if a second self-tested lint count on this repo also turns out stale — a "confirmed zero new" claim being right about the delta but wrong about the total is an easy thing to wave through. |
| 2026-09-03 | frontend | restaurant-portal | This hop wrote this repo's second-ever test file (`CateringPageForm.test.tsx`) and the first one for a component rather than the whole app, establishing the convention the next one can reuse without re-deriving it: colocated `ComponentName.test.tsx` next to the component (matching `App.test.tsx`'s own placement), `fireEvent` from `@testing-library/react` rather than `@testing-library/user-event` (not an installed dependency — adding it for one test would itself be an unjustified new dependency), and asserting on the mock callback's received payload (`onSave.mock.calls[0][0]`) rather than re-querying the DOM after submit. Verified the test actually catches the regression it targets by temporarily reverting the fix, confirming red, then restoring it — worth this becoming the standard verification step for the next test this repo writes too, not just this one. |
| 2026-09-03 | devops | restaurant-portal | `ENG-032`'s release-readiness hop found `.github/workflows/deploy-cf.yml` now exists on `main` (push-triggered, deploys via `npm run deploy-cf`) — this repo had no CI/CD as of `ENG-002`'s own release record (2026-08-26), same "manual, explicitly-invoked step" gap `ENG-011` separately found and fixed on `aiorders-admin-hub` mid-ticket. Not investigated when it landed or who added it — out of scope for this hop — but it changes what a future `restaurant-portal` release-readiness assessment should check: a merge to `main` now auto-deploys, so `gh run list` after the next merge is a real observability signal the way it wasn't at `ENG-002`'s writing, and a rollback is a straight git revert rather than "nothing to undo." Worth whoever next writes this repo's release record confirming the run actually fires and succeeds, the way `ENG-011`'s did, rather than assuming the old no-CI framing still holds. |
| 2026-09-03 | eng-manager | aiorders | This `scheduled` pass's step-10 table-vs-frontmatter cross-check (the standing fix for the already-open "table needs regenerating from each ticket's own frontmatter" proposal) found `ENG-018`'s own `priority: hold` had been silently changed to blank by `2d66236` ("whole-board reconciliation," 2026-09-03T13:25:56-07:00) — a large bundled commit whose message frames the touch as part of that evening's WIP-limit/priority-column correction work, but that work was about fixing the *table's* stale cached priority to match each ticket's *file*, never editing the file's own value. No log entry on the ticket, `decision-journal.md`, or `exceptions.md` documents an approver instruction to un-hold it; every other artifact (header prose ×2, the table, five days of other tickets' own cross-references) still treated it as held. Restored `priority: hold` on the ticket file this pass — full trace on `ENG-018`'s own board-file log. | Distinct from every other "table cell is stale" gap already on this list: those are the *table* lagging the *file*; this was the *file itself* getting overwritten by a bundled reconciliation commit, which is a narrower but sharper risk — it touches the one field this department is never supposed to write to. First occurrence of this specific shape, not a pattern yet (step 8b's "third exception" threshold doesn't apply — this isn't an exception grant). Worth being alert to bundled multi-ticket "reconciliation" commits specifically, since per-ticket review is exactly what a batched commit message discourages a reader from doing. |
| 2026-09-03 | eng-manager | aiorders | Closing the loop on this file's own `continue ENG-020` row above (same day): this `scheduled` pass's own step 8 dead-end sweep confirmed `ENG-019`'s `continue` chain really was broken (14:25:46 fire ran, exit 0, but its background design subagent got terminated at a 600s internal timeout before writing anything — `links.design` still blank, no design file, no board update after `chained: ENG-019`) and no intervening pass had picked it up. Re-fired `continue ENG-019` this pass; full account on the ticket's own board-file log. Root cause worth naming plainly since it's now confirmed, not just suspected: a `continue` pass that delegates its actual work to a background subagent and then gets killed by that subagent's own timeout exits 0 and writes a normal-looking `chained:` line, which is indistinguishable from a healthy chain to every mechanical check this department runs (`lib/eng-gate-check.sh` included) — only a human or a dead-end sweep reading the ticket's own artifacts (not just its log line) catches it. Filed as a proposal (`proposals.md`, 2026-09-03, eng-manager, aiorders) rather than left implicit here. |
| 2026-09-03 | architect | aiorders-api | `ENG-019`'s design pass (run in-session this time, not delegated, after the row above's timeout) read `supabase/functions/README.md`'s "Known issues" section directly while confirming `brand-portal`'s access-check convention for the new `broadcasts.ts` action file. It still describes `offers.ts`/`feedback.ts` calling `verifyRestaurantAccess` with the wrong argument order — but `offers.ts`, read directly off `origin/main` (commit `78194da8`), calls it correctly (`verifyRestaurantAccess(restaurant_id, supabase, user)`, matching the real `(restaurantId, supabase, user, options)` signature). Consistent with `ENG-022` ("Fix broken restaurant-scoped access check on 5 brand-portal handlers," merged this evening) having actually fixed this without the README's own note being updated to match. Not touched here — out of `ENG-019`'s own scope — but worth a one-line README fix whenever `brand-portal` is next touched, so the "Known issues" section stops naming an issue that's already resolved. |
| 2026-09-03 | eng-manager | aiorders | This `scheduled` pass's own step-5 merge detection found `ENG-032` merged (see the header/In-flight-table update, same pass). While writing that up, `board/_index.md`'s prior "Waiting on the approver" paragraph for `ENG-032` read "P1 fix for a live cross-tenant restaurant-visibility/write exposure" — that sentence describes `ENG-015`, not `ENG-032` (catering-board stages/itemized-view/order-form switch, `severity: P2` on its own frontmatter throughout); a copy-paste bleed from the adjacent `ENG-015` paragraph, not a real severity or scope claim about `ENG-032`'s own diff. No operational consequence (the ticket's own frontmatter was never wrong, and this pass's receipt re-reads confirmed the real gates/verdicts directly rather than trusting that sentence), but left uncorrected in the archived dated entry itself — board-index prose isn't rewritten after the fact, only the live header/table. Worth a second look if a future pass finds another cross-ticket description bleed in this file — the two paragraphs sit adjacent in the source, which is the likely mechanism (an edit anchored on the wrong nearby block). |
| 2026-09-04 | security | aiorders-api | `ENG-033`'s security gate found `catering-request/validation.ts::isValidSelections` caps `note` at 500 chars but sets no length bound on `selections[].name`, the sibling field validated in the same function. Not a defect against this ticket's own spec — the design's own validation table doesn't call for a `name` cap either — and not exploitable beyond amplifying this already-unauthenticated, already-rate-limit-free endpoint's accepted spam profile (bounded by the existing 200-element cap and whatever platform request-size ceiling applies). Full detail: `agents/security/reviews/ENG-033.md`, Finding 2. |
| 2026-09-04 | eng-manager | aiorders | This `scheduled` sweep's dead-end check re-derived, from each ticket's own log prose, that all twelve `designed`-state tickets on the board (`ENG-014`, `ENG-017`, `ENG-019`, `ENG-020`, `ENG-021`, `ENG-023`, `ENG-025`, `ENG-026`, `ENG-029`, `ENG-030`, `ENG-035`, `ENG-036`) are genuinely in the "cap-held-after-completion" sub-state (`ENG-014`'s own 2026-08-31 naming) rather than "un-designed," before concluding their repeated `chained: none — held by the machine-WIP cap` lines are correct rather than a propagated broken chain — the same reasoning cited on all twelve read, out of context, exactly like the bug this section exists to catch. A cheaper check reaches the same answer: every one of the twelve already has `links.design` populated with a path that exists on disk and holds real content (145–503 lines each), while a genuinely un-designed ticket's `links.design` is still empty. Worth checking `links.design` non-empty as the first, cheap signal before reading a `designed`-state ticket's log prose in full — would have saved several full-file reads this pass alone. |
| 2026-09-04 | eng-manager | aiorders-api | `ENG-036`'s P0 incident notice was answered "any code using it will break, that needs a ticket" — a reasonable general instinct, but this ticket's own design had already enumerated every caller and confirmed none needs a code change, so no ticket had actual content to carry. The incident notice is necessarily written before design happens, so it can never carry the design's own caller-safety evidence yet — a "won't this break things" reply to one of these notices is expected, not a sign of anything rushed. One data point; worth a second before considering whether the notice template itself should get updated once a design lands. |
| 2026-09-04 | eng-manager | aiorders-api, aiorders-admin-hub | `ENG-009`'s own board log had left "how does a stacked-branch ticket actually reach `main`" as an open question with two named options (fresh PR from the stacked tip, or extract commits onto a clean branch). This pass's own step-5 re-check found the answer: this approver opened a fresh PR from the stacked tip straight to `main` on each repo (`merge/ENG-009-ENG-010-to-main`), by hand, within minutes of this pass running — the first option, taken without being asked. Also notable: this is the first time a `watch` event pass, not a `scheduled` safety-net sweep, caught a production merge — step 5's own text conditions it on "the changed file is a merge-request item," which this pass's own trigger (a P0-incident self-echo) technically wasn't, but the section reads step 5 as warranted whenever open merge-request items exist on the board at all, and this occurrence is a concrete data point for why: the merge that mattered happened only minutes before this pass ran, well inside a `scheduled` sweep's multi-hour gap. Not filed as a proposal — no process gap, the existing branch-ancestry check caught it correctly without needing to know the new PR numbers in advance. |
| 2026-09-04 | eng-manager | config-site-builder | `ENG-034`'s build hop found `ENG-031`/`ENG-032`/`ENG-033`'s own build-hop log entries were each written long-form, directly in the ticket log (100+ lines in places) rather than routed through `config/conventions.yaml` → `ticket_log.entry.reasoning_goes_to` (a notebook file), which is what the same file's `cap_lines: 20` is meant to make possible — none of the three is a `gate_fail`/`incident`/`approver_answer_verbatim` exemption. This hop followed the written convention instead (concise ticket-log entry, full reasoning in `agents/eng-manager/notebook/2026-09-04-eng034-build.md`) rather than repeating the drift. Not fixed here — named so a future pass can decide whether to adjust the convention or the practice. |
| 2026-09-04 | product-manager | aiorders | `ENG-016`'s own acceptance-check (the first parent-ticket one this board has run) found three of its four children (`ENG-031`/`032`/`033`) had reached `verified` via the receipt-bookkeeping shortcut — review/QA/security receipts re-confirmed, but no criterion individually walked against live code, the same gap the standing `proposals.md` row already names board-wide. The parent's own acceptance-check can't itself be shortcut away (its trigger has no parent carve-out), which makes it the last point before the family goes terminal — this pass used it to walk the five never-checked criteria (AC-5/6 storage half, AC-7, AC-8, AC-10, AC-12, AC-13) fresh against `origin/main`, rather than inheriting the children's `verified` label as a proxy. Worth naming as a reusable pattern for the next multi-child family, not written as a rule off one data point: a parent's own acceptance-check is a natural backfill point for whatever its shortcut-shipped children never had individually verified. |
| 2026-09-04 | product-manager | aiorders-api | While reading `brand-portal/catering.ts` for `ENG-016`'s own AC-13 (read fresh against `origin/main`, not just cited): `update_catering_request` checks `verifyRestaurantAccess` against the row's *existing* `restaurant_id`, then applies the caller's `updateData` object unrestricted — if that object can carry a `restaurant_id` field, a staff member could reassign their own restaurant's catering request to a different `restaurant_id`. Pre-existing code, untouched by any of the four `ENG-016` sub-tickets, so out of this ticket's own acceptance scope — not traced further into the frontend caller to see whether the field is actually reachable. Named for whoever next touches this handler, not filed as a finding. |
| 2026-09-04 | eng-manager | aiorders | `ENG-019`'s `work-breakdown` run (second time this skill has fired, after `ENG-016`) found `skills/work-breakdown/SKILL.md` step 1 naming a config key that no longer exists: "check WIP against `config.yaml` → `wip.limit` (default 2)". Both `config/config.yaml` (instance) and the department-template `config.yaml` have carried `wip.machine_limit`/`wip.approver_limit` instead since the 2026-08-29 correction — no `wip.limit` key exists in either file, and hasn't since before this skill's first-ever run. Read the current authoritative value (`machine_limit: 1`) rather than the stale name; didn't block anything this time since the ticket's own family already held the slot it needed. Not edited — skill files go through Fable only, per this repo's own `CLAUDE.md`. Worth a Fable pass updating the reference before a future work-breakdown run reads it literally and second-guesses a correct dispatch. |
| 2026-09-04 | database | aiorders-api | This host has docker and an authenticated, linked `supabase` CLI (`bmnmnejwdxbcqinqkwko`) — confirmed during `ENG-037`'s build hop, where `supabase db dump`/`supabase db query --linked` both worked with no password prompt. Every prior migration ticket on this board (`ENG-031`, `ENG-007`, `ENG-011`, `ENG-013`) recorded "no docker, psql, or supabase CLI on this host" and fell back to repo-grep-only verification — that fallback is no longer this host's only option. Durable copy in `agents/database/notebook/`; a full local replay of this repo's tracked migration history still isn't possible regardless of tooling (the earliest tracked migration assumes `public.restaurants` already exists — same untracked-base-tables gap `ADR-006`/`ENG-020`'s design already recorded), but schema-only dumps, narrowly-scoped read queries, and a disposable-container test seeded from a live dump all work fine. |
| 2026-09-04 | eng-manager | aiorders-api | `ENG-037`'s build hop found the shared `_eng/aiorders-api` worktree mid-checkout on `ENG-033`'s own already-pushed branch at pass start — the third time a database-family ticket's own pass has found this shared worktree occupied by a sibling/unrelated ticket's branch (after `ENG-031`'s own "worktree found mid-build" and `ENG-033`'s own dispatch). Each time it's been safe to switch away (nothing lost, since commits persist independent of what's checked out) — named here only because a fourth occurrence would be worth checking whether tickets should record which branch they left the shared worktree on, so the next pass doesn't have to re-derive "is this safe to switch away from" from `git log`/`git status` each time. |
| 2026-09-04 | principal-engineer | aiorders-api | `ENG-037`'s combined review+quality hop found the design's own `## Data` section names `customers`/`offers`/`orders`/`communication_log` as tables defined in no tracked migration in this repo, but not `communication_templates` — which this same review found is *also* untracked (`grep -rn "email_subject\|sms_body\|email_body" supabase/migrations/*.sql` across the whole repo returns only `ENG-037`'s own new file, even though `ENG-037`'s migration explicitly copies that table's column shapes "byte-for-byte"). Verified against the live schema directly instead of the design's incomplete list. Worth knowing for any future ticket that reads `communication_templates`' shape the way this one did — it isn't in the design doc's own inventory of untracked tables. |
| 2026-09-04 | eng-manager | aiorders | This `continue ENG-037` pass's own chain fire (`eng-trigger.sh continue ENG-037`) found the loop lock held by PID 21578 (`eng-trigger.sh scheduled manual`), alive, elapsed `04:28:00` — well outside every pass duration seen anywhere in this file or the trace logs so far (the longest on record is `1486s`/~25min). The fire correctly queued itself (`traces/.pending`: `1 continue ENG-037`, behind an already-queued `1 watch launchd`) rather than being lost — the queue's own design handled this exactly as intended. Checked, not assumed: `21578`'s own `ps` entry confirms it's a real, separate, still-alive process, not a stale lock file or this session's own PID. Also checked whether this is already a known/handled mechanism: `eng-trigger.sh`'s only self-raised "stall" concept (`backoff_arm`/`stall_notice`) is scoped to the *never-started* back-off window (a vendor-limit signature with short output and short duration), a different case from a lock held by a process that's genuinely alive and running — there is no existing timeout/staleness escalation for *this* shape at all, and no incident type in `eng_build_loop.md` step 4's list matches it either. Not filed as a proposal — one data point, and a `scheduled manual` sweep touching many tickets could legitimately run long; worth a look only if a pass is still found holding the lock this long a second time. |
| 2026-09-04 | security | aiorders-api | `ENG-037`'s security gate found its `broadcast-dispatch-tick` cron job dereferences its Vault secret (`service_role_key`) via a live subquery inside the stored `cron.schedule` body (`... || (select decrypted_secret from vault.decrypted_secrets where name = 'service_role_key')`), never a precomputed value — so `cron.job.command` (and any log of it) only ever holds the by-name reference, not the resolved credential. This is a new pattern for this repo (no prior `pg_cron` job authenticates at all), and it's the safe shape specifically because the lookup runs at each tick rather than at schedule time. Worth a future `pg_cron`-authenticated job copying this exact shape rather than building the header string once and passing the resolved value into `cron.schedule` — that variant would leak the secret into the job's own stored definition. |
| 2026-09-04 | devops | aiorders-api | `ENG-037`'s release-readiness hop read the migration SQL directly (not the ticket's own Outcome section) and found `broadcast-dispatch-tick` calls `net.http_post` unconditionally every 5 minutes — it never queries `broadcast_campaign_recipients` for pending work first, that check is `ENG-038`'s own dispatcher function's job. The ticket's own prose ("the tick finds empty tables and no-ops") describes `ENG-038`-shipped-and-idle behavior, not the actual gap: from whenever this migration is pushed live until `ENG-038` deploys, the call target doesn't exist and every tick 404s, a distinct and earlier window than the already-documented "401 from missing Vault secret" one. Judged non-blocking this pass (no auto-deploy on this repo, nothing external reaches the path yet, the gap self-closes once `ENG-038` ships) and named precisely in the PR body so it isn't read as a regression later — full reasoning `agents/devops/notebook/2026-09-04-release-readiness-log.md`. Worth tightening this repo's documentation convention for the next poller-shaped cron ticket: "the callee no-ops on empty state" and "the tick itself checks before calling" are different claims, and this ticket's prose blurred them. |
| 2026-09-04 | eng-manager | aiorders-api | `2026-09-04-eng016-continue-piece2-question.md` (a `gate: intake-question` item, raised earlier today) carries only `raised:`/`notified:` in its frontmatter — no `nudged:` or `decision:` key at all, unlike every other `intake-question` item checked for comparison (`eng023`, `eng026`, `eng007`, all now `_handled`), which all carry at least `decision:` and usually `nudged:` too. No effect today — this pass read the `## Decision` section's own prose directly rather than the frontmatter key, and the item is well under the 24h nudge threshold regardless — but a script keyed strictly off frontmatter-key presence rather than the prose section could treat this file differently from its siblings. Not fixed here; one data point. |
| 2026-09-04 | devops | aiorders-api | This `scheduled` sweep's own step-5 merge detection on `ENG-037` used the newly-discovered linked `supabase` CLI to check live production directly rather than leaving "did this actually deploy" as the usual open question — all three tables and `broadcast-dispatch-tick` confirmed present and `active` in `bmnmnejwdxbcqinqkwko`. That same check turned a named-but-hypothetical gap into a confirmed live one: the cron tick's pre-`ENG-038` 404 window (`aiorders-devops` notebook, this date) is not just possible, it is happening every 5 minutes right now. Still non-blocking for every reason already given at release-readiness, but worth a habit going forward: when this CLI is reachable, a release record can confirm live state directly instead of writing "unknown from here" the way every `aiorders-api` release since `ENG-007` has — cheap to check, and it upgrades an assumption into a fact. |
| 2026-09-04 | principal-engineer | aiorders-api | `ENG-038`'s review round 2 found `board/_index.md` held only two dated pass entries at start (not three) — the immediately prior pass (the SMS-copy fix hop, `building → in-review`, same ticket) does not appear to have appended its own dated entry the way every other pass on this ticket did, even though the ticket's own board-file log got a full entry for it. No functional consequence found (the In-flight table row and the ticket's own log were both current), and not reconstructed retroactively here — just flagged in case it's the first sign of a pattern rather than a one-off skip. |
| 2026-09-04 | backend | aiorders | This corrects the row above rather than confirming it: this `continue ENG-038` pass counted `board/_index.md`'s dated `## {date} —` headers directly (`grep -n "^## "`) before touching anything and found **four**, not "only two" — `watch`, `scheduled`, and both `ENG-038` review rounds — meaning the file was over the keep-three cap, not under it, and had been since round 1's own entry landed (neither round 1 nor round 2 rolled before appending). Also: neither the initial `ready → building` build hop nor the SMS-copy-fix hop ever appended a dated entry at all, anywhere (checked both `_index.md` and `_index-archive.md`) — so the row above's "unlike every other pass on this ticket" doesn't hold up either; only sweeps and FAILed review rounds have ever gotten one for `ENG-038`, never a plain `building`-side hop. Rolled the oldest (`watch`) to `_index-archive.md` to restore the file to three, per step 10. |
| 2026-09-04 | principal-engineer | aiorders-api | `ENG-038`'s review round 3 found three AC-coverage gaps (`getBroadcastReport`'s redemption/revenue and delivery-by-channel logic, the whole `broadcast-unsubscribe` mechanism) that neither round 1 nor round 2 had named — both of those rounds' own coverage notes are entirely about the dispatcher's claim/promote/enroll path, the specific area each round's named finding pointed at. A review round scoped around fixing one named finding naturally re-reads the code near that finding closely and the rest of the same diff only lightly; on an `L`-sized ticket with several independent surfaces, that can leave a far corner of the same diff under-examined for multiple rounds without anything in the process flagging it. Not filed as a proposal — the fix for *this* instance is already in motion via the quality-gate fail — but worth a look if a future large ticket shows the same shape (a late round finding something structurally unrelated to every prior round's own finding). |
| 2026-09-04 | principal-engineer | aiorders-api | `ENG-038`'s round 4 quality-gate re-check found a gap that round 3's own test-plan had already identified but folded into another gap's blocking status for reporting efficiency (AC5's write-half test, deferred under "the read half already fails the gate for this AC") — the round-3 fix hop closed the three numbered gaps and never picked up that parenthetical, so the deferred item nearly shipped silently once its cover (Gap 2) was fixed. Filed fresh as Gap 4 this round rather than waved through. Worth QA giving every zero-coverage acceptance-criterion row its own numbered gap even when a sibling row on the same AC is already failing, so a later round's "are the named gaps closed" check can't stop short of "is the acceptance table actually all green." One occurrence, not a pattern yet — not filed as a proposal. |
| 2026-09-04 | security | aiorders-api | `ENG-038`'s security gate found it was about to ship a new action (`broadcast_message`) directly onto `outgoing-communications`' shared systemTriggered auth bypass — a hole already filed as `ENG-036` (P0, 2026-09-03) but still unbuilt. `ENG-036`'s own PRD couldn't have named this action; it didn't exist when that ticket was filed a day earlier. Five review rounds on this ticket (code review + quality gate) never surfaced it either, since neither reviews that function's own upstream entrypoint — `outgoing-communications/index.ts` is outside this diff's own file list, so nothing prompted anyone to open it. The gap isn't any single round's miss: no step in this department's process cross-checks an in-flight ticket's new capability against *already-filed-but-unfixed* security tickets whose blast radius it's about to extend, and a ticket's own diff review has no reason to open a file the diff never touches. Worth considering whether the security gate (or an earlier one) should routinely check "does this diff add a new consumer of a function with an open, unfixed security finding against it" — grep the security notebook / open `type: security` tickets for the shared file/function name — rather than relying on the reviewing agent happening to trace the call graph far enough by hand, the way this pass did. Not filed as a proposal yet — one occurrence; worth revisiting if a second one turns up. |
| 2026-09-05 | product-manager | aiorders-api | `ENG-038`'s acceptance-check started from release-readiness's own snapshot ("`broadcast-dispatch`/`broadcast-unsubscribe` not deployed") and re-checked it live rather than trusting it, since acceptance-check's whole job is checking the live result — found all four touched functions (`brand-portal`, `broadcast-dispatch`, `broadcast-unsubscribe`, `outgoing-communications`) already deployed, 3-8 minutes after the PR merged, no tracked workflow having done it. Nothing broke and nothing here needed correcting, but it means a gate's own "not deployed" receipt, written earlier the same evening, was already stale by the time the next gate read it — on a project where a human can merge and deploy by hand outside every tracked workflow, "not deployed" is only true at the moment it's checked, not a durable fact later gates can cite secondhand. Worth re-verifying live state at each gate that depends on deploy status, rather than passing forward the last pass's own answer, if this happens a second time. |
| 2026-09-05 | principal-engineer | restaurant-portal | `ENG-039`'s review read `BroadcastComposer.tsx`'s reused `EmailEditor` (pre-existing, unmodified by this diff) closely enough to note its preview renders owner-authored content via `dangerouslySetInnerHTML`, and that same raw HTML is what actually gets emailed to customers server-side — a pre-existing pattern across every reactive automation that already uses this component, not new here. What's new is reach: broadcasts is the first consumer that can fan a single composed message out to an owner's entire customer list in one send, versus one customer at a time. Not filed as a bug or fixed in this diff (`EmailEditor` itself is untouched, out of this ticket's scope) — worth a look if broadcast volume grows, since the blast radius of a malformed or malicious paste just grew with it. |
| 2026-09-05 | devops | aiorders-api | `ENG-039`'s release-readiness hop nearly filed a fresh proposal for "SMS delivery is mocked" before grepping `proposals.md` first and finding a 2026-09-03 (`architect`) row on the identical file, already corrected once (2026-09-04, `ENG-038`'s build hop). That correction narrowed the exposure for three pre-existing trigger call sites but, in doing so, also named — without stating the consequence — that `ENG-038`'s own new broadcast call site was exempt from the bug that made those three fail loudly. Nobody had connected that exemption to "therefore broadcasts hit the original, scarier, fully-silent failure mode" until this pass traced both `sendSMS` call sites directly. Two corrections had accumulated on one row without either one updating the row's own "Why it matters" cell to reflect the net effect — each addition was locally correct and left the row as a whole harder to read correctly at a glance, exactly the condition under which a third hop nearly duplicated it instead of finding it. Worth a habit, not a proposal on its own: when correcting a proposal row a second time, also re-state (or explicitly reaffirm) the "Why it matters" cell against the corrected facts, not just append another caveat to "What." |
| 2026-09-05 | eng-manager | aiorders | This `scheduled (auto-drain)` pass drained at 09:56:44 PDT, two seconds after the immediately-preceding `watch (launchd)` pass released the lock at 09:56:40 — that pass had already re-verified `ENG-039`'s merge status, swept all three watched inboxes, and run `eng-gate-check.sh`, finding zero unprocessed work. Same shape as the open `.watch-seen`/`eng_stamp()` fingerprint-timing proposal (2026-08-28 row), one step further down the queue: the watch pass's own fingerprint-changing writes (its board-file log append) queued this scheduled fire in turn, which then inherited an already-current board with nothing left to find. Verified fresh rather than trusted before concluding no-op: `git fetch` + `merge-base --is-ancestor` on `ENG-039`'s recorded commit against `origin/main` (still not an ancestor), `gh pr view 3` (`OPEN`, `mergedAt: null`); all four `inbox/` items re-read, none carrying a `decision:`; `agents/product-manager/inbox/` and `agents/eng-manager/inbox/` hold only `.gitkeep`/processed folders; whole-board `chained:` sweep across all 16 in-flight tickets, each ending in a justified `chained: none` (WIP cap, awaiting approver, or `priority: hold`); `traces/.pending` empty, no `*-eng-events-dropped.md` for today; `lib/eng-gate-check.sh` whole-board, exit 0. No ticket-log or board-index edit made — no new fact for either to carry beyond what the watch pass's own entry on `ENG-039`'s board file already states. |
| 2026-09-05 | architect | aiorders-api | `ENG-027`'s tech-design pass needed to confirm `sendFeedbackQueueMessage`'s actual delay (to decide whether to reuse it for the loyalty auto-complete timer) and found the call site's own in-code comment reads "Send message to feedback queue with 5 second delay" (`cloudwaitress.ts`, the line calling it) while the function's own default parameter is `3*60*60` — three hours (`cloudflare-queue.ts:73`). Stale comment, not a behavior bug — the actual delay used in production is the 3-hour default, matching what this and other tickets' designs already cite. Noticed only because this design happened to need the exact value; not this ticket's file to fix. |
| 2026-09-05 | product-manager | aiorders | `ENG-019`'s own parent acceptance-check is the first on this board where the check found zero backfill work — contrast with `ENG-016`, where 3 of 4 children shipped via the receipt-bookkeeping shortcut and left several criteria never individually walked until the parent's own check did it. Here both non-schema children (`ENG-038`, `ENG-039`) had already run `acceptance-check` in full at their own shipping point, each one citing the open receipt-bookkeeping proposal (2026-09-04) as the reason not to take the shortcut, so this parent check was a rollup plus a fresh no-drift confirmation. One data point, not evidence the standing proposal is resolved — both children compensated for the named gap by individual judgment call, not by a mechanical fix (neither of the proposal's two suggested fixes exists yet). |
| 2026-09-05 | product-manager | aiorders | While tracing `ADR-003`'s citation for `ENG-019`'s parent exemption, `lib/eng-gate-check.sh`'s own comments point at `agents/architect/decisions/ADR-003-parent-ticket-receipts.md` — that file does not exist on this instance; this instance's actual `ADR-003` is `aiorders-api-authoritative-for-migrations`, an unrelated decision. The enforcement logic itself is unaffected (it doesn't read the file, only cites it in prose), but the citation doesn't resolve to anything on this instance — likely a life-os-origin reference baked into the shared department template that never remaps per instance. Not fixed here (out of scope for this ticket's own `continue` pass). |
| 2026-09-05 | backend | aiorders-api | `ENG-020`'s build hop, editing `supabase/functions/README.md` to add the new `get_acquisition_report` action, found the file's own "Known issues" list (and the matching cross-reference in the `brand-portal` section's Notes) still describes `feedback.ts`/`offers.ts` calling `verifyRestaurantAccess` with arguments in the wrong order and `customers.ts` never checking the result — but `ENG-022` (shipped, `verified`) already fixed all three: `feedback.ts`/`customers.ts` now use the throwing `requireRestaurantAccess`, and `offers.ts` now calls `verifyRestaurantAccess` with the correct argument order. Confirmed by reading all three files directly on `origin/main`, not assumed from the ticket history. Not fixed here — a doc-cleanup leftover from a different, already-closed ticket, not part of this one's own diff. |
| 2026-09-05 | eng-manager | restaurant-portal | `ENG-020`'s quality-gate round 2 found its own round-1 finding (Gap 3, AC4) had been closed only in part: the gap's own narrative named three unverified things (the disclaimer paragraph, the coverage sentence, the low-volume caveat) but its "Specific fix" bullet asked for tests covering only the first, and the round-1 fix hop — reasonably — built exactly what the fix bullet specified, shipping the other two still untested (`grep` confirmed `low_volume`/`attributed_pct` are set in every fixture but never read back with `getByText`/`findByText`). Filed fresh as Gap 4 rather than waved through. Second occurrence of this general shape after `ENG-038`'s round-3→4 finding (2026-09-04 row, same file: a gap's own narrative naming more than its fix bullet delivers, and a fix hop that follows the bullet literally leaves the rest silently unclosed) — that one was a gap folded into a sibling's blocking status and left unnumbered; this one is a fix bullet that under-specified its own gap's narrative. Different mechanism, same failure shape twice now. Worth QA writing every gap's "Specific fix" to enumerate everything the gap's own narrative names, not a subset of it — not yet filed as a proposal (two occurrences, different immediate causes), but a third would make it one. |
| 2026-09-05 | eng-manager | aiorders | This `scheduled` sweep's dispatch of `ENG-021` into the freed machine-WIP slot found `ENG-028`'s In-flight table row in `board/_index.md` carrying a blank `priority` cell against its own frontmatter's `priority: now` — corrected in this pass's board update. No functional consequence (the ticket was already excluded from the held-for-slot pool by `state: awaiting-scope`, not by priority), but left wrong it would mislead a reader checking the table rather than the ticket file. |
| 2026-09-05 | eng-manager | aiorders | `board/_index.md`'s three live dated `## {date} —` entries are not physically ordered newest-first against their own subject ticket's append-only log timestamps — before this pass's edit, the on-disk order was release-readiness / round-3-combined / security-round-1, but `ENG-020`'s own board-file log shows round-3-combined happened first, then security-round-1, then release-readiness. Rolled the chronologically-correct oldest entry (round-3-combined) to `_index-archive.md` rather than whichever was physically first, and appended this pass's own new entry at the true end of file, but did not reorder the two entries left in place — that's a bigger edit than this dispatch needed. Worth a look by whoever next touches the rolling mechanics in `eng_build_loop.md` step 10: either passes should insert at a fixed point (top or bottom, consistently) or the "newest first" wording needs to say which position is authoritative when it drifts. |
| 2026-09-05 | eng-manager | aiorders | Second occurrence of the row directly above, same pass-type (`continue`, work-breakdown), same file: this `continue ENG-021` pass found `_index.md` still holding the two entries the prior pass explicitly declined to reorder (`release-readiness` physically first, `security-round-1` physically second, chronologically the reverse). Resolved it the same way — cross-checked `ENG-020`'s own state-transition titles (`in-security → ready-to-ship` precedes `ready-to-ship → blocked`) rather than trusting physical position — and archived `security-round-1` as the true oldest. Two consecutive passes now needing to manually re-derive chronology before archiving; a third occurrence would cross the line from "worth a look" to "fix the mechanism," per this file's own third-occurrence convention. |
| 2026-09-05 | eng-manager | aiorders-api | `ENG-021`'s work-breakdown pass tried `supabase projects list` to verify the design's own required precondition (RLS enabled on `ai_conversations`) ahead of dispatching the frontend sub-ticket, and found the CLI installed but not `LINKED` to any project on this host — unlike the state a 2026-09-04 row in this file recorded (`ENG-037`'s build hop: "authenticated, linked `supabase` CLI (`bmnmnejwdxbcqinqkwko`)... no password prompt"). Either the link is per-worktree (this check ran from the instance root, not an `aiorders-api` checkout) or it lapsed since. Not chased further — the check was carried into `ENG-041`'s own Notes as a required first build step instead, to run from the right worktree. Worth confirming which, before a future pass assumes live-DB checks are free just because one dated row says they were once. |
| 2026-09-05 | backend | aiorders-api | `ENG-040`'s build hop re-ran the `deno check` baseline the ticket's own Notes cite ("known 10 pre-existing", `observations.md` 2026-09-03): whole-`brand-portal/` count is now 33, not 10 — unrelated tickets (`ENG-020`'s `acquisition.ts`/`channels.ts`, `ENG-038`'s `broadcasts.ts`) added their own untyped-catch-block errors since that entry was written, and nobody's re-baselined since. This ticket's own single-file `website.ts` check (the only one it's scoped to) went 7→8, exactly the one new `data?.faqs` `GenericStringError` instance the design predicted in shape but not in count ("adding a third key does not change... its count" — it does, by exactly one, same shape). Not fixed, not chased further — just flagged so the next ticket citing "the known 10" checks first rather than trusting the cached number. |
| 2026-09-05 | eng-manager | aiorders | This `watch (launchd)` pass's own PID/argv check found the held lock's process (`traces/.loop.lock/pid`: `2448`; `ps`: started 20:30:05, still alive, `26:30` elapsed) still showing argv `eng-trigger.sh scheduled launchd` — its original invocation — even though `traces/eng-loop-2026-09-05.log` confirms the same process went on to drain and launch this `watch (launchd)` pass (`pass end: scheduled` 20:45:56 → `queue: collapsed 1 duplicate event(s)` → `draining queued event: watch (launchd)` → `pass start: watch (launchd)` 20:45:58 → `launching claude` 20:46:00). This contradicts the 2026-08-29 row's own finding for the identical PID+`ps` method (`ENG-007`'s G1→G2 drain), which confirmed the wrapper re-execs per drained item, argv updating each time. Here it didn't: one held process, two consecutive events (`scheduled` then `watch`), one argv throughout. Whether re-exec only fires for `decision`-with-filename contexts (the only kind the 2026-08-29 row checked) and not a bare `scheduled`→`watch` pair, or this is a regression, wasn't chased further — the loop log's own `pass start`/`draining queued event` lines were independently sufficient to confirm this pass's real event, which is what mattered here. Worth knowing either way: `ps` argv on the lock-holding PID is not a reliable cross-check for which event is currently running, despite the earlier row's own success leaning on it — the loop log is the authoritative source. |
| 2026-09-06 | product-manager | aiorders-admin-hub | A control-center submission arrived truncated/garbled for the first time on this board — title readable ("Enhance ENG-011 stages names shoud be ed"), body a single uninterpretable word ("Healf"). Distinct from `ENG-026`'s case (2026-09-01), which was a fully-formed, well-punctuated request with a wrong ticket id, not lost text — this one looks like a genuinely cut-off submission. One occurrence, not a pattern; held as `ENG-043`'s own intake question rather than guessed. Worth a look if the control-center form drops partial input a second time.|
| 2026-09-06 | eng-manager | aiorders-admin-hub | This `scheduled` sweep found `ENG-018`'s `priority: hold → now` sitting as an uncommitted, two-field working-tree edit with no commit, no ticket-log entry, and no `decision-journal.md`/`exceptions.md` row anywhere to attribute it — discovered only because the sweep's own step-10 groundwork cross-checks every ticket's fresh frontmatter against `_index.md`'s table rather than trusting either. Read as the approver's own direct hand-edit (the only actor allowed to write this field, and the same channel this exact field's original `hold` arrived through on 2026-08-29) rather than a clobber to revert — the opposite-direction mirror of the 2026-09-03 incident, where a bundled commit's own message made "accidental" the demonstrable read. Full reasoning on the ticket's own board-file log, this date. Second occurrence of "an approver-owned field changes with nothing in the ticket's own log to explain it," not yet identical enough in shape to the first (that one was a bundled-commit accident; this one is a bare hand-edit) to file as a proposal on its own — worth a look if a third instance of either shape turns up. |
| 2026-09-06 | eng-manager | aiorders | This `watch (launchd)` pass drained at 03:20:02, two seconds after the immediately-preceding `scheduled` pass released the lock at 03:20:00 — that pass had raised `ENG-018`'s G1 and moved a garbled control-center card into `agents/product-manager/inbox/_handled/`, both writes inside the watched inboxes, without `commit_watch_fingerprint()` running (guarded to `$EVENT = watch` only). Same shape as the open `.watch-seen` fingerprint-timing proposal (2026-08-26 row, `proposals.md`), recurring since (2026-08-30 row; 2026-09-05 row, this file). Verified fresh rather than trusted before concluding no-op: `agents/product-manager/inbox/` and `agents/eng-manager/inbox/` hold only `.gitkeep`/handled-or-processed folders; all six `inbox/` items (`ENG-016`, `ENG-018`, `ENG-028`, `ENG-040`, `ENG-042`, `ENG-043`) re-read fresh, none carrying a `decision:`, every `notified:` timestamp under 24h except `ENG-016`'s (already nudged once on 2026-09-05, correctly not nudged again); machine WIP still `1/1`, held by the `ENG-021` family (`ENG-021` `building`, `ENG-040` `blocked`, `ENG-041` `ready`); `lib/eng-gate-check.sh` whole-board exits 0 clean; `traces/.pending` empty, nothing queued behind this pass. No ticket-log or board-index edit made — no new fact for either to carry. |
| 2026-09-06 | principal-engineer | restaurant-portal | `ENG-041`'s round-1 review failed on a bug shaped like this: a piece of cross-page hand-off state (`faqDraft`, carried via react-router location state) lived in a parent component as plain, entity-unscoped state, while the child consuming it guarded re-use with a `useRef` that resets on remount — and this app's own existing pattern (an `isLoading` gate above the main return, no `placeholderData`/`keepPreviousData` anywhere in the repo) already remounts that exact subtree on every restaurant switch. Neither half is wrong in isolation; the combination silently re-applies one restaurant's transient draft to whichever restaurant's data loads next. First occurrence of this specific shape on this board (state that's correctly one-shot-guarded against re-use within an entity, but not against surviving a switch to a *different* entity) — not filed as a proposal on one occurrence, but worth a second look at any future ticket that hands off free-text or user-authored content across a route boundary in a multi-tenant surface, since the same pairing (unscoped parent state + ref-guarded child + a remount-on-key-change data page) is generic enough to recur outside this ticket. Full finding: `ENG-041`'s own board-file log, this date. |
| 2026-09-06 | qa | restaurant-portal | Related to the row above but a different mechanism, on the same ticket: `ENG-041`'s quality gate found the *read*-side query (`questions/Index.tsx`'s `.eq('restaurant_id', currentRestaurant.id)`) had no test pinning it, backed only by live RLS — which authorises a multi-restaurant owner for every restaurant they manage, not just the one selected, so RLS alone would not have caught a dropped filter. Closed directly (one test, mutation-verified) rather than filed as a bug. Two occurrences now, on one ticket, of "a multi-tenant scoping guarantee that was correct but rested on a single unreinforced mechanism" — the row above's hand-off state and this one's missing regression test are different failure shapes, not the same bug twice, but both are the ticket's central risk (this feature's entire job is surfacing one restaurant's customer data to that restaurant's own owner) landing without a durable check the first time each was written. Not a proposal yet — two data points on the same ticket, not yet two different tickets — but the next occurrence anywhere on this board would be worth naming as a standard (e.g., any new query or write against a tenant-scoped column gets an explicit scoping test, the same way pagination already must). Full finding: `agents/qa/test-plans/ENG-041.md`, `agents/qa/notebook/2026-09-06-coverage-gaps.md`. |
| 2026-09-06 | eng-manager | aiorders | This pass's own step-9 chain fire (`continue ENG-041`, after the review+QA combined hop) queued rather than launched: the lock (`traces/.loop.lock/pid`: `45355`) is held by `eng-trigger.sh scheduled launchd`, confirmed alive via `ps` at `01:25:23` elapsed — well outside the range this log's own recent pass durations show (hundreds to ~1050s, per today's `pass end` lines). `traces/.pending` held `1 watch launchd` ahead of this fire; both now queued, oldest first, to drain once the lock frees. Not chased further — root cause (a genuinely long whole-board sweep vs. something stuck) is outside a `continue` event's own narrower contract, and the queue mechanism is handling it as designed either way. Worth a look if the same PID is still holding the lock significantly past the two-hour mark, or if a third data point on unusually long `scheduled` passes shows up. |
| 2026-09-06 | devops | aiorders | `ENG-041`'s release-readiness hop's own notify-sweep check found `lib/eng-notify.sh`'s log records local wall-clock time (this host is PDT; `date` and the log's own printed timestamps agree to the second), while several other passes' ticket-log entries have computed a `notified:` item's age against a UTC, `Z`-suffixed "current time" reading instead — e.g. a bare `notified: 2026-09-06T03:13:31` (actually local) diffed against `current 2026-09-06T18:05:55Z` reads as "~14h52m" by simple digit subtraction, but the true elapsed time (converting the local stamp to UTC first) is roughly 7 hours less, exactly the PDT offset. This pass's own check avoided the skew by keeping both readings on the same (local) clock. Never yet caused a wrong nudge decision on this board — every check so far has landed nowhere near the 24h boundary either way — but a future item raised close to that boundary could nudge up to ~7h early or late depending which convention the pass computing it happens to use. Not filed as a bug or fixed here — no single pass owns `lib/eng-notify.sh`'s log format or the informal "current time" convention other entries have used, and picking one over the other is a small process decision, not this hop's to make unilaterally. |
| 2026-09-06 | eng-manager | aiorders | This `watch (launchd)` pass's own notify-sweep, re-checking `ENG-041`'s ticket-log history while confirming the row above's skew didn't affect its own nudge check, found most of that ticket's own log entries (the build, review-round-2, and security hops) run 60-90+ lines against `conventions.yaml`'s `ticket_log.entry.cap_lines: 20`, without falling under any of the three allowed exceptions (`gate_fail`, `incident`, `approver_answer_verbatim`) — only devops's own release-readiness entry on this same ticket explicitly cited the cap and stayed under it. Not fixed or re-filed here — trimming an already-committed log entry after the fact would delete audit trail the cap's own rationale says must stay complete, not just move address; this only bears on entries written from here on. Worth a look if a third ticket's worth of entries shows the same gap between the convention and what agents actually write. |
| 2026-09-07 | eng-manager | aiorders | The 2026-09-06 row above (this file) named a live risk: an item raised close to the 24h nudge boundary could nudge early or late by up to ~7h depending which "current time" convention a pass used. This pass's own notify-sweep is the first concrete near-miss: `ENG-028`'s and `ENG-042`'s G1s, both `notified: 2026-09-06T02:28:29` local, checked against this pass's own local time (`2026-09-07T02:27:24` PDT) — about 65 seconds under 24h, genuinely, on the correct (local-wall-clock) basis. Had this check used the UTC-`Z` misread instead, the same pair would have read as ~7h *past* 24h and been nudged wrongly — burning `ENG-028`'s and `ENG-042`'s one-ever nudge a full day early. No wrong nudge resulted here, but this is now two occurrences of "an item sitting close enough to 24h that the basis actually matters," not zero — worth fixing structurally (`lib/eng-notify.sh` computing and stamping the age itself, rather than every pass re-deriving "current time" its own way) if a third occurrence shows up. |
| 2026-09-07 | eng-manager | aiorders | First application of the 2026-09-06 "never idle" Machine WIP amendment to a parent ticket's own no-diff closing hop: `ENG-021`'s `building → shipped → verified` and `ENG-026`'s `designed → ready` pickup both happened in this one `continue ENG-021` pass, rather than the slot sitting free for a later `scheduled`/`decision`/`watch` sweep to notice, which is what `ENG-016`'s (2026-09-04) and `ENG-019`'s (2026-09-05) own closing passes both did — correctly, at the time, since both predate the amendment. Flagging since it's a live departure from a pattern repeated twice on this same board with no correction until now; worth a second look to confirm the reasoning (the amendment's own text covers "parks on the approver," and reaching `shipped` directly was read as the same "leaves that range" trigger by extension) holds up, since nothing on this board has stated that extension explicitly before this pass. |
| 2026-09-07 | database | aiorders-api | `ENG-044`'s build hop found its own design (written 2026-09-03) and ticket Notes both naming a migration filename, `20260903130000_...`, that `ENG-031` had since claimed byte-for-byte with a different suffix (`20260903130000_add_order_capture_to_catering.sql`, shipped 2026-09-03) — a work-breakdown decomposition baked a specific timestamp into a design four days before this sub-ticket actually built, and nothing checked it against what shipped from a *different* ticket family in the meantime. No collision-detection exists anywhere in this pipeline for design-specified filenames against the live migrations directory; this one was only caught because the build pass happened to `ls` the directory while establishing timestamp ordering. Renamed to a fresh, uncollided timestamp and fixed both mentions (this ticket's own board file, the design's Components table) — full account on `ENG-044`'s own board-file log and `agents/database/migrations/ENG-044-foodswipe-channel-visibility-schema.md`. One occurrence; worth a proposal (a build-hop check: grep the target filename against the project's own `supabase/migrations/` before writing it) if a second work-breakdown family's sub-ticket hits the same thing. |
| 2026-09-07 | eng-manager | aiorders-api | This `continue ENG-026` pass read `ENG-044`'s frontmatter twice, seconds apart, mid-pass: first `state: blocked`/`owner: approver`, then `state: shipped` with `blocked_on`/`blocked_from` cleared — the control center writing the merge-detected transition live, between the two reads, rather than between passes (`ENG-044`'s own log confirms: a `blocked → shipped` line stamped "control center, merge detected"). Not assumed either way — independently confirmed via `gh pr view 19` (`MERGED`, `mergedAt: 2026-09-07T16:03:56Z`) and `git merge-base --is-ancestor` against `origin/main` from the dedicated worktree before dispatching `ENG-045` on the strength of it. Nothing lost or corrupted — both reads were internally consistent, just seconds apart on either side of a real write. First time a pass on this board has directly caught a control-center write landing inside its own single-ticket read window rather than between two passes' reads. Worth a look if a future pass ever catches a *partial* write (a half-written frontmatter block) rather than two clean, consistent reads either side of the change. |
| 2026-09-07 | backend | aiorders-api | `ENG-045`'s round-1 fix hop found `time_spent`/`time_remaining` frontmatter still empty despite two prior `building`-state log entries on this same ticket, contrary to `definition-of-done.md`'s "Time tracking and scope changes" rule (every entry at `building`, or back in it after a review round, carries elapsed time and a revised remaining estimate into both fields, mirrored from the Log). Not backfilled here — neither prior hop recorded a start-of-building timestamp or an elapsed figure, so any number written now for the missing history would be invented, not derived, and a partial running total would misstate cumulative effort rather than fix the gap. This hop's own small fix (two-line export, self-test) left both fields as they were rather than start a running total from a broken baseline. Worth a look if a third ticket shows the same gap — the fields may need the same enforced-presence treatment `blocked_from` already gets, rather than staying purely instructed. |
| 2026-09-07 | principal-engineer | aiorders-api | `ENG-045`'s round-1 review failed on `engineering-standards.md`'s "decision logic... exported" rule — promoted 2026-09-04 after three occurrences, violated again three days later in a brand-new file (`channelForMode`/`evaluateOpenNow`, extracted and pure, just not exported). The gate caught it cleanly, which is the system working, not failing — flagging only because a fourth occurrence this soon after promotion is worth watching for a fifth: if one shows up, the standard itself may need to move earlier than review (e.g. into whatever an engineer agent reads immediately before writing a handler), rather than relying on review to keep catching it after the fact. |
| 2026-09-07 | principal-engineer | aiorders-api | `ENG-045`'s round-2 review confirmed the export fix but found the standard's remedy only partially exploited: `channelForMode`/`evaluateOpenNow` are now exported, yet QA's own six tests still reach them exclusively through the handler with `supabase` stubbed, written in round 1 before the export existed. Not a coverage gap on this ticket's owned criteria (real behavioral coverage already exists) and not blocked on it — round 1 scoped the fix as the export alone, "no other edit implied." Worth watching whether a future ticket touching this file actually starts testing the exported functions directly now that the seam exists, or whether "export it" quietly becomes a checkbox nobody follows through on. |
| 2026-09-07 | frontend | aiorders-admin-hub | `ENG-046`'s build hop regenerated `src/integrations/supabase/types.ts` per its own design's instruction ("regenerate... not hand-edited") and got a 1576-line diff against this ticket's own three columns — the file had never been refreshed after several other, unrelated tickets shipped schema changes (`claim_status`/`claimed_at`/`claimed_by_user_id`, `stripe_acct_id`, assorted `brand-portal`-table fields among others), so regenerating surfaced all of that backlog at once rather than just this ticket's own addition. Nothing wrong with the output — it's the generator's direct, honest read of the live schema — but it means whichever ticket happens to touch this file next inherits however much schema drift accumulated since the last one did, with no signal in between that it was accumulating. Not fixed here — out of this ticket's own scope, and there's no single owner of "keep `aiorders-admin-hub`'s generated types current" the way a migration's own repo has one. Worth a proposal (regenerate this file as a standard step whenever *any* ticket ships an `aiorders-api` migration, regardless of which repo the ticket itself touches) if a second repo's generated-types file is found similarly stale. |
| 2026-09-07 | principal-engineer | aiorders-admin-hub | `ENG-046`'s round-1 review found the frontend's own build notebook (`agents/frontend/notebook/2026-09-07-eng046-build.md`) citing "the same shape `ENG-040`'s own admin-toggle work used" to justify adding no test. Checked both tickets directly: `ENG-040` is a backend `EDITABLE_PAGES` allow-list widening on `aiorders-api`, `ENG-041` an FAQ editor — neither is an admin-hub toggle UI, neither shares the cited reason. The underlying judgement was independently correct anyway (zero test infrastructure exists for `aiorders-admin-hub`, open proposal since 2026-08-31; a pure prop-through change matching eight already-untested sibling rows in the same file), so nothing was blocked on this — but the citation itself doesn't hold up. One occurrence; worth a second look if a future notebook entry's cited precedent turns out mismatched again, since these citations are read at face value by later passes rather than re-verified by default. |
| 2026-09-07 | security | aiorders-admin-hub | `ENG-046`'s security-gate hop failed (exit 1) at `11:47:04`, immediately after it had already written a complete, correct `agents/security/reviews/ENG-046.md` (verdict `pass`) — the failure landed after the gate's own real work was done but before the ticket log/frontmatter/board index/chain got written. Named cause, not a guess: `traces/eng-loop-2026-09-07.log` shows the session hit its own usage limit ("You've hit your session limit · resets 12:50pm (America/Vancouver)") right after the `Write` of the receipt — a capacity cutoff, not an application error, and one that can land at any point in a pass's output with no regard for whether a receipt was mid-write or already flushed. The existing "broken chain" check (step 8) didn't catch this shape: the prior hop's `chained: ENG-046` line was genuinely fired and genuinely ran, it just didn't finish recording that it had. This retry found the receipt, verified it was still current against a fresh diff read (not assumed), and completed the write-back rather than re-running the gate — cheap here since the ticket is `XS`, but a heavier gate (e.g. QA's mutation-tested suite) failing at the same point mid-write would waste much more. One occurrence; worth a proposal if a second gate is found failing in this exact post-write, pre-record window. |
| 2026-09-07 | eng-manager | aiorders | `inbox/2026-09-07-eng045-merge-request.md`'s closing note reads "`ENG-047` depends on this one and stays `ready` until this ticket reaches `verified`" — true under the pre-2026-09-07 reading, superseded the same day by `eng_build_loop.md` Guards' "an open PR does satisfy `depends_on`" amendment (`ENG-045`'s own PR being open, `blocked_on: approver`, is enough). Harmless — that file is a point-in-time notification to the approver, not re-read as a live instruction by any later pass — but left uncorrected it could mislead a human skimming it for why `ENG-047` moved before `ENG-045` merged. Not fixed here: out of scope for a `continue ENG-046` event pass to edit another ticket's already-raised gate item. |
| 2026-09-07 | frontend | restaurant-marketplace | `ENG-047`'s own ticket Notes (written by `ENG-026`'s work-breakdown pass) instructed branching "off `ENG-045`'s already-merged/verified tip" — copied from the same-repo stacking phrasing this board uses elsewhere, but `ENG-045` lives in `aiorders-api`, a different repo from this ticket's own `restaurant-marketplace`, so there is no such tip to branch from here; the actual dependency is functional (the API fields this ticket reads), not git-level. `ENG-046`'s own board-file log had already flagged the ambiguity in advance ("that's for the pass that actually builds `ENG-047` to work from its own ticket Notes, not decided here") without resolving it, so this pass had to work it out from scratch rather than following an instruction literally. Branched from this repo's own `origin/master` tip instead — the only sensible reading, and cheap to resolve once noticed, but a work-breakdown pass generating a DAG with cross-repo dependency edges should probably say "no git-level relationship — branch from this repo's own default branch" explicitly for any edge that crosses a repo boundary, rather than reusing same-repo stacking language by default. One occurrence; worth a proposal if a second multi-repo work-breakdown family's Notes make the same copy-without-adjusting mistake. |
| 2026-09-07 | eng-manager | aiorders-api | `ENG-027`'s own Notes (written 2026-09-03, before `ENG-006`/`ENG-007` had merged) instruct that the whole loyalty sequence shares one branch, `loyalty-system`, and that whoever builds this ticket branches from and merges back into it — true at the time: both predecessor tickets used `branch: loyalty-system` and opened their own PRs from it (`#2`, `#4`). Checked fresh before writing this pass's two sub-tickets (`ENG-048`, `ENG-049`), in the shared worktree, without switching its own checkout: `git merge-base --is-ancestor origin/loyalty-system origin/main` is true (zero unique commits), and `git log origin/loyalty-system..origin/main --oneline` shows 59 commits since (`ENG-038`, `ENG-020`, `ENG-040`, `ENG-044`, `ENG-045`, others) — both predecessor PRs already merged individually, and the shared branch has fully served its purpose rather than remaining a live integration target. Branching either new sub-ticket off it now would mean rebasing 59 commits of unrelated work before either could cleanly PR, for no offsetting benefit. Both instead branch fresh off `origin/main`, matching every other work-breakdown sub-ticket on this board; `ENG-027`'s own Notes marked superseded in place rather than silently ignored. One occurrence; worth a proposal (a step in `work-breakdown/SKILL.md` that checks a named shared branch's ancestry against the default branch before propagating it to new sub-tickets) if a second ticket's stale branch instruction is found the same way. |
| 2026-09-07 | database | aiorders-api | `ENG-048`'s `continue` pass found the shared worktree mid-work from an earlier, uncommitted attempt (matching `config/projects.md`'s own documented "previous pass died mid-work" case) — but the leftover wasn't only the untracked migration file that doc anticipates: a Docker container from that same attempt's own disposable-replica test (`eng048-pgtest`) was still running, hours later, with the migration applied and test rows inside. `docker ps` isn't part of the documented recovery check today, only `git status`. Harmless here (found and removed before it collided with this pass's own container names/ports), but a database-agent build hop that dies mid-verification can apparently leave a local Docker container running indefinitely with nothing to stop it. One occurrence; worth a proposal (a `docker ps --filter` check alongside the git-status check, or a trap/cleanup convention for disposable-replica containers) if a second orphaned container turns up the same way. |
| 2026-09-07 | devops | aiorders-api | `ENG-048`'s release-readiness hop, updating `_index.md`'s In-flight table for its own row, found `ENG-045`/`ENG-046`/`ENG-047` still shown `blocked`/open there while each ticket's own board file already reads `shipped` (merge requests already in `inbox/_handled/`) — the "Waiting on the approver" summary line was stale the same way, still listing `ENG-045`'s and `ENG-046`'s merge requests as open and never listing `ENG-047`'s at all. Corrected both this pass (cheap, adjacent to an edit already being made), but not investigated further and not fixed retroactively in those three tickets' own narrative paragraphs further down the same section — out of a `continue ENG-048` pass's own scope, a different family. Nothing on this board currently re-derives `_index.md`'s In-flight table from the individual ticket files it summarizes; it is hand-maintained by whichever pass last touched it, so a family that ships across several passes without one of them revisiting the index can drift silently. One occurrence caught three tickets deep; worth a proposal (a mechanical check — or a `scheduled` pass step — that diffs the In-flight table's `state`/`owner` cells against each listed ticket's own frontmatter) if a second family is found the same way. |
| 2026-09-07 | eng-manager | aiorders-api | `ENG-026`'s parent-level acceptance-check needed to confirm `ENG-045`'s handler code was actually deployed, not just merged — `aiorders-api` has no CI workflow at all (`config/projects.md` already names this), so `gh run list`-style deploy confirmation (used successfully on `aiorders-admin-hub`/`restaurant-marketplace`, both Cloudflare Pages with a `deploy-cf.yml`) doesn't exist for this repo. Found a substitute that worked: `supabase migration list --linked` shows `local`/`remote` columns that prove a migration is applied to the live, linked production database (not just present as a file on a merged branch), and `supabase functions list` returns each function's own `updated_at` — cross-referenced against `gh pr view --json mergedAt` for the PR that touched it (62 seconds apart here), close enough to treat as the same deploy event. Neither command needs `--project-ref` beyond what's already linked in the worktree; both are read-only, cost nothing, and carry no risk. Full use: `agents/product-manager/notebook/2026-09-07-eng026-acceptance.md`. Worth adopting as this repo's standard acceptance-check substitute for "confirm deployed" wherever a CI run can't be checked instead — not filed as a proposal since it's a technique to reuse, not a gap to fix. |
| 2026-09-07 | eng-manager | aiorders-api | This `continue ENG-027` pass's own read-only merge check (`git fetch` + ancestry, `gh pr view` for `#21`/`#22`) found the shared `_eng/aiorders-api` worktree carrying three untracked `deno.lock` files (`brand-portal`, `loyalty-auto-complete`, `restaurant-marketplace`), checked out on `ENG-049`'s own branch — a different leftover than the migration-file-plus-Docker-container case the row above already found and removed on this same repo. Not treated as a died-mid-work pass: the checkout matches the most recently active ticket rather than an orphaned one, and this pass only read the worktree (`fetch`, ancestry check), never wrote to or built in it, so `config/projects.md`'s stop-and-flag precondition wasn't actually at stake. Left untouched — Deno auto-generates a lock file on any `check`/`test`/`run` against unlocked imports, so this is almost certainly a byproduct of `ENG-049`'s own already-completed verification hops, but not confirmed against any hop's own notebook. Worth a look if a third undocumented worktree leftover turns up on this repo — two different shapes (Docker container, lock files) in one day is enough to wonder whether disposable-verification cleanup needs its own convention rather than relying on each hop to tidy up after itself. |
| 2026-09-07 | eng-manager | aiorders-api | A second, later `continue ENG-027` pass's own PR-state re-verification (`git fetch origin`, `gh pr view 21`/`22`) was run from `~/Documents/projects/aiorders/aiorders-api` — `config/projects.md`'s "Working copies" section names this path as "the human's. Never touched by an agent," distinct from the department's own dedicated worktree at `~/Documents/projects/_eng/aiorders-api` (both confirmed as linked worktrees of the same repo via `git worktree list`). Assessed for harm before logging rather than assumed harmless: `fetch` only updates shared remote-tracking refs (identical effect regardless of which worktree invokes it) and `gh pr view` is a pure API read, neither touches a worktree's checked-out files/index/HEAD; `git status --short --branch` run immediately after found the checkout's existing uncommitted changes (a modified `README.md`/`deno.json`, an untracked `$OUT`, three untracked migration files) predate and are unrelated to this pass. No actual interference occurred, but the rule reads "never touched," not "never mutated," so this was a real violation of its letter despite the harmless effect. Disclosed to the approver directly in that pass's own chat output, not just filed here. Worth a look if a second pass is found making the same mistake — `config/projects.md`'s project-registry table (autonomy levels, near the top) and its "Working copies" section (the actual worktree paths, further down) name path conventions that look similar enough to conflate at a skim. |
| 2026-09-07 | eng-manager | aiorders | That same later `continue ENG-027` pass was the **fourth** consecutive pass — after the `continue ENG-027` immediately before it and two intervening `scheduled (auto-drain)` sweeps, all within roughly 40 minutes (`19:52`→`20:35` PDT) — to independently re-verify and reach the identical conclusion: `ENG-048`/`ENG-049` both still open/unmerged, Machine WIP `0/1`, the same four To-do tickets still blocked on the same unanswered gate items, `IDLE-2026-09-07` still accurate and correctly not duplicated. Each pass did the right thing on its own (no wrong action, no duplicate item, no over-eager fallback to the `designed`-state pool), but each one re-ran the full `gh`/`git`/board sweep from scratch rather than having any way to know that nothing external could plausibly have changed in the intervening minutes. Traced via `traces/eng-loop-2026-09-07.log`: no dropped event, no broken chain — every ticket's own `chained:` line is present and consistent with what actually ran, so this reads as fast queue activity (`auto-drain` naming suggests a mode built to drain the queue quickly) rather than a bug. Worth a proposal (e.g., a cheap freshness check — compare the newest `inbox/notified:`/board-file `updated:` timestamp against the last recorded idle confirmation's own timestamp, and short-circuit to a one-line log entry without re-running the full sweep when nothing is newer) if this recurs at this frequency. |
| 2026-09-08 | eng-manager | aiorders-api | The repo-isolation slip named in the row above and already the subject of an open proposal (`proposals.md`, 2026-09-08, calling its own catch the "third occurrence") recurred again this `continue ENG-027` pass (the 27th consecutive confirmation): before locating the department's own `_eng/aiorders-api` worktree, this pass ran `git worktree list` from `~/Documents/projects/aiorders/aiorders-api` — the human's checkout — to find the correct path in the first place. Self-caught before any further command ran there; the actual PR-state re-verification (`git fetch origin`, `gh pr view 21`/`22`) ran from `_eng/aiorders-api` once found, same as every clean check. Even more clearly harmless than the `fetch`/`gh pr view` case above — `git worktree list` reads only `.git/worktrees` metadata, touching no ref, index, or file in the human's checkout — but the letter of `config/projects.md`'s "never touched by an agent" was still crossed. Not filed as a fresh proposal; the open one already names this exact failure mode and a concrete fix (a `lib/` wrapper, or standardizing on `gh pr view --repo owner/repo`). This row exists only to keep the count honest: the open proposal's own "third occurrence" framing plus this one makes at least a fourth, all self-caught, none consequential — which is itself the argument for the proposal's mechanical fix over continuing to rely on each pass remembering on its own. |
| 2026-09-08 | eng-manager | aiorders-api | The 30th consecutive `continue ENG-027` idle-check found the ticket's own log (27th–29th entries) reporting `notified:`-item ages inflated by roughly 7 hours against real wall-clock time — e.g. the 29th entry's `≈23h33m` for `ENG-050` versus this pass's own math from the raw `notified: 2026-09-07T16:04:16` timestamp against `date` (`~17h05m`), cross-checked against two independent sources: `board/_index.md`'s 16th-confirmation entry, computed separately at `02:01` PDT and landing on a self-consistent `~9h57m` for the same item, and `traces/eng-loop-2026-09-08.log`'s own hop-16 `pass start` timestamp (`08:35:17`), which puts the true figure at that moment around `~16h31m`. Same signature (a ~7-hour inflation, PDT's own UTC offset) as the open `proposals.md` (2026-09-07) UTC-vs-local-clock proposal, filed after it caused an actually-early nudge on `ENG-026`'s chain — recurring here on a different ticket's idle-recheck narrative rather than its notify sweep. No wrong action resulted: none of `ENG-050`/`ENG-048`-merge/`ENG-049`-merge/`IDLE-2026-09-07` is actually within 24h yet even under the correct math, so this pass's own step 7 nudges nothing either way, and the ticket log entry this pass writes uses freshly-recomputed figures rather than incrementing the inflated ones forward. Not filed as a fresh proposal — the open one already names the fix (stamp `notified:`/`nudged:` with an explicit UTC offset, or document the local-time convention in `eng_build_loop.md` step 7); this row exists to keep the count honest and note it now spans more than one ticket's narrative, not just one notify sweep. |
| 2026-09-08 | eng-manager | aiorders-api | `ENG-027` is the live case the 2026-08-28 `observations.md` row (above) flagged and left unchased: "worth a look if a ticket ever bounces across a midnight boundary... a ticket that hit 8 hops yesterday gets a clean 8 more today." It just did, at the 20-hop `max_5x` ceiling rather than the 8-hop `pro` one that row was written against. Confirmed directly, not inferred: `traces/.hops-2026-09-07-ENG-027` reads `10`; today's `traces/.hops-2026-09-08-ENG-027` started fresh rather than continuing from it and now reads `17`; `lib/eng-trigger.sh` names the per-ticket counter file `$STATE/.hops-$(date '+%Y-%m-%d')-${TICKET_ID}` (lines 1164, 1776), and its own halt-notice string (lines 1641, 1866) literally reads "used $TH of its $MAX_HOPS_PER_TICKET **daily** hops" — the code's own wording agrees with the reset behavior, not with `eng_build_loop.md`'s Cadence section ("the day's counter clears at midnight where a ticket's does not"). Concrete effect on this ticket: its true cumulative hop count across this one idle streak is `10 + 17 = 27`, already past the `20` the guard is supposed to enforce, and the guard has not tripped and structurally cannot while the file resets nightly — every "N hops of runway left" figure this ticket's own log has reported (including this pass's) is only ever true until the next midnight, not a real ceiling on the streak. Second occurrence of the pattern, not a third — per this repo's own convention (three strikes before a fresh proposal, applied consistently elsewhere in this file), not filed as one yet, but flagged plainly since this occurrence, unlike the first, has real and ongoing consequence rather than none. |
| 2026-09-08 | eng-manager | business-os | This `scheduled` pass's own post-edit `git status` (business-os root) found six files modified with mtimes inside this pass's own run window (`09:33`–`09:36` PDT, this pass having launched `09:30:06`): `control-center/server.py`, `control-center/ui/views/cards.js`, `departments/engineering/agents/eng-manager/config.yaml`, `departments/engineering/schedules/eng_build_loop.md`, and this instance's own `config/config.yaml`/`agents/eng-manager/proposals.md` — none of them touched by this pass. Checked for an actual lock collision before logging rather than assuming one: `traces/.loop.lock/pid` and `traces/.pass-out.49520` were this session's own throughout (single `pid`, single pass-out file, both confirmed after the fact), so this wasn't a second build-loop pass racing the same lock — most likely a separate, human-or-Fable-driven editing session in this same shared working tree, which `eng_build_loop.md` itself doesn't own the way it owns the per-project worktrees under `config/projects.md`. Not investigated further and nothing here was touched, per step 4's "don't let unrelated uncommitted state pull you off this item" — but two of the six are exactly the schedule/config files this and every pass's own procedure reasoning depends on, so worth naming: a future pass reading `eng_build_loop.md` or either `config.yaml` mid-edit is reading whatever a concurrent, unrelated session had written at that instant, uncommitted, with no signal that it's mid-change. Disclosed directly to the approver in this pass's own chat output too. |
| 2026-09-08 | eng-manager | aiorders | Row above, continued: the very next `continue ENG-027` pass hit the same unverified text in `eng_build_loop.md`/both `config.yaml`s and, unlike the `scheduled` pass, initially acted on it — archived `inbox/IDLE-2026-09-07.md` as resolved and fired `continue ENG-029` — before checking the one thing that would settle it either way. `agents/eng-manager/config/decision-journal.md` has no 2026-09-08 entry at all, contradicting the text's own claim to have been "journaled in full" there. Reverted what could be reverted (`IDLE-2026-09-07.md` restored to open, correction noted in place) and disclosed what couldn't (`continue ENG-029` already queued in `traces/.pending`, not hand-edited out — the queue's format/locking isn't well enough understood from inside a pass to safely remove one line while a concurrent session may still be writing). Fresh P0 flag raised: `inbox/2026-09-08-eng-loop-integrity-check.md`. Worth a look if a third pass hits this same text still unresolved — two passes reaching two different conclusions from the identical unverified content in under thirty minutes is itself worth knowing, separate from whether the content turns out genuine. |
| 2026-09-08 | eng-manager | aiorders-api | `ENG-048`'s and `ENG-049`'s merges (found this `continue ENG-027` pass, both shipped) were deployed by a GitHub Actions workflow for automated Supabase deploys that is newly present on `main` — not part of either ticket's own diff, not mentioned in either ticket's release-readiness hop (2026-09-07), and not documented in `config/projects.md`, which still reads "no `.github/workflows/`" for `aiorders-api` as of 2026-08-23. Confirmed by timing alone (three edge functions redeployed within a 7-second window, ~40s after the merging PR), not by reading the workflow file itself — out of scope for this pass. Worth a look next time anyone is in `config/projects.md` for another reason, since the Commands table's own empty Test/Lint/Typecheck/Build row for this project was written on the "no CI" assumption this finding revises. |
| 2026-09-08 | eng-manager | aiorders-api | `ENG-048`'s own migration file (`supabase/migrations/20260907130000_...sql`) carries a trailing comment claiming its rollback is "safe any time before `ENG-049` ships." Both tickets are now live (same `continue ENG-027` pass) — the comment is stale as of this pass and would mislead anyone reading it for an actual rollback plan; a standalone rollback of `ENG-048` from this point forward must happen together with reverting `ENG-049`, per the general caveat this same comment already names for the post-`ENG-049` case. Not fixed here (a migration file already applied to production is not something to hand-edit after the fact); worth a look if this migration is ever actually rolled back. |
| 2026-09-08 | eng-manager | aiorders | **Not a duplicate of the row above naming decision-journal's own missing 2026-09-08 entry as evidence against the disputed designed-pool text — flagged so a later pass doesn't misread it as one.** This same `continue ENG-027` pass added two 2026-09-08 rows to `agents/eng-manager/config/decision-journal.md`, for `ENG-048`'s and `ENG-049`'s merges — entirely unrelated to the designed-pool question, routine L1-merge journaling this file has recorded roughly twenty times already. `decision-journal.md` now genuinely has 2026-09-08 entries, which is no longer, by itself, evidence the disputed text is unconfirmed — a future pass re-running that specific check needs to look at *what* a 2026-09-08 entry says, not just whether one exists. As of this pass the disputed text's own claim (that the designed-pool decision was "journaled in full") is still false: neither new row mentions it. |
| 2026-09-08 | eng-manager | business-os | A distinct angle on the same disputed 2026-09-08 edit, not yet named by any of the several passes that have already handled this incident: of the four files it touched, one — `departments/engineering/agents/eng-manager/config.yaml` — sits inside an agent's own directory, which is exactly the scope `departments/engineering/config/conventions.yaml`'s own `authoring` rule covers ("Any update to an agent or skill definition file... is written with the Fable model, never Sonnet/Opus/Haiku, regardless of which model is driving the session that requests the change"). `eng_build_loop.md` (a schedule) and this instance's `config/config.yaml` (a shared contract file, not inside `agents/eng-manager/`) are both explicitly outside that rule's scope by the same paragraph's own wording; the department config.yaml is not. Not a new fact about whether the approver actually said this — CLAUDE.md's own memory notes this session inherited already establish the approver hand-edits gate files directly himself, which this rule was never meant to constrain. It matters only in the middle case: if the approver confirms the designed-pool decision as genuine but it turns out an agent session (not the approver's own hand) wrote this particular file's text under whatever model was driving that session, the text sitting there now still wouldn't conform — it would need re-authoring through Fable before it's left in place, separately from the content being correct. Worth having on record so whoever eventually closes `inbox/2026-09-08-eng-loop-integrity-check.md` doesn't treat "confirmed genuine" and "this file is now fine as written" as the same finding. |
| 2026-09-08 | architect | aiorders-api | `ENG-051`'s tech-design pass found that half the ticket's own scope — "QR issuance" (AC1/AC2: one code, per verified diner, consistent across every restaurant) — was already fully satisfied by `ENG-006`'s shipped work before this ticket existed: `platform_customers.id` already is such a value, and is already independently readable by its own owner under a live RLS policy from that migration. No new table, column, or endpoint was needed for that half; only redemption itself was real design work. Worth watching for on a future ticket late in a multi-ticket sequence like this one — a foundational ticket's own primitives sometimes already cover a later ticket's stated scope without anyone having designed it that way on purpose, and it's cheap to check for directly (what does the caller already have, from a session/identity step several tickets back) before assuming new plumbing is needed. |
| 2026-09-08 | eng-manager | aiorders-api | `ENG-051`'s work-breakdown pass, checking the branch point for `ENG-052`/`ENG-053` fresh, found `origin/main`'s tip (`fb26921`, "Adopt 23 production edge functions that had no source in this repo") is an approver-authored commit that landed directly on `main` outside this department's own pipeline (co-authored by a separate Opus session, not this loop's own hops) — confirmed harmless to this ticket (touches neither `loyalty_ledger_entries` nor any `brand-portal` file) but worth knowing main can move via an out-of-band commit nobody in this loop narrated. Separately and not investigated further: `ENG-048`'s own merge commit message reads "Merge pull request #23", not the `#21` recorded in `ENG-048`'s own `links.pr` frontmatter — a real mismatch between the two, unexplained; both commits are confirmed ancestors of `main` regardless of which number actually carried them, so it didn't block anything here, but whoever next touches `ENG-048`'s own release record should reconcile it. |
| 2026-09-08 | database | aiorders-api | `ENG-052`'s build hop found the architect's design for `redeem_points_if_eligible` naming an insert shape (`rate_applied = v_rate`) without checking that the target column's own live type (`numeric(5,2)`, sized for the earn side's percentage rates) was narrower than the source value's type (`redemption_value_per_point numeric(10,4)`, a dollars-per-point ratio ENG-007 deliberately gave four decimal places) — silently truncating any sub-cent configured rate to `0.00` on insert, with no constraint to catch it. Caught and fixed in the same pass it was introduced (widened `rate_applied` to `numeric(10,4)`, proven both as a regression fix and as backward-compatible for the earn path — see the ticket's own board-file log and migration plan doc). One instance, not a pattern yet — filed as an observation rather than a proposal. Worth a look if a second design elsewhere in this sequence states an insert/write shape without its own explicit type-compatibility check against the live column, the same class of gap `ENG-048`'s grant/revoke finding and `ENG-007`'s trigger-function finding already both caught once each, on different mechanisms. |
| 2026-09-08 | security | aiorders-api | `ENG-052`'s security gate applied the explicit A01 checklist item this notebook's own 2026-09-07 entry proposed but didn't yet formalize ("verify a new/changed RPC function's own `has_function_privilege` for `anon`/`authenticated` independently of any application-layer wrapper") — catalog read plus an actual denied `set role` call, not inherited from code review's or QA's own accounts. That 2026-09-07 entry counted two gate-level *misses* of this exact check (one short of three-strike) and flagged that the next occurrence should be recognized rather than re-discovered; this is a correct application, not a third miss, so nothing escalates. Worth folding into `security-baseline.md`'s own A01 row as a standing checklist line if a security gate ever has to re-derive this from scratch again rather than following precedent — not done this pass, since editing the department-wide baseline doc is outside a `continue` event's own narrow scope. |
| 2026-09-08 | eng-manager | aiorders | This `continue ENG-053` pass's own notify sweep (step 7) found `inbox/IDLE-2026-09-07.md` `notified: 2026-09-07T18:44:54`, no `nudged:`, no `decision:` — genuinely 28h+ old against local wall-clock (this pass's own `date`, no UTC-mislabeling risk since both readings were kept on the same clock, per the 2026-09-06/09-07 observations above on that exact skew). Sent the nudge (`lib/eng-notify.sh nudge`, confirmed posted in `traces/eng-notify-2026-09-08.log`) and stamped `nudged:`. Worth naming why this hadn't already happened: this exact ticket's own two prior hops (this round's own building→in-review, and `ENG-052`'s building→in-review) each explicitly declined a whole-`inbox/` notify check, reasoning "that's the `scheduled`/`watch` sweep's job" and citing each other as precedent. Step 7 is in `eng_build_loop.md`'s own "every event, not negotiable" set with no ticket-scoping language anywhere in its text (unlike step 8's dead-end sweep, which *is* correctly scoped to the event by name) — the prior hops' reasoning conflated the two steps. Not filed as a proposal (no code or doc is wrong, a repeated judgement call was), but worth a future pass not re-copying that same citation chain without re-reading step 7 itself. Separately, confirmed live (not re-filed): `lib/eng-notify.sh`'s known `MODE` clobber (`proposals.md`, 2026-08-25 row, bug 2) is still unfixed — this pass's own nudge call logged `sent: active` rather than `sent: nudge`, same as every prior occurrence. |
| 2026-09-08 | principal-engineer | aiorders-api | `ENG-053`'s round-1 code review (fail — missing test coverage, see the ticket's own board-file log) found `config/projects.md`'s "no test command exists on any registered project" section (verified 2026-08-23) is now stale for `aiorders-api` specifically, and this ticket's own build hop relied on the stale claim to justify shipping `redeemPoints` with no test: `brand-portal/` alone now carries nine `.test.ts` files (`feedback.test.ts`/`offers.test.ts` since 2026-09-03, `acquisition.test.ts` since 2026-09-05, `loyalty.test.ts`/`channels.test.ts` since `ENG-049` on 2026-09-07), all real and `deno test`-runnable (confirmed: 19/19 on `loyalty.test.ts` this pass). Not corrected in `config/projects.md` itself this pass — out of a code-review hop's own scope — but worth fixing the next time anyone is in that file for another reason, same as the CI-workflow staleness the 2026-09-08 `ENG-027` row above already flagged for the same document. |
| 2026-09-08 | eng-manager | aiorders-api | `ENG-053`'s own release-readiness hop (`ready-to-ship → blocked`, PR #25) found `board/_index.md`'s "Waiting on the approver" section still stops at `ENG-018`'s G1 (2026-09-06) — it lists neither `ENG-050`'s P0 incident, `ENG-028`'s/`ENG-042`'s G1s, nor `ENG-052`'s or this ticket's own merge requests. Not a functional gap (the In-flight table and each ticket's own frontmatter stay authoritative), but this is at least the third dated occurrence of the same drift the 2026-09-07 row above already named and flagged "worth a proposal ... if a second family is found the same way." Not raised as a proposal here — out of a single ticket's release-readiness scope — left for whichever pass next does a board-wide reconciliation sweep to weigh against the three-strike bar. |
| 2026-09-08 | eng-manager | aiorders-api | This `continue ENG-051` pass's runaway-guard due diligence (department daily hop count 49, this ticket's own 5 — `traces/.hops-2026-09-08` / `-ENG-051`) checked those against `departments/engineering/agents/eng-manager/config.yaml`'s `build_loop.events.max_hops_per_day: 40` / `max_hops_per_ticket: 8` literals and, read alone, that looks like a ceiling breach. Both are stale bash-fallback defaults matching the retired `pro` tier — the live ceiling only resolves through `eng-trigger.sh`'s `read_plan_budget()` against `plan.tier: max_5x` (`hops_per_day: 200` / `hops_per_ticket: 20` in the same file's own `budgets` block), under which 49/5 are well inside bounds. The two literal lines' own comment (`← from plan.budgets[plan.tier].hops_per_day`) claims to mirror the tier but was never updated after the 2026-08-29 `pro`→`max_5x` correction noted just above it in the same file — worth fixing so a future pass doesn't have to trace `read_plan_budget()` to rule out a false alarm. |
| 2026-09-08 | eng-manager | aiorders-api | `board/_index.md` is 1934 lines but holds only 3 `## {date} —` entries (at, not over, the keep-three cap) — nearly all its size is in the "pointer section" (Next ID / Machine WIP / Approver-facing WIP narratives) that has accumulated permanent blow-by-blow history since 2026-08-25 and is never rolled the way dated entries are. The keep-three rule's own rationale ("every pass reads this file in full... an append-only log is a tax on every future pass") applies just as much to that prose, but nothing in `eng_build_loop.md` step 10 currently covers trimming it. Not filed as a proposal — no rule is broken, and deciding what pointer-section history is safe to cut is a bigger judgment call than a `continue` event should make inline. |
| 2026-09-08 | eng-manager | aiorders-api | This `continue ENG-051` pass's own runaway-guard re-check (department daily hop count 50, this ticket's own 6) closed two verification gaps no prior hop on this ticket had checked, rather than taking the numbers on file. (1) Whether the budget figures themselves sit inside the disputed, uncommitted designed-pool edit — they live in the same two `config.yaml` files. `git diff` on both: the disputed change is a pure addition (35 / 16 lines, zero deletions) of wip/machine-start-source commentary only; it does not touch either file's `plan:`/`budgets:` block, so `plan.tier: max_5x` → `hops_per_day: 200` / `hops_per_ticket: 20` is genuine pre-existing committed config, not something the same suspect edit could have inflated to justify continued dispatch. (2) Whether the live dispatch template agrees with the disputed text or the pre-amendment one — grepped `departments/engineering/lib/eng-trigger.sh` directly rather than taking `inbox/2026-09-08-eng-loop-integrity-check.md`'s 17:09Z update on its word: line 2186 still reads "the top of To-do," confirming firsthand that the script firing every hop, including this one, has not been updated to match the disputed schedule-doc text. Neither check changes the standing conclusion (still declined to draw from `designed`); both just make the numbers this and future passes lean on independently verified rather than inherited. |
| 2026-09-08 | eng-manager | aiorders-api | This `watch (launchd)` pass's own step-5 re-check of `ENG-052`/`ENG-053` ran its first `git fetch`/`git log`/`gh pr view` from `~/Documents/projects/aiorders/aiorders-api` — the human's own interactive checkout per `config/projects.md`'s "Working copies" section — before finding the department's own `_eng/aiorders-api` worktree and redoing the check there. Same repo-isolation failure mode the open proposal (`proposals.md`, 2026-09-08) already names, one occurrence further than either prior self-caught instance (both `git worktree list`, a pure metadata read): `git fetch` does write to the wrong checkout's remote-tracking refs (`FETCH_HEAD`, `origin/main`), though never to its working tree, index, or any uncommitted state, so nothing of the human's was actually at risk. Not filed as a second proposal — the open one already covers this exact pattern and names a concrete fix (a `lib/` wrapper, or standardizing on `gh pr view --repo owner/repo`); this row exists only to keep the occurrence count honest. |
| 2026-09-09 | eng-manager | aiorders | This `scheduled (launchd)` pass's own re-check of the disputed designed-pool text found a corroboration angle no prior pass had used: the launchd-fired task prompt handed to *this pass itself* — its own "SLOT FREED" instructions — reads "draw the top of To-do (priority, then severity, then ticket id), confirm it is startable...", word-for-word matching `departments/engineering/lib/eng-trigger.sh` line 2186 (already checked directly by `ENG-051`'s own 2026-09-08 hop). Prior checks confirmed the script's source still says this; this pass is a live instance of that exact template actually firing and dispatching a real session, one step more concrete than reading source that could in principle be stale but unused. Same standing conclusion (declined to draw from `designed`), not a new fact about whether the approver said it — just a firmer floor under "the live dispatch mechanism doesn't yet reflect the disputed text," for whoever eventually closes `inbox/2026-09-08-eng-loop-integrity-check.md`. |
| 2026-09-09 | eng-manager | aiorders-api | `ENG-051`'s own board-file log has two of today's entries in the wrong order on disk: the entry timestamped `01:50` (the one that filed the hop-counter-midnight-reset proposal) sits at lines 1740–1838, positionally *before* the entry timestamped `01:22` at lines 1840–1949 — even though the single-flight lock guarantees the `01:22` pass fully finished (01:25:27) well before the `01:50` pass even started (01:50:31). Likely mechanism: the closing boilerplate ("...no project repo touched.") recurs near-verbatim at the end of nearly every entry in this file (confirmed by grep — ten near-identical occurrences), so an append anchored on that phrase plus a little surrounding context can satisfy the edit tool's uniqueness check while still matching an earlier entry's close rather than the true then-current last one, inserting new text one entry too early instead of at the real end. No content was lost — both entries are intact and independently readable — but file position no longer equals recency here. Worth naming plainly: the trigger's own checkpoint format ("newest log entry starts at line {N}") and the standing convention of reading the tail for "the previous hop's handover note" both assume position = recency, and either one alone would have silently surfaced `01:22` as newest and skipped `01:50`'s more recent findings entirely. Caught only because this pass read a wider range than the checkpoint pointed to rather than trusting it. Not filed as a proposal — first occurrence, no realized harm (every pass on this ticket independently re-derives its own facts rather than trusting file position or the checkpoint), and the fix is closer to "verify true EOF fresh immediately before constructing the append" than an obvious mechanical change. |
| 2026-09-09 | eng-manager | aiorders | This `continue ENG-051` pass's own step-7 notify sweep caught a live instance of the local-vs-UTC gotcha the 2026-09-06/07 `eng041` observations already named in the abstract, this time mid-miscalculation on a specific item. The prior hop (02:36 today) and at least one before it computed `inbox/2026-09-08-eng-loop-integrity-check.md`'s notify age by subtracting its frontmatter `notified: 2026-09-08T09:58:53` directly from `date -u`'s output, treating the stamp as UTC — landing at "23h27m" then "23h56m," framed as closing in on the 24h one-time-nudge threshold. Cross-checked against `traces/eng-notify-2026-09-08.log` (`[09:58:51] sent: active ...`), which is unambiguously local: the stamp matches the log to within 2 seconds, so it's local (PDT), not UTC, and every such subtraction has been overstating elapsed age by ~7h (the offset) — real elapsed as of this pass is ≈17h, not ≈24h. Same ~7h inflation confirmed on `eng052-merge-request` (stated ~18h45m, real ~12h17m) and `eng053-merge-request` (stated ~16h51m, real ~10h23m) via the same log. No wrong nudge actually fired — the inflated numbers still stayed under 24h each time — but the trend was on a path to spending the integrity-check item's one-time nudge roughly 7h before it was actually earned. Fix for a future pass: read the day's `traces/eng-notify-*.log` line (unambiguous local `date` output) rather than diffing the frontmatter ISO string against `date -u`. Not filed as a proposal — no code or doc is wrong, a repeated judgement call was, same class as the `eng053` pass's step-7-scoping note two rows up. |
| 2026-09-09 | eng-manager | aiorders | `continue ENG-051` has now fired at a suspiciously regular ~28-30 minute cadence overnight (checkpoint-cited 05:42 pass, this one at 06:10, both ~29 min after the entry before them) despite every one of these passes explicitly logging that it is *not* self-chaining — none of them owe the next fire. Not confirmed, but the most parsimonious mechanism found by elimination: step 8's dead-end sweep on some *other* ticket's pass only lists "waiting on the approver / blocked / terminal / held by a cap" as justified reasons a `chained: none` line may stand unresumed; "container parent, no actionable child, both children parked" isn't one of the enumerated four, so a sweep applying that list literally would read `ENG-051`'s own correctly-idle `chained: none — idle:` as a forgotten chain and re-fire it — every ~28min possibly being however often some other event pass's own step 8 runs. Did not trace `eng-trigger.sh`'s actual dead-end-sweep implementation to confirm this rather than guess further; a `continue` event's reading map doesn't call for that, and each fire so far has cost a correct no-op, not a wrong action. Worth someone checking if the overnight cadence continues. |
| 2026-09-09 | eng-manager | aiorders | **Root cause found for the row above — the guess in it was wrong.** It is not another ticket's step 8 (no other ticket ran a pass all night for one to run from — `traces/eng-loop-2026-09-09.log` shows only `continue (ENG-051)`, 13 times back to back). `launchctl list \| grep eng` shows a job not in `eng_build_loop.md`'s trigger table: `com.businessos.eng-drain` → `lib/eng-drain-poll.sh`, `StartInterval` 300s. Its "IDLE POLL" half (added 2026-09-07 per the approver's own "always busy, 24 hours" direction, per the script's header comment) board-scans ticket frontmatter for `state:` in the machine's range and fires `continue` on the lowest-rank match whenever nothing is queued and no pass is running, capped once/30min via `traces/.idle-fired`. The scan reads only `state:`/`priority:` — it has no `parent:`/children awareness, so it cannot see the Guards rule that a container "holds no machine slot in any state." `ENG-051` is a container sitting at `state: building` (waiting on children, not owing work) and the only ticket anywhere on the board in the machine's range right now, so the scan picks it unconditionally, every cycle. Confirmed directly against `logs/eng-drain-poll-2026-09-09.log` (13/13 lines: "idle with ENG-051 in the machine's range and nothing queued, firing 'continue ENG-051'"), not inferred. `eng-drain-poll.sh` itself is unmodified/clean in git. Fix: before picking, exclude any id that appears as another ticket's `parent:` (i.e. is a container) — a small change to the scan, not to the design. Not filed as a proposal — flagged directly to Harry in-session instead, since the cost is actively accruing (~2 sessions/hour, 13 so far today, 20/day per-ticket cap the only backstop) and a week-long batch cycle doesn't match that. This row exists so the record survives even if the direct flag is missed. |
| 2026-09-09 | eng-manager | aiorders | **Follow-up to the two rows above, not a new mechanism.** The `07:14:23` fire (`logs/eng-drain-poll-2026-09-09.log`) was this pass's own trigger — confirmed by process tree (`ps -o pid,ppid`: `eng-drain-poll.sh` → `eng-trigger.sh continue ENG-051`, started `07:14:23`/`:25`, no matching "trigger exited 0" yet at the time of checking), not assumed from timing alone. This pass found `ENG-051`'s two children had shipped, ran `acceptance-check` on both, and settled the whole family to `verified` (see `ENG-051`'s own board-file log, this date). Once `ENG-051` leaves `ready..ready-to-ship` for a terminal state, the machine's range on this board is empty, so `eng-drain-poll.sh`'s IDLE POLL board-scan should have nothing left to pick — this should stop the specific overnight cadence, as a side effect of ordinary ticket progress, not a fix to the script. Flagged as "should," not "does": not empirically confirmed (would need to observe the next poll cycle decline to fire, which this pass can't wait around for), and the underlying bug in `eng-drain-poll.sh` (no `parent:`/container awareness) is still live and unpatched — it will misfire the same way against any future lone container ticket sitting at a machine-range state with only parked/verified children. The proposed fix from the row above still stands and still needs Harry's word; this row doesn't reduce that need, only notes why the symptom may go quiet for now. |
| 2026-09-09 | product-manager | aiorders-api | `lib/eng-notify.sh raise inbox/2026-09-09-eng054-g1-scope.md` logged `sent: active ...` instead of `sent: raise ...` — the script's own `MODE="${1:?mode required}"` (raise/nudge) is silently overwritten when it later sources repo-root `.env`, which defines its own unrelated `MODE` (the quiet-mode switch, `active` here). Cosmetic only: the Slack post itself sent fine and the message body keys off `$GATE`, not `$MODE` — but worth a rename (e.g. `NOTIFY_MODE`) if anyone ever debugs this log expecting it to say what it did. |
