# Security notebook — 2026-09-04

## ENG-033 (aiorders-api) — pre-existing HTML-injection into the owner-notification email, found reading `catering-request/index.ts` in full (proposal filed)

**Category:** A03 Injection, missing output encoding for an HTML sink.
`index.ts:348-378` (unchanged by `ENG-033`'s diff) builds `message_html`/
`message` via raw template-literal interpolation of `full_name`, `phone`,
`email`, `event_type`, `event_location`, `event_timings`, `number_of_guests`,
`normalizedDeliveryMethod`, and `requirements` — every one attacker-controlled
on this intentionally unauthenticated public endpoint — with zero
HTML-escaping. `send-notification/index.ts:142-162` forwards both fields
verbatim to a third-party workflow webhook (Pabbly Connect) that actually
emails/SMS/WhatsApps the restaurant owner. Confirmed by reading both files
directly.

**Severity: Medium.** Bounded impact — most email clients strip `<script>`/
inline event handlers, so this is an HTML-formatting/link-injection
(phishing/spoofing) vector inside an otherwise-legitimate transactional
email, not script execution or data exposure — but real, reachable by
anyone, zero cost, zero authentication, today, independent of this ticket.

**Not blocking `ENG-033`'s gate:** the vulnerable lines are untouched by this
diff, and `action_type`/`selections` — the only two fields this ticket adds —
are not included in either template. Filed as a proposal
(`agents/eng-manager/proposals.md`, 2026-09-04, security, `aiorders-api`),
same routing `ENG-032`'s own review used for an analogous pre-existing/
out-of-diff finding.

Full detail: `agents/security/reviews/ENG-033.md`, Finding 1.

## ENG-033 (aiorders-api) — RLS-on-`catering` still empirically unverified (3rd occurrence: `ENG-015`→`restaurants`, `ENG-031`→`catering`, now `ENG-033`)

**Category:** A01-adjacent, verification gap rather than a confirmed defect —
carried forward across three tickets now on the identical root cause (no
live Postgres or Supabase MCP reachable from any session on this host).
`ENG-031`'s 2026-09-03 entry in this notebook named this gate specifically —
`ENG-033` — as the point to close it empirically, since it's the first
ticket making `selections`/`action_type` reachable through a real path.
Checked this pass for a way to do that: searched for a Supabase/Postgres MCP
tool; none available this session either.

Re-derived the static evidence directly rather than carrying forward the
prior account on trust: independently `grep`-confirmed no tracked migration
ever runs `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` on `catering` or
`restaurants` (other tables do, in tracked history, confirming the
convention exists here), and read `20250729143357_initial_restaurant_rls.sql`
in full — its own framing ("Phase 1: Critical Database Security Fixes",
removing "dangerous policies" that were "exposing all restaurant data") only
makes sense if RLS was already being enforced; a policy has no effect
otherwise, dangerous-sounding or not. Slightly stronger evidence than the
prior entries cited (the migration's own stated intent, not just the bare
fact that policies existed) — same non-blocking conclusion, reached
independently rather than assumed.

**Not re-routed to a future ticket this time** — there isn't an obviously
better-positioned one (`ENG-034` doesn't touch reads of this table either),
and mechanically punting a third time without closing anything would just be
a fourth occurrence waiting to happen. Instead: surfaced as a direct,
low-cost, concrete ask in this ticket's own L1 merge-request item
(`inbox/2026-09-04-eng033-merge-request.md`) — a ten-second dashboard check
(`relrowsecurity` on both tables) only the approver can currently do, given
no session on this host has the live access to do it. Not a gate condition,
not a G3 — an FYI. The standing infrastructure gap itself (no live DB access
from this host) is already `proposals.md`'s 2026-08-29 row; not re-filed.

Full detail: `agents/security/reviews/ENG-033.md`, RLS verification section.
