# Security notebook — 2026-08-31

## ENG-013 (aiorders-api) — raw `error.message` returned in a 500 response body

**Category:** A05 Security Misconfiguration (verbose error).
**Severity:** Low. Occurrence: 1 of 3 (three-strike tracking starts here).

`setStageOverride`/`resetStageOverride` in `admin-portal/handlers/foodswipe.ts`
both catch and return `error instanceof Error ? error.message : 'Unknown
error'` directly in the client-facing 500 body. Not blocking this ticket's
gate: the route is role-gated before either function runs (only
admin/sub-admin ever sees the message), and the pattern is copied verbatim
from this same file's pre-existing `GET` catch block, not introduced fresh
by this diff. Worth tracking because it is now present in at least this one
handler in two write actions; if it recurs in a third handler, it is a
standards-promotion candidate for `engineering-standards.md`
(principal-engineer owns that edit, per the three-strike rule — security
proposes, doesn't write).

**Next occurrence:** check this entry before re-filing as a fresh finding.

## ENG-008 (aiorders-api) — raw `error.message` returned in a 500 response body (2nd occurrence)

**Category:** A05 Security Misconfiguration (verbose error).
**Severity:** Low. Occurrence: 2 of 3 — see the ENG-013 entry above for the first.

Same shape, same file family: `getInfluencer`/`updateInfluencer` in the new
`admin-portal/handlers/influencers.ts` both catch and return `error
instanceof Error ? error.message : 'Unknown error'` directly in the
client-facing 500 body. Not blocking, for the same reasons as the first
occurrence: the route is role-gated before either function runs, and the
pattern is copied from this repo's own pre-existing convention, not
introduced fresh by this diff.

**Context checked this pass, not assumed.** A repo-wide grep across
`admin-portal/handlers/` finds this exact pattern in **8 files total**
(`brands.ts`, `activation.ts`, `restaurants.ts`, `foodswipe.ts`, `billing.ts`,
`influencers.ts`, `partners.ts`, `auth.ts`) — six of them pre-date this
department's review process entirely. Three-strike tracking here counts
occurrences *this gate has reviewed and flagged*, not the repo's pre-existing
total, since the latter would make every legacy file an instant third strike
the first time any of them is next touched, regardless of whether that
ticket's own diff has anything to do with error handling.

**Next occurrence:** 3rd — propose promoting to `engineering-standards.md`
per the three-strike rule (security proposes, principal-engineer writes).
