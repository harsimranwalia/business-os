---
ticket: ENG-047
project: restaurant-marketplace
reviewer: principal-engineer
date: 2026-09-07
verdict: pass
round: 1
diff: "feat/ENG-047-foodswipe-open-now-and-channel-display (5dbb76c) vs origin/master"
---

# Code review — ENG-047 (round 1)

## Scope

Six-file diff, `git diff origin/master...HEAD --stat`: `FilterBar.tsx` (+11/-1),
`RestaurantCard.tsx` (+9/-1), `RestaurantList.tsx` (+2), `useRestaurants.tsx`
(+2), `api.ts` (+4), `types/index.ts` (+5) — 35 insertions, 2 deletions. No
drift (`git rev-list --left-right --count origin/master...HEAD` → `0 1`).

## Automatic-failure scan (`engineering-standards.md`)

| # | Check | Result |
|---|---|---|
| 1 | Secret/credential/token/key committed | Clean |
| 2 | Silent exception swallow | N/a — no exception handling touched |
| 3 | Missing test on a bug fix | N/a — feature, not a bug fix |
| 4 | `any`/untyped public interface | Clean — new fields are plain `boolean` / `string \| null` optional, no `any` |
| 5 | Unbounded query / missing pagination | N/a — no query changed; this diff only threads an existing param and renders an existing field |
| 6 | New dependency, no justification | Clean — none |
| 7 | Unrelated refactor bundled in | Clean — all six files are exactly the design's `## Components` rows for `restaurant-marketplace`; nothing else touched |
| 8 | Commented-out code / unowned `TODO` | Clean — the two added comments (`api.ts`'s snake-case note, `RestaurantCard.tsx`'s ADR-010 note) both explain *why*, not *what* |
| 9 | Direct datastore write bypassing the data layer | N/a — frontend only, no datastore access |
| 10 | Auth/payment/deletion path changed, no failure-case test | N/a — unrelated path |

**0/10 open.**

## Design conformance

Checked against `agents/architect/designs/ENG-026-foodswipe-channel-visibility.md`'s
`## Components` (`restaurant-marketplace` rows) and `## Interfaces` →
`### FilterBar.tsx`, and independently against `ENG-045`'s actual shipped
contract rather than taken on this ticket's own Notes:

- **Wire contract, confirmed against `aiorders-api`'s shipped
  `feat/ENG-045-foodswipe-channel-visibility-discovery-handlers` branch
  directly**, not this ticket's word for it: `utils/validation.ts` reads
  `params.get('open_now')` (snake_case) and `handlers/restaurants.ts` returns
  `status` on discovery-list rows (`statusById.get(...) ?? null`, line 146).
  This diff's `api.ts` appends `open_now` and `RestaurantCard.status` reads
  the same key — matches exactly.
- **Chip visibility.** Read the JSX directly: the new button sits *outside*
  the `mode === 'order-food' || mode === 'dine-in'` block (`FilterBar.tsx`
  lines 214-233) that gates Offers/Rating 4+ — renders on all three modes,
  per the design's explicit instruction.
- **`openNow` reset behaviour, traced rather than taken on the ticket's
  claim.** The tab-change effect (`RestaurantList.tsx:213-221`) is a merge
  (`setFilters(prev => ({...prev, hasOffers: undefined, rating4Plus:
  undefined, ...}))`) that never names `openNow` — correctly leaves it alone
  across a tab switch. `handleClearFilters` (line 264) is a full
  state-replacement literal that also never names `openNow` — correctly
  drops it to `undefined` on Clear All. Two opposite code shapes, both
  already correct before this diff touched either function; nothing needed
  to change in either to get the design's documented divergence.
- **`RestaurantCard.tsx`'s `status` render** — added as a fourth clause in
  the existing growing-separator meta line, OR-ing all three earlier
  conditions (`cuisine`, dine-in `address`, `distance`) plus `priceText`
  before its own `metaDot` — matches the row's own established pattern
  exactly, extended rather than copy-paste-mutated.

## Review of the whole diff for shape

One concern (thread and render one new server-provided signal), fully
additive. The new "Open Now" button duplicates the existing Offers/Rating 4+
button's JSX shape rather than factoring a shared sub-component — but that
duplication (two un-factored, near-identical buttons) already existed before
this ticket; matching it is "reads like the code around it," and factoring a
shared component now would itself be an unrelated refactor bundled into a
feature change (automatic failure #7) for a file this ticket didn't create
the duplication in. Could not usefully be smaller.

## Review of the lines

`filters.openNow`/`restaurant.status` both optional and read only behind a
truthy check — undefined/null correctly render nothing (default-off chip,
no status text) rather than throwing or rendering `"undefined"`. The
`useRestaurants.tsx` dependency array gets `params.openNow` appended
alongside its siblings, so the fetch effect re-fires on toggle, same as
`hasOffers`/`rating4Plus`. Toggle handler (`!filters.openNow`) is idempotent.
No concurrency, ordering, or timezone concerns — the diff computes nothing,
it only threads and renders values `ENG-045` already computed server-side.

## Review of what isn't there

- **`RestaurantCard`'s three new `has_*` fields are currently dead weight on
  every real call site of this type.** Confirmed independently: grepped
  `aiorders-api`'s shipped `ENG-045` handler and found the discovery/list
  code paths never populate `has_order_food`/`has_dine_in`/`has_catering` on
  a row object (only `handleRestaurantDetail`'s separately-typed response
  does), and grepped this repo for every consumer of the `RestaurantCard`
  type (`RestaurantList.tsx`, `Collections.tsx`, `Deals.tsx`,
  `Favorites.tsx`, `useRestaurants.tsx`) — none is the detail page. So these
  three fields have zero producers today. **Not a defect** — the design's
  own `## Components` row names exactly these four fields for this exact
  file, and its Interfaces section calls the detail-endpoint echo
  "not required by any AC but natural," i.e. deliberate, forward-looking
  type design, not a slip. The frontend's own build notebook already flagged
  this as an uncertainty worth a review question rather than staying silent
  about it — confirmed here rather than taken on that word. Non-blocking:
  zero runtime cost, three optional fields, trivially removable later;
  revising an already-approved design is outside this gate's mandate.
- **No automated test** — addressed at the quality gate, not held here; see
  `agents/qa/test-plans/ENG-047.md`. Same zero-test-infrastructure gap this
  whole `ENG-026` family has hit on `aiorders-admin-hub` (`ENG-046`) — except
  `restaurant-marketplace` had no proposal tracking it yet (unlike its three
  registered siblings). Filed this pass, see Notebook below.

## Test quality

N/a — no test written this round, none required (no harness exists; see
Design conformance / What isn't there and `agents/qa/test-plans/ENG-047.md`).
Evidence class substituted: line-by-line diff trace above plus independent
re-execution of typecheck/build, not taken on the build hop's own account —
see Independent re-verification.

## Independent re-verification

Not trusted from the ticket log: `npm run typecheck` (`tsc --noEmit`) —
clean, zero errors. `npm run build` — clean, 1803 modules, no new warnings,
matches the build hop's own account exactly. `npm run lint` reproduced the
identical pre-existing failure (`.eslintrc.cjs:18`, `SyntaxError: Unexpected
token ']'`); confirmed via `git diff origin/master...HEAD -- .eslintrc.cjs`
(empty) that this diff does not touch that file.

## One inaccuracy found and corrected in this pass's own proposal filing, not
a code defect

`agents/eng-manager/proposals.md`'s 2026-09-07 `restaurant-marketplace` lint
row (filed by `frontend` in the same build hop) cited `ENG-032`/`ENG-034` as
prior diffs the broken lint config had "silently checked nothing" for.
Checked both directly: `ENG-032` is `restaurant-portal`, `ENG-034` is
`config-site-builder` — neither touches this repo, and a repo-wide board
search confirms `ENG-047` is in fact the first engineering-department ticket
ever filed against `restaurant-marketplace` with a real diff (`ENG-026` is a
work-breakdown container, no diff of its own). Corrected in place rather
than left to mislead the next reader; not a shipped-code defect, so not
blocking. Full note: `agents/qa/notebook/2026-09-07-coverage-gaps.md`.

## Style / conventions

Branch `feat/ENG-047-foodswipe-open-now-and-channel-display` matches
`{type}/{ENG-NNN}-{slug}`. Commit (`5dbb76c`) imperative, present tense,
cites the ticket.

## Notebook

Full method: `agents/principal-engineer/notebook/2026-09-07-review-log.md`.
No new finding pattern promoted — the proposal-citation inaccuracy above is
the second occurrence of this general class (first: `ENG-046`'s notebook
mischaracterizing `ENG-040`), not yet a third, and this round's own reuse of
`ENG-040` as a "thin, no new logic branch" precedent was checked directly
against `ENG-040`'s own ticket file and found accurate this time — not a
repeat of that specific inaccuracy.

## Verdict: PASS

0/10 automatic failures, no divergence from design, wire contract
independently confirmed against `ENG-045`'s actual shipped code, both reset
effects traced rather than assumed, typecheck/build independently re-run
clean. `links.review` set to this file. Continues to `in-security` — QA's
gate ran concurrently on the same diff, also pass:
`agents/qa/test-plans/ENG-047.md`.
