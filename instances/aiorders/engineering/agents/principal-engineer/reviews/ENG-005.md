---
ticket: ENG-005
project: aiorders-admin-hub
reviewer: principal-engineer
date: 2026-08-27
round: 1
verdict: pass
diff: chore/ENG-005-a4-poster-generator-wire-in (51cdb29) vs origin/main
---

# Code review — ENG-005 (round 1)

## Recovery context — this diff predates this review's own pass

The commit under review was not produced by this pass. `building`'s own
session pushed it (`51cdb29`, "Wire A4PosterGenerator into RestaurantDetails",
committer timestamp 2026-08-27 14:02:21 -0700) and the branch was already
`up to date with origin` when this pass opened the worktree, but the ticket's
board file and log still read `state: ready` with no record of any of it —
`traces/.hops-2026-08-27-ENG-005` (`2`) confirms this is the second dispatch
of `continue ENG-005` today, so the first one did the work and died before
writing back. Treated exactly like `ENG-002`'s precedent for the same
situation: verified independently below rather than taken on faith, checked
for a live competing process before touching anything (none — the other
`eng-trigger.sh`-ancestored processes on this host are this pass's own
lock-holding orchestrator, confirmed via `ps`/ancestry walk), and confirmed
the worktree carries no other uncommitted stray changes (`git status`: clean).

## Automatic-failure scan (engineering-standards.md, 10 items)

None hit: no secret/credential in the diff, no exception handling touched (no
new failure path introduced), no bug fix here so the missing-regression-test
item doesn't apply, no untyped public interface (`A4PosterGeneratorHandle`/
`Props` in the component are unchanged and already typed; the new call site
passes only already-typed values), no query of any kind, no new dependency
(`package.json`/lockfile untouched — confirmed via `git show --stat 51cdb29`),
no unrelated refactor bundled (single file, single concern), no commented-out
code or unowned `TODO`, no datastore write, no auth/payment/deletion path
touched.

## Design conformance

Matches `agents/architect/designs/ENG-005-a4-poster-generator-wire-in.md`
exactly: one new `<Card>` in the page's existing `grid gap-6 md:grid-cols-2`
(confirmed the grid opens at line 279 and the new Card sits immediately before
that grid's own closing `</div>` at what is now line 913 — inside it, not
appended after), rendering `<A4PosterGenerator>` with the four real props
(`restaurantName`, `websiteUrl`, `logoUrl`, `restaurantId`) plus
`primaryColor={null}`. No deviation from the approach, the component list, or
the "no new dependency" claim.

## Diff shape

One file, one concern — an import line and one `<Card>` block. Eighteen lines
added, nothing removed, nothing touched outside `RestaurantDetails.tsx`. No
abstraction beyond what this ticket needs.

## Line-level

- `import { A4PosterGenerator } from '@/components/A4PosterGenerator';` —
  matches the existing import style in this file (`@/` alias, named import).
- `primaryColor={null}` — independently confirmed against the component's own
  `Props` interface (`src/components/A4PosterGenerator.tsx`): `primaryColor:
  string | null`, so this type-checks without a cast. Also confirmed
  `logoUrl: string | null` accepts `restaurant.logo_url` (typed `string` on
  the page's own `Restaurant` interface, line 34) without narrowing.
  `restaurantName`/`websiteUrl`/`restaurantId` are all typed `string` on both
  sides — no mismatch.
- Copy ("Marketing Poster" / "Printable A4 poster with a QR code linking to
  this restaurant's ordering page") is a plain descriptive admin-UI label, not
  customer-facing brand copy — outside the "brand owner" restriction
  `agents/frontend/agent.md` names, since this project has no dedicated
  content agent and the audience here is the restaurant operator, not the end
  customer.

## What isn't there

- No automated test — no test command exists for `aiorders-admin-hub` at all
  (`config/projects.md` Commands table: lint + build only), and this ticket's
  own PRD non-goal is explicit ("does not pursue comprehensive testing...
  smoke-level at most"). Not a gap for this ticket; see the QA test plan for
  what stands in for it.
- No migration, no rollback plan: `touches_data: false`, confirmed by reading
  the diff (no schema, no new query, no new table access).
- No new log line: nothing here can fail in a way distinct from the rest of
  the page's already-logged fetch — the component itself already degrades
  silently on a missing/failed session (see the security review for the
  traced failure path).

## Independent verification this round (not taken on the prior pass's word, because there was no prior pass's word on record)

- Read the full diff directly (`git show 51cdb29`) rather than trusting the
  commit message's own summary.
- Confirmed `Restaurant`'s `id`/`name`/`website`/`logo_url` fields exist with
  those exact names, by grep against the interface and its other usages
  elsewhere on the same page (e.g. `restaurant.logo_url` already read/written
  above, at the existing Logo card).
- `grep -rn "A4PosterGenerator" src/` — exactly one import, one usage. No
  duplicate render path.
- `npm run lint`, this branch: 181 problems (150 errors / 31 warnings). Then
  `git checkout origin/main --detach`, re-ran `npm run lint`: identical, 181
  problems (150 errors / 31 warnings), same last line
  (`tailwind.config.ts:130` `no-require-imports`). **Zero new lint findings.**
  Checked back out to `chore/ENG-005-a4-poster-generator-wire-in` afterward;
  `git status` confirmed clean, `git log` confirmed the same commit still at
  the tip — the comparison checkout left nothing behind.
- `npm run build`: succeeds, `dist/` emitted (`index.html`, one CSS bundle,
  the usual chunk set); only pre-existing chunk-size/browserslist-age
  warnings, unrelated to this diff.
- No dependency change is possible to audit for — `package.json` and the
  lockfile are absent from this diff's file list.
- No live browser render performed this pass — `aiorders-admin-hub` requires
  an authenticated Supabase session and real restaurant data to reach this
  route, and this instance's own precedent (`ENG-002`, `ENG-004`) has never
  used a live dev-server check as part of this gate. Recorded as a manual-
  verification gap with its reason, per `definition-of-done.md`'s own
  checklist item, not silently assumed away — the production `build` above is
  the real signal available here (it type-checks the JSX tree and would fail
  on a bad prop type or broken import).

## Verdict: PASS

Small, complete, reversible, exactly as scoped. The one thing worth naming for
whoever reads this ticket later: the actual coding happened in a session that
never got to record it, so this review did more independent re-derivation than
a normal round-1 would — that redundancy is the correct response to an
unrecorded prior pass, not evidence of anything wrong with the diff itself.
