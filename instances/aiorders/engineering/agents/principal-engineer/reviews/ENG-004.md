---
ticket: ENG-004
project: aiorders-admin-hub
reviewer: principal-engineer
date: 2026-08-27
round: 1
verdict: pass
diff: none — verification ticket, no diff in any registered project (ADR-003, ADR-004)
---

# Code review — ENG-004 (round 1)

## What this reviews

Per `ADR-004` (`agents/architect/decisions/ADR-004-eng004-verification-ticket-second-occurrence.md`),
extending `ADR-001`'s pattern, ENG-004 is a **verification ticket**: all five
acceptance criteria are satisfied without producing a diff in this ticket's
own `building` state — the diff already exists on `origin/main` in both
`aiorders-admin-hub` and `aiorders-api`, produced by the approver directly on
2026-08-24, two days before this ticket reached `designed`. There is no
branch, no PR, no code of this ticket's own to review for shape or style.
What review means here is confirming the claims recorded at `building` are
**actually true on disk and in git history right now** — independently,
not on the strength of the design doc's or the `building` pass's own prior
citations.

## Independent re-verification (this round, not cited from any prior pass)

Ran fresh in the department's own worktrees (`~/Documents/projects/_eng/aiorders-admin-hub`,
`~/Documents/projects/_eng/aiorders-api`; the human's checkouts were never
touched, per `config/projects.md`), `git fetch origin` in both first.

**AC1 — project linkage.** `grep project_id supabase/config.toml` in both
repos, read directly this round: both name `project_id =
"bmnmnejwdxbcqinqkwko"`, identical. **Confirmed.**

**AC2/AC3 — authoritative repo, and consolidation under a different name.**
`git ls-tree -r --name-only origin/main -- supabase` on `aiorders-admin-hub`
returns exactly one entry, `supabase/config.toml` — no `migrations/` or
`functions/` directory exists on `origin/main`. Both matched consolidation
commit pairs are present, unchanged, on their respective `origin/main`s this
round (`git show --stat` on all four): `aiorders-api@4b6a835` /
`aiorders-admin-hub@c90c02c` ("Add restaurant/profile migrations moved
from aiorders-admin-hub" / "Remove migrations moved to aiorders-api"), and
`aiorders-api@5b3bac2` / `aiorders-admin-hub@919d355` ("Consolidate remaining
migrations from aiorders-admin-hub" / "Remove supabase/migrations, fully
consolidated into aiorders-api"). Re-hashed all six named files this round
(`shasum -a 256`, not re-run from a script but issued directly), admin-hub's
tree at `7009f18` against `aiorders-api`'s current `origin/main` — all six
**identical**, pairwise:

| File (admin-hub, pre-removal) | SHA-256 | Matches `aiorders-api` origin/main |
|---|---|---|
| `20250729143432-1040fac4-...sql` (→ renamed `20250729143432_updated_at_functions.sql`) | `a459678e…6c61` | yes |
| `20250814063528_6a950847-...sql` | `996fc0e7…9e647` | yes |
| `20250814064923_3be8f472-...sql` | `12961054…6fb556` | yes |
| `20250814065439_d1efe4c6-...sql` | `534f704c…d61f29` | yes |
| `20250814065606_3be00703-...sql` | `ef7a1ba7…9f60a9` | yes |
| `20260312000001_restaurant_activations.sql` | `057dea33…2175897` | yes |

Zero content drift; one filename rename, five unchanged filenames.
**Confirmed**, and by a stronger check than a byte-diff (a cryptographic
hash over full file content).

**AC4 — replay integrity.** `git ls-tree -r --name-only origin/main --
supabase/migrations` on `aiorders-api`, piped through `sort`, lists **22**
files; `20260312000001_restaurant_activations.sql` (line 12) immediately
precedes `20260408000001_google_review_history.sql` (line 13) — the table
the six files' one schema-creating migration produces is created before the
later migration that alters it. **Confirmed.**

**AC5 — pending deletion resolved.** `git rev-list --left-right --count
main...origin/main` in `_eng/aiorders-admin-hub` returns `0	0`; `git branch
-vv` shows local `main` at `919d355 [origin/main]`, the same commit — no
ahead, no behind. Ref-level only, per `config/projects.md`'s repo isolation;
the human's own working-tree checkout was not read. **Confirmed.**

All five hold exactly as `designed` and `building` recorded them — nothing
has drifted in the day since.

## What isn't here, and why that's correct per ADR-004

- No line-level diff comments — there is no diff belonging to this ticket.
- No automatic-failure scan against `engineering-standards.md` — that
  checklist presumes source code written for this ticket; none was.
- No confirmation of the RLS/`search_path` policy *content* inside the six
  files beyond byte-identity — per `ADR-004`, that substantive check belongs
  to the security gate, not this one, and must not be waved through there as
  ceremony. This review confirms the files are unmodified and in the right
  place; it does not re-audit what they say.
- No dependency check — no `package.json`/lockfile change anywhere in this
  ticket's evidence trail.

## Verdict: PASS

All five acceptance criteria hold on disk and in git history, independently
re-checked this round via fresh commands rather than inherited from the
design doc or the `building` log entry. The one substantive engineering
judgement this ticket required — what `in-review` means without a diff, and
that a second occurrence of `ADR-001`'s shape doesn't yet warrant an
internal-lane G2 — was made by the architect in `ADR-004`, with alternatives
weighed. Nothing to flag back.
