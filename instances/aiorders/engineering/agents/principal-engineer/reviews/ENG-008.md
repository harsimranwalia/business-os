---
ticket: ENG-008
project: aiorders-admin-hub
reviewer: principal-engineer
date: 2026-09-02
verdict: pass
round: 3
diff: "aiorders-api: feat/ENG-008-influencer-admin-management (7c6e4b8) vs origin/main, merge-base 40d7c36; aiorders-admin-hub: feat/ENG-008-influencer-admin-management (141f2eb) vs origin/main, merge-base edf6947"
---

# Code review — ENG-008 (round 3)

## Why this round exists

Round 2 passed (2026-08-31) and both PRs opened. The approver's merge-request
reply (`inbox/_handled/2026-08-31-eng008-merge-request.md`, answered
2026-09-01, only reached this Mac via the 2026-09-02 `1b72b26` merge) rejected
`accepts_barter` as redundant with the pre-existing `barter_visit` column:
"There is no need for a new column when we already have a column to signify
the same intent." Per this ticket's own log, the fix hop applied a four-point
mechanical change (migration, handler, test, frontend) and re-chained for a
fresh review — round 2's receipts do not carry over to this diff.

## What changed since round 2

Re-derived from the diff directly, not from the ticket log's own account.

- **`aiorders-api`**, one commit ahead of round 2's `57f8c4b`: `7c6e4b8` drops
  `ADD COLUMN accepts_barter` and its backfill from the migration;
  `EDITABLE_FIELDS`/the PATCH validation block now validate `barter_visit`
  (unchanged boolean-typed, optional-field shape — an identifier rename of
  the validation, not new logic); the one test targeting the dropped field's
  rejection path was renamed end to end (test name, request body key,
  expected error string).
- **`aiorders-admin-hub`**, one commit ahead of round 2's `63be255`: `141f2eb`
  widens `Influencer.barter_visit`/`InfluencerEditForm.barter_visit` to
  `boolean | null` (matches the live schema; previously understated as
  non-nullable), removes `accepts_barter` from both interfaces,
  `openInfluencer`/`handleSaveInfluencer`/the Payment Type badge/the
  edit-dialog checkbox all now read and write `barter_visit` directly
  (including the DOM id, `edit-accepts-barter → edit-barter-visit`, so no
  label points at a field the form no longer has).

## Automatic-failure scan (`engineering-standards.md`)

Re-run in full against the whole diff, not just this round's delta.

| # | Check | Result |
|---|---|---|
| 1 | Secret/credential/token/key committed | Clean — no key/secret/token/bearer/PEM pattern anywhere in either repo's diff |
| 2 | Silent exception swallow | Clean — unchanged from round 2, both backend catches log via `console.error` before returning 500; the frontend catch logs and surfaces a `toast` |
| 3 | Missing test on a bug fix | The rename itself has a direct test (`rejects a non-boolean barter_visit`) — **mutation-verified this round**: removed the validation's type check by hand, re-ran, and exactly that one test went red (18/19), confirming it tests the real branch and not a vacuous rename. Frontend still has no test framework on this repo (reconfirmed fresh — no `vitest`/`jest`/`@testing-library`, no `test` script, zero `*.test.*`/`*.spec.*` files); same non-blocking, already-proposed gap every round on this ticket has carried |
| 4 | `any`/untyped public interface, undocumented | `AuthenticatedRequest` unchanged since round 1/2 — not touched by this round's diff. Already tracked (not a fresh occurrence): `agents/principal-engineer/notebook/2026-08-31-review-log.md` names this exact file as one of two untyped occurrences (`foodswipe.ts`, `influencers.ts`) with a proposal already filed 2026-08-29 (`hasXAccess`/`AuthenticatedRequest` duplication, `proposals.md`); not re-litigated here |
| 5 | Unbounded query / missing pagination | Clean — unchanged, `GET`/`PATCH` both single-row by id |
| 6 | New dependency, no justification | Clean — no new package on either repo, no new import this round |
| 7 | Unrelated refactor bundled in | Clean — every changed line in this round's own delta is part of the rename; grepped `accepts_barter` across both repos' tracked `.ts`/`.tsx`/`.sql` files: zero remaining references |
| 8 | Commented-out code / unowned `TODO` | Clean — grepped `TODO\|FIXME` across every changed file: zero matches |
| 9 | Direct datastore write bypassing the data layer | N/a — unchanged, `auth.adminSupabase` directly, matches house style |
| 10 | Auth/payment/deletion path changed, no failure-case test | Unchanged from round 2 (0/10 there too) — this round touches validation content, not the access-control gate itself; all 6 `hasInfluencerAdminAccess` unit tests plus the 403/405 integration tests are untouched and still pass |

**0/10 open.**

## Design conformance

Checked against the PRD's 8 acceptance criteria (unchanged by this round —
the rename doesn't touch AC scope) — still satisfied, same reasoning as
round 2's own AC1–AC8 walk, now reading `barter_visit` wherever it
previously read `accepts_barter`.

**One real finding: the architect's design doc is now stale, not just the
old review receipt.** `agents/architect/designs/ENG-008-influencer-profile-admin-management.md`
lines 99–101 and 128–131 still specify `accepts_paid` / `accepts_barter` as
"two new `boolean` columns... backfilled from the existing `barter_visit`" and
list `accepts_barter` as a valid `PATCH` body field. Both are now false: the
running code, the migration, and the migration doc all agree on
`accepts_paid` (new) + `barter_visit` (reused, not duplicated). This is
prose drift, not a code defect — the code is correct and matches the
approver's own explicit, authoritative correction — but it's exactly the
kind of stale "instruction" file `eng_build_loop.md` step 6b warns about: a
future reader (the next engineer touching this handler, or a gate re-reading
"what was this supposed to do") who trusts the design doc over the code
would be misled into thinking `accepts_barter` is still the intended shape.
**Not fixed in this review** — `agents/architect/designs/` is the
architect's owned artifact per this role's own `never_touches` list
("architecture decisions — the architect owns those — but you flag drift");
flagged here and in `observations.md` rather than edited directly.

## Verification reproduced this review

Real execution this round, not hand-tracing — this host has `deno` on `PATH`
(`2.9.6`), same as the build hop that applied this fix.

- **`deno check`** on the modified handler: clean.
- **`deno test`**, executed directly: **19 passed, 0 failed** (12ms),
  including the renamed `rejects a non-boolean barter_visit` case.
- **Mutation-tested the renamed validation** (see automatic-failure #3
  above): reverted the `typeof body.barter_visit !== 'boolean'` guard by
  hand, re-ran — `handleInfluencers PATCH rejects a non-boolean barter_visit`
  failed and was the *only* failure (18/19) — confirms the test is wired to
  the actual branch it claims to cover, not passing vacuously.
- **`npm run lint`** (`aiorders-admin-hub`, fresh): 150 errors / 31 warnings
  total, identical to this ticket's recorded baseline across every prior
  round. Isolated `Influencers.tsx`: exactly the one pre-existing
  `react-hooks/exhaustive-deps` warning on `fetchData`, zero new.
- **`npm run build`** (fresh): clean, 3340 modules, same pre-existing
  chunk-size notice only.
- **Grepped `accepts_barter` across both repos' full tracked trees** (not
  just the diff): zero remaining code references. The migration doc's own
  four prose mentions are explanatory ("no `accepts_barter` column exists
  anywhere in this ticket's shipped shape") — correct, not stale.

## A cross-ticket finding, more consequential than the design-doc drift above

**`ENG-009`'s branch does not contain this fix, and `ENG-009` is already
`blocked`/`blocked_on: approver` with an open, unanswered merge request
(`inbox/2026-09-02-eng009-merge-request.md`, `decision:` blank).** Checked
directly, not assumed: `git merge-base --is-ancestor 7c6e4b8
origin/feat/ENG-009-influencer-engagement-info` (aiorders-api) and the
equivalent for `141f2eb` (aiorders-admin-hub) both return false — `ENG-009`'s
branch tip predates this fix on both repos. `origin/main` on both repos is
still clean of `accepts_barter` (confirmed by direct grep) — neither ticket
has merged yet, so there is a live window to catch this before it becomes a
production problem.

**Why this matters concretely.** `ENG-009`'s own design doc
(`agents/eng-manager/board/ENG-009-influencer-engagement-info.md` lines
712–723, 747–751, 874–878, 962–967) documents that `ENG-009` was built,
reviewed, quality-gated, and security-gated *against* `ENG-008`'s round-2 tip
(`57f8c4b`/`63be255`) — the version that still had `accepts_barter` and the
unconditional-send shape in `handleSaveInfluencer`'s object literal.
`ENG-009`'s own diff inserts `social_stats_platform` into that same object
literal and records a clean rebase against that now-superseded code. None of
`ENG-009`'s four gates have been re-run against this ticket's round-3 fix.
Merging `ENG-009`'s PR as it stands today — stacked on `ENG-008`'s pre-fix
commits — risks reintroducing exactly the redundant column the approver just
rejected, or at minimum leaves `ENG-009`'s own passed gates describing code
that no longer matches what `ENG-008` actually ships.

**Not a new failure shape — this is the second occurrence today of a gap
already found and proposed.** `ENG-009`'s own round-1 review (this same
date, `agents/principal-engineer/notebook/2026-09-02-review-log.md`) found
this exact pattern once already — `ENG-009` stale against `ENG-008`'s
round-2 fix — failed the round on it, and a proposal is already filed
(`proposals.md`, 2026-09-02, `principal-engineer`, sized `S`: check a
ticket's branch against the sibling it forked from, not just against
`main`). `ENG-009` was rebased onto `ENG-008`'s round-2 tip and passed round
2 on the strength of that rebase — then `ENG-008` moved again (this round's
fix) *after* that rebase, so the same ticket is stale against the same
sibling for the second time in one day. The existing proposal, if approved,
would have caught this occurrence too; this is confirming evidence for it,
not a new problem needing a second proposal.

**Not this review's ticket to fix.** Rebasing `ENG-009` and re-running its
four gates is new implementation work on a different ticket that is already
sitting at its own human gate — out of this event's contract (`continue
ENG-008`, resume this ticket only). Not a P0 (nothing is in production,
nothing is being exploited, both PRs are still open and unmerged), so not
escalated to the approver directly. Recorded here, cross-referenced into
`ENG-009`'s own ticket log (informational only, no state change), and noted
in `observations.md` against the existing proposal rather than filed as a
new one — so the next pass that touches either ticket, or the next
scheduled whole-board sweep, catches it before either PR is merged.

## Test quality (shape, not coverage)

Unchanged from round 2's own assessment — this round only renamed one
validation branch and its test, it didn't restructure the suite. The
allowlist test is still mutation-sensitive (asserts the exact object passed
to `.update()`), no test asserts on private/internal structure, naming still
matches the repo's `loyalty-config.test.ts`/`foodswipe.test.ts` convention.

## Named gaps (not blocking, carried forward)

- **Frontend has no automated test** — unchanged, proposal already filed.
- **`AuthenticatedRequest`'s `any` fields** — unchanged, already tracked,
  proposal already filed 2026-08-29.
- **`min_visit_payment` stale-value-on-uncheck** (round 2's own new finding,
  P3) — unchanged by this round's diff, still present, still non-blocking,
  still has a workaround.
- **Raw `error.message` in the 500 body** (security review's own finding,
  2nd tracked occurrence) — unchanged, tracked in the security notebook.

## New findings this round (both non-blocking to this ticket)

1. **Architect design-doc drift** — see Design conformance above.
2. **`ENG-009` branch staleness** — see the dedicated section above. The more
   consequential of the two since it risks an actual regression on a
   sibling ticket already at the approver's desk, not just stale prose.

## Style / conventions

Unchanged from round 2 — branch naming, `EDITABLE_FIELDS`/per-field
validation style, and the edit-form dialog pattern all still match this
repo's established conventions.

## Notebook

Entry added: `agents/principal-engineer/notebook/2026-09-02-review-log.md` —
the `ENG-009` stale-stacked-branch finding, since this is a new failure
shape for this board (a downstream ticket going stale against an upstream
fix hop after already passing its own gates) worth watching for on future
stacked-branch tickets.

## Verdict: PASS

The four-point rename is complete and correct: zero remaining
`accepts_barter` references in either repo's tracked tree, the renamed
validation is mutation-verified to actually guard the field it claims to,
the null-safety discipline round 1/round 2 established survives by
construction (no coalescing fallback reintroduced), and both `deno test`
(19/19, real execution) and the frontend's `lint`/`build` reproduce clean at
the established baseline. No automatic-failure item open. Two new findings
named above, neither blocking this ticket: one is documentation drift this
role doesn't own: the design doc; one is a real risk to a *different*
ticket, out of this event's scope to fix but recorded loudly enough not to
be missed before either PR is merged. `links.review` set to this file.

## Prior pass (superseded)

Round 2 (2026-08-31, `aiorders-api@57f8c4b` / `aiorders-admin-hub@63be255`)
reached PASS reviewing the null-coalescing fix and the missing-profile/
allowlist test gaps. That diff no longer exists on this branch — every
finding from round 2 not restated above (the round-1 recap, the detailed
AC1–AC8 walk, the full test-quality analysis, the `min_visit_payment`
finding's full repro steps) is still accurate for the surviving code, just
not re-derived line-by-line here; see that round's own file history via
`git log -p` on this receipt, or the ticket's own log, for the complete
prior text.
