---
ticket: ENG-024
project: aiorders-api
reviewer: principal-engineer
date: 2026-09-03
verdict: pass
round: 1
lane: fast
diff: "aiorders-api: fix/ENG-024-onboarding-marketplace-visibility (8c97bd3) vs origin/main (93617c6)"
---

# Code review — ENG-024 (round 1, fast lane — combined review + suite + OWASP)

## Scope

Single repo, single branch, first review round. Fast lane (`size: XS`, `type:
bug`): this receipt covers all three dimensions definition-of-done.md assigns
to principal-engineer on this lane — code review, suite run, and an OWASP walk
on the touched surface — in one file, per `code-review-gate/SKILL.md`'s own
Outputs note ("Applies on the fast lane too — no new artifact, no extra hop,
no new gate").

Worktree (`~/Documents/projects/_eng/aiorders-api`) confirmed clean before
reading anything except one pre-existing, unrelated untracked file
(`supabase/functions/brand-portal/deno.lock`, left over from `ENG-022`'s work
in this same shared worktree — not touched). Branch tip `8c97bd3` confirmed
directly (`git log`), `origin/main` confirmed at `93617c6` after a fresh
`git fetch` — matches the ticket's own frontmatter exactly, independently
re-derived rather than taken on trust. Full diff read via
`git diff origin/main...HEAD`: 3 files, 86 insertions, 1 deletion.

## Automatic-failure scan (`engineering-standards.md`)

| # | Check | Result |
|---|---|---|
| 1 | Secret/credential/token/key committed | Clean — full diff read; one commit on the branch, on top of clean `origin/main`; nothing sensitive |
| 2 | Silent exception swallow | N/a — diff touches no catch block |
| 3 | Missing test on a bug fix | Clean — `restaurants.test.ts`, 2 new `Deno.test` cases: the regression case and one positive control on the pre-existing guard clause |
| 4 | `any`/untyped public interface, undocumented | `createRestaurant(supabase, brandId, restaurantData: any)`'s signature is untouched by this diff — pre-existing, matches every other function in this same file (`getRestaurants`, `updateRestaurant`, etc. all take `any`). Not a fresh violation |
| 5 | Unbounded query / missing pagination | N/a — no new query; one field added to an existing single-row insert |
| 6 | New dependency, no justification | Clean — no new import beyond the test file's use of this repo's existing `deno.land/std` testing convention |
| 7 | Unrelated refactor bundled in | Clean — exactly one line added to the insert (`show_in_marketplace: true`), nothing else in `restaurants.ts` touched |
| 8 | Commented-out code / unowned `TODO` | Clean — grepped the diff for `TODO\|FIXME`: zero matches |
| 9 | Direct datastore write bypassing the data layer | N/a — direct `supabase.from(...).insert()` is this file's own established convention (every function in it does this); no separate data-layer abstraction exists to bypass |
| 10 | Auth, payment, or data-deletion path changed, no failure-case test | N/a — this change touches neither auth, payment, nor deletion; it sets a visibility flag on already-`approved: true` rows |

**0/10 open.**

## Design conformance

No architect design doc exists (fast lane skips it) — checked against the PRD
(`agents/product-manager/specs/ENG-024-onboarded-restaurants-missing-from-marketplace-search.md`)
instead, all three acceptance criteria independently re-verified against live
code, not taken from the PRD's or the build hop's own account:

- **AC1** (new signups appear in search, no manual step) — confirmed
  `show_in_marketplace` is genuinely hard-required by every marketplace read
  path: `grep -rn "show_in_marketplace" supabase/functions/restaurant-marketplace/`
  in this same repo shows it gating `handlers/restaurants.ts:146` (fallback
  query), `:308` (a second site), and `handlers/sitemap.ts:82` — all three
  independently read, all three `.eq('show_in_marketplace', true)`. Also
  confirmed the separate `restaurant-marketplace` repo's own
  `20240302_optimize_restaurant_discovery.sql` gates the primary RPC on the
  same column. The one-line fix closes the actual gate every read path
  enforces, not a guess at what might matter.
- **AC2** (backfill for existing rows) — migration exists
  (`supabase/migrations/20260903120000_backfill_onboarding_show_in_marketplace.sql`).
  WHERE-clause scoping independently re-derived, not trusted from the
  migration's own comment: read `restaurant-claims/index.ts` directly — its
  insert (line 132) sets `approved: false` **and** `claimed_by_user_id`
  together, always, so `claimed_by_user_id IS NULL` is a correct, real
  belt-and-suspenders exclusion of that flow's rows, not decorative.
  `IS DISTINCT FROM true` is the right NULL-safe comparison given the
  column's default is confirmed unset (below).
- **AC3** (set consistently with the `approved: true` intent) — one field,
  same object literal, same literal `true`. Direct.

Also independently verified: `updateRestaurantDetails` (same file, runs
immediately after `createRestaurant` in the onboarding flow) cannot clobber
the new field — its `mapPlaceToRestaurantRow` column whitelist
(`_shared/googlePlaces.ts:349-356`, `RESTAURANT_PLACE_COLUMNS`) does not
include `show_in_marketplace`. Read directly, not assumed.

**No divergence from the PRD.**

## Verification reproduced this round (fresh, not trusted from the build hop's own log)

- **`deno check`**, run correctly this time after an initial mistake caught
  mid-round: a first attempt used `git stash -u`, which found nothing to
  stash (the diff is committed, not working-tree-dirty) and silently
  re-checked the *already-fixed* file against itself. Caught by the line
  numbers not shifting the way the diff implied they should. Redone
  correctly: `git show origin/main:{path} > {path}` to materialize the true
  pre-edit content in place (imports still resolve, unlike an isolated
  `/tmp` copy — that was also tried first and failed for the wrong reason,
  a missing `_shared/` import), `deno check`, then `git checkout -- {path}`
  to restore. Pre-edit: **6 `TS18046` errors** (`error.message` on a caught
  `unknown`, lines 41/91/148/189/241/340). Post-edit (current branch tip):
  **6 `TS18046` errors**, same class, each shifted by exactly the diff's own
  +1 line (42/92/149/190/242/341). Byte-identical file confirmed via
  `git diff --stat` after restore (no output). Zero introduced, none on a
  touched line — matches the build hop's claim, now independently
  re-derived correctly rather than assumed from a flawed repro attempt.
- **`deno test --no-check restaurants.test.ts`**: **2 passed, 0 failed.**
  Matches the build hop's claim exactly.
- **Mutation check, executed myself, not hand-traced and not trusted from
  the build hop's own account.** Removed `show_in_marketplace: true` from
  the insert (`Edit`, not a shell patch), reran the test file: exactly
  `createRestaurant sets show_in_marketplace: true...` went red
  (`AssertionError: undefined` vs `true`, at the exact assertion this
  ticket's fix is about), the unrelated guard-clause test stayed green.
  Restored the fix byte-for-byte (`git diff --stat` empty after), reran:
  **2 passed, 0 failed** again. Independent confirmation the regression test
  is wired to the actual defect, not vacuous.
- **No open P0/P1 elsewhere** — `agents/qa/bugs/_index.md` has one open item
  (`BUG-001`, P2, unrelated `platform-analytics` cron).

## Test quality (shape, not coverage)

- Mutation-sensitive by direct, executed verification (above) — the
  strongest form of evidence this class of finding gets.
- The regression test asserts on the actual captured insert payload
  (`captured.payload?.show_in_marketplace`), not on a return-value shape a
  stub could fake vacuously — the fake `.from('restaurants').insert()`
  chain captures what was actually passed in, so a stub that always
  resolved successfully would still be caught if the payload itself were
  wrong.
- Positive control (`still requires brandId and restaurantData`) proves the
  pre-existing guard is unchanged, not merely that a test file exists.
- Naming and colocation (`restaurants.test.ts`, `Deno.test`, domain-language
  names) matches this repo's established convention.

## OWASP walk (fast lane — folded into this gate per `definition-of-done.md`)

**Threat-model first:**
1. New attacker-controlled input? None — the added field is a hardcoded
   literal, not derived from any request body; the backfill's WHERE clause
   is static SQL with no interpolated input.
2. New capability granted, to whom? None — no new caller, no new route; the
   existing `createRestaurant` call already required an authenticated brand
   owner, unchanged by this diff.
3. New data exposed, to whom? Restaurant listing fields (name, address,
   cuisine) become visible in the **public** marketplace search — this is
   the feature's own stated purpose (a restaurant that completed onboarding
   is supposed to be discoverable) and matches the pre-existing
   `approved: true` auto-approve intent on the same row. Data class is
   "Public" business-listing information per `security-baseline.md`'s
   classification table — no PII, no restricted data.
4. Fully compromised component? Same blast radius as before this diff — an
   attacker who already compromised `restaurant-portal-onboarding` could
   already write arbitrary restaurant rows; this diff adds one literal field
   to a write path that was already reachable, it does not open a new one.

| # | Category | Assessment |
|---|---|---|
| A01 Broken Access Control | n/a — no new route, no new authz decision. `createRestaurant`'s own caller-authentication is unchanged by this diff |
| A02 Cryptographic Failures | n/a — no crypto, no secrets, no PII |
| A03 Injection | n/a — literal boolean via the Supabase query builder (parameterised), not string-built; migration's `UPDATE` has a static `WHERE`, no interpolation |
| A04 Insecure Design | n/a — inherits the pre-existing `approved: true` auto-approve design; no new abuse surface (still one row per onboarding action, same as before) |
| A05 Security Misconfiguration | n/a — no config, CORS, or header surface touched |
| A06 Vulnerable Components | n/a — no new dependency |
| A07 Identification & Auth Failures | n/a — no session/token/auth logic touched |
| A08 Software & Data Integrity | n/a — plain SQL migration through the standard pipeline, no runtime remote-code execution, no deserialization |
| A09 Logging & Monitoring Failures | Migration reports its row count via `RAISE NOTICE` on apply — adequate for a one-time backfill. Code path's existing `console.error('Error creating restaurant:', ...)` (unchanged, line 78) already covers the insert's failure case |
| A10 SSRF | n/a — no server-side fetch of a user-supplied URL |

Secrets scan: full diff read, plus the branch's one commit — clean.
Dependencies: none added, nothing to review.
Negative-authz-case check: n/a — this diff introduces no new authz decision
(see A01), so there is no wrong-tenant/wrong-role case for a test to cover.

## Non-blocking findings

1. **This migration has not been through `database`'s own dedicated
   migration gate** (`schema-change/SKILL.md`). Fast lane skips architect
   design entirely, so nothing ever fired that skill's normal
   `touches_data: true` trigger — the backfill was written directly by the
   build hop. `principal-engineer`'s own scope explicitly excludes the
   migration gate ("other agents own those"), and `definition-of-done.md`'s
   fast-lane row names only review + suite + OWASP as folded into this gate,
   not migration — so this is outside what this review can or should
   verdict. Assessed anyway, informally, against `schema-change`'s own
   7 failure conditions, since the risk doesn't disappear just because no
   state exists to hold it: not destructive, not irreversible (an inverse
   `UPDATE` with the same `WHERE` undoes it), no schema/code coexistence
   question (pure DML), table very unlikely to be large or hot at this
   business's actual scale. One real soft miss: **no stated runtime
   estimate, no batching** — but this repo's own only precedent backfill
   migration (`20260221000001_normalize_customer_phone_numbers.sql`, which
   this migration was deliberately styled on) has neither either, and nothing
   suggests it caused a problem. Not blocking this verdict. Filed as a
   proposal (below) — the process gap, not this specific migration, is what
   needs a decision. `release-runner/SKILL.md` step 2 ("a missing verdict is
   a fail, not an assumption") is the documented backstop if this risk read
   is wrong.
2. **No tested rollback** on the backfill migration — no live Postgres
   reachable from this build host (no Docker, no `psql`, no `supabase` CLI).
   Not unique to this ticket: already a standing, already-filed proposal
   (`proposals.md`, 2026-08-29, eng-manager). Verified instead by direct
   comparison against this repo's own established backfill-migration
   pattern and by independently re-deriving the WHERE-clause logic against
   live source (above), not by execution.
3. **6 pre-existing `TS18046` errors**, confirmed unrelated to this diff,
   confirmed by a proper before/after comparison this round (see
   Verification) — not this ticket's to fix.

Zero blocking findings.

## Notebook

Entry added to `agents/principal-engineer/notebook/2026-09-03-review-log.md`.

## Proposal filed

`agents/eng-manager/proposals.md`, 2026-09-03 row: fast lane has no mechanism
to route a migration discovered at build time (as opposed to one the
architect's design already flagged via `touches_data: true`) to `database`'s
own gate before `ready-to-ship`. This ticket's migration is low-risk on its
own merits (assessed above), but the gap in the *mechanism* is general.

## Verdict: PASS

Zero automatic-failure items. Diff matches the PRD's three acceptance
criteria, independently re-verified against live code rather than assumed.
Suite and typecheck results independently reproduced (after catching and
correcting my own flawed first repro attempt). Mutation-sensitivity of the
regression test independently executed, not hand-traced. OWASP walk complete,
every category marked with a reason. `links.review` set to this file. The one
real gap (migration gate never triggered on the fast lane) is named, assessed
informally as low-risk, and routed to a proposal rather than silently passed
over or used to block an otherwise-clean XS fix.
