---
ticket: ENG-001
project: aiorders
reviewer: principal-engineer
date: 2026-08-26
round: 1
verdict: pass
diff: none — verification ticket, no diff in any registered project (ADR-001)
---

# Code review — ENG-001 (round 1)

## What this reviews

Per `ADR-001` (`agents/architect/decisions/ADR-001-verification-ticket-building-and-receipts.md`),
ENG-001 is a **verification ticket**: all four acceptance criteria are
satisfied without producing a diff in any registered project. There is no
branch, no PR, no code to review for shape or style. What principal-engineer
review means here, per the ADR's own Decision text, is confirming the
claims made at `building` are **actually true on disk** — independently,
not on the strength of the ticket log's prior citations.

## Independent re-verification (this round, not taken on any prior pass's word)

**AC1 — every repo registered in `config/projects.md` at L1, approver-approved.**
Read `agents/eng-manager/config/projects.md` → Registered projects directly
this round: five rows (`aiorders-api`, `aiorders-admin-hub`,
`config-site-builder`, `restaurant-marketplace`, `restaurant-portal`), every
one marked **L1** in the Autonomy column. The table's own preamble states
they were "approved 2026-07-28" and "re-verified against the working trees
on 2026-08-23." No sixth project, no project below L1. **Confirmed.**

**AC2 — a department-owned worktree under `_eng/` for each.** Listed
`~/Documents/projects/_eng/` directly this round and confirmed each of the
five directories present, and ran `git rev-parse --abbrev-ref HEAD` inside
each to confirm they are real, resolvable git worktrees rather than empty
placeholders: `aiorders-api`, `aiorders-admin-hub`, `config-site-builder`,
`restaurant-marketplace` on `eng/base`; `restaurant-portal` on
`chore/ENG-002-smoke-test-harness` (expected — `ENG-002` is mid-flight on
it). **Confirmed.**

**AC3 — `lib/eng-gate-check.sh` exits 0 against this instance.** Ran it
myself this round, fresh, from the instance root:
`/bin/sh departments/engineering/lib/eng-gate-check.sh` → **exit 0**, no
violation or parse-error lines on stdout. **Confirmed**, not re-cited from
an earlier pass.

**AC4 — one real ticket has moved `intake → shaped` and the board renders
it.** Read `agents/eng-manager/board/ENG-002-restaurant-portal-smoke-test-harness.md`
directly. `ENG-002` is not merely at `shaped` — it has since passed
`awaiting-scope`, `designed`, `ready`, `building`, `in-review`,
`in-security`, and `ready-to-ship`, and now sits at `blocked`,
`blocked_on: approver`, with a real PR open
(https://github.com/harsimranwalia/restaurant-portal/pull/1) and its own
principal-engineer/QA/security receipts on disk. The board's `_index.md`
renders it in the In-flight table. AC4's literal bar ("moved intake →
shaped") is satisfied several states over. **Confirmed, a fortiori.**

## What isn't here, and why that's correct per ADR-001

- No line-level diff comments — there is no diff. Reviewing prose that says
  "no diff exists" for style would be theatre ADR-001 explicitly rejected.
- No automatic-failure scan against `engineering-standards.md` — that
  checklist (secrets, silent exception swallow, untyped interface, etc.)
  presumes source code. None was written for this ticket.
- No dependency check — no `package.json`/lockfile change anywhere in this
  ticket's evidence trail.

## Verdict: PASS

All four acceptance criteria hold on disk, independently re-checked this
round rather than inherited from the ticket log. The one substantive
engineering judgement this ticket required — what `building` and the three
full-lane receipts mean without a diff — was made by the architect in
`ADR-001`, considered two real alternatives, and left no undocumented
improvisation for this review to catch. Nothing to flag back.
