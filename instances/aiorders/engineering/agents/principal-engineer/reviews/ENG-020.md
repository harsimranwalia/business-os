---
ticket: ENG-020
project: restaurant-portal
reviewer: principal-engineer
date: 2026-09-05
verdict: pass
round: 3
diff: "restaurant-portal: feat/ENG-020-marketing-roi-attribution-reporting (f129f14..5783a2d) vs origin/main; aiorders-api unchanged since round 1 (cd82579), not re-reviewed"
---

# Code review — ENG-020 (round 3)

## Scope

Rounds 1 and 2's own PASS stand and are not re-litigated (`code-review-gate/SKILL.md`
step 9). This round reviews only the diff since round 2's reviewed commit.

`git fetch origin` in both worktrees first: `aiorders-api` HEAD `cd82579`,
`restaurant-portal` HEAD `5783a2d`; `git log HEAD..origin/main` empty in both —
no drift, no other pass landed work here since the round-2 fix hop.
`aiorders-api` has **zero new commits** since round 1 — nothing to review there
this round. `restaurant-portal` diff `f129f14..5783a2d`: 1 file, 25 insertions
/ 0 deletions, one commit (`5783a2d`) — the round-2 quality gate's one fix item
(Gap 4 from `agents/qa/test-plans/ENG-020.md`): three new `it()` blocks in
`src/pages/acquisition/Index.test.tsx`, no production code touched.

## Automatic-failure scan (`engineering-standards.md`)

All ten checked fresh against this round's diff specifically. **0/10 open** —
the diff is pure test additions against already-shipped rendering branches;
nothing here touches secrets, exceptions, untyped interfaces, queries,
dependencies, unrelated scope, dead code, the data layer, or an auth/payment/
deletion path.

## Test quality (shape, not coverage — QA's gate owns coverage)

Read the three new tests alongside `Index.tsx:102-114` (the coverage-sentence
paragraph and the `data.low_volume &&` conditional), not just the test names
and not just trusted from the gap report.

- **"renders the coverage sentence with the fixture's actual numbers (AC4)"**
  — asserts `/61% of orders in this period could be traced to a source/`
  against `baseReport()`'s `coverage.attributed_pct: 61`, an exact-value
  match rather than a loose "some percentage" regex — a test that only
  checked for the word "traced" would still pass if the number were wrong or
  hardcoded.
- **"renders the low-volume caveat when low_volume is true (AC4)"** and
  **"does not render the low-volume caveat when low_volume is false (AC4)"**
  — a show/suppress pair on the same conditional, mirroring
  `ChannelBreakdown.test.tsx`'s existing pattern for the sibling `lowVolume`
  prop. The negative case waits for the loaded totals
  (`findByText('40')`) before asserting the caveat's absence, so it can't
  pass merely because the query is still loading — the same discipline this
  ticket's round-1 fix hop already established for the organic-search
  disclaimer's own negative case.

## Independent verification (fresh, this session)

- `restaurant-portal`: `npm run test -- --run` — **32/32 pass**, 9 suites (29
  pre-existing + 3 new), matches the fix hop's own count exactly. `npm run
  lint` — 62 errors/34 warnings, **identical to every prior round's
  baseline**, zero in `acquisition`/`dateRange` (grepped the full lint
  output). `npm run build` — clean, same pre-existing chunk-size-only
  warning.
- `aiorders-api`: not re-run — no commits since round 1, round 1's 40/40
  stands.
- **Two mutation tests, one per new assertion, both restored via a
  matching `Edit` and confirmed with `git status --short` empty after:**
  flipped `Index.tsx`'s `{data.low_volume && (...)}` to `{true && (...)}`
  and reran `Index.test.tsx` — exactly the "does not render... when
  low_volume is false" test failed (the other 8, including the "renders...
  when true" test, stayed green, since the caveat now always renders);
  changed the coverage sentence's `{data.coverage.attributed_pct}%` to a
  hardcoded `{99}%` and reran — exactly the "renders the coverage sentence"
  test failed (its regex is pinned to `61%`), the other 8 stayed green. Both
  confirm the new assertions have real teeth rather than passing regardless
  of what the branch renders.

## Step 6b — artifact-mention grep

Not run. This hop introduces no artifact — no receipt path, state name,
config key, or file another agent is instructed to produce; the diff is
three test assertions against UI text two prior rounds already shipped. Same
reasoning the round-2 fix hop already recorded for its own no-artifact test
additions.

## Verdict: PASS, round 3 — third consecutive pass

Zero automatic-failure items. Both new assertions confirmed to have real
teeth by mutation, not inferred from the test names. `links.review` stays
set to this file (rewritten in place per this ticket's own convention — the
receipt holds only the latest passing round).

**This PASS covers code review only.** The quality gate ran in the same
combined hop and found the acceptance table now fully passing — see
`agents/qa/test-plans/ENG-020.md`. Per `code-review-gate/SKILL.md` step 9,
both gates now passing on this diff: the ticket advances `building →
in-security`.
