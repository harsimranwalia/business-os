---
ticket: ENG-046
project: aiorders-admin-hub
reviewer: principal-engineer
date: 2026-09-07
verdict: pass
round: 1
diff: "feat/ENG-046-foodswipe-channel-visibility-admin-toggles (a2c51af) vs origin/main"
---

# Code review — ENG-046 (round 1)

## Scope

Two files, `git diff origin/main...HEAD --stat`: `src/pages/RestaurantDetails.tsx`
(44 lines) and `src/integrations/supabase/types.ts` (1576 lines, generated).
No drift (`git rev-list --left-right --count origin/main...HEAD` → `0 1`).

## Automatic-failure scan (`engineering-standards.md`)

| # | Check | Result |
|---|---|---|
| 1 | Secret/credential/token/key committed | Clean |
| 2 | Silent exception swallow | N/a — no exception handling touched |
| 3 | Missing test on a bug fix | N/a — feature, not a bug fix |
| 4 | `any`/untyped public interface | Clean — three plain `boolean` fields, no `any` introduced |
| 5 | Unbounded query / missing pagination | N/a — no query changed |
| 6 | New dependency, no justification | Clean — none |
| 7 | Unrelated refactor bundled in | Clean — `RestaurantDetails.tsx`'s diff is exactly the interface/JSX change the design describes. `types.ts`'s 1576-line diff is 100% generated output (`supabase gen types typescript`, per the design's own instruction, not hand-edited) reflecting schema drift from several unrelated, already-shipped tickets that never regenerated this file — confirmed mechanical, not authored, by inspection of the diff itself |
| 8 | Commented-out code / unowned `TODO` | Clean — grepped both files, zero matches |
| 9 | Direct datastore write bypassing the data layer | N/a — save still goes through the existing `PUT /admin-portal/restaurants/:id`, unchanged |
| 10 | Auth/payment/deletion path changed, no failure-case test | N/a — unrelated path |

**0/10 open.**

## Design conformance

Checked against `agents/architect/designs/ENG-026-foodswipe-channel-visibility.md`'s
`## Components` (`aiorders-admin-hub` row) and `## Interfaces` →
`### aiorders-admin-hub — the toggle surface`, and against `ENG-044`'s own
board-file Log (the live-schema finding this ticket's own branch depends on):

- **Repoint, not fresh addition** — confirmed correct: `ENG-044` found `dine_in`
  real, live, backfilled forward. The local `Restaurant` interface's `dine_in:
  boolean` is replaced by `has_order_food`/`has_dine_in`/`has_catering`, and the
  existing Switch's bound field moves from `dine_in` to `has_dine_in` — matches.
- **Two new rows** (`has_order_food`, `has_catering`) added, markup identical to
  the six pre-existing rows in the same card (`flex items-center justify-between`
  / `Label` / editing-gated `Switch` vs. read-only `Badge`) — confirmed by diff,
  not merely by the ticket's own claim.
- **No new API call** — verified directly, not taken on the ticket's word:
  `handleSave` (line ~124) sends `JSON.stringify(restaurant)`, the entire state
  object, unchanged by this diff. The GET side already returns all fields too —
  `fetchRestaurant` calls the `admin-portal` `RESTAURANT_GET` endpoint, whose
  server-side `getRestaurantById` uses `select('*')` (confirmed against
  `ENG-045`'s own board-file Log, which independently verified this same
  handler needs no code change).
- **`types.ts` regenerated, not hand-edited** — confirmed: `has_catering`,
  `has_dine_in`, `has_order_food` all present in the `restaurants` table's Row/
  Insert/Update blocks; the old `dine_in: boolean` column is still present
  (correctly — `ENG-044` kept the column, a follow-up ticket drops it), not
  removed by this diff.

No divergence from the design in either direction.

## Review of the whole diff for shape

One concern (three toggle rows on one card), minimal, reads exactly like the
six rows already in the file — a reviewer could not tell this diff from
something the original author wrote. Could not usefully be smaller.

## Review of the lines

Each of the three rows independently traced end to end — `id`, `htmlFor`,
`checked`, and the `onCheckedChange` setter all reference the *same* field
name, consistently, for all three (`has_order_food`/`has_dine_in`/
`has_catering`) — no cross-wired copy/paste bug (checked precisely because
the block was copied three times). Null-handling on `onCheckedChange`
(`prev ? { ...prev, X: checked } : null`) matches the existing pattern used
by every other row in this card, unchanged.

## Review of what isn't there

- **No automated test.** Addressed at the quality gate, not held here — see
  `agents/qa/test-plans/ENG-046.md`: this project has zero test
  infrastructure (`config/projects.md`'s Test column is empty for
  `aiorders-admin-hub`; no `test` script in `package.json`; zero `*.test.*`/
  `*.spec.*` files anywhere in `src/`, confirmed this pass), consistent with
  every one of the six pre-existing toggle rows in this same card, none of
  which have a test either. An open proposal already tracks the missing
  harness (`proposals.md`, 2026-08-31).
- **Row placement** (the two new rows grouped with Dine In at the top of the
  card, rather than appended at the end) is a judgement call the design left
  unspecified — checked for a conflicting layout expectation elsewhere
  (PRD, design) and found none. Not a defect.
- **One inaccurate citation in the frontend's own build notebook**, not a
  code defect: `agents/frontend/notebook/2026-09-07-eng046-build.md` cites
  "the same shape `ENG-040`'s own admin-toggle work used" as precedent for
  skipping a test. Checked directly — `ENG-040` is a backend allow-list
  change on `aiorders-api` (`EDITABLE_PAGES` widening for `faqs`), not an
  admin-hub toggle UI, and `ENG-041` is an FAQ editor, also not this shape.
  The underlying judgement (no test added) is independently correct for the
  reasons above regardless of the citation; filed as an observation
  (`observations.md`, this date), not a blocking finding — it's a notebook
  citation, not a shipped-code defect.

## Test quality

N/a in the conventional sense — no test written this round, and none is being
required (see Design conformance / What isn't there, and the quality gate's
own reasoning in `agents/qa/test-plans/ENG-046.md`). The evidence class
substituted for it: line-by-line diff trace (above) plus independent
re-execution of build/lint/typecheck, not taken on the build hop's own
account — see Independent re-verification.

## Independent re-verification

Not trusted from the ticket log: `npm run build` — clean, same pre-existing
`>500 kB` chunk-size notice only, no new warning. `npx tsc --noEmit -p
tsconfig.app.json` — zero errors in either touched file (40 pre-existing,
unrelated repo-wide errors untouched). `npx eslint src/pages/
RestaurantDetails.tsx src/integrations/supabase/types.ts` — one warning
total, the pre-existing `react-hooks/exhaustive-deps` hit at line 81
(untouched by this diff); `types.ts` produces zero lint output. All three
match the build hop's own account exactly.

## Style / conventions

Branch `feat/ENG-046-foodswipe-channel-visibility-admin-toggles` matches
`{type}/{ENG-NNN}-{slug}`. Commit (`a2c51af`) imperative, present tense,
cites the ticket.

## Notebook

Full method: `agents/principal-engineer/notebook/2026-09-07-review-log.md`.
No new finding pattern.

## Verdict: PASS

0/10 automatic failures, no divergence from design, all three rows'
per-field wiring independently traced with no cross-wiring, build/lint/
typecheck independently re-run clean. `links.review` set to this file.
Continues to `in-security` — QA's gate ran on the same diff, also pass:
`agents/qa/test-plans/ENG-046.md`.
