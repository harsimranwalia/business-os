---
ticket: ENG-033
project: aiorders-api
reviewer: principal-engineer
date: 2026-09-04
verdict: pass
round: 4
diff: "aiorders-api: feat/ENG-033-catering-request-order-capture-endpoint (697df79) vs origin/main"
---

# Code review — ENG-033 (round 4)

## Scope

Round 4, following round 3's own quality-gate finding (AC-5/6/7's
status-derivation branching had no exported entry point to test — see
`agents/qa/test-plans/ENG-033.md`). `git fetch origin` then
`git diff origin/main...HEAD --stat`: 4 files, 201 insertions / 1 deletion —
up from round 3's 138/1. Isolated the actual change since round 3's own
reviewed commit rather than re-reviewing the whole cumulative diff:
`git diff b319a82..697df79 --stat` shows exactly 3 files touched
(`index.test.ts`, `index.ts`, `validation.ts`); `website.ts` untouched since
round 3. `git log HEAD..origin/main`: two new merges landed since round 3's
own check (`ENG-013`, `ENG-015`) — neither touches `catering-request/` or
`brand-portal/website.ts` (`git diff HEAD...origin/main --stat`: `admin-portal/`
handlers/tests and two migrations only). Worktree
(`~/Documents/projects/_eng/aiorders-api`) confirmed on
`feat/ENG-033-catering-request-order-capture-endpoint@697df79`, clean aside
from the standing untracked `brand-portal/deno.lock`.

## Automatic-failure scan (`engineering-standards.md`)

Re-run fresh against the full current diff, not carried forward from round 3.

| # | Check | Result |
|---|---|---|
| 1 | Secret/credential/token/key committed | Clean |
| 2 | Silent exception swallow | Clean — no new `try`/`catch` |
| 3 | Missing test on a bug fix | N/a this round — the diff is a coverage extraction of already-correct logic (round 3's own manual trace), not a bug fix. `deriveActionStatus` now carries 5 tests, closing round 3's actual finding |
| 4 | `any`/untyped public interface, undocumented | Clean — `deriveActionStatus(actionType: unknown, selections: unknown): DerivedActionStatus`, fully typed, no `any` |
| 5 | Unbounded query / missing pagination | N/a — no query in this diff |
| 6 | New dependency, no justification | Clean — no new imports besides the existing local `./validation.ts` |
| 7 | Unrelated refactor bundled in | Clean — the extraction is exactly what round 3's quality-gate finding specified, nothing extra rides along |
| 8 | Commented-out code / unowned `TODO` | Clean |
| 9 | Direct datastore write bypassing the data layer | Clean — unchanged, still `supabase.from("catering").insert(...)` |
| 10 | Auth/payment/deletion path changed, no failure-case test | N/a — none touched |

**0/10 open.**

## Verification (independent, not taken on the fix hop's own account)

- **Behaviour-preservation check.** Diffed `b319a82..697df79` directly rather
  than re-reading the cumulative diff against `origin/main`: the extraction is
  a pure lift of the inline branching (previously `index.ts:247-264`) into
  `deriveActionStatus()` in `validation.ts`, with `selectionsInvalid`
  replacing the inline early `return new Response(...400...)`. Traced every
  branch against the pre-extraction code line by line — absent/unrecognised
  `action_type`, `MANUAL_CONTACT_REQUESTED`, `QUOTE_SUBMITTED` valid, and
  `QUOTE_SUBMITTED` invalid — all four produce identical stored values and
  identical HTTP behaviour (same 400 body, same headers, same status). The
  insert-object code below needed no edit, confirmed unchanged in the diff.
- **Suite:** `deno test --no-check index.test.ts` → 17 passed, 0 failed (12
  `isValidSelections` cases carried from round 3 + 5 new `deriveActionStatus`
  cases).
- **Mutation-tested independently, not trusted from the fix hop's own
  self-mutation account.** Two separate mutations against `validation.ts`,
  each restored via `git checkout --` against the committed `697df79`
  baseline (safe here, unlike the fix hop's own noted mistake doing this
  against an uncommitted diff):
  - Removed the force-null on `MANUAL_CONTACT_REQUESTED` (the
    discard-on-purpose branch QA's finding named as the highest-risk case) →
    16 passed, 1 failed — only the discard-on-purpose test, failing with
    `selections` an array instead of `null`. Confirms the fix hop's own
    self-mutation-test claim rather than taking it on faith.
  - Corrupted the fallback branch's `status` value (the absent/unrecognised
    `action_type` case) → 15 passed, 2 failed — exactly the two tests
    exercising that branch (`stores no status...when action_type is absent`,
    `treats an unrecognized action_type the same as absent`), nothing else
    flipped.
  - Both restores confirmed clean via `git diff --stat` (empty) before
    re-running the suite green (17/17) again.
- **Lint:** all four touched files together → 11 problems, reconciling
  exactly against round 3's own combined count — 0 new. Attributed each hit
  to its file and line this round: 5× `no-import-prefix` + 1×
  `no-unversioned-import` + 1× `no-prototype-builtins`, all on pre-existing,
  untouched lines in `index.ts` / `index.test.ts` / `website.ts`; 4×
  `no-explicit-any` on pre-existing `website.ts` lines untouched by this
  ticket. **`validation.ts` — this hop's own file — carries zero lint
  problems.**
- **`deno check`:** fails identically before and after, same pre-existing
  `npm:openai` resolution gap (no `deno.json` anywhere in this repo).
- **Header-comment correction verified, not just accepted.** `validation.ts`'s
  header now credits `platform-customer-auth/validation.ts` as precedent
  (round 3's own correction, applied by the fix hop). Read that file directly:
  `validatePhoneStrict` is exactly a pure predicate split out of
  `index.ts`/`handler.ts` for testability — the same shape as this file's own
  split. The claim is now accurate.
- **No other caller of the moved symbols.**
  `grep -rn "VALID_ACTION_TYPES\|isValidSelections\|deriveActionStatus"`
  across `supabase/` outside `catering-request/`: no hits. The extraction
  broke nothing else.
- **Test quality:** all 5 new cases assert `deriveActionStatus`'s full return
  value (observable behaviour) via `assertEquals` against a complete object,
  not a partial/implementation-detail assertion; none mock anything (pure
  function, no I/O); each shown capable of failing via the mutation tests
  above, not just written and left green.

## Three notes carried forward from rounds 1–3, unchanged — not re-derived,
## since `isValidSelections` itself is byte-identical to the version round 3
## already reviewed

- Empty `name: ""` still passes.
- Empty `selections: []` with `action_type: 'QUOTE_SUBMITTED'` still yields
  `status: 'Quote Generated'` with zero items.
- The selections-validation block's placement (now inside `deriveActionStatus`,
  previously inline in `index.ts`) still isn't grouped with the two earlier
  boundary checks — genuinely coupled to the `QUOTE_SUBMITTED` branch, not a
  universal gate; letting this go, same reasoning as rounds 1–3.

## Verdict: PASS

Zero automatic-failure items. The extraction is behaviour-preserving,
independently verified rather than trusted, and its own new tests are
independently mutation-verified rather than trusted. `links.review` remains
set to this file (round 3's content superseded by this round's).

**This PASS covers code review only.** The quality gate ran in the same
combined hop and also passed this round — see
`agents/qa/test-plans/ENG-033.md` for its own round-2 verdict. Per
`code-review-gate/SKILL.md` step 9, the ticket continues to `in-security`
once QA has also passed; it has, so this round moves the ticket through
`in-review → in-qa`.
