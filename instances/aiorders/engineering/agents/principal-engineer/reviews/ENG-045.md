# Review — ENG-045 (round 2)

**Verdict: PASS**

**Project:** `aiorders-api`. **Branch:**
`feat/ENG-045-foodswipe-channel-visibility-discovery-handlers` at `5f35b92`
(round 2's fix-only delta: `a9693b6..5f35b92`, 1 file, +2/-2 — round 1's own
diff, `b647508` against `origin/main@5be48ed`, plus QA's own `a9693b6`
test-file commit, both already reviewed/written and not re-litigated here).
Round 1's blocking finding
(`agents/principal-engineer/notebook/2026-09-07-review-log.md`):
`channelForMode`/`evaluateOpenNow` extracted and pure but not exported —
`engineering-standards.md`'s "decision logic... exported" rule, fourth
occurrence.

## Automatic-failure scan: 0/10

Re-ran against the whole diff, not just the delta. No hit. Notable non-hits,
checked directly:

- **#4 (untyped public interface):** the two functions are now public — both
  keep explicit signatures (`channelForMode(mode: string | undefined): string
  | null`; `evaluateOpenNow(openingHours: unknown, openNow: boolean |
  undefined): { status: string | null; include: boolean }`). `unknown` on
  `openingHours` matches `_shared/openingHours.ts`'s own exported
  `getOpenState`'s parameter — the deliberate "caller must narrow" type, not
  an untyped escape hatch.
- **#7 (unrelated refactor bundled in):** `git show 5f35b92` — the entire
  commit is two `export` keywords, nothing else touched.
- **#2 (silent exception swallow):** re-checked `_shared/openingHours.ts`'s
  `try/catch`s again since round 1 flagged them as a note, not a fail —
  unchanged this round, still returns a defined sentinel on every path, still
  not the automatic-failure shape.

## Shape and lines

Exactly the fix round 1 asked for, and round 1 explicitly scoped it as the
entire fix ("zero behavior change, no other edit implied") — confirmed true
rather than assumed: `git diff a9693b6..5f35b92` is two lines, both `export`
insertions, same two declarations round 1's own finding named.

## Line-level correctness

Nothing new to trace — the diff adds no logic, only a visibility modifier.
Re-read both function bodies anyway: unchanged since round 1's own
line-level pass, still correct against `ADR-010` (fail-open on unknown/open,
exclude only confirmed-closed-and-requested).

## What isn't there

**Non-blocking, carried forward, not a new finding.** The two newly-exported
functions still have no test that imports and calls them directly —
`handlers/restaurants.test.ts`'s six tests (written in round 1, before the
export existed) all go through `handleRestaurantDiscovery`/
`handleRestaurantDiscoveryFallback`/`handleRestaurantDetail` with the
`supabase` singleton stubbed — the same mocked-client shape
`engineering-standards.md`'s rule exists to make unnecessary. The export
makes the functions *reachable* by a direct unit test without a client mock;
real behavioral coverage of both already exists through the handler tests
(confirmed independently below), so this isn't a coverage gap on this
ticket's owned criteria, and round 1 already fixed exactly what it named.
Noted so a future touch of this file doesn't have to rediscover it — not
blocking this round on a bar round 1 didn't set.

**Still true, unchanged:** `openingHours.ts`'s silent catch (round 1's
non-blocking note) — not a regression, ported behavior, not re-litigated.

## Independent re-verification

Not trusted from the ticket log: `deno check` on all four touched/relevant
files (`restaurants.ts`, `validation.ts`, `types/api.ts`,
`_shared/openingHours.ts`) — clean. `deno test --no-check` on both suites —
9/9 (`openingHours.test.ts`) + 6/6 (`restaurants.test.ts`) = **15/15 pass**.
`deno lint` on `restaurants.ts` — 5 problems, all `no-explicit-any`, matching
the build/QA hops' own account exactly (baseline 4 + the one load-bearing
`any` on `visibleRestaurants`); confirmed not a registered gate for this
project (`config/projects.md`'s Commands table, `aiorders-api` row is empty
across the board).

**Mutation check not re-run this round** — the delta is a visibility keyword
only, with no code-execution difference from round 1's own diff. Round 1's
own mutation check (`channelForMode` forced to always return `null`: exactly
the 3 channel-gate tests went red; `evaluateOpenNow`'s exclusion branch
forced to `include: true`: exactly the 1 exclusion test went red; both
reverted clean) stands — see
`agents/qa/notebook/2026-09-07-coverage-gaps.md`.

## Good work

Round 1's fix request was two words with "no other edit implied" — round 2
confirms that's exactly what shipped, no scope creep in either direction.

## Route

**Pass.** Receipt written; `links.review` set on the ticket. Continues to
`in-security` — QA's own gate also passed this hop (combined; see
`agents/qa/test-plans/ENG-045.md`).
