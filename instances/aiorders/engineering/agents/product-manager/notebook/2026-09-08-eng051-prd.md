# ENG-051 — PRD-writing hop, 2026-09-08

`continue ENG-051` event, resuming after the prior pass's step-1b stub
(filed mid-`continue ENG-027`, per `acceptance-check/SKILL.md` step 6b).
Ticket log's most recent entry (line 77+) verified against disk before
trusting it, per `prd-writer/SKILL.md`'s own checkpoint-verification rule:
board file existed → 1b done; no `## Readback` on the ticket and lane is
`full` → 1c not yet done; no spec file at
`agents/product-manager/specs/ENG-051-*.md` → step 4 not started. Resumed
from 1c.

## Step 1c — readback, and why it was not re-run fresh

Read `skills/request-readback/SKILL.md` in full. Its own "When this does NOT
run" list doesn't literally name "item in an already-approved sequence," so
checked precedent directly rather than assume the gap meant "run it":
`ENG-027` (item 3, structurally the same situation — filed via the same step
6b mechanism, same "not agent-originated" status per `eng_build_loop.md`
step 3's carve-out) explicitly recorded **not** running a fresh two-reader
process, and gave a reason that generalizes cleanly: no raw ambiguous input
exists to run two blind readings against — the two readings that mattered
were run once, at `ENG-006`, and already converged. Applied the same
reasoning here rather than inventing a different rule for this ticket. The
PRD's own Readback section documents this explicitly, same as `ENG-027`'s
did, rather than silently skipping the section.

One thing checked before leaning on that precedent: whether `ENG-006`'s
convergence is still good evidence four tickets later, or whether it's gone
stale. Corroboration found, unprompted, in two later documents that didn't
need to mention this ticket at all: `ENG-007`'s own PRD (2026-08-28) and
`ENG-027`'s own PRD (2026-09-03) each independently named "redemption
execution or QR code generation/scanning" as ticket 4's boundary, in their
own non-goals, written at different times by drafting different tickets.
Three independent write-ups landing on the same boundary is a stronger
signal than one reading alone would be — treated as reason for more
confidence, not less.

## Step 2 — evidence gathered before writing the problem

Read, in this order: `ENG-006`'s spec (identity/session model, and its
`## Feature shape and sequencing` section verbatim for all five items —
this is where "one QR, cross-restaurant" and item 5's actual scope
("admin/support surfaces... manual ledger adjustment/void") come from);
`ENG-007`'s spec (confirmed the redemption-value field exists per
restaurant, effective-dated, shipped 2026-08-30, and confirmed its own
non-goals already named this ticket); `ENG-027`'s spec and its architect
design doc (confirmed: `loyalty_ledger_entries` already tolerates a negative
value by design intent, `requireRestaurantAccess()` is an existing,
proven pattern at `brand-portal/utils.ts:117`, balance is `SUM(points)` on
read not a maintained counter, and item 4 — this ticket — is named
explicitly in that design's own "Out of scope" section).

Also checked for anything that would make this urgent or evidence-backed
rather than pure sequencing: `decision-journal.md` and `observations.md`
grepped for "redemption"/"QR" — nothing beyond the sequence itself.
`proposals.md` grepped — nothing. No hidden urgency; this is exactly what
it looks like, the next item in an approved sequence.

**One naming collision checked and ruled a non-issue:** `ENG-014` ("Brand
portal self-service — restaurant QR codes and marketing media downloads")
also uses "QR codes," but for a restaurant's own onboarding/marketing
materials, unrelated to diner identity. Confirmed by reading its spec
opening. Not mentioned in the PRD itself since it's a different feature
with no shared code path or scope overlap — didn't rise to the level of an
observations.md entry either, on reflection: flagging a name collision
between two already-shipped/independent specs without a concrete confusion
it caused would be closer to noise than signal.

## Step 3 — the filter

1. Takes work off the approver's plate? Neutral-to-positive: it's the next
   item in a sequence he already approved the shape of; filing it is
   exactly what he asked the department to do without being asked again.
2. Freedom vs. new surface area: adds a debit-shaped write path, the first
   one against real point balances — real new surface, named plainly in
   Risks rather than minimized.
3. Current or anticipated: anticipated — no diner has a redeemable balance
   yet (every restaurant's earn rate table was empty until `ENG-027`
   shipped, and `ENG-027` itself just verified). Said plainly in Why now,
   same as every prior ticket in this sequence.
4. What it displaces: nothing — machine WIP is currently held by other work
   and this ticket does no building yet, only shaping (paperwork, not
   slotted). Noted for completeness, not a real constraint here.
5. Fine not to build: no — the sequence's own G1 already settled that a
   points balance with no way to spend it isn't a real loyalty program;
   revisiting that here would be relitigating a decided question, not fresh
   analysis.

Conclusion: build it. Recommendation in the G1 item.

## Sizing — M, not the stub's provisional L

The provisional `L` was step-1b's own guess with nothing behind it yet
(the ticket's own frontmatter said as much). Sized fresh against
`definition-of-done.md`'s table after the evidence above: no new external
integration (unlike `ENG-027`'s CloudWaitress webhook work, which is what
actually pushed that ticket to `L`), no cron, identity/session/rate/ledger
data models all already exist. Structurally this is closer to "one new
record shape plus a couple of thin surfaces" — the PM's own description,
on the immediately preceding ticket, of what would have been `ENG-027`'s
size *before* the webhook discovery pushed it to `L`. The one real
open question — whether the diner's code needs its own stored/revocable
row (pushing toward `L`) or can be a stateless derived value (staying
`M`) — is a technical-design call, explicitly left to the architect rather
than guessed here, and named in the PRD's own Cost section so it isn't
lost.

## Dissent (step 8b)

`agents/critic/agent.md` still doesn't exist at department or instance
level — checked fresh (`find` both roots, zero hits), same gap `ENG-016`,
`ENG-017`, `ENG-042`, and `ENG-027` itself already logged. Not re-filed as
a new proposal; the open one (`proposals.md`, 2026-08-25 row) already
covers it.

## Output

- PRD: `agents/product-manager/specs/ENG-051-redemption-api-and-qr-issuance-scanning.md`
- G1 raised: `inbox/2026-09-08-eng051-g1-scope.md`
- Ticket: `intake → awaiting-scope`, owner `product-manager → approver`,
  size `L → M`
- `chained: none` — the ticket now sits at a state owned by the approver;
  see the ticket's own log entry.
