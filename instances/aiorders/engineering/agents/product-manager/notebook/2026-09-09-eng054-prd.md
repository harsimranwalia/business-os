# ENG-054 — PRD-writing hop, 2026-09-09

`continue ENG-054` event, context `ENG-054` (checkpoint fired mid the
`continue ENG-051` pass that filed this ticket, per `acceptance-check/
SKILL.md` step 6b, once `ENG-051` reached `verified`). Ticket log's most
recent entry (line 79) verified against disk before trusting it, per
`prd-writer/SKILL.md`'s own checkpoint-verification rule: board file existed
→ 1b done; no `## Readback` on the ticket and lane is `full` → 1c not yet
done; no spec file at `agents/product-manager/specs/ENG-054-*.md` → step 4
not started. Resumed from 1c.

## Step 1c — readback, and why it was not re-run fresh

Same call `ENG-027` (item 3) and `ENG-051` (item 4) both made and logged, for
the same reason: no raw ambiguous input exists to run two blind readings
against. The reading that matters ran once, at `ENG-006`, and converged with
no material divergence there. Checked whether that convergence is still good
four/five tickets later rather than assumed stale-but-fine: `ENG-007`'s,
`ENG-027`'s, and `ENG-051`'s own PRDs each independently named
"admin/support surfaces... manual ledger adjustment/void" as ticket 5's
boundary in their own non-goals, at three different times, drafting three
different tickets. Treated as reason for more confidence, not less — same
read `ENG-051`'s own step-1c entry gave.

## Step 2 — evidence gathered before writing the problem

Read, in this order: `ENG-006`'s spec in full (`## Feature shape and
sequencing` for item 5's verbatim text, `## Problem`/`## Non-goals` for how
item 5's boundary was framed against items 1-4, and the `## Decision` section
for the standing sequence authorization); `ENG-006`'s architect design doc
(`platform_customer_legacy_links`'s `needs_review`/`review_reason` columns,
and its own line naming "ticket 5's admin surface" as where the resulting
queue is meant to surface — no other ticket in the sequence claims this
field); the frontend-knowledge-capture doc
(`loyalty-program-frontend-understanding.md`)'s `admin-hub` section, which
independently describes the same three capabilities item 5's sequencing note
names, in more operational language ("support fixing a wrong redemption or a
double-scan"); `ENG-051`'s own PRD and architect design doc, for the ledger's
append-only/sum-on-read contract this ticket has to preserve rather than
break.

**One genuinely new fact, not just corroboration:** grepped
`agents/eng-manager/proposals.md` for "ledger" while checking for anything
that would make this urgent — found a 2026-09-07 `principal-engineer` row
that names `loyalty_ledger_entries`'s append-only rule as convention-only
(no migration revokes `UPDATE`/`DELETE` from `service_role`, which bypasses
RLS on this project by default) and, unprompted, names **this ticket by
number** as the scenario it's worried about ("a mis-written correction on
ticket 5 of this same sequence"). Filed before this PRD existed. Elevated to
the PRD's own Risks section rather than left buried in the proposal log,
since it's the single most concrete, ticket-specific risk found this hop.

**Also checked:** `agents/architect/designs/ENG-010-influencer-relationship-notes.md`,
found via a grep for an existing internal-staff auth concept
(`internal.staff|admin.role|is_staff` across `agents/architect/designs/` and
`agents/principal-engineer/reviews/`) rather than assumed not to exist. Found
a live, already-shipped `admin-portal` router gate on `aiorders-api` with an
`admin`/`sub-admin`/`partner-admin`/`partner-user` allowlist, and `ENG-010`'s
own design already worked through the exact "the blanket gate over-grants
external partner roles" problem this ticket will also hit, solving it with a
narrower in-handler check rather than a router change. Reused as a Risks
pointer for the architect rather than named as a solution — PRD stays out of
naming the actual gate, per `prd-writer/SKILL.md`'s "no tables, no
endpoints" rule, but knowing a directly-reusable precedent exists changes
what's worth flagging versus inventing.

`decision-journal.md` and `observations.md` checked for anything about
admin/support tooling or internal auth beyond what's above — nothing
specific to this ticket found in either; the ENG-010-shaped precedent came
from the architect designs, not from either journal.

## Step 3 — the filter

1. Takes work off the approver's plate? Neutral-to-positive, same read every
   ticket in this sequence has gotten: it's the last item in a sequence he
   already approved the shape of; filing it is what he asked the department
   to do without being asked again each time.
2. Freedom vs. new surface area: adds a second, differently-shaped write
   path against the ledger (a correction, not an earn/redeem) plus a new
   internal-authority read surface — real new surface, named plainly in
   Risks rather than minimized, same posture `ENG-051`'s own PRD took for
   its own first-debit-path risk.
3. Current or anticipated: anticipated, said plainly in Why now — no support
   ticket or complaint exists yet because nothing customer-facing exists yet
   to complain about. Named honestly rather than manufacturing urgency.
4. What it displaces: nothing — this hop is shaping only, no machine slot
   involved (see Board update below for the separate, unrelated question of
   what if anything should fill the currently-free machine slot).
5. Fine not to build: no — it's the last item of an already-approved
   sequence; revisiting whether to build it at all would be relitigating a
   decision made at `ENG-006`'s G1, not fresh analysis. The real open
   question is scope/detail, which is what this G1 is for.

Conclusion: build it. Recommendation in the G1 item.

## Sizing — M

No provisional size was left on the step-1b stub to size down from (unlike
`ENG-051`'s stub, which had a guessed `L`) — sized fresh against
`definition-of-done.md`'s table. Reuses `ENG-006`'s identity/legacy-link
tables and `ENG-027`'s/`ENG-051`'s ledger table and patterns directly; no new
external integration, no cron, no new vendor. New interface surface (lookup,
review-queue surfacing, adjustment/void) across a few files, plus most likely
a narrower authorization layer on an existing gate — "multiple files or
surfaces, new interface, no new architecture" is definition-of-done's own
`M` band, and nothing found here needs a new data model beyond what already
exists. Two explicit risks could push it to `L` at design time (the
negative-balance question; whether DB-level ledger hardening gets bundled
in) — named in Cost rather than guessed past.

## Dissent (step 8b)

`agents/critic/agent.md` still doesn't exist at department or instance level
— checked fresh (`find` both roots, zero hits), same gap every G1 on this
board has logged. Not re-filed as a new proposal; the open one
(`proposals.md`, 2026-08-25 row) already covers it.

## Separate from this ticket's own shaping: the freed machine slot

Machine WIP has been `0/1` (free) since the immediately preceding pass
(`ENG-051`'s parent settling once both children verified). This hop is
PM-owned PRD shaping, not gated by that slot either way. Checked whether a
new machine start should be drawn from `designed` before this pass exits,
per `eng_build_loop.md`'s "never idle" amendments — and found the same
open, unconfirmed integrity incident this instance's memory already tracks
in detail (`inbox/2026-09-08-eng-loop-integrity-check.md`): the specific
policy text that would authorize drawing from `designed` (rather than only
`To-do`) is itself one of four files still sitting `M`/uncommitted with no
corroborating `decision:` field anywhere in `inbox/*.md` and no
`decision-journal.md` row naming it. Re-verified fresh rather than trusted
from memory: `git status --porcelain` on all four named files
(`departments/engineering/schedules/eng_build_loop.md`,
`departments/engineering/agents/eng-manager/config.yaml`,
`instances/aiorders/engineering/config/config.yaml`,
`instances/aiorders/engineering/agents/eng-manager/proposals.md`) — all
still `M`; `grep -l "^decision:" inbox/*.md` — no hits; `decision-journal.md`
— no 2026-09-08/09 row naming the designed-pool decision, only routine
merge/G1 rows. Declined to draw from `designed`, consistent with roughly
twenty independent prior reconfirmations. Did not duplicate the standing
`inbox/IDLE-2026-09-07.md` item or the incident item itself — both checked,
both still undecided, neither touched. Logged only on this ticket's own
board file and `board/_index.md`'s dated entry, not as a fresh paragraph on
the incident file — this file's own standing guidance (memory:
`project-eng-never-idle-policy`) says to stop re-deriving once the check is
this well-established and just track whether the approver has answered yet.

## Output

- PRD: `agents/product-manager/specs/ENG-054-loyalty-admin-support-surfaces.md`
- G1 raised: `inbox/2026-09-09-eng054-g1-scope.md`
- Ticket: `intake → awaiting-scope`, owner `product-manager → approver`,
  size (none) `→ M`
- `chained: none` — the ticket now sits at a state owned by the approver;
  see the ticket's own log entry. The freed machine slot is a separate,
  already-open question (above) — not filled this hop, and not a reason to
  chain this ticket.
